---
title: 從Newtonsoft.Json遷移至System.Text.Json -.net
author: tdykstra
ms.author: tdykstra
no-loc:
- System.Text.Json
- Newtonsoft.Json
ms.date: 01/10/2020
helpviewer_keywords:
- JSON serialization
- serializing objects
- serialization
- objects, serializing
ms.openlocfilehash: 0828a5654171df39230055215903d3a49690155d
ms.sourcegitcommit: 465547886a1224a5435c3ac349c805e39ce77706
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 04/21/2020
ms.locfileid: "81739243"
---
# <a name="how-to-migrate-from-newtonsoftjson-to-systemtextjson"></a><span data-ttu-id="7d4d0-102">如何從 Newtonsoft 遷移至 System.web. Json</span><span class="sxs-lookup"><span data-stu-id="7d4d0-102">How to migrate from Newtonsoft.Json to System.Text.Json</span></span>

<span data-ttu-id="7d4d0-103">本文說明如何從[Newtonsoft](https://www.newtonsoft.com/json)遷移至<xref:System.Text.Json>。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-103">This article shows how to migrate from [Newtonsoft.Json](https://www.newtonsoft.com/json) to <xref:System.Text.Json>.</span></span>

<span data-ttu-id="7d4d0-104">`System.Text.Json`命名空間提供從 JAVASCRIPT 物件標記法（JSON）序列化及還原序列化的功能。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-104">The `System.Text.Json` namespace provides functionality for serializing to and deserializing from JavaScript Object Notation (JSON).</span></span> <span data-ttu-id="7d4d0-105">連結`System.Text.Json`庫包含在[.net Core 3.0](https://aka.ms/netcore3download)共用架構中。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-105">The `System.Text.Json` library is included in the [.NET Core 3.0](https://aka.ms/netcore3download) shared framework.</span></span> <span data-ttu-id="7d4d0-106">若是其他目標 framework，請安裝[System.web](https://www.nuget.org/packages/System.Text.Json) NuGet 封裝。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-106">For other target frameworks, install the [System.Text.Json](https://www.nuget.org/packages/System.Text.Json) NuGet package.</span></span> <span data-ttu-id="7d4d0-107">封裝支援：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-107">The package supports:</span></span>

* <span data-ttu-id="7d4d0-108">.NET Standard 2.0 和更新版本</span><span class="sxs-lookup"><span data-stu-id="7d4d0-108">.NET Standard 2.0 and later versions</span></span>
* <span data-ttu-id="7d4d0-109">.NET Framework 4.7.2 和更新版本</span><span class="sxs-lookup"><span data-stu-id="7d4d0-109">.NET Framework 4.7.2 and later versions</span></span>
* <span data-ttu-id="7d4d0-110">.NET Core 2.0、2.1 和2。2</span><span class="sxs-lookup"><span data-stu-id="7d4d0-110">.NET Core 2.0, 2.1, and 2.2</span></span>

<span data-ttu-id="7d4d0-111">`System.Text.Json`主要著重于效能、安全性和標準合規性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-111">`System.Text.Json` focuses primarily on performance, security, and standards compliance.</span></span> <span data-ttu-id="7d4d0-112">它在預設行為上有一些主要的差異，並不是要與`Newtonsoft.Json`的功能同位。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-112">It has some key differences in default behavior and doesn't aim to have feature parity with `Newtonsoft.Json`.</span></span> <span data-ttu-id="7d4d0-113">在某些情況下`System.Text.Json` ，沒有內建功能，但有建議的因應措施。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-113">For some scenarios, `System.Text.Json` has no built-in functionality, but there are recommended workarounds.</span></span> <span data-ttu-id="7d4d0-114">針對其他案例，因應措施並不實用。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-114">For other scenarios, workarounds are impractical.</span></span> <span data-ttu-id="7d4d0-115">如果您的應用程式相依于遺失的功能，請考慮提出[問題](https://github.com/dotnet/runtime/issues/new)，以瞭解是否可以新增您案例的支援。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-115">If your application depends on a missing feature, consider [filing an issue](https://github.com/dotnet/runtime/issues/new) to find out if support for your scenario can be added.</span></span>

<!-- For information about which features might be added in future releases, see the [Roadmap](https://github.com/dotnet/runtime/tree/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/libraries/System.Text.Json/roadmap/README.md). [Restore this when the roadmap is updated.]-->

<span data-ttu-id="7d4d0-116">本文大部分是<xref:System.Text.Json.JsonSerializer>關於如何使用 API，但它也包含如何使用（代表檔物件模型或 DOM）、 <xref:System.Text.Json.JsonDocument> <xref:System.Text.Json.Utf8JsonReader>和<xref:System.Text.Json.Utf8JsonWriter>類型的指引。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-116">Most of this article is about how to use the <xref:System.Text.Json.JsonSerializer> API, but it also includes guidance on how to use the <xref:System.Text.Json.JsonDocument> (which represents the Document Object Model or DOM), <xref:System.Text.Json.Utf8JsonReader>, and <xref:System.Text.Json.Utf8JsonWriter> types.</span></span>

## <a name="table-of-differences-between-newtonsoftjson-and-systemtextjson"></a><span data-ttu-id="7d4d0-117">Newtonsoft 與 system.string 之間的差異表</span><span class="sxs-lookup"><span data-stu-id="7d4d0-117">Table of differences between Newtonsoft.Json and System.Text.Json</span></span>

<span data-ttu-id="7d4d0-118">下表列出`Newtonsoft.Json`功能和`System.Text.Json`對應項。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-118">The following table lists `Newtonsoft.Json` features and `System.Text.Json` equivalents.</span></span> <span data-ttu-id="7d4d0-119">對等專案屬於下列類別：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-119">The equivalents fall into the following categories:</span></span>

* <span data-ttu-id="7d4d0-120">受到內建功能的支援。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-120">Supported by built-in functionality.</span></span> <span data-ttu-id="7d4d0-121">從`System.Text.Json`取得類似的行為，可能需要使用屬性或全域選項。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-121">Getting similar behavior from `System.Text.Json` may require the use of an attribute or global option.</span></span>
* <span data-ttu-id="7d4d0-122">不支援，可能的解決方法。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-122">Not supported, workaround is possible.</span></span> <span data-ttu-id="7d4d0-123">因應措施是[自訂轉換器](system-text-json-converters-how-to.md)，這可能不會提供`Newtonsoft.Json`完整的同位檢查功能。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-123">The workarounds are [custom converters](system-text-json-converters-how-to.md), which may not provide complete parity with `Newtonsoft.Json` functionality.</span></span> <span data-ttu-id="7d4d0-124">其中一些範例程式碼會以範例的形式提供。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-124">For some of these, sample code is provided as examples.</span></span> <span data-ttu-id="7d4d0-125">如果您依賴這些`Newtonsoft.Json`功能，則需要修改您的 .net 物件模型或其他程式碼變更。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-125">If you rely on these `Newtonsoft.Json` features, migration will require modifications to your .NET object models or other code changes.</span></span>
* <span data-ttu-id="7d4d0-126">不支援，因應措施不可行或無法解決。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-126">Not supported, workaround is not practical or possible.</span></span> <span data-ttu-id="7d4d0-127">如果您依賴這些`Newtonsoft.Json`功能，則不會有重大變更，因此無法進行遷移。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-127">If you rely on these `Newtonsoft.Json` features, migration will not be possible without significant changes.</span></span>

| <span data-ttu-id="7d4d0-128">Newtonsoft Json 功能</span><span class="sxs-lookup"><span data-stu-id="7d4d0-128">Newtonsoft.Json feature</span></span>                               | <span data-ttu-id="7d4d0-129">System.object 對等</span><span class="sxs-lookup"><span data-stu-id="7d4d0-129">System.Text.Json equivalent</span></span> |
|-------------------------------------------------------|-----------------------------|
| <span data-ttu-id="7d4d0-130">預設不區分大小寫的還原序列化</span><span class="sxs-lookup"><span data-stu-id="7d4d0-130">Case-insensitive deserialization by default</span></span>           | <span data-ttu-id="7d4d0-131">✔️ [PropertyNameCaseInsensitive 全域設定](#case-insensitive-deserialization)</span><span class="sxs-lookup"><span data-stu-id="7d4d0-131">✔️ [PropertyNameCaseInsensitive global setting](#case-insensitive-deserialization)</span></span> |
| <span data-ttu-id="7d4d0-132">Camel 大小寫屬性名稱</span><span class="sxs-lookup"><span data-stu-id="7d4d0-132">Camel-case property names</span></span>                             | <span data-ttu-id="7d4d0-133">✔️ [PropertyNamingPolicy 全域設定](system-text-json-how-to.md#use-camel-case-for-all-json-property-names)</span><span class="sxs-lookup"><span data-stu-id="7d4d0-133">✔️ [PropertyNamingPolicy global setting](system-text-json-how-to.md#use-camel-case-for-all-json-property-names)</span></span> |
| <span data-ttu-id="7d4d0-134">最少字元的轉義</span><span class="sxs-lookup"><span data-stu-id="7d4d0-134">Minimal character escaping</span></span>                            | <span data-ttu-id="7d4d0-135">✔️[嚴格的字元轉義，可](#minimal-character-escaping)設定</span><span class="sxs-lookup"><span data-stu-id="7d4d0-135">✔️ [Strict character escaping, configurable](#minimal-character-escaping)</span></span> |
| <span data-ttu-id="7d4d0-136">`NullValueHandling.Ignore`全域設定</span><span class="sxs-lookup"><span data-stu-id="7d4d0-136">`NullValueHandling.Ignore` global setting</span></span>             | <span data-ttu-id="7d4d0-137">✔️ [IgnoreNullValues global 選項](system-text-json-how-to.md#exclude-all-null-value-properties)</span><span class="sxs-lookup"><span data-stu-id="7d4d0-137">✔️ [IgnoreNullValues global option](system-text-json-how-to.md#exclude-all-null-value-properties)</span></span> |
| <span data-ttu-id="7d4d0-138">允許批註</span><span class="sxs-lookup"><span data-stu-id="7d4d0-138">Allow comments</span></span>                                        | <span data-ttu-id="7d4d0-139">✔️ [ReadCommentHandling 全域設定](#comments)</span><span class="sxs-lookup"><span data-stu-id="7d4d0-139">✔️ [ReadCommentHandling global setting](#comments)</span></span> |
| <span data-ttu-id="7d4d0-140">允許尾端逗號</span><span class="sxs-lookup"><span data-stu-id="7d4d0-140">Allow trailing commas</span></span>                                 | <span data-ttu-id="7d4d0-141">✔️ [AllowTrailingCommas 全域設定](#trailing-commas)</span><span class="sxs-lookup"><span data-stu-id="7d4d0-141">✔️ [AllowTrailingCommas global setting](#trailing-commas)</span></span> |
| <span data-ttu-id="7d4d0-142">自訂轉換器註冊</span><span class="sxs-lookup"><span data-stu-id="7d4d0-142">Custom converter registration</span></span>                         | <span data-ttu-id="7d4d0-143">✔️[優先順序的順序不同](#converter-registration-precedence)</span><span class="sxs-lookup"><span data-stu-id="7d4d0-143">✔️ [Order of precedence differs](#converter-registration-precedence)</span></span> |
| <span data-ttu-id="7d4d0-144">預設無最大深度</span><span class="sxs-lookup"><span data-stu-id="7d4d0-144">No maximum depth by default</span></span>                           | <span data-ttu-id="7d4d0-145">✔️[預設的最大深度64，可](#maximum-depth)設定</span><span class="sxs-lookup"><span data-stu-id="7d4d0-145">✔️ [Default maximum depth 64, configurable](#maximum-depth)</span></span> |
| <span data-ttu-id="7d4d0-146">支援廣泛的類型</span><span class="sxs-lookup"><span data-stu-id="7d4d0-146">Support for a broad range of types</span></span>                    | <span data-ttu-id="7d4d0-147">⚠️[某些類型需要自訂轉換器](#types-without-built-in-support)</span><span class="sxs-lookup"><span data-stu-id="7d4d0-147">⚠️ [Some types require custom converters](#types-without-built-in-support)</span></span> |
| <span data-ttu-id="7d4d0-148">將字串還原序列化為數字</span><span class="sxs-lookup"><span data-stu-id="7d4d0-148">Deserialize strings as numbers</span></span>                        | <span data-ttu-id="7d4d0-149">⚠️[不支援、](#quoted-numbers)因應措施、範例</span><span class="sxs-lookup"><span data-stu-id="7d4d0-149">⚠️ [Not supported, workaround, sample](#quoted-numbers)</span></span> |
| <span data-ttu-id="7d4d0-150">`Dictionary`使用非字串索引鍵進行還原序列化</span><span class="sxs-lookup"><span data-stu-id="7d4d0-150">Deserialize `Dictionary` with non-string key</span></span>          | <span data-ttu-id="7d4d0-151">⚠️[不支援、](#dictionary-with-non-string-key)因應措施、範例</span><span class="sxs-lookup"><span data-stu-id="7d4d0-151">⚠️ [Not supported, workaround, sample](#dictionary-with-non-string-key)</span></span> |
| <span data-ttu-id="7d4d0-152">多型序列化</span><span class="sxs-lookup"><span data-stu-id="7d4d0-152">Polymorphic serialization</span></span>                             | <span data-ttu-id="7d4d0-153">⚠️[不支援、](#polymorphic-serialization)因應措施、範例</span><span class="sxs-lookup"><span data-stu-id="7d4d0-153">⚠️ [Not supported, workaround, sample](#polymorphic-serialization)</span></span> |
| <span data-ttu-id="7d4d0-154">多型還原序列化</span><span class="sxs-lookup"><span data-stu-id="7d4d0-154">Polymorphic deserialization</span></span>                           | <span data-ttu-id="7d4d0-155">⚠️[不支援、](#polymorphic-deserialization)因應措施、範例</span><span class="sxs-lookup"><span data-stu-id="7d4d0-155">⚠️ [Not supported, workaround, sample](#polymorphic-deserialization)</span></span> |
| <span data-ttu-id="7d4d0-156">將推斷的類型`object`還原序列化為屬性</span><span class="sxs-lookup"><span data-stu-id="7d4d0-156">Deserialize inferred type to `object` properties</span></span>      | <span data-ttu-id="7d4d0-157">⚠️[不支援、](#deserialization-of-object-properties)因應措施、範例</span><span class="sxs-lookup"><span data-stu-id="7d4d0-157">⚠️ [Not supported, workaround, sample](#deserialization-of-object-properties)</span></span> |
| <span data-ttu-id="7d4d0-158">將 JSON `null`常值還原序列化為不可為 null 的實數值型別</span><span class="sxs-lookup"><span data-stu-id="7d4d0-158">Deserialize JSON `null` literal to non-nullable value types</span></span> | <span data-ttu-id="7d4d0-159">⚠️[不支援、](#deserialize-null-to-non-nullable-type)因應措施、範例</span><span class="sxs-lookup"><span data-stu-id="7d4d0-159">⚠️ [Not supported, workaround, sample](#deserialize-null-to-non-nullable-type)</span></span> |
| <span data-ttu-id="7d4d0-160">還原序列化為不可變的類別和結構</span><span class="sxs-lookup"><span data-stu-id="7d4d0-160">Deserialize to immutable classes and structs</span></span>          | <span data-ttu-id="7d4d0-161">⚠️[不支援、](#deserialize-to-immutable-classes-and-structs)因應措施、範例</span><span class="sxs-lookup"><span data-stu-id="7d4d0-161">⚠️ [Not supported, workaround, sample](#deserialize-to-immutable-classes-and-structs)</span></span> |
| <span data-ttu-id="7d4d0-162">`[JsonConstructor]` 屬性</span><span class="sxs-lookup"><span data-stu-id="7d4d0-162">`[JsonConstructor]` attribute</span></span>                         | <span data-ttu-id="7d4d0-163">⚠️[不支援、](#specify-constructor-to-use)因應措施、範例</span><span class="sxs-lookup"><span data-stu-id="7d4d0-163">⚠️ [Not supported, workaround, sample](#specify-constructor-to-use)</span></span> |
| <span data-ttu-id="7d4d0-164">`Required`在屬性`[JsonProperty]`上設定</span><span class="sxs-lookup"><span data-stu-id="7d4d0-164">`Required` setting on `[JsonProperty]` attribute</span></span>        | <span data-ttu-id="7d4d0-165">⚠️[不支援、](#required-properties)因應措施、範例</span><span class="sxs-lookup"><span data-stu-id="7d4d0-165">⚠️ [Not supported, workaround, sample](#required-properties)</span></span> |
| <span data-ttu-id="7d4d0-166">`NullValueHandling`在屬性`[JsonProperty]`上設定</span><span class="sxs-lookup"><span data-stu-id="7d4d0-166">`NullValueHandling` setting on `[JsonProperty]` attribute</span></span> | <span data-ttu-id="7d4d0-167">⚠️[不支援、](#conditionally-ignore-a-property)因應措施、範例</span><span class="sxs-lookup"><span data-stu-id="7d4d0-167">⚠️ [Not supported, workaround, sample](#conditionally-ignore-a-property)</span></span>  |
| <span data-ttu-id="7d4d0-168">`DefaultValueHandling`在屬性`[JsonProperty]`上設定</span><span class="sxs-lookup"><span data-stu-id="7d4d0-168">`DefaultValueHandling` setting on `[JsonProperty]` attribute</span></span> | <span data-ttu-id="7d4d0-169">⚠️[不支援、](#conditionally-ignore-a-property)因應措施、範例</span><span class="sxs-lookup"><span data-stu-id="7d4d0-169">⚠️ [Not supported, workaround, sample](#conditionally-ignore-a-property)</span></span>  |
| <span data-ttu-id="7d4d0-170">`DefaultValueHandling`全域設定</span><span class="sxs-lookup"><span data-stu-id="7d4d0-170">`DefaultValueHandling` global setting</span></span>                 | <span data-ttu-id="7d4d0-171">⚠️[不支援、](#conditionally-ignore-a-property)因應措施、範例</span><span class="sxs-lookup"><span data-stu-id="7d4d0-171">⚠️ [Not supported, workaround, sample](#conditionally-ignore-a-property)</span></span> |
| <span data-ttu-id="7d4d0-172">`DefaultContractResolver`排除屬性</span><span class="sxs-lookup"><span data-stu-id="7d4d0-172">`DefaultContractResolver` to exclude properties</span></span>       | <span data-ttu-id="7d4d0-173">⚠️[不支援、](#conditionally-ignore-a-property)因應措施、範例</span><span class="sxs-lookup"><span data-stu-id="7d4d0-173">⚠️ [Not supported, workaround, sample](#conditionally-ignore-a-property)</span></span> |
| <span data-ttu-id="7d4d0-174">`DateTimeZoneHandling`， `DateFormatString`設定</span><span class="sxs-lookup"><span data-stu-id="7d4d0-174">`DateTimeZoneHandling`, `DateFormatString` settings</span></span>   | <span data-ttu-id="7d4d0-175">⚠️[不支援、](#specify-date-format)因應措施、範例</span><span class="sxs-lookup"><span data-stu-id="7d4d0-175">⚠️ [Not supported, workaround, sample](#specify-date-format)</span></span> |
| <span data-ttu-id="7d4d0-176">回撥</span><span class="sxs-lookup"><span data-stu-id="7d4d0-176">Callbacks</span></span>                                             | <span data-ttu-id="7d4d0-177">⚠️[不支援、](#callbacks)因應措施、範例</span><span class="sxs-lookup"><span data-stu-id="7d4d0-177">⚠️ [Not supported, workaround, sample](#callbacks)</span></span> |
| <span data-ttu-id="7d4d0-178">支援公用和非公用欄位</span><span class="sxs-lookup"><span data-stu-id="7d4d0-178">Support for public and non-public fields</span></span>              | <span data-ttu-id="7d4d0-179">⚠️[不支援，](#public-and-non-public-fields)因應措施</span><span class="sxs-lookup"><span data-stu-id="7d4d0-179">⚠️ [Not supported, workaround](#public-and-non-public-fields)</span></span> |
| <span data-ttu-id="7d4d0-180">支援內部和私用屬性 setter 和 getter</span><span class="sxs-lookup"><span data-stu-id="7d4d0-180">Support for internal and private property setters and getters</span></span> | <span data-ttu-id="7d4d0-181">⚠️[不支援，](#internal-and-private-property-setters-and-getters)因應措施</span><span class="sxs-lookup"><span data-stu-id="7d4d0-181">⚠️ [Not supported, workaround](#internal-and-private-property-setters-and-getters)</span></span> |
| <span data-ttu-id="7d4d0-182">`JsonConvert.PopulateObject` 方法</span><span class="sxs-lookup"><span data-stu-id="7d4d0-182">`JsonConvert.PopulateObject` method</span></span>                   | <span data-ttu-id="7d4d0-183">⚠️[不支援，](#populate-existing-objects)因應措施</span><span class="sxs-lookup"><span data-stu-id="7d4d0-183">⚠️ [Not supported, workaround](#populate-existing-objects)</span></span> |
| <span data-ttu-id="7d4d0-184">`ObjectCreationHandling`全域設定</span><span class="sxs-lookup"><span data-stu-id="7d4d0-184">`ObjectCreationHandling` global setting</span></span>               | <span data-ttu-id="7d4d0-185">⚠️[不支援，](#reuse-rather-than-replace-properties)因應措施</span><span class="sxs-lookup"><span data-stu-id="7d4d0-185">⚠️ [Not supported, workaround](#reuse-rather-than-replace-properties)</span></span> |
| <span data-ttu-id="7d4d0-186">新增至不含 setter 的集合</span><span class="sxs-lookup"><span data-stu-id="7d4d0-186">Add to collections without setters</span></span>                    | <span data-ttu-id="7d4d0-187">⚠️[不支援，](#add-to-collections-without-setters)因應措施</span><span class="sxs-lookup"><span data-stu-id="7d4d0-187">⚠️ [Not supported, workaround](#add-to-collections-without-setters)</span></span> |
| <span data-ttu-id="7d4d0-188">`PreserveReferencesHandling`全域設定</span><span class="sxs-lookup"><span data-stu-id="7d4d0-188">`PreserveReferencesHandling` global setting</span></span>           | <span data-ttu-id="7d4d0-189">❌ [不受支援](#preserve-object-references-and-handle-loops)</span><span class="sxs-lookup"><span data-stu-id="7d4d0-189">❌ [Not supported](#preserve-object-references-and-handle-loops)</span></span> |
| <span data-ttu-id="7d4d0-190">`ReferenceLoopHandling`全域設定</span><span class="sxs-lookup"><span data-stu-id="7d4d0-190">`ReferenceLoopHandling` global setting</span></span>                | <span data-ttu-id="7d4d0-191">❌ [不受支援](#preserve-object-references-and-handle-loops)</span><span class="sxs-lookup"><span data-stu-id="7d4d0-191">❌ [Not supported](#preserve-object-references-and-handle-loops)</span></span> |
| <span data-ttu-id="7d4d0-192">屬性的`System.Runtime.Serialization`支援</span><span class="sxs-lookup"><span data-stu-id="7d4d0-192">Support for `System.Runtime.Serialization` attributes</span></span> | <span data-ttu-id="7d4d0-193">❌ [不受支援](#systemruntimeserialization-attributes)</span><span class="sxs-lookup"><span data-stu-id="7d4d0-193">❌ [Not supported](#systemruntimeserialization-attributes)</span></span> |
| <span data-ttu-id="7d4d0-194">`MissingMemberHandling`全域設定</span><span class="sxs-lookup"><span data-stu-id="7d4d0-194">`MissingMemberHandling` global setting</span></span>                | <span data-ttu-id="7d4d0-195">❌ [不受支援](#missingmemberhandling)</span><span class="sxs-lookup"><span data-stu-id="7d4d0-195">❌ [Not supported](#missingmemberhandling)</span></span> |
| <span data-ttu-id="7d4d0-196">允許沒有引號的屬性名稱</span><span class="sxs-lookup"><span data-stu-id="7d4d0-196">Allow property names without quotes</span></span>                   | <span data-ttu-id="7d4d0-197">❌ [不受支援](#json-strings-property-names-and-string-values)</span><span class="sxs-lookup"><span data-stu-id="7d4d0-197">❌ [Not supported](#json-strings-property-names-and-string-values)</span></span> |
| <span data-ttu-id="7d4d0-198">允許字串值前後加上單引號</span><span class="sxs-lookup"><span data-stu-id="7d4d0-198">Allow single quotes around string values</span></span>              | <span data-ttu-id="7d4d0-199">❌ [不受支援](#json-strings-property-names-and-string-values)</span><span class="sxs-lookup"><span data-stu-id="7d4d0-199">❌ [Not supported](#json-strings-property-names-and-string-values)</span></span> |
| <span data-ttu-id="7d4d0-200">允許字串屬性的非字串 JSON 值</span><span class="sxs-lookup"><span data-stu-id="7d4d0-200">Allow non-string JSON values for string properties</span></span>    | <span data-ttu-id="7d4d0-201">❌ [不受支援](#non-string-values-for-string-properties)</span><span class="sxs-lookup"><span data-stu-id="7d4d0-201">❌ [Not supported](#non-string-values-for-string-properties)</span></span> |

<span data-ttu-id="7d4d0-202">這不是完整的`Newtonsoft.Json`功能清單。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-202">This is not an exhaustive list of `Newtonsoft.Json` features.</span></span> <span data-ttu-id="7d4d0-203">此清單包含[GitHub 問題](https://github.com/dotnet/runtime/issues?q=is%3Aopen+is%3Aissue+label%3Aarea-System.Text.Json)或[StackOverflow](https://stackoverflow.com/questions/tagged/system.text.json)文章中所要求的許多案例。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-203">The list includes many of the scenarios that have been requested in [GitHub issues](https://github.com/dotnet/runtime/issues?q=is%3Aopen+is%3Aissue+label%3Aarea-System.Text.Json) or [StackOverflow](https://stackoverflow.com/questions/tagged/system.text.json) posts.</span></span> <span data-ttu-id="7d4d0-204">如果您針對此處所列的其中一個案例執行因應措施，但目前沒有範例程式碼，而且您想要共用您的方案，請在此頁面底部的 [**意見**反應] 區段中選取**此頁面**。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-204">If you implement a workaround for one of the scenarios listed here that doesn't currently have sample code, and if you want to share your solution, select **This page** in the **Feedback** section at the bottom of this page.</span></span> <span data-ttu-id="7d4d0-205">這會在本檔的 GitHub 存放庫中建立問題，並將其列在此頁面的**意見**反應區段中。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-205">That creates an issue in this documentation's GitHub repo and lists it in the **Feedback** section on this page too.</span></span>

## <a name="differences-in-default-jsonserializer-behavior-compared-to-newtonsoftjson"></a><span data-ttu-id="7d4d0-206">相較于 Newtonsoft，預設 JsonSerializer 行為的差異</span><span class="sxs-lookup"><span data-stu-id="7d4d0-206">Differences in default JsonSerializer behavior compared to Newtonsoft.Json</span></span>

<span data-ttu-id="7d4d0-207"><xref:System.Text.Json>預設為嚴格，並可避免代表呼叫端的任何猜測或解讀，強調決定性的行為。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-207"><xref:System.Text.Json> is strict by default and avoids any guessing or interpretation on the caller's behalf, emphasizing deterministic behavior.</span></span> <span data-ttu-id="7d4d0-208">此程式庫是故意以這種方式設計來實現效能和安全性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-208">The library is intentionally designed this way for performance and security.</span></span> <span data-ttu-id="7d4d0-209">`Newtonsoft.Json`預設為彈性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-209">`Newtonsoft.Json` is flexible by default.</span></span> <span data-ttu-id="7d4d0-210">這項設計的基本差異在於預設行為的下列許多特定差異背後。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-210">This fundamental difference in design is behind many of the following specific differences in default behavior.</span></span>

### <a name="case-insensitive-deserialization"></a><span data-ttu-id="7d4d0-211">不區分大小寫的還原序列化</span><span class="sxs-lookup"><span data-stu-id="7d4d0-211">Case-insensitive deserialization</span></span>

<span data-ttu-id="7d4d0-212">在還原序列化`Newtonsoft.Json`期間，預設會執行不區分大小寫的屬性名稱比對。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-212">During deserialization, `Newtonsoft.Json` does case-insensitive property name matching by default.</span></span> <span data-ttu-id="7d4d0-213"><xref:System.Text.Json>預設值會區分大小寫，這可提供較佳的效能，因為它會執行完全相符的作業。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-213">The <xref:System.Text.Json> default is case-sensitive, which gives better performance since it's doing an exact match.</span></span> <span data-ttu-id="7d4d0-214">如需如何執行不區分大小寫比對的相關資訊，請參閱不[區分大小寫的屬性](system-text-json-how-to.md#case-insensitive-property-matching)比對。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-214">For information about how to do case-insensitive matching, see [Case-insensitive property matching](system-text-json-how-to.md#case-insensitive-property-matching).</span></span>

<span data-ttu-id="7d4d0-215">如果您使用 ASP.NET Core `System.Text.Json`間接地使用，則不需要執行任何動作來取得之類`Newtonsoft.Json`的行為。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-215">If you're using `System.Text.Json` indirectly by using ASP.NET Core, you don't need to do anything to get behavior like `Newtonsoft.Json`.</span></span> <span data-ttu-id="7d4d0-216">ASP.NET Core 指定使用`System.Text.Json`時， [camel 大小寫屬性名稱](system-text-json-how-to.md#use-camel-case-for-all-json-property-names)和不區分大小寫比對的設定。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-216">ASP.NET Core specifies the settings for [camel-casing property names](system-text-json-how-to.md#use-camel-case-for-all-json-property-names) and case-insensitive matching when it uses `System.Text.Json`.</span></span>

### <a name="minimal-character-escaping"></a><span data-ttu-id="7d4d0-217">最少字元的轉義</span><span class="sxs-lookup"><span data-stu-id="7d4d0-217">Minimal character escaping</span></span>

<span data-ttu-id="7d4d0-218">在序列化期間`Newtonsoft.Json` ，相對於讓字元通過，而不將它們進行轉義，相當寬鬆。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-218">During serialization, `Newtonsoft.Json` is relatively permissive about letting characters through without escaping them.</span></span> <span data-ttu-id="7d4d0-219">也就是說，它不會以`\uxxxx`字元的`xxxx`程式碼點取代它們。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-219">That is, it doesn't replace them with `\uxxxx` where `xxxx` is the character's code point.</span></span> <span data-ttu-id="7d4d0-220">當它將其轉義時，會在字元`\`前面發出（例如， `"`變成`\"`）。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-220">Where it does escape them, it does so by emitting a `\` before the character (for example, `"` becomes `\"`).</span></span> <span data-ttu-id="7d4d0-221"><xref:System.Text.Json>根據預設，會將更多字元轉義以提供跨網站腳本（XSS）或資訊洩漏攻擊的深層防禦保護，並藉由使用六個字元的順序來執行此動作。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-221"><xref:System.Text.Json> escapes more characters by default to provide defense-in-depth protections against cross-site scripting (XSS) or information-disclosure attacks and does so by using the six-character sequence.</span></span> <span data-ttu-id="7d4d0-222">`System.Text.Json`預設會將所有非 ASCII 字元加上轉義，因此如果您在中`StringEscapeHandling.EscapeNonAscii` `Newtonsoft.Json`使用，就不需要執行任何動作。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-222">`System.Text.Json` escapes all non-ASCII characters by default, so you don't need to do anything if you're using `StringEscapeHandling.EscapeNonAscii` in `Newtonsoft.Json`.</span></span> <span data-ttu-id="7d4d0-223">`System.Text.Json`根據預設，也會對 HTML 敏感字元進行轉義。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-223">`System.Text.Json` also escapes HTML-sensitive characters, by default.</span></span> <span data-ttu-id="7d4d0-224">如需如何覆寫預設`System.Text.Json`行為的相關資訊，請參閱[自訂字元編碼](system-text-json-how-to.md#customize-character-encoding)。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-224">For information about how to override the default `System.Text.Json` behavior, see [Customize character encoding](system-text-json-how-to.md#customize-character-encoding).</span></span>

### <a name="comments"></a><span data-ttu-id="7d4d0-225">評價</span><span class="sxs-lookup"><span data-stu-id="7d4d0-225">Comments</span></span>

<span data-ttu-id="7d4d0-226">在還原序列化`Newtonsoft.Json`期間，預設會忽略 JSON 中的批註。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-226">During deserialization, `Newtonsoft.Json` ignores comments in the JSON by default.</span></span> <span data-ttu-id="7d4d0-227"><xref:System.Text.Json>預設為會擲回批註的例外狀況，因為[RFC 8259](https://tools.ietf.org/html/rfc8259)規格不會包含它們。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-227">The <xref:System.Text.Json> default is to throw exceptions for comments because the [RFC 8259](https://tools.ietf.org/html/rfc8259) specification doesn't include them.</span></span> <span data-ttu-id="7d4d0-228">如需如何允許批註的相關資訊，請參閱[允許批註和尾端逗號](system-text-json-how-to.md#allow-comments-and-trailing-commas)。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-228">For information about how to allow comments, see [Allow comments and trailing commas](system-text-json-how-to.md#allow-comments-and-trailing-commas).</span></span>

### <a name="trailing-commas"></a><span data-ttu-id="7d4d0-229">尾端逗號</span><span class="sxs-lookup"><span data-stu-id="7d4d0-229">Trailing commas</span></span>

<span data-ttu-id="7d4d0-230">在還原序列化`Newtonsoft.Json`期間，預設會忽略尾端逗號。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-230">During deserialization, `Newtonsoft.Json` ignores trailing commas by default.</span></span> <span data-ttu-id="7d4d0-231">它也會忽略多個尾端逗號（例如`[{"Color":"Red"},{"Color":"Green"},,]`）。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-231">It also ignores multiple trailing commas (for example, `[{"Color":"Red"},{"Color":"Green"},,]`).</span></span> <span data-ttu-id="7d4d0-232"><xref:System.Text.Json>預設值是擲回尾端逗號的例外狀況，因為[RFC 8259](https://tools.ietf.org/html/rfc8259)規格不允許它們。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-232">The <xref:System.Text.Json> default is to throw exceptions for trailing commas because the [RFC 8259](https://tools.ietf.org/html/rfc8259) specification doesn't allow them.</span></span> <span data-ttu-id="7d4d0-233">如需`System.Text.Json`如何接受這些專案的相關資訊，請參閱[允許批註和尾端逗號](system-text-json-how-to.md#allow-comments-and-trailing-commas)。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-233">For information about how to make `System.Text.Json` accept them, see [Allow comments and trailing commas](system-text-json-how-to.md#allow-comments-and-trailing-commas).</span></span> <span data-ttu-id="7d4d0-234">沒有任何方法可以允許多個尾端逗號。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-234">There's no way to allow multiple trailing commas.</span></span>

### <a name="converter-registration-precedence"></a><span data-ttu-id="7d4d0-235">轉換器註冊優先順序</span><span class="sxs-lookup"><span data-stu-id="7d4d0-235">Converter registration precedence</span></span>

<span data-ttu-id="7d4d0-236">自`Newtonsoft.Json`定義轉換器的註冊優先順序如下所示：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-236">The `Newtonsoft.Json` registration precedence for custom converters is as follows:</span></span>

* <span data-ttu-id="7d4d0-237">屬性上的屬性</span><span class="sxs-lookup"><span data-stu-id="7d4d0-237">Attribute on property</span></span>
* <span data-ttu-id="7d4d0-238">類型上的屬性</span><span class="sxs-lookup"><span data-stu-id="7d4d0-238">Attribute on type</span></span>
* <span data-ttu-id="7d4d0-239">[轉換器](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonSerializerSettings_Converters.htm)集合</span><span class="sxs-lookup"><span data-stu-id="7d4d0-239">[Converters](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonSerializerSettings_Converters.htm) collection</span></span>

<span data-ttu-id="7d4d0-240">此順序表示`Converters`集合中的自訂轉換器會由在類型層級套用屬性所註冊的轉換器覆寫。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-240">This order means that a custom converter in the `Converters` collection is overridden by a converter that is registered by applying an attribute at the type level.</span></span> <span data-ttu-id="7d4d0-241">這兩種註冊都是由屬性層級的屬性所覆寫。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-241">Both of those registrations are overridden by an attribute at the property level.</span></span>

<span data-ttu-id="7d4d0-242">自<xref:System.Text.Json>定義轉換器的註冊優先順序不同：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-242">The <xref:System.Text.Json> registration precedence for custom converters is different:</span></span>

* <span data-ttu-id="7d4d0-243">屬性上的屬性</span><span class="sxs-lookup"><span data-stu-id="7d4d0-243">Attribute on property</span></span>
* <span data-ttu-id="7d4d0-244"><xref:System.Text.Json.JsonSerializerOptions.Converters>集合</span><span class="sxs-lookup"><span data-stu-id="7d4d0-244"><xref:System.Text.Json.JsonSerializerOptions.Converters> collection</span></span>
* <span data-ttu-id="7d4d0-245">類型上的屬性</span><span class="sxs-lookup"><span data-stu-id="7d4d0-245">Attribute on type</span></span>

<span data-ttu-id="7d4d0-246">此處的差異在於， `Converters`集合中的自訂轉換器會覆寫類型層級的屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-246">The difference here is that a custom converter in the `Converters` collection overrides an attribute at the type level.</span></span> <span data-ttu-id="7d4d0-247">此優先順序的目的是要讓執行時間變更覆寫設計階段的選擇。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-247">The intention behind this order of precedence is to make run-time changes override design-time choices.</span></span> <span data-ttu-id="7d4d0-248">沒有任何方法可以變更優先順序。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-248">There's no way to change the precedence.</span></span>

<span data-ttu-id="7d4d0-249">如需自訂轉換器註冊的詳細資訊，請參閱[註冊自訂轉換子](system-text-json-converters-how-to.md#register-a-custom-converter)。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-249">For more information about custom converter registration, see [Register a custom converter](system-text-json-converters-how-to.md#register-a-custom-converter).</span></span>

### <a name="maximum-depth"></a><span data-ttu-id="7d4d0-250">最大深度</span><span class="sxs-lookup"><span data-stu-id="7d4d0-250">Maximum depth</span></span>

<span data-ttu-id="7d4d0-251">`Newtonsoft.Json`預設不具有最大深度限制。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-251">`Newtonsoft.Json` doesn't have a maximum depth limit by default.</span></span> <span data-ttu-id="7d4d0-252">預設<xref:System.Text.Json>限制為64，且可透過設定來進行設定<xref:System.Text.Json.JsonSerializerOptions.MaxDepth?displayProperty=nameWithType>。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-252">For <xref:System.Text.Json> there's a default limit  of 64, and it's configurable by setting <xref:System.Text.Json.JsonSerializerOptions.MaxDepth?displayProperty=nameWithType>.</span></span>

### <a name="json-strings-property-names-and-string-values"></a><span data-ttu-id="7d4d0-253">JSON 字串（屬性名稱和字串值）</span><span class="sxs-lookup"><span data-stu-id="7d4d0-253">JSON strings (property names and string values)</span></span>

<span data-ttu-id="7d4d0-254">在還原序列化`Newtonsoft.Json`期間，接受以雙引號、單引號或不含引號括住的屬性名稱。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-254">During deserialization, `Newtonsoft.Json` accepts property names surrounded by double quotes, single quotes, or without quotes.</span></span> <span data-ttu-id="7d4d0-255">它接受以雙引號或單引號括住的字串值。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-255">It accepts string values surrounded by double quotes or single quotes.</span></span> <span data-ttu-id="7d4d0-256">例如， `Newtonsoft.Json`接受下列 JSON：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-256">For example, `Newtonsoft.Json` accepts the following JSON:</span></span>

```json
{
  "name1": "value",
  'name2': "value",
  name3: 'value'
}
```

<span data-ttu-id="7d4d0-257">`System.Text.Json`只接受以雙引號括住的屬性名稱和字串值，因為[RFC 8259](https://tools.ietf.org/html/rfc8259)規格需要該格式，而且是唯一視為有效 JSON 的格式。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-257">`System.Text.Json` only accepts property names and string values in double quotes because that format is required by the [RFC 8259](https://tools.ietf.org/html/rfc8259) specification and is the only format considered valid JSON.</span></span>

<span data-ttu-id="7d4d0-258">以單引號括住的值會產生含有下列訊息的[JsonException](xref:System.Text.Json.JsonException) ：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-258">A value enclosed in single quotes results in a [JsonException](xref:System.Text.Json.JsonException) with the following message:</span></span>

```
''' is an invalid start of a value.
```

### <a name="non-string-values-for-string-properties"></a><span data-ttu-id="7d4d0-259">字串屬性的非字串值</span><span class="sxs-lookup"><span data-stu-id="7d4d0-259">Non-string values for string properties</span></span>

<span data-ttu-id="7d4d0-260">`Newtonsoft.Json`接受非字串值（例如數位或常`true`值和`false`），以還原序列化為 string 類型的屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-260">`Newtonsoft.Json` accepts non-string values, such as a number or the literals `true` and `false`, for deserialization to properties of type string.</span></span> <span data-ttu-id="7d4d0-261">以下是可`Newtonsoft.Json`成功還原序列化為下列類別的 JSON 範例：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-261">Here's an example of JSON that `Newtonsoft.Json` successfully deserializes to the following class:</span></span>

```json
{
  "String1": 1,
  "String2": true,
  "String3": false
}
```

```csharp
public class ExampleClass
{
    public string String1 { get; set; }
    public string String2 { get; set; }
    public string String3 { get; set; }
}
```

<span data-ttu-id="7d4d0-262">`System.Text.Json`不會將非字串值還原序列化成字串屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-262">`System.Text.Json` doesn't deserialize non-string values into string properties.</span></span> <span data-ttu-id="7d4d0-263">針對字串欄位接收的非字串值會導致[JsonException](xref:System.Text.Json.JsonException) ，並出現下列訊息：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-263">A non-string value received for a string field results in a [JsonException](xref:System.Text.Json.JsonException) with the following message:</span></span>

```
The JSON value could not be converted to System.String.
```

## <a name="scenarios-using-jsonserializer-that-require-workarounds"></a><span data-ttu-id="7d4d0-264">使用需要因應措施之 JsonSerializer 的案例</span><span class="sxs-lookup"><span data-stu-id="7d4d0-264">Scenarios using JsonSerializer that require workarounds</span></span>

<span data-ttu-id="7d4d0-265">內建功能不支援下列案例，但可能的因應措施。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-265">The following scenarios aren't supported by built-in functionality, but workarounds are possible.</span></span> <span data-ttu-id="7d4d0-266">因應措施是[自訂轉換器](system-text-json-converters-how-to.md)，這可能不會提供`Newtonsoft.Json`完整的同位檢查功能。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-266">The workarounds are [custom converters](system-text-json-converters-how-to.md), which may not provide complete parity with `Newtonsoft.Json` functionality.</span></span> <span data-ttu-id="7d4d0-267">其中一些範例程式碼會以範例的形式提供。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-267">For some of these, sample code is provided as examples.</span></span> <span data-ttu-id="7d4d0-268">如果您依賴這些`Newtonsoft.Json`功能，則需要修改您的 .net 物件模型或其他程式碼變更。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-268">If you rely on these `Newtonsoft.Json` features, migration will require modifications to your .NET object models or other code changes.</span></span>

### <a name="types-without-built-in-support"></a><span data-ttu-id="7d4d0-269">沒有內建支援的類型</span><span class="sxs-lookup"><span data-stu-id="7d4d0-269">Types without built-in support</span></span>

<span data-ttu-id="7d4d0-270"><xref:System.Text.Json>不提供下列類型的內建支援：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-270"><xref:System.Text.Json> doesn't provide built-in support for the following types:</span></span>

* <span data-ttu-id="7d4d0-271"><xref:System.Data.DataTable>和相關類型</span><span class="sxs-lookup"><span data-stu-id="7d4d0-271"><xref:System.Data.DataTable> and related types</span></span>
* <span data-ttu-id="7d4d0-272">F # 類型，例如[區分](../../fsharp/language-reference/discriminated-unions.md)等位、[記錄類型](../../fsharp/language-reference/records.md)和[匿名記錄類型](../../fsharp/language-reference/anonymous-records.md)。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-272">F# types, such as [discriminated unions](../../fsharp/language-reference/discriminated-unions.md), [record types](../../fsharp/language-reference/records.md), and [anonymous record types](../../fsharp/language-reference/anonymous-records.md).</span></span>
* <xref:System.Dynamic.ExpandoObject>
* <xref:System.TimeZoneInfo>
* <xref:System.Numerics.BigInteger>
* <xref:System.TimeSpan>
* <xref:System.DBNull>
* <xref:System.Type>
* <span data-ttu-id="7d4d0-273"><xref:System.ValueTuple>及其相關聯的泛型型別</span><span class="sxs-lookup"><span data-stu-id="7d4d0-273"><xref:System.ValueTuple> and its associated generic types</span></span>

<span data-ttu-id="7d4d0-274">針對沒有內建支援的類型，可以實作為自訂轉換器。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-274">Custom converters can be implemented for types that don't have built-in support.</span></span>

### <a name="quoted-numbers"></a><span data-ttu-id="7d4d0-275">加上引號的數位</span><span class="sxs-lookup"><span data-stu-id="7d4d0-275">Quoted numbers</span></span>

<span data-ttu-id="7d4d0-276">`Newtonsoft.Json`可以序列化或還原序列化 JSON 字串所代表的數位（以引號括住）。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-276">`Newtonsoft.Json` can serialize or deserialize numbers represented by JSON strings (surrounded by quotes).</span></span> <span data-ttu-id="7d4d0-277">例如，它可以接受： `{"DegreesCelsius":"23"}`而不是`{"DegreesCelsius":23}`。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-277">For example, it can accept: `{"DegreesCelsius":"23"}` instead of `{"DegreesCelsius":23}`.</span></span> <span data-ttu-id="7d4d0-278">若要在中<xref:System.Text.Json>啟用該行為，請執行如下列範例所示的自訂轉換子。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-278">To enable that behavior in <xref:System.Text.Json>, implement a custom converter like the following example.</span></span> <span data-ttu-id="7d4d0-279">轉換器會處理定義為的`long`屬性：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-279">The converter handles properties defined as `long`:</span></span>

* <span data-ttu-id="7d4d0-280">它會將它們序列化為 JSON 字串。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-280">It serializes them as JSON strings.</span></span>
* <span data-ttu-id="7d4d0-281">它會在還原序列化時，接受引號內的 JSON 數位和數位。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-281">It accepts JSON numbers and numbers within quotes while deserializing.</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/LongToStringConverter.cs)]

<span data-ttu-id="7d4d0-282">使用個別`long`屬性上的[屬性](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-property)，或[將轉換器加入](system-text-json-converters-how-to.md#registration-sample---converters-collection)至<xref:System.Text.Json.JsonSerializerOptions.Converters>集合，以註冊此自訂轉換器。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-282">Register this custom converter by [using an attribute](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-property) on individual `long` properties or by [adding the converter](system-text-json-converters-how-to.md#registration-sample---converters-collection) to the <xref:System.Text.Json.JsonSerializerOptions.Converters> collection.</span></span>

### <a name="dictionary-with-non-string-key"></a><span data-ttu-id="7d4d0-283">具有非字串索引鍵的字典</span><span class="sxs-lookup"><span data-stu-id="7d4d0-283">Dictionary with non-string key</span></span>

<span data-ttu-id="7d4d0-284">`Newtonsoft.Json`支援類型`Dictionary<TKey, TValue>`的集合。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-284">`Newtonsoft.Json` supports collections of type `Dictionary<TKey, TValue>`.</span></span> <span data-ttu-id="7d4d0-285">內<xref:System.Text.Json>建的字典集合支援僅限於`Dictionary<string, TValue>`。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-285">The built-in support for dictionary collections in <xref:System.Text.Json> is limited to `Dictionary<string, TValue>`.</span></span> <span data-ttu-id="7d4d0-286">也就是，索引鍵必須是字串。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-286">That is, the key must be a string.</span></span>

<span data-ttu-id="7d4d0-287">若要支援具有整數或其他類型做為索引鍵的字典，請建立如[如何撰寫自訂轉換器](system-text-json-converters-how-to.md#support-dictionary-with-non-string-key)中的範例所示的轉換器。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-287">To support a dictionary with an integer or some other type as the key, create a converter like the example in [How to write custom converters](system-text-json-converters-how-to.md#support-dictionary-with-non-string-key).</span></span>

### <a name="polymorphic-serialization"></a><span data-ttu-id="7d4d0-288">多型序列化</span><span class="sxs-lookup"><span data-stu-id="7d4d0-288">Polymorphic serialization</span></span>

<span data-ttu-id="7d4d0-289">`Newtonsoft.Json`自動執行多型序列化。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-289">`Newtonsoft.Json` automatically does polymorphic serialization.</span></span> <span data-ttu-id="7d4d0-290">如需的有限多型序列化功能的<xref:System.Text.Json>相關資訊，請參閱[序列化衍生類別的屬性](system-text-json-how-to.md#serialize-properties-of-derived-classes)。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-290">For information about the limited polymorphic serialization capabilities of <xref:System.Text.Json>, see [Serialize properties of derived classes](system-text-json-how-to.md#serialize-properties-of-derived-classes).</span></span>

<span data-ttu-id="7d4d0-291">此處所述的因應措施，是定義可能包含衍生類別作為`object`類型的屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-291">The workaround described there is to define properties that may contain derived classes as type `object`.</span></span> <span data-ttu-id="7d4d0-292">如果無法這樣做，另一個選項是使用整個繼承類型階層`Write`的方法來建立轉換器，如[如何撰寫自訂轉換器](system-text-json-converters-how-to.md#support-polymorphic-deserialization)中的範例所示。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-292">If that isn't possible, another option is to create a converter with a `Write` method for the whole inheritance type hierarchy like the example in [How to write custom converters](system-text-json-converters-how-to.md#support-polymorphic-deserialization).</span></span>

### <a name="polymorphic-deserialization"></a><span data-ttu-id="7d4d0-293">多型還原序列化</span><span class="sxs-lookup"><span data-stu-id="7d4d0-293">Polymorphic deserialization</span></span>

<span data-ttu-id="7d4d0-294">`Newtonsoft.Json`具有在`TypeNameHandling`序列化時將類型名稱中繼資料新增至 JSON 的設定。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-294">`Newtonsoft.Json` has a `TypeNameHandling` setting that adds type name metadata to the JSON while serializing.</span></span> <span data-ttu-id="7d4d0-295">它會在還原序列化時使用中繼資料來執行多型還原序列化。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-295">It uses the metadata while deserializing to do polymorphic deserialization.</span></span> <span data-ttu-id="7d4d0-296"><xref:System.Text.Json>可以執行有限範圍的多型序列化，但不能進行多型的還原[序列化](system-text-json-how-to.md#serialize-properties-of-derived-classes)。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-296"><xref:System.Text.Json> can do a limited range of [polymorphic serialization](system-text-json-how-to.md#serialize-properties-of-derived-classes) but not polymorphic deserialization.</span></span>

<span data-ttu-id="7d4d0-297">若要支援多型還原序列化，請建立如[如何撰寫自訂轉換器](system-text-json-converters-how-to.md#support-polymorphic-deserialization)中的範例所示的轉換器。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-297">To support polymorphic deserialization, create a converter like the example in [How to write custom converters](system-text-json-converters-how-to.md#support-polymorphic-deserialization).</span></span>

### <a name="deserialization-of-object-properties"></a><span data-ttu-id="7d4d0-298">物件屬性的還原序列化</span><span class="sxs-lookup"><span data-stu-id="7d4d0-298">Deserialization of object properties</span></span>

<span data-ttu-id="7d4d0-299">當`Newtonsoft.Json`還原序列化<xref:System.Object>為時，它會：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-299">When `Newtonsoft.Json` deserializes to <xref:System.Object>, it:</span></span>

* <span data-ttu-id="7d4d0-300">推斷 JSON 裝載中基本值的類型（不是`null`），並以已裝箱的`string`物件`long`傳回`double`預`boolean`存、 `DateTime` 、、或。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-300">Infers the type of primitive values in the JSON payload (other than `null`) and returns the stored `string`, `long`, `double`, `boolean`, or `DateTime` as a boxed object.</span></span> <span data-ttu-id="7d4d0-301">*基本值*是單一 json 值，例如 json 數位、字串、 `true` `false`、或。 `null`</span><span class="sxs-lookup"><span data-stu-id="7d4d0-301">*Primitive values* are single JSON values such as a JSON number, string, `true`, `false`, or `null`.</span></span>
* <span data-ttu-id="7d4d0-302">針對 JSON `JObject`承載`JArray`中的複雜值，傳回或。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-302">Returns a `JObject` or `JArray` for complex values in the JSON payload.</span></span> <span data-ttu-id="7d4d0-303">*複雜值*是以括弧（`{}`）括住的 JSON 索引鍵/值組集合，或括弧內`[]`的值清單（）。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-303">*Complex values* are collections of JSON key-value pairs within braces (`{}`) or lists of values within brackets (`[]`).</span></span> <span data-ttu-id="7d4d0-304">大括弧或括弧內的屬性和值可以有額外的屬性或值。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-304">The properties and values within the braces or brackets can have additional properties or values.</span></span>
* <span data-ttu-id="7d4d0-305">當裝載具有`null` JSON 常值時，會傳回 null 參考。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-305">Returns a null reference when the payload has the `null` JSON literal.</span></span>

<span data-ttu-id="7d4d0-306"><xref:System.Text.Json>會在還原`JsonElement`序列化為<xref:System.Object>時，儲存基本和複雜值的已裝箱，例如：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-306"><xref:System.Text.Json> stores a boxed `JsonElement` for both primitive and complex values whenever deserializing to <xref:System.Object>, for example:</span></span>

* <span data-ttu-id="7d4d0-307">`object` 屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-307">An `object` property.</span></span>
* <span data-ttu-id="7d4d0-308">`object`字典值。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-308">An `object` dictionary value.</span></span>
* <span data-ttu-id="7d4d0-309">`object`陣列值。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-309">An `object` array value.</span></span>
* <span data-ttu-id="7d4d0-310">根`object`。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-310">A root `object`.</span></span>

<span data-ttu-id="7d4d0-311">不過， `System.Text.Json`會`null`將相同的`Newtonsoft.Json`視為，並在裝載具有`null` JSON 常值時傳回 null 參考。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-311">However, `System.Text.Json` treats `null` the same as `Newtonsoft.Json` and returns a null reference when the payload has the `null` JSON literal in it.</span></span>

<span data-ttu-id="7d4d0-312">若要執行屬性的`object`型別推斷，請建立如[如何撰寫自訂轉換器](system-text-json-converters-how-to.md#deserialize-inferred-types-to-object-properties)中的範例所示的轉換器。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-312">To implement type inference for `object` properties, create a converter like the example in [How to write custom converters](system-text-json-converters-how-to.md#deserialize-inferred-types-to-object-properties).</span></span>

### <a name="deserialize-null-to-non-nullable-type"></a><span data-ttu-id="7d4d0-313">將 null 還原序列化為不可為 null 的類型</span><span class="sxs-lookup"><span data-stu-id="7d4d0-313">Deserialize null to non-nullable type</span></span>

<span data-ttu-id="7d4d0-314">`Newtonsoft.Json`在下列案例中，不會擲回例外狀況：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-314">`Newtonsoft.Json` doesn't throw an exception in the following scenario:</span></span>

* <span data-ttu-id="7d4d0-315">`NullValueHandling`設定為`Ignore`，而</span><span class="sxs-lookup"><span data-stu-id="7d4d0-315">`NullValueHandling` is set to `Ignore`, and</span></span>
* <span data-ttu-id="7d4d0-316">在還原序列化期間，JSON 會針對不可為 null 的實數值型別包含 null 值。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-316">During deserialization, the JSON contains a null value for a non-nullable value type.</span></span>

<span data-ttu-id="7d4d0-317">在相同的案例中<xref:System.Text.Json> ，會擲回例外狀況。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-317">In the same scenario, <xref:System.Text.Json> does throw an exception.</span></span> <span data-ttu-id="7d4d0-318">（對應的 null 處理設定為<xref:System.Text.Json.JsonSerializerOptions.IgnoreNullValues?displayProperty=nameWithType>。）</span><span class="sxs-lookup"><span data-stu-id="7d4d0-318">(The corresponding null handling setting is <xref:System.Text.Json.JsonSerializerOptions.IgnoreNullValues?displayProperty=nameWithType>.)</span></span>

<span data-ttu-id="7d4d0-319">如果您擁有目標型別，最佳的解決方法是讓屬性成為可為 null （例如，變更`int`為`int?`）。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-319">If you own the target type, the best workaround is to make the property in question nullable (for example, change `int` to `int?`).</span></span>

<span data-ttu-id="7d4d0-320">另一個因應措施是建立類型的轉換器，例如下列範例，其會處理`DateTimeOffset`類型的 null 值：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-320">Another workaround is to make a converter for the type, such as the following example that handles null values for `DateTimeOffset` types:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/DateTimeOffsetNullHandlingConverter.cs)]

<span data-ttu-id="7d4d0-321">藉由[使用屬性上的屬性](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-property)，或[將轉換器加入](system-text-json-converters-how-to.md#registration-sample---converters-collection)至<xref:System.Text.Json.JsonSerializerOptions.Converters>集合，來註冊此自訂轉換子。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-321">Register this custom converter by [using an attribute on the property](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-property) or by [adding the converter](system-text-json-converters-how-to.md#registration-sample---converters-collection) to the <xref:System.Text.Json.JsonSerializerOptions.Converters> collection.</span></span>

<span data-ttu-id="7d4d0-322">**注意：** 前面的轉換器**處理 null 值**的方式`Newtonsoft.Json`與指定預設值的 poco 不同。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-322">**Note:** The preceding converter **handles null values differently** than `Newtonsoft.Json` does for POCOs that specify default values.</span></span> <span data-ttu-id="7d4d0-323">例如，假設下列程式碼代表您的目標物件：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-323">For example, suppose the following code represents your target object:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/WeatherForecast.cs?name=SnippetWFWithDefault)]

<span data-ttu-id="7d4d0-324">而且假設下列 JSON 會使用上述的轉換器還原序列化：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-324">And suppose the following JSON is deserialized by using the preceding converter:</span></span>

```json
{
  "Date": null,
  "TemperatureCelsius": 25,
  "Summary": null
}
```

<span data-ttu-id="7d4d0-325">在還原序列化之後`Date` ，屬性具有 1/1/0001`default(DateTimeOffset)`（），也就是會覆寫在此函式中設定的值。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-325">After deserialization, the `Date` property has 1/1/0001 (`default(DateTimeOffset)`), that is, the value set in the constructor is overwritten.</span></span> <span data-ttu-id="7d4d0-326">假設有相同的 POCO 和 JSON `Newtonsoft.Json` ，還原序列化會在`Date`屬性中留下1/1/2001。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-326">Given the same POCO and JSON, `Newtonsoft.Json` deserialization would leave 1/1/2001 in the `Date` property.</span></span>

### <a name="deserialize-to-immutable-classes-and-structs"></a><span data-ttu-id="7d4d0-327">還原序列化為不可變的類別和結構</span><span class="sxs-lookup"><span data-stu-id="7d4d0-327">Deserialize to immutable classes and structs</span></span>

<span data-ttu-id="7d4d0-328">`Newtonsoft.Json`可以還原序列化為不可變的類別和結構，因為它可以使用具有參數的函數。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-328">`Newtonsoft.Json` can deserialize to immutable classes and structs because it can use constructors that have parameters.</span></span> <span data-ttu-id="7d4d0-329"><xref:System.Text.Json>僅支援公用無參數的函式。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-329"><xref:System.Text.Json> supports only public parameterless constructors.</span></span> <span data-ttu-id="7d4d0-330">因應措施是，您可以在自訂轉換子中呼叫具有參數的函式。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-330">As a workaround, you can call a constructor with parameters in a custom converter.</span></span>

<span data-ttu-id="7d4d0-331">以下是具有多個函數參數的不可變結構：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-331">Here's an immutable struct with multiple constructor parameters:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/ImmutablePoint.cs#ImmutablePoint)]

<span data-ttu-id="7d4d0-332">以下是將此結構序列化和還原序列化的轉換器：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-332">And here's a converter that serializes and deserializes this struct:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/ImmutablePointConverter.cs)]

<span data-ttu-id="7d4d0-333">藉由[將轉換器加入](system-text-json-converters-how-to.md#registration-sample---converters-collection) <xref:System.Text.Json.JsonSerializerOptions.Converters>集合中，來註冊此自訂的轉換器。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-333">Register this custom converter by [adding the converter](system-text-json-converters-how-to.md#registration-sample---converters-collection) to the <xref:System.Text.Json.JsonSerializerOptions.Converters> collection.</span></span>

<span data-ttu-id="7d4d0-334">如需處理開放式泛型屬性之類似轉換器的範例，請參閱索引[鍵/值組的內建轉換器](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/libraries/System.Text.Json/src/System/Text/Json/Serialization/Converters/JsonValueConverterKeyValuePair.cs)。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-334">For an example of a similar converter that handles open generic properties, see the [built-in converter for key-value pairs](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/libraries/System.Text.Json/src/System/Text/Json/Serialization/Converters/JsonValueConverterKeyValuePair.cs).</span></span>

### <a name="specify-constructor-to-use"></a><span data-ttu-id="7d4d0-335">指定要使用的構造函式</span><span class="sxs-lookup"><span data-stu-id="7d4d0-335">Specify constructor to use</span></span>

<span data-ttu-id="7d4d0-336">`Newtonsoft.Json`屬性可讓您指定在還原序列化為 POCO 時要呼叫的函`[JsonConstructor]`式。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-336">The `Newtonsoft.Json` `[JsonConstructor]` attribute lets you specify which constructor to call when deserializing to a POCO.</span></span> <span data-ttu-id="7d4d0-337"><xref:System.Text.Json>僅支援無參數的函式。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-337"><xref:System.Text.Json> supports only parameterless constructors.</span></span> <span data-ttu-id="7d4d0-338">因應措施是，您可以在自訂轉換器中呼叫所需的任何一個方法。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-338">As a workaround, you can call whichever constructor you need in a custom converter.</span></span> <span data-ttu-id="7d4d0-339">請參閱還原序列化[為不可變類別和結構](#deserialize-to-immutable-classes-and-structs)的範例。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-339">See the example for [Deserialize to immutable classes and structs](#deserialize-to-immutable-classes-and-structs).</span></span>

### <a name="required-properties"></a><span data-ttu-id="7d4d0-340">必要屬性</span><span class="sxs-lookup"><span data-stu-id="7d4d0-340">Required properties</span></span>

<span data-ttu-id="7d4d0-341">在`Newtonsoft.Json`中，您可以透過在`Required` `[JsonProperty]`屬性上設定來指定屬性是必要的。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-341">In `Newtonsoft.Json`, you specify that a property is required by setting `Required` on the `[JsonProperty]` attribute.</span></span> <span data-ttu-id="7d4d0-342">`Newtonsoft.Json`如果在 JSON 中未收到標示為必要的屬性值，則會擲回例外狀況。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-342">`Newtonsoft.Json` throws an exception if no value is received in the JSON for a property marked as required.</span></span>

<span data-ttu-id="7d4d0-343"><xref:System.Text.Json>如果沒有針對目標型別的其中一個屬性收到任何值，則不會擲回例外狀況。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-343"><xref:System.Text.Json> doesn't throw an exception if no value is received for one of the properties of the target type.</span></span> <span data-ttu-id="7d4d0-344">例如，如果您有一個`WeatherForecast`類別：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-344">For example, if you have a `WeatherForecast` class:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/WeatherForecast.cs?name=SnippetWF)]

<span data-ttu-id="7d4d0-345">下列 JSON 會進行還原序列化，而不會發生錯誤：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-345">The following JSON is deserialized without error:</span></span>

```json
{
    "TemperatureCelsius": 25,
    "Summary": "Hot"
}
```

<span data-ttu-id="7d4d0-346">若要讓還原序列化失敗`Date` ，如果 JSON 中沒有屬性，請執行自訂轉換子。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-346">To make deserialization fail if no `Date` property is in the JSON, implement a custom converter.</span></span> <span data-ttu-id="7d4d0-347">如果在還原序列化完成後未設定此`Date`屬性，下列範例轉換器程式碼會擲回例外狀況：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-347">The following sample converter code throws an exception if the `Date` property isn't set after deserialization is complete:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/WeatherForecastRequiredPropertyConverter.cs)]

<span data-ttu-id="7d4d0-348">[使用 POCO 類別上的屬性](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-type)，或[將轉換器加入](system-text-json-converters-how-to.md#registration-sample---converters-collection)至<xref:System.Text.Json.JsonSerializerOptions.Converters>集合，以註冊此自訂轉換器。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-348">Register this custom converter by [using an attribute on the POCO class](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-type) or by [adding the converter](system-text-json-converters-how-to.md#registration-sample---converters-collection) to the <xref:System.Text.Json.JsonSerializerOptions.Converters> collection.</span></span>

<span data-ttu-id="7d4d0-349">如果您遵循此模式，請不要在遞迴呼叫<xref:System.Text.Json.JsonSerializer.Serialize%2A>或<xref:System.Text.Json.JsonSerializer.Deserialize%2A>時傳入 options 物件。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-349">If you follow this pattern, don't pass in the options object when recursively calling <xref:System.Text.Json.JsonSerializer.Serialize%2A> or <xref:System.Text.Json.JsonSerializer.Deserialize%2A>.</span></span> <span data-ttu-id="7d4d0-350">Options 物件包含<xref:System.Text.Json.JsonSerializerOptions.Converters%2A>集合。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-350">The options object contains the <xref:System.Text.Json.JsonSerializerOptions.Converters%2A> collection.</span></span> <span data-ttu-id="7d4d0-351">如果您將它傳遞給`Serialize`或`Deserialize`，自訂轉換器會呼叫自己的，使無限迴圈產生堆疊溢位例外狀況。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-351">If you pass it in to `Serialize` or `Deserialize`, the custom converter calls into itself, making an infinite loop that results in a stack overflow exception.</span></span> <span data-ttu-id="7d4d0-352">如果預設選項不可行，請使用您所需的設定來建立選項的新實例。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-352">If the default options are not feasible, create a new instance of the options with the settings that you need.</span></span> <span data-ttu-id="7d4d0-353">這個方法將會變慢，因為每個新的實例會獨立快取。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-353">This approach will be slow since each new instance caches independently.</span></span>

<span data-ttu-id="7d4d0-354">上述的轉換器程式碼是簡化的範例。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-354">The preceding converter code is a simplified example.</span></span> <span data-ttu-id="7d4d0-355">如果您需要處理屬性（例如[[JsonIgnore]](xref:System.Text.Json.Serialization.JsonIgnoreAttribute)或不同的選項（例如自訂編碼器），則需要額外的邏輯。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-355">Additional logic would be required if you need to handle attributes (such as [[JsonIgnore]](xref:System.Text.Json.Serialization.JsonIgnoreAttribute) or different options (such as custom encoders).</span></span> <span data-ttu-id="7d4d0-356">此外，範例程式碼不會處理在此函式中設定預設值的屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-356">Also, the example code doesn't handle properties for which a default value is set in the constructor.</span></span> <span data-ttu-id="7d4d0-357">而這種方法不會區分下列案例：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-357">And this approach doesn't differentiate between the following scenarios:</span></span>

* <span data-ttu-id="7d4d0-358">JSON 中遺漏了屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-358">A property is missing from the JSON.</span></span>
* <span data-ttu-id="7d4d0-359">JSON 中有不可為 null 類型的屬性，但值為類型的預設值，例如為零`int`。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-359">A property for a non-nullable type is present in the JSON, but the value is the default for the type, such as zero for an `int`.</span></span>
* <span data-ttu-id="7d4d0-360">JSON 中有可為 null 的實數值型別的屬性，但值為 null。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-360">A property for a nullable value type is present in the JSON, but the value is null.</span></span>

### <a name="conditionally-ignore-a-property"></a><span data-ttu-id="7d4d0-361">有條件地忽略屬性</span><span class="sxs-lookup"><span data-stu-id="7d4d0-361">Conditionally ignore a property</span></span>

<span data-ttu-id="7d4d0-362">`Newtonsoft.Json`有數種方式可以有條件地忽略序列化或還原序列化上的屬性：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-362">`Newtonsoft.Json` has several ways to conditionally ignore a property on serialization or deserialization:</span></span>

* <span data-ttu-id="7d4d0-363">`DefaultContractResolver`可讓您根據任意條件，選取要包含或排除的屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-363">`DefaultContractResolver` lets you select properties to include or exclude, based on arbitrary criteria.</span></span>
* <span data-ttu-id="7d4d0-364">上`NullValueHandling` `JsonSerializerSettings`的`DefaultValueHandling`和設定可讓您指定應該忽略所有的 null 值或預設值屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-364">The `NullValueHandling` and `DefaultValueHandling` settings on `JsonSerializerSettings` let you specify that all null-value or default-value properties should be ignored.</span></span>
* <span data-ttu-id="7d4d0-365">屬性的`NullValueHandling`和`DefaultValueHandling`設定可讓您指定在設定為 null 或預設值時，應該忽略的個別屬性。 `[JsonProperty]`</span><span class="sxs-lookup"><span data-stu-id="7d4d0-365">The `NullValueHandling` and `DefaultValueHandling` settings on the `[JsonProperty]` attribute let you specify individual properties that should be ignored when set to null or the default value.</span></span>

<span data-ttu-id="7d4d0-366"><xref:System.Text.Json>提供下列方法來在序列化時省略屬性：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-366"><xref:System.Text.Json> provides the following ways to omit properties while serializing:</span></span>

* <span data-ttu-id="7d4d0-367">屬性[（property）上的 [JsonIgnore] 屬性（attribute）](system-text-json-how-to.md#exclude-individual-properties)會在序列化期間，從 JSON 中省略屬性（property）。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-367">The [[JsonIgnore]](system-text-json-how-to.md#exclude-individual-properties) attribute on a property causes the property to be omitted from the JSON during serialization.</span></span>
* <span data-ttu-id="7d4d0-368">[IgnoreNullValues](system-text-json-how-to.md#exclude-all-null-value-properties) global 選項可讓您排除所有 null 值屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-368">The [IgnoreNullValues](system-text-json-how-to.md#exclude-all-null-value-properties) global option lets you exclude all null-value properties.</span></span>
* <span data-ttu-id="7d4d0-369">[IgnoreReadOnlyProperties](system-text-json-how-to.md#exclude-all-read-only-properties) global 選項可讓您排除所有唯讀屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-369">The [IgnoreReadOnlyProperties](system-text-json-how-to.md#exclude-all-read-only-properties) global option lets you exclude all read-only properties.</span></span>

<span data-ttu-id="7d4d0-370">這些選項**不**會讓您：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-370">These options **don't** let you:</span></span>

* <span data-ttu-id="7d4d0-371">忽略所有具有類型之預設值的屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-371">Ignore all properties that have the default value for the type.</span></span>
* <span data-ttu-id="7d4d0-372">忽略具有類型之預設值的選取屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-372">Ignore selected properties that have the default value for the type.</span></span>
* <span data-ttu-id="7d4d0-373">如果其值為 null，則忽略選取的屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-373">Ignore selected properties if their value is null.</span></span>
* <span data-ttu-id="7d4d0-374">根據在執行時間評估的任意準則，忽略選取的屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-374">Ignore selected properties based on arbitrary criteria evaluated at run time.</span></span>

<span data-ttu-id="7d4d0-375">針對該功能，您可以撰寫自訂的轉換器。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-375">For that functionality, you can write a custom converter.</span></span> <span data-ttu-id="7d4d0-376">以下是範例 POCO 和適用于它的自訂轉換器，其說明此方法：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-376">Here's a sample POCO and a custom converter for it that illustrates this approach:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/WeatherForecast.cs?name=SnippetWF)]

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/WeatherForecastRuntimeIgnoreConverter.cs)]

<span data-ttu-id="7d4d0-377">如果參數的值`Summary`為 null、空字串或 "N/A"，則轉換器會使屬性從序列化中省略。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-377">The converter causes the `Summary` property to be omitted from serialization if its value is null, an empty string, or "N/A".</span></span>

<span data-ttu-id="7d4d0-378">藉由[使用類別上的屬性](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-type)，或[將轉換器加入](system-text-json-converters-how-to.md#registration-sample---converters-collection)至<xref:System.Text.Json.JsonSerializerOptions.Converters>集合，來註冊此自訂轉換子。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-378">Register this custom converter by [using an attribute on the class](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-type) or by [adding the converter](system-text-json-converters-how-to.md#registration-sample---converters-collection) to the <xref:System.Text.Json.JsonSerializerOptions.Converters> collection.</span></span>

<span data-ttu-id="7d4d0-379">這種方法需要額外的邏輯，如下所示：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-379">This approach requires additional logic if:</span></span>

* <span data-ttu-id="7d4d0-380">POCO 包含複雜的屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-380">The POCO includes complex properties.</span></span>
* <span data-ttu-id="7d4d0-381">您需要處理屬性（例如） `[JsonIgnore]`或選項（例如自訂編碼器）。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-381">You need to handle attributes such as `[JsonIgnore]` or options such as custom encoders.</span></span>

### <a name="specify-date-format"></a><span data-ttu-id="7d4d0-382">指定日期格式</span><span class="sxs-lookup"><span data-stu-id="7d4d0-382">Specify date format</span></span>

<span data-ttu-id="7d4d0-383">`Newtonsoft.Json`提供數種方式來控制和`DateTime` `DateTimeOffset`類型的屬性如何序列化和還原序列化：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-383">`Newtonsoft.Json` provides several ways to control how properties of `DateTime` and `DateTimeOffset` types are serialized and deserialized:</span></span>

* <span data-ttu-id="7d4d0-384">`DateTimeZoneHandling`設定可以用來將所有`DateTime`值序列化為 UTC 日期。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-384">The `DateTimeZoneHandling` setting can be used to serialize all `DateTime` values as UTC dates.</span></span>
* <span data-ttu-id="7d4d0-385">`DateFormatString`設定和`DateTime`轉換器可以用來自訂日期字串的格式。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-385">The `DateFormatString` setting and `DateTime` converters can be used to customize the format of date strings.</span></span>

<span data-ttu-id="7d4d0-386">在<xref:System.Text.Json>中，具有內建支援的唯一格式是 ISO 8601-1:2019，因為它廣泛採用、明確，並會精確地進行往返。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-386">In <xref:System.Text.Json>, the only format that has built-in support is ISO 8601-1:2019 since it's widely adopted, unambiguous, and makes round trips precisely.</span></span> <span data-ttu-id="7d4d0-387">若要使用任何其他格式，請建立自訂轉換器。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-387">To use any other format, create a custom converter.</span></span> <span data-ttu-id="7d4d0-388">如需詳細資訊，請參閱[system.object 中的 DateTime 和 DateTimeOffset 支援](../datetime/system-text-json-support.md)。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-388">For more information, see [DateTime and DateTimeOffset support in System.Text.Json](../datetime/system-text-json-support.md).</span></span>

### <a name="callbacks"></a><span data-ttu-id="7d4d0-389">回撥</span><span class="sxs-lookup"><span data-stu-id="7d4d0-389">Callbacks</span></span>

<span data-ttu-id="7d4d0-390">`Newtonsoft.Json`可讓您在序列化或還原序列化程式的數個點執行自訂程式碼：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-390">`Newtonsoft.Json` lets you execute custom code at several points in the serialization or deserialization process:</span></span>

* <span data-ttu-id="7d4d0-391">OnDeserializing （開始還原序列化物件時）</span><span class="sxs-lookup"><span data-stu-id="7d4d0-391">OnDeserializing (when beginning to deserialize an object)</span></span>
* <span data-ttu-id="7d4d0-392">OnDeserialized （完成還原序列化物件時）</span><span class="sxs-lookup"><span data-stu-id="7d4d0-392">OnDeserialized (when finished deserializing an object)</span></span>
* <span data-ttu-id="7d4d0-393">OnSerializing （開始序列化物件時）</span><span class="sxs-lookup"><span data-stu-id="7d4d0-393">OnSerializing (when beginning to serialize an object)</span></span>
* <span data-ttu-id="7d4d0-394">OnSerialized （完成序列化物件時）</span><span class="sxs-lookup"><span data-stu-id="7d4d0-394">OnSerialized (when finished serializing an object)</span></span>

<span data-ttu-id="7d4d0-395">在<xref:System.Text.Json>中，您可以藉由撰寫自訂的轉換器來模擬回呼。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-395">In <xref:System.Text.Json>, you can simulate callbacks by writing a custom converter.</span></span> <span data-ttu-id="7d4d0-396">下列範例顯示 POCO 的自訂轉換子。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-396">The following example shows a custom converter for a POCO.</span></span> <span data-ttu-id="7d4d0-397">轉換器包含的程式碼會在每個對應至`Newtonsoft.Json`回呼的點上顯示訊息。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-397">The converter includes code that displays a message at each point that corresponds to a `Newtonsoft.Json` callback.</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/WeatherForecastCallbacksConverter.cs)]

<span data-ttu-id="7d4d0-398">藉由[使用類別上的屬性](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-type)，或[將轉換器加入](system-text-json-converters-how-to.md#registration-sample---converters-collection)至<xref:[!OP.NO-LOC(System.Text.Json)].JsonSerializerOptions.Converters>集合，來註冊此自訂轉換子。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-398">Register this custom converter by [using an attribute on the class](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-type) or by [adding the converter](system-text-json-converters-how-to.md#registration-sample---converters-collection) to the <xref:System.Text.Json.JsonSerializerOptions.Converters> collection.</span></span>

<span data-ttu-id="7d4d0-399">如果您使用遵循上述範例的自訂轉換器：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-399">If you use a custom converter that follows the preceding sample:</span></span>

* <span data-ttu-id="7d4d0-400">程式`OnDeserializing`代碼沒有新 POCO 實例的存取權。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-400">The `OnDeserializing` code doesn't have access to the new POCO instance.</span></span> <span data-ttu-id="7d4d0-401">若要在還原序列化開始時操作新的 POCO 實例，請將該程式碼放在 POCO 函式中。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-401">To manipulate the new POCO instance at the start of deserialization, put that code in the POCO constructor.</span></span>
* <span data-ttu-id="7d4d0-402">當遞迴呼叫`Serialize`或`Deserialize`時，請勿傳入 options 物件。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-402">Don't pass in the options object when recursively calling `Serialize` or `Deserialize`.</span></span> <span data-ttu-id="7d4d0-403">Options 物件包含`Converters`集合。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-403">The options object contains the `Converters` collection.</span></span> <span data-ttu-id="7d4d0-404">如果您將它傳遞給`Serialize`或`Deserialize`，則會使用轉換器，並產生無限迴圈，導致堆疊溢位例外狀況。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-404">If you pass it in to `Serialize` or `Deserialize`, the converter will be used, making an infinite loop that results in a stack overflow exception.</span></span>

### <a name="public-and-non-public-fields"></a><span data-ttu-id="7d4d0-405">公用和非公用欄位</span><span class="sxs-lookup"><span data-stu-id="7d4d0-405">Public and non-public fields</span></span>

<span data-ttu-id="7d4d0-406">`Newtonsoft.Json`可以序列化和還原序列化欄位，以及屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-406">`Newtonsoft.Json` can serialize and deserialize fields as well as properties.</span></span> <span data-ttu-id="7d4d0-407"><xref:System.Text.Json>僅適用于公用屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-407"><xref:System.Text.Json> only works with public properties.</span></span> <span data-ttu-id="7d4d0-408">自訂轉換器可以提供這種功能。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-408">Custom converters can provide this functionality.</span></span>

### <a name="internal-and-private-property-setters-and-getters"></a><span data-ttu-id="7d4d0-409">內部和私用屬性 setter 和 getter</span><span class="sxs-lookup"><span data-stu-id="7d4d0-409">Internal and private property setters and getters</span></span>

<span data-ttu-id="7d4d0-410">`Newtonsoft.Json`可以透過`JsonProperty`屬性使用私用和內部屬性 setter 和 getter。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-410">`Newtonsoft.Json` can use private and internal property setters and getters via the `JsonProperty` attribute.</span></span> <span data-ttu-id="7d4d0-411"><xref:System.Text.Json>僅支援公用 setter。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-411"><xref:System.Text.Json> supports only public setters.</span></span> <span data-ttu-id="7d4d0-412">自訂轉換器可以提供這種功能。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-412">Custom converters can provide this functionality.</span></span>

### <a name="populate-existing-objects"></a><span data-ttu-id="7d4d0-413">填入現有的物件</span><span class="sxs-lookup"><span data-stu-id="7d4d0-413">Populate existing objects</span></span>

<span data-ttu-id="7d4d0-414">在`JsonConvert.PopulateObject`中`Newtonsoft.Json` ，方法會將 JSON 檔案還原序列化為類別的現有實例，而不是建立新的實例。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-414">The `JsonConvert.PopulateObject` method in `Newtonsoft.Json` deserializes a JSON document to an existing instance of a class, instead of creating a new instance.</span></span> <span data-ttu-id="7d4d0-415"><xref:System.Text.Json>一律使用預設的公用無參數函式來建立目標型別的新實例。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-415"><xref:System.Text.Json> always creates a new instance of the target type by using the default public parameterless constructor.</span></span> <span data-ttu-id="7d4d0-416">自訂轉換器可以還原序列化為現有的實例。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-416">Custom converters can deserialize to an existing instance.</span></span>

### <a name="reuse-rather-than-replace-properties"></a><span data-ttu-id="7d4d0-417">重複使用，而不是取代屬性</span><span class="sxs-lookup"><span data-stu-id="7d4d0-417">Reuse rather than replace properties</span></span>

<span data-ttu-id="7d4d0-418">此`Newtonsoft.Json` `ObjectCreationHandling`設定可讓您指定屬性中的物件應該重複使用，而不是在還原序列化期間取代。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-418">The `Newtonsoft.Json` `ObjectCreationHandling` setting lets you specify that objects in properties should be reused rather than replaced during deserialization.</span></span> <span data-ttu-id="7d4d0-419"><xref:System.Text.Json>一律取代屬性中的物件。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-419"><xref:System.Text.Json> always replaces objects in properties.</span></span>  <span data-ttu-id="7d4d0-420">自訂轉換器可以提供這種功能。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-420">Custom converters can provide this functionality.</span></span>

### <a name="add-to-collections-without-setters"></a><span data-ttu-id="7d4d0-421">新增至不含 setter 的集合</span><span class="sxs-lookup"><span data-stu-id="7d4d0-421">Add to collections without setters</span></span>

<span data-ttu-id="7d4d0-422">在還原序列化`Newtonsoft.Json`期間，即使屬性沒有 setter，也會將物件加入至集合。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-422">During deserialization, `Newtonsoft.Json` adds objects to a collection even if the property has no setter.</span></span> <span data-ttu-id="7d4d0-423"><xref:System.Text.Json>忽略沒有 setter 的屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-423"><xref:System.Text.Json> ignores properties that don't have setters.</span></span> <span data-ttu-id="7d4d0-424">自訂轉換器可以提供這種功能。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-424">Custom converters can provide this functionality.</span></span>

## <a name="scenarios-that-jsonserializer-currently-doesnt-support"></a><span data-ttu-id="7d4d0-425">JsonSerializer 目前不支援的案例</span><span class="sxs-lookup"><span data-stu-id="7d4d0-425">Scenarios that JsonSerializer currently doesn't support</span></span>

<span data-ttu-id="7d4d0-426">在下列案例中，因應措施不可行或無法解決。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-426">For the following scenarios, workarounds are not practical or possible.</span></span> <span data-ttu-id="7d4d0-427">如果您依賴這些`Newtonsoft.Json`功能，則不會有重大變更，因此無法進行遷移。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-427">If you rely on these `Newtonsoft.Json` features, migration will not be possible without significant changes.</span></span>

### <a name="preserve-object-references-and-handle-loops"></a><span data-ttu-id="7d4d0-428">保留物件參考並處理迴圈</span><span class="sxs-lookup"><span data-stu-id="7d4d0-428">Preserve object references and handle loops</span></span>

<span data-ttu-id="7d4d0-429">根據預設， `Newtonsoft.Json`會以傳值方式序列化。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-429">By default, `Newtonsoft.Json` serializes by value.</span></span> <span data-ttu-id="7d4d0-430">例如，如果物件包含兩個屬性，其中包含相同`Person`物件的參考，則該`Person`物件屬性的值會在 JSON 中重複。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-430">For example, if an object contains two properties that contain a reference to the same `Person` object, the values of that `Person` object's properties are duplicated in the JSON.</span></span>

<span data-ttu-id="7d4d0-431">`Newtonsoft.Json`具有上`PreserveReferencesHandling` `JsonSerializerSettings`的設定，可讓您以傳址方式序列化：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-431">`Newtonsoft.Json` has a `PreserveReferencesHandling` setting on `JsonSerializerSettings` that lets you serialize by reference:</span></span>

* <span data-ttu-id="7d4d0-432">識別碼中繼資料會加入至為第一個`Person`物件建立的 JSON。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-432">An identifier metadata is added to the JSON created for the first `Person` object.</span></span>
* <span data-ttu-id="7d4d0-433">針對第二個`Person`物件所建立的 JSON 包含該識別碼的參考，而不是屬性值。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-433">The JSON that is created for the second `Person` object contains a reference to that identifier instead of property values.</span></span>

<span data-ttu-id="7d4d0-434">`Newtonsoft.Json`也有一個`ReferenceLoopHandling`設定，可讓您忽略迴圈參考，而不是擲回例外狀況。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-434">`Newtonsoft.Json` also has a `ReferenceLoopHandling` setting that lets you ignore circular references rather than throw an exception.</span></span>

<span data-ttu-id="7d4d0-435"><xref:System.Text.Json>僅支援以傳值方式序列化，並擲回迴圈參考的例外狀況。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-435"><xref:System.Text.Json> only supports serialization by value and throws an exception for circular references.</span></span>

### <a name="systemruntimeserialization-attributes"></a><span data-ttu-id="7d4d0-436">System.object。序列化屬性</span><span class="sxs-lookup"><span data-stu-id="7d4d0-436">System.Runtime.Serialization attributes</span></span>

<span data-ttu-id="7d4d0-437"><xref:System.Text.Json>不支援命名空間中`System.Runtime.Serialization`的屬性，例如`DataMemberAttribute`和`IgnoreDataMemberAttribute`。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-437"><xref:System.Text.Json> doesn't support attributes from the `System.Runtime.Serialization` namespace, such as `DataMemberAttribute` and `IgnoreDataMemberAttribute`.</span></span>

### <a name="octal-numbers"></a><span data-ttu-id="7d4d0-438">八進位數位</span><span class="sxs-lookup"><span data-stu-id="7d4d0-438">Octal numbers</span></span>

<span data-ttu-id="7d4d0-439">`Newtonsoft.Json`將前置零的數位視為八進位數位。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-439">`Newtonsoft.Json` treats numbers with a leading zero as octal numbers.</span></span> <span data-ttu-id="7d4d0-440"><xref:System.Text.Json>不允許前置零，因為[RFC 8259](https://tools.ietf.org/html/rfc8259)規格不允許它們。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-440"><xref:System.Text.Json> doesn't allow leading zeroes because the [RFC 8259](https://tools.ietf.org/html/rfc8259) specification doesn't allow them.</span></span>

### <a name="missingmemberhandling"></a><span data-ttu-id="7d4d0-441">MissingMemberHandling</span><span class="sxs-lookup"><span data-stu-id="7d4d0-441">MissingMemberHandling</span></span>

<span data-ttu-id="7d4d0-442">`Newtonsoft.Json`如果 JSON 包含目標型別中遺漏的屬性，可以設定為在還原序列化期間擲回例外狀況。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-442">`Newtonsoft.Json` can be configured to throw exceptions during deserialization if the JSON includes properties that are missing in the target type.</span></span> <span data-ttu-id="7d4d0-443"><xref:System.Text.Json>除非您使用[[JsonExtensionData] 屬性](system-text-json-how-to.md#handle-overflow-json)，否則會忽略 JSON 中的額外屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-443"><xref:System.Text.Json> ignores extra properties in the JSON, except when you use the [[JsonExtensionData] attribute](system-text-json-how-to.md#handle-overflow-json).</span></span> <span data-ttu-id="7d4d0-444">缺少的成員功能沒有因應措施。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-444">There's no workaround for the missing member feature.</span></span>

### <a name="tracewriter"></a><span data-ttu-id="7d4d0-445">TraceWriter</span><span class="sxs-lookup"><span data-stu-id="7d4d0-445">TraceWriter</span></span>

<span data-ttu-id="7d4d0-446">`Newtonsoft.Json`可讓您使用`TraceWriter`來查看由序列化或還原序列化所產生的記錄，以進行 debug。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-446">`Newtonsoft.Json` lets you debug by using a `TraceWriter` to view logs that are generated by serialization or deserialization.</span></span> <span data-ttu-id="7d4d0-447"><xref:System.Text.Json>不會進行記錄。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-447"><xref:System.Text.Json> doesn't do logging.</span></span>

## <a name="jsondocument-and-jsonelement-compared-to-jtoken-like-jobject-jarray"></a><span data-ttu-id="7d4d0-448">相較于 JToken 的 JsonDocument 和 JsonElement （例如 JObject、JArray）</span><span class="sxs-lookup"><span data-stu-id="7d4d0-448">JsonDocument and JsonElement compared to JToken (like JObject, JArray)</span></span>

<span data-ttu-id="7d4d0-449"><xref:System.Text.Json.JsonDocument?displayProperty=fullName>提供從現有 JSON 承載剖析和建立**唯讀**檔物件模型（DOM）的功能。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-449"><xref:System.Text.Json.JsonDocument?displayProperty=fullName> provides the ability to parse and build a **read-only** Document Object Model (DOM) from existing JSON payloads.</span></span> <span data-ttu-id="7d4d0-450">DOM 提供 JSON 承載中資料的隨機存取。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-450">The DOM provides random access to data in a JSON payload.</span></span> <span data-ttu-id="7d4d0-451">組成裝載的 JSON 元素可以透過<xref:System.Text.Json.JsonElement>類型來存取。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-451">The JSON elements that compose the payload can be accessed via the <xref:System.Text.Json.JsonElement> type.</span></span> <span data-ttu-id="7d4d0-452">`JsonElement`類型會提供 api，將 JSON 文字轉換成常見的 .net 類型。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-452">The `JsonElement` type provides APIs to convert JSON text to common .NET types.</span></span> <span data-ttu-id="7d4d0-453">`JsonDocument`公開<xref:System.Text.Json.JsonDocument.RootElement>屬性。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-453">`JsonDocument` exposes a <xref:System.Text.Json.JsonDocument.RootElement> property.</span></span>

### <a name="jsondocument-is-idisposable"></a><span data-ttu-id="7d4d0-454">JsonDocument 是 IDisposable</span><span class="sxs-lookup"><span data-stu-id="7d4d0-454">JsonDocument is IDisposable</span></span>

<span data-ttu-id="7d4d0-455">`JsonDocument`將資料的記憶體中視圖建立成集區緩衝區。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-455">`JsonDocument` builds an in-memory view of the data into a pooled buffer.</span></span> <span data-ttu-id="7d4d0-456">因此，與`JObject`或`JArray`不同`Newtonsoft.Json`， `JsonDocument`型別會`IDisposable`執行，而且必須在 using 區塊內使用。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-456">Therefore, unlike `JObject` or `JArray` from `Newtonsoft.Json`, the `JsonDocument` type implements `IDisposable` and needs to be used inside a using block.</span></span>

<span data-ttu-id="7d4d0-457">如果您想`JsonDocument`要將存留期擁有權和處置責任轉移給呼叫者，則只會從您的 API 傳回。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-457">Only return a `JsonDocument` from your API if you want to transfer lifetime ownership and dispose responsibility to the caller.</span></span> <span data-ttu-id="7d4d0-458">在大部分的情況下，這並不是必要的。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-458">In most scenarios, that isn't necessary.</span></span> <span data-ttu-id="7d4d0-459">如果呼叫端需要使用整個 JSON 檔， <xref:System.Text.Json.JsonElement.Clone%2A>則會傳回的<xref:System.Text.Json.JsonDocument.RootElement%2A>，其為。 <xref:System.Text.Json.JsonElement></span><span class="sxs-lookup"><span data-stu-id="7d4d0-459">If the caller needs to work with the entire JSON document, return the <xref:System.Text.Json.JsonElement.Clone%2A> of the <xref:System.Text.Json.JsonDocument.RootElement%2A>, which is a <xref:System.Text.Json.JsonElement>.</span></span> <span data-ttu-id="7d4d0-460">如果呼叫端需要使用 JSON 檔中的特定元素， <xref:System.Text.Json.JsonElement.Clone%2A>會<xref:System.Text.Json.JsonElement>傳回的。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-460">If the caller needs to work with a particular element within the JSON document, return the <xref:System.Text.Json.JsonElement.Clone%2A> of that <xref:System.Text.Json.JsonElement>.</span></span> <span data-ttu-id="7d4d0-461">如果您直接傳回`RootElement`或子專案`Clone`，但未建立，則呼叫端將無法存取擁有它的所傳回`JsonElement`的（ `JsonDocument`在它被處置之後）。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-461">If you return the `RootElement` or a sub-element directly without making a `Clone`, the caller won't be able to access the returned `JsonElement` after the `JsonDocument` that owns it is disposed.</span></span>

<span data-ttu-id="7d4d0-462">以下是需要您建立的`Clone`範例：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-462">Here's an example that requires you to make a `Clone`:</span></span>

```csharp
public JsonElement LookAndLoad(JsonElement source)
{
    string json = File.ReadAllText(source.GetProperty("fileName").GetString());

    using (JsonDocument doc = JsonDocument.Parse(json))
    {
        return doc.RootElement.Clone();
    }
}
```

<span data-ttu-id="7d4d0-463">上述`JsonElement`程式碼需要包含`fileName`屬性的。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-463">The preceding code expects a `JsonElement` that contains a `fileName` property.</span></span> <span data-ttu-id="7d4d0-464">它會開啟 JSON 檔案，並建立`JsonDocument`。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-464">It opens the JSON file and creates a `JsonDocument`.</span></span> <span data-ttu-id="7d4d0-465">方法假設呼叫端想要使用整份檔，因此它會`Clone`傳回的。 `RootElement`</span><span class="sxs-lookup"><span data-stu-id="7d4d0-465">The method assumes that the caller wants to work with the entire document, so it returns the `Clone` of the `RootElement`.</span></span>

<span data-ttu-id="7d4d0-466">如果您收到`JsonElement` ，且傳回子項目，則不需要傳回子項目`Clone`的。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-466">If you receive a `JsonElement` and are returning a sub-element, it's not necessary to return a `Clone` of the sub-element.</span></span> <span data-ttu-id="7d4d0-467">呼叫端負責讓傳入的`JsonDocument` `JsonElement`屬於保持運作狀態。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-467">The caller is responsible for keeping alive the `JsonDocument` that the passed-in `JsonElement` belongs to.</span></span> <span data-ttu-id="7d4d0-468">例如：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-468">For example:</span></span>

```csharp
public JsonElement ReturnFileName(JsonElement source)
{
   return source.GetProperty("fileName");
}
```

### <a name="jsondocument-is-read-only"></a><span data-ttu-id="7d4d0-469">JsonDocument 是唯讀的</span><span class="sxs-lookup"><span data-stu-id="7d4d0-469">JsonDocument is read-only</span></span>

<span data-ttu-id="7d4d0-470"><xref:System.Text.Json> DOM 無法加入、移除或修改 JSON 元素。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-470">The <xref:System.Text.Json> DOM can't add, remove, or modify JSON elements.</span></span> <span data-ttu-id="7d4d0-471">其設計是以這種方式來進行效能，並減少剖析一般 JSON 承載大小的配置（也就是 < 1 MB）。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-471">It's designed this way for performance and to reduce allocations for parsing common JSON payload sizes (that is, < 1 MB).</span></span> <span data-ttu-id="7d4d0-472">如果您的案例目前使用可修改的 DOM，下列其中一個因應措施可能是可行的：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-472">If your scenario currently uses a modifiable DOM, one of the following workarounds might be feasible:</span></span>

* <span data-ttu-id="7d4d0-473">若要從頭`JsonDocument`開始建立（也就是不會將現有的 JSON 承載傳遞至`Parse`方法），請使用寫入 JSON 文字， `Utf8JsonWriter`並剖析該輸出，使其成為新`JsonDocument`的。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-473">To build a `JsonDocument` from scratch (that is, without passing in an existing JSON payload to the `Parse` method), write the JSON text by using the `Utf8JsonWriter` and parse the output from that to make a new `JsonDocument`.</span></span>
* <span data-ttu-id="7d4d0-474">若要修改現有`JsonDocument`的，請使用它來寫入 JSON 文字，並在您撰寫時進行變更，並剖析該輸出以建立`JsonDocument`新的。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-474">To modify an existing `JsonDocument`, use it to write JSON text, making changes while you write, and parse the output from that to make a new `JsonDocument`.</span></span>
* <span data-ttu-id="7d4d0-475">若要合併現有的 JSON 檔，相當`JObject.Merge`于`JContainer.Merge`來自`Newtonsoft.Json`的或 api，請參閱[此 GitHub 問題](https://github.com/dotnet/corefx/issues/42466#issuecomment-570475853)。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-475">To merge existing JSON documents, equivalent to the `JObject.Merge` or `JContainer.Merge` APIs from `Newtonsoft.Json`, see [this GitHub issue](https://github.com/dotnet/corefx/issues/42466#issuecomment-570475853).</span></span>

### <a name="jsonelement-is-a-union-struct"></a><span data-ttu-id="7d4d0-476">JsonElement 是聯集結構</span><span class="sxs-lookup"><span data-stu-id="7d4d0-476">JsonElement is a union struct</span></span>

<span data-ttu-id="7d4d0-477">`JsonDocument`將公開`RootElement`為類型<xref:System.Text.Json.JsonElement>的屬性，也就是包含任何 JSON 專案的聯集、結構類型。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-477">`JsonDocument` exposes the `RootElement` as a property of type <xref:System.Text.Json.JsonElement>, which is a union, struct type that encompasses any JSON element.</span></span> <span data-ttu-id="7d4d0-478">`Newtonsoft.Json`會使用專用的階層`JObject`類型`JArray`， `JToken`例如、、等。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-478">`Newtonsoft.Json` uses dedicated hierarchical types like `JObject`,`JArray`, `JToken`, and so forth.</span></span> <span data-ttu-id="7d4d0-479">`JsonElement`是您可以搜尋和列舉的專案，您可以使用`JsonElement`將 JSON 元素具體化成 .net 類型。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-479">`JsonElement` is what you can search and enumerate over, and you can use `JsonElement` to materialize JSON elements into .NET types.</span></span>

### <a name="how-to-search-a-jsondocument-and-jsonelement-for-sub-elements"></a><span data-ttu-id="7d4d0-480">如何搜尋子項目的 JsonDocument 和 JsonElement</span><span class="sxs-lookup"><span data-stu-id="7d4d0-480">How to search a JsonDocument and JsonElement for sub-elements</span></span>

<span data-ttu-id="7d4d0-481">使用`JObject`或`JArray`從`Newtonsoft.Json`搜尋 JSON 權杖通常會相對快速，因為它們是在某個字典中的查閱。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-481">Searches for JSON tokens using `JObject` or `JArray` from `Newtonsoft.Json` tend to be relatively fast because they're lookups in some dictionary.</span></span> <span data-ttu-id="7d4d0-482">相較之下，搜尋`JsonElement`需要順序搜尋屬性，因此速度相對緩慢（例如使用`TryGetProperty`時）。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-482">By comparison, searches on `JsonElement` require a sequential search of the properties and hence is relatively slow (for example when using `TryGetProperty`).</span></span> <span data-ttu-id="7d4d0-483"><xref:System.Text.Json>的設計目的是要將初始剖析時間減至最少，而不是查閱時間。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-483"><xref:System.Text.Json> is designed to minimize initial parse time rather than lookup time.</span></span> <span data-ttu-id="7d4d0-484">因此，在搜尋`JsonDocument`物件時，請使用下列方法來優化效能：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-484">Therefore, use the following approaches to optimize performance when searching through a `JsonDocument` object:</span></span>

* <span data-ttu-id="7d4d0-485">使用內建的列舉值（<xref:System.Text.Json.JsonElement.EnumerateArray%2A>和<xref:System.Text.Json.JsonElement.EnumerateObject%2A>），而不是執行您自己的索引或迴圈。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-485">Use the built-in enumerators (<xref:System.Text.Json.JsonElement.EnumerateArray%2A> and <xref:System.Text.Json.JsonElement.EnumerateObject%2A>) rather than doing your own indexing or loops.</span></span>
* <span data-ttu-id="7d4d0-486">請不要`JsonDocument`透過每個屬性，使用`RootElement`來執行順序搜尋。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-486">Don't do a sequential search on the whole `JsonDocument` through every property by using `RootElement`.</span></span> <span data-ttu-id="7d4d0-487">相反地，請根據 JSON 資料的已知結構來搜尋嵌套的 JSON 物件。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-487">Instead, search on nested JSON objects based on the known structure of the JSON data.</span></span> <span data-ttu-id="7d4d0-488">例如，如果`Grade`您要尋找物件中`Student`的屬性，請對`Student`物件執行迴圈，並取得每一個的`Grade`值，而不是搜尋所有`JsonElement`尋找`Grade`屬性的物件。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-488">For example, if you're looking for a `Grade` property in `Student` objects, loop through the `Student` objects and get the value of `Grade` for each, rather than searching through all `JsonElement` objects looking for `Grade` properties.</span></span> <span data-ttu-id="7d4d0-489">執行後者會導致相同資料的不必要傳遞。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-489">Doing the latter will result in unnecessary passes over the same data.</span></span>

<span data-ttu-id="7d4d0-490">如需程式碼範例，請參閱[使用 JsonDocument 來存取資料](system-text-json-how-to.md#use-jsondocument-for-access-to-data)。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-490">For a code example, see [Use JsonDocument for access to data](system-text-json-how-to.md#use-jsondocument-for-access-to-data).</span></span>

## <a name="utf8jsonreader-compared-to-jsontextreader"></a><span data-ttu-id="7d4d0-491">Utf8JsonReader 與 JsonTextReader 的比較</span><span class="sxs-lookup"><span data-stu-id="7d4d0-491">Utf8JsonReader compared to JsonTextReader</span></span>

<span data-ttu-id="7d4d0-492"><xref:System.Text.Json.Utf8JsonReader?displayProperty=fullName>為 UTF-8 編碼 JSON 文字的高效能、低配置、順向讀取器、從[\<ReadOnlySpan 的位元組讀取>](xref:System.ReadOnlySpan%601)或 ReadOnlySequence [ \<的位元組>](xref:System.Buffers.ReadOnlySequence%601)。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-492"><xref:System.Text.Json.Utf8JsonReader?displayProperty=fullName> is a high-performance, low allocation, forward-only reader for UTF-8 encoded JSON text, read from a [ReadOnlySpan\<byte>](xref:System.ReadOnlySpan%601) or [ReadOnlySequence\<byte>](xref:System.Buffers.ReadOnlySequence%601).</span></span> <span data-ttu-id="7d4d0-493">`Utf8JsonReader`是可以用來建立自訂剖析器和還原序列化程式的低層級類型。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-493">The `Utf8JsonReader` is a low-level type that can be used to build custom parsers and deserializers.</span></span>

<span data-ttu-id="7d4d0-494">下列各節說明使用`Utf8JsonReader`的建議程式設計模式。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-494">The following sections explain recommended programming patterns for using `Utf8JsonReader`.</span></span>

### <a name="utf8jsonreader-is-a-ref-struct"></a><span data-ttu-id="7d4d0-495">Utf8JsonReader 是 ref 結構</span><span class="sxs-lookup"><span data-stu-id="7d4d0-495">Utf8JsonReader is a ref struct</span></span>

<span data-ttu-id="7d4d0-496">因為`Utf8JsonReader`型別是*ref 結構*，所以有[一些限制](../../csharp/language-reference/builtin-types/struct.md#ref-struct)。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-496">Because the `Utf8JsonReader` type is a *ref struct*, it has [certain limitations](../../csharp/language-reference/builtin-types/struct.md#ref-struct).</span></span> <span data-ttu-id="7d4d0-497">例如，它無法在 ref 結構以外的類別或結構上儲存為欄位。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-497">For example, it can't be stored as a field on a class or struct other than a ref struct.</span></span> <span data-ttu-id="7d4d0-498">為了達到高效能，此型別必須是`ref struct` ，因為它需要快取輸入[ReadOnlySpan\<byte>](xref:System.ReadOnlySpan%601)，這本身就是 ref 結構。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-498">To achieve high performance, this type must be a `ref struct` since it needs to cache the input [ReadOnlySpan\<byte>](xref:System.ReadOnlySpan%601), which itself is a ref struct.</span></span> <span data-ttu-id="7d4d0-499">此外，這種類型是可變動的，因為它會保存狀態。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-499">In addition, this type is mutable since it holds state.</span></span> <span data-ttu-id="7d4d0-500">因此，請以 ref 而非傳值方式來**傳遞它**。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-500">Therefore, **pass it by ref** rather than by value.</span></span> <span data-ttu-id="7d4d0-501">以傳值方式傳遞會導致結構複製，而且呼叫端看不到狀態變更。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-501">Passing it by value would result in a struct copy and the state changes would not be visible to the caller.</span></span> <span data-ttu-id="7d4d0-502">這與不同`Newtonsoft.Json` `Newtonsoft.Json` `JsonTextReader` ，因為是類別。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-502">This differs from `Newtonsoft.Json` since the `Newtonsoft.Json` `JsonTextReader` is a class.</span></span> <span data-ttu-id="7d4d0-503">如需如何使用 ref 結構的詳細資訊，請參閱[撰寫安全且有效率的 c # 程式碼](../../csharp/write-safe-efficient-code.md)。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-503">For more information about how to use ref structs, see [Write safe and efficient C# code](../../csharp/write-safe-efficient-code.md).</span></span>

### <a name="read-utf-8-text"></a><span data-ttu-id="7d4d0-504">讀取 UTF-8 文字</span><span class="sxs-lookup"><span data-stu-id="7d4d0-504">Read UTF-8 text</span></span>

<span data-ttu-id="7d4d0-505">若要在使用時達到最佳效能`Utf8JsonReader`，請讀取已編碼為 utf-8 文字，而不是 utf-16 字串的 JSON 承載。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-505">To achieve the best possible performance while using the `Utf8JsonReader`, read JSON payloads already encoded as UTF-8 text rather than as UTF-16 strings.</span></span> <span data-ttu-id="7d4d0-506">如需程式碼範例，請參閱[使用 Utf8JsonReader 來篩選資料](system-text-json-how-to.md#filter-data-using-utf8jsonreader)。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-506">For a code example, see [Filter data using Utf8JsonReader](system-text-json-how-to.md#filter-data-using-utf8jsonreader).</span></span>

### <a name="read-with-a-stream-or-pipereader"></a><span data-ttu-id="7d4d0-507">讀取資料流程或 PipeReader</span><span class="sxs-lookup"><span data-stu-id="7d4d0-507">Read with a Stream or PipeReader</span></span>

<span data-ttu-id="7d4d0-508">支援從 UTF-8 編碼的[\<ReadOnlySpan 位元組讀取>](xref:System.ReadOnlySpan%601)或[\<ReadOnlySequence byte>](xref:System.Buffers.ReadOnlySequence%601) （這是從讀取的結果<xref:System.IO.Pipelines.PipeReader>）。 `Utf8JsonReader`</span><span class="sxs-lookup"><span data-stu-id="7d4d0-508">The `Utf8JsonReader` supports reading from a UTF-8 encoded [ReadOnlySpan\<byte>](xref:System.ReadOnlySpan%601) or [ReadOnlySequence\<byte>](xref:System.Buffers.ReadOnlySequence%601) (which is the result of reading from a <xref:System.IO.Pipelines.PipeReader>).</span></span>

<span data-ttu-id="7d4d0-509">對於同步讀取，您可以讀取 JSON 承載，直到資料流程結尾到位元組陣列，然後將它傳遞到讀取器。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-509">For synchronous reading, you could read the JSON payload until the end of the stream into a byte array and pass that into the reader.</span></span> <span data-ttu-id="7d4d0-510">若要從字串讀取（編碼為 UTF-16），請呼叫<xref:System.Text.Encoding.UTF8>。<xref:System.Text.Encoding.GetBytes%2A></span><span class="sxs-lookup"><span data-stu-id="7d4d0-510">For reading from a string (which is encoded as UTF-16), call <xref:System.Text.Encoding.UTF8>.<xref:System.Text.Encoding.GetBytes%2A></span></span> <span data-ttu-id="7d4d0-511">首先，將字串轉碼為 UTF-8 編碼的位元組陣列。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-511">to first transcode the string to a UTF-8 encoded byte array.</span></span> <span data-ttu-id="7d4d0-512">然後將它傳遞給`Utf8JsonReader`。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-512">Then pass that to the `Utf8JsonReader`.</span></span>

<span data-ttu-id="7d4d0-513">因為會`Utf8JsonReader`將輸入視為 JSON 文字，所以會將 utf-8 位元組順序標記（BOM）視為不正確 json。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-513">Since the `Utf8JsonReader` considers the input to be JSON text, a UTF-8 byte order mark (BOM) is considered invalid JSON.</span></span> <span data-ttu-id="7d4d0-514">呼叫端必須先篩選掉，再將資料傳遞給讀取器。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-514">The caller needs to filter that out before passing the data to the reader.</span></span>

<span data-ttu-id="7d4d0-515">如需程式碼範例，請參閱[使用 Utf8JsonReader](system-text-json-how-to.md#use-utf8jsonreader)。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-515">For code examples, see [Use Utf8JsonReader](system-text-json-how-to.md#use-utf8jsonreader).</span></span>

### <a name="read-with-multi-segment-readonlysequence"></a><span data-ttu-id="7d4d0-516">閱讀多區段 ReadOnlySequence</span><span class="sxs-lookup"><span data-stu-id="7d4d0-516">Read with multi-segment ReadOnlySequence</span></span>

<span data-ttu-id="7d4d0-517">如果您的 JSON 輸入是[ReadOnlySpan\<的位元組>](xref:System.ReadOnlySpan%601)，則當您執行讀取迴圈時`ValueSpan` ，可以從讀取器上的屬性存取每個 json 元素。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-517">If your JSON input is a [ReadOnlySpan\<byte>](xref:System.ReadOnlySpan%601), each JSON element can be accessed from the `ValueSpan` property on the reader as you go through the read loop.</span></span> <span data-ttu-id="7d4d0-518">不過，如果您的輸入是[ReadOnlySequence\<的位元組>](xref:System.Buffers.ReadOnlySequence%601) （這是從讀取的結果<xref:System.IO.Pipelines.PipeReader>），某些 JSON 元素可能會跨`ReadOnlySequence<byte>`物件的多個區段。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-518">However, if your input is a [ReadOnlySequence\<byte>](xref:System.Buffers.ReadOnlySequence%601) (which is the result of reading from a <xref:System.IO.Pipelines.PipeReader>), some JSON elements might straddle multiple segments of the `ReadOnlySequence<byte>` object.</span></span> <span data-ttu-id="7d4d0-519"><xref:System.Text.Json.Utf8JsonReader.ValueSpan%2A>在連續記憶體區塊中無法存取這些元素。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-519">These elements would not be accessible from <xref:System.Text.Json.Utf8JsonReader.ValueSpan%2A> in a contiguous memory block.</span></span> <span data-ttu-id="7d4d0-520">相反地，每當您有多個區段`ReadOnlySequence<byte>`做為輸入時， <xref:System.Text.Json.Utf8JsonReader.HasValueSequence%2A>請輪詢讀取器上的屬性，以瞭解如何存取目前的 JSON 元素。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-520">Instead, whenever you have a multi-segment `ReadOnlySequence<byte>` as input, poll the <xref:System.Text.Json.Utf8JsonReader.HasValueSequence%2A> property on the reader to figure out how to access the current JSON element.</span></span> <span data-ttu-id="7d4d0-521">以下是建議的模式：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-521">Here's a recommended pattern:</span></span>

```csharp
while (reader.Read())
{
    switch (reader.TokenType)
    {
        // ...
        ReadOnlySpan<byte> jsonElement = reader.HasValueSequence ?
            reader.ValueSequence.ToArray() :
            reader.ValueSpan;
        // ...
    }
}
```

### <a name="use-valuetextequals-for-property-name-lookups"></a><span data-ttu-id="7d4d0-522">使用 ValueTextEquals 進行屬性名稱查閱</span><span class="sxs-lookup"><span data-stu-id="7d4d0-522">Use ValueTextEquals for property name lookups</span></span>

<span data-ttu-id="7d4d0-523">請勿使用<xref:System.Text.Json.Utf8JsonReader.ValueSpan%2A> ，藉由呼叫<xref:System.MemoryExtensions.SequenceEqual%2A>屬性名稱查閱來進行逐位元組比較。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-523">Don't use <xref:System.Text.Json.Utf8JsonReader.ValueSpan%2A> to do byte-by-byte comparisons by calling <xref:System.MemoryExtensions.SequenceEqual%2A> for property name lookups.</span></span> <span data-ttu-id="7d4d0-524">請<xref:System.Text.Json.Utf8JsonReader.ValueTextEquals%2A>改為呼叫，因為該方法會將任何在 JSON 中進行轉義的字元。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-524">Call <xref:System.Text.Json.Utf8JsonReader.ValueTextEquals%2A> instead, because that method unescapes any characters that are escaped in the JSON.</span></span> <span data-ttu-id="7d4d0-525">以下範例顯示如何搜尋名為 "name" 的屬性：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-525">Here's an example that shows how to search for a property that is named "name":</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/ValueTextEqualsExample.cs?name=SnippetDefineUtf8Var)]

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/ValueTextEqualsExample.cs?name=SnippetUseUtf8Var&highlight=11)]

### <a name="read-null-values-into-nullable-value-types"></a><span data-ttu-id="7d4d0-526">將 null 值讀取成可為 null 的實數值型別</span><span class="sxs-lookup"><span data-stu-id="7d4d0-526">Read null values into nullable value types</span></span>

<span data-ttu-id="7d4d0-527">`Newtonsoft.Json`提供傳回的 api <xref:System.Nullable%601>，例如`ReadAsBoolean`，其會藉由`Null` `TokenType`傳回來為您處理`bool?`。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-527">`Newtonsoft.Json` provides APIs that return <xref:System.Nullable%601>, such as `ReadAsBoolean`, which handles a `Null` `TokenType` for you by returning a `bool?`.</span></span> <span data-ttu-id="7d4d0-528">內`System.Text.Json`建 api 只會傳回不可為 null 的實數值型別。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-528">The built-in `System.Text.Json` APIs return only non-nullable value types.</span></span> <span data-ttu-id="7d4d0-529">例如， <xref:System.Text.Json.Utf8JsonReader.GetBoolean%2A?displayProperty=nameWithType>會傳回`bool`。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-529">For example, <xref:System.Text.Json.Utf8JsonReader.GetBoolean%2A?displayProperty=nameWithType> returns a `bool`.</span></span> <span data-ttu-id="7d4d0-530">如果它在 JSON 中找到`Null` ，它會擲回例外狀況。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-530">It throws an exception if it finds `Null` in the JSON.</span></span> <span data-ttu-id="7d4d0-531">下列範例示範兩種處理 null 的方式：一個是藉由傳回可為 null 的實值型別，另一個則是傳回預設值。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-531">The following examples show two ways to handle nulls, one by returning a nullable value type and one by returning the default value:</span></span>

```csharp
public bool? ReadAsNullableBoolean()
{
    _reader.Read();
    if (_reader.TokenType == JsonTokenType.Null)
    {
        return null;
    }
    if (_reader.TokenType != JsonTokenType.True && _reader.TokenType != JsonTokenType.False)
    {
        throw new JsonException();
    }
    return _reader.GetBoolean();
}
```

```csharp
public bool ReadAsBoolean(bool defaultValue)
{
    _reader.Read();
    if (_reader.TokenType == JsonTokenType.Null)
    {
        return defaultValue;
    }
    if (_reader.TokenType != JsonTokenType.True && _reader.TokenType != JsonTokenType.False)
    {
        throw new JsonException();
    }
    return _reader.GetBoolean();
}
```

### <a name="multi-targeting"></a><span data-ttu-id="7d4d0-532">多目標</span><span class="sxs-lookup"><span data-stu-id="7d4d0-532">Multi-targeting</span></span>

<span data-ttu-id="7d4d0-533">如果您需要繼續針對特定目標`Newtonsoft.Json` framework 使用，您可以多個目標，而且有兩個執行。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-533">If you need to continue to use `Newtonsoft.Json` for certain target frameworks, you can multi-target and have two implementations.</span></span> <span data-ttu-id="7d4d0-534">不過，這並不簡單，而且需要進行`#ifdefs`一些和來源的重複。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-534">However, this is not trivial and would require some `#ifdefs` and source duplication.</span></span> <span data-ttu-id="7d4d0-535">盡可能共用程式碼的方法之一， `ref struct`就是在`Utf8JsonReader`和`Newtonsoft.Json` `JsonTextReader`之間建立包裝函式。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-535">One way to share as much code as possible is to create a `ref struct` wrapper around `Utf8JsonReader` and `Newtonsoft.Json` `JsonTextReader`.</span></span> <span data-ttu-id="7d4d0-536">此包裝函式會統一公用介面區，同時隔離行為差異。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-536">This wrapper would unify the public surface area while isolating the behavioral differences.</span></span> <span data-ttu-id="7d4d0-537">這可讓您隔離主要變更與型別的結構，並以傳址方式傳遞新的型別。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-537">This lets you isolate the changes mainly to the construction of the type, along with passing the new type around by reference.</span></span> <span data-ttu-id="7d4d0-538">這是[DependencyModel](https://www.nuget.org/packages/Microsoft.Extensions.DependencyModel/3.1.0/)程式庫所遵循的模式：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-538">This is the pattern that the [Microsoft.Extensions.DependencyModel](https://www.nuget.org/packages/Microsoft.Extensions.DependencyModel/3.1.0/) library follows:</span></span>

* [<span data-ttu-id="7d4d0-539">UnifiedJsonReader.JsonTextReader.cs</span><span class="sxs-lookup"><span data-stu-id="7d4d0-539">UnifiedJsonReader.JsonTextReader.cs</span></span>](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/installer/managed/Microsoft.Extensions.DependencyModel/UnifiedJsonReader.JsonTextReader.cs)
* [<span data-ttu-id="7d4d0-540">UnifiedJsonReader.Utf8JsonReader.cs</span><span class="sxs-lookup"><span data-stu-id="7d4d0-540">UnifiedJsonReader.Utf8JsonReader.cs</span></span>](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/installer/managed/Microsoft.Extensions.DependencyModel/UnifiedJsonReader.Utf8JsonReader.cs)

## <a name="utf8jsonwriter-compared-to-jsontextwriter"></a><span data-ttu-id="7d4d0-541">Utf8JsonWriter 與 Json.net jsoNtextwriter, 的比較</span><span class="sxs-lookup"><span data-stu-id="7d4d0-541">Utf8JsonWriter compared to JsonTextWriter</span></span>

<span data-ttu-id="7d4d0-542"><xref:System.Text.Json.Utf8JsonWriter?displayProperty=fullName>是從常見的 .net 類型（例如`String`、 `Int32`和`DateTime`）撰寫 utf-8 編碼 JSON 文字的高效能方式。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-542"><xref:System.Text.Json.Utf8JsonWriter?displayProperty=fullName> is a high-performance way to write UTF-8 encoded JSON text from common .NET types like `String`, `Int32`, and `DateTime`.</span></span> <span data-ttu-id="7d4d0-543">寫入器是可以用來建立自訂序列化程式的低層級類型。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-543">The writer is a low-level type that can be used to build custom serializers.</span></span>

<span data-ttu-id="7d4d0-544">下列各節說明使用`Utf8JsonWriter`的建議程式設計模式。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-544">The following sections explain recommended programming patterns for using `Utf8JsonWriter`.</span></span>

### <a name="write-with-utf-8-text"></a><span data-ttu-id="7d4d0-545">以 UTF-8 文字撰寫</span><span class="sxs-lookup"><span data-stu-id="7d4d0-545">Write with UTF-8 text</span></span>

<span data-ttu-id="7d4d0-546">若要在使用時達到最佳效能`Utf8JsonWriter`，請撰寫已編碼為 utf-8 文字，而不是 utf-16 字串的 JSON 承載。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-546">To achieve the best possible performance while using the `Utf8JsonWriter`, write JSON payloads already encoded as UTF-8 text rather than as UTF-16 strings.</span></span> <span data-ttu-id="7d4d0-547">使用<xref:System.Text.Json.JsonEncodedText>快取和預先編碼已知的字串屬性名稱和值做為靜態，並將其傳遞給寫入器，而不是使用 utf-16 字串常值。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-547">Use <xref:System.Text.Json.JsonEncodedText> to cache and pre-encode known string property names and values as statics, and pass those to the writer, rather than using UTF-16 string literals.</span></span> <span data-ttu-id="7d4d0-548">這比快取和使用 UTF-8 位元組陣列更快速。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-548">This is faster than caching and using UTF-8 byte arrays.</span></span>

<span data-ttu-id="7d4d0-549">如果您需要執行自訂的轉義，此方法也適用。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-549">This approach also works if you need to do custom escaping.</span></span> <span data-ttu-id="7d4d0-550">`System.Text.Json`不允許您在寫入字串時停用轉義。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-550">`System.Text.Json` doesn't let you disable escaping while writing a string.</span></span> <span data-ttu-id="7d4d0-551">不過，您可以將自己的自訂<xref:System.Text.Encodings.Web.JavaScriptEncoder>做為寫入器的選項傳入，或建立自己`JsonEncodedText`的來使用`JavascriptEncoder`來執行轉義，然後撰寫， `JsonEncodedText`而不是字串。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-551">However, you could pass in your own custom <xref:System.Text.Encodings.Web.JavaScriptEncoder> as an option to the writer, or create your own `JsonEncodedText` that uses your `JavascriptEncoder` to do the escaping, and then write the `JsonEncodedText` instead of the string.</span></span> <span data-ttu-id="7d4d0-552">如需詳細資訊，請參閱[自訂字元編碼](system-text-json-how-to.md#customize-character-encoding)。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-552">For more information, see [Customize character encoding](system-text-json-how-to.md#customize-character-encoding).</span></span>

### <a name="write-raw-values"></a><span data-ttu-id="7d4d0-553">寫入原始值</span><span class="sxs-lookup"><span data-stu-id="7d4d0-553">Write raw values</span></span>

<span data-ttu-id="7d4d0-554">`Newtonsoft.Json`方法會寫入原始 JSON，其中應為`WriteRawValue`值。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-554">The `Newtonsoft.Json` `WriteRawValue` method writes raw JSON where a value is expected.</span></span> <span data-ttu-id="7d4d0-555"><xref:System.Text.Json>沒有直接的對等用法，但以下是可確保只會寫入有效 JSON 的因應措施：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-555"><xref:System.Text.Json> has no direct equivalent, but here's a workaround that ensures only valid JSON is written:</span></span>

```csharp
using JsonDocument doc = JsonDocument.Parse(string);
doc.WriteTo(writer);
```

### <a name="customize-character-escaping"></a><span data-ttu-id="7d4d0-556">自訂字元轉義</span><span class="sxs-lookup"><span data-stu-id="7d4d0-556">Customize character escaping</span></span>

<span data-ttu-id="7d4d0-557">的[StringEscapeHandling](https://www.newtonsoft.com/json/help/html/T_Newtonsoft_Json_StringEscapeHandling.htm)設定會`JsonTextWriter`提供選項來將所有非 ASCII 字元**或**HTML 字元全部轉義。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-557">The [StringEscapeHandling](https://www.newtonsoft.com/json/help/html/T_Newtonsoft_Json_StringEscapeHandling.htm) setting of `JsonTextWriter` offers options to escape all non-ASCII characters **or** HTML characters.</span></span> <span data-ttu-id="7d4d0-558">根據預設， `Utf8JsonWriter`會將所有非 ASCII**和**HTML 字元全部轉義。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-558">By default, `Utf8JsonWriter` escapes all non-ASCII **and** HTML characters.</span></span> <span data-ttu-id="7d4d0-559">這項轉義是針對深層防禦的安全性原因而完成。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-559">This escaping is done for defense-in-depth security reasons.</span></span> <span data-ttu-id="7d4d0-560">若要指定不同的轉義原則，請<xref:System.Text.Encodings.Web.JavaScriptEncoder>建立， <xref:System.Text.Json.JsonWriterOptions.Encoder?displayProperty=nameWithType>並設定。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-560">To specify a different escaping policy, create a <xref:System.Text.Encodings.Web.JavaScriptEncoder> and set <xref:System.Text.Json.JsonWriterOptions.Encoder?displayProperty=nameWithType>.</span></span> <span data-ttu-id="7d4d0-561">如需詳細資訊，請參閱[自訂字元編碼](system-text-json-how-to.md#customize-character-encoding)。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-561">For more information, see [Customize character encoding](system-text-json-how-to.md#customize-character-encoding).</span></span>

### <a name="customize-json-format"></a><span data-ttu-id="7d4d0-562">自訂 JSON 格式</span><span class="sxs-lookup"><span data-stu-id="7d4d0-562">Customize JSON format</span></span>

<span data-ttu-id="7d4d0-563">`JsonTextWriter`包含下列設定，其中`Utf8JsonWriter`沒有對等的：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-563">`JsonTextWriter` includes the following settings, for which `Utf8JsonWriter` has no equivalent:</span></span>

* <span data-ttu-id="7d4d0-564">[縮排](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_Indentation.htm)-指定要縮排的字元數。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-564">[Indentation](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_Indentation.htm) - Specifies how many characters to indent.</span></span> <span data-ttu-id="7d4d0-565">`Utf8JsonWriter`一律會進行2個字元的縮排。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-565">`Utf8JsonWriter` always does 2-character indentation.</span></span>
* <span data-ttu-id="7d4d0-566">[IndentChar](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_IndentChar.htm) -指定要用於縮排的字元。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-566">[IndentChar](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_IndentChar.htm) - Specifies the character to use for indentation.</span></span>  <span data-ttu-id="7d4d0-567">`Utf8JsonWriter`一律使用空白字元。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-567">`Utf8JsonWriter` always uses whitespace.</span></span>
* <span data-ttu-id="7d4d0-568">[QuoteChar](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_QuoteChar.htm) -指定用來括住字串值的字元。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-568">[QuoteChar](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_QuoteChar.htm) - Specifies the character to use to surround string values.</span></span>  <span data-ttu-id="7d4d0-569">`Utf8JsonWriter`一律使用雙引號。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-569">`Utf8JsonWriter` always uses double quotes.</span></span>
* <span data-ttu-id="7d4d0-570">[QuoteName](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_QuoteName.htm) -指定是否要以引號括住屬性名稱。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-570">[QuoteName](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_QuoteName.htm) - Specifies whether or not to surround property names with quotes.</span></span>  <span data-ttu-id="7d4d0-571">`Utf8JsonWriter`一律以引號括住它們。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-571">`Utf8JsonWriter` always surrounds them with quotes.</span></span>

<span data-ttu-id="7d4d0-572">沒有任何因應措施可讓您自訂`Utf8JsonWriter`以這些方式產生的 JSON。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-572">There are no workarounds that would let you customize the JSON produced by `Utf8JsonWriter` in these ways.</span></span>

### <a name="write-null-values"></a><span data-ttu-id="7d4d0-573">寫入 null 值</span><span class="sxs-lookup"><span data-stu-id="7d4d0-573">Write null values</span></span>

<span data-ttu-id="7d4d0-574">若要使用`Utf8JsonWriter`寫入 null 值，請呼叫：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-574">To write null values by using `Utf8JsonWriter`, call:</span></span>

* <span data-ttu-id="7d4d0-575"><xref:System.Text.Json.Utf8JsonWriter.WriteNull%2A>寫入具有 null 值的索引鍵/值組。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-575"><xref:System.Text.Json.Utf8JsonWriter.WriteNull%2A> to write a key-value pair with null as the value.</span></span>
* <span data-ttu-id="7d4d0-576"><xref:System.Text.Json.Utf8JsonWriter.WriteNullValue%2A>寫入 null 做為 JSON 陣列的元素。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-576"><xref:System.Text.Json.Utf8JsonWriter.WriteNullValue%2A> to write null as an element of a JSON array.</span></span>

<span data-ttu-id="7d4d0-577">若為字串屬性，則為，如果字串為<xref:System.Text.Json.Utf8JsonWriter.WriteString%2A> null <xref:System.Text.Json.Utf8JsonWriter.WriteStringValue%2A> ，而且相當`WriteNull`于`WriteNullValue`和。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-577">For a string property, if the string is null, <xref:System.Text.Json.Utf8JsonWriter.WriteString%2A> and <xref:System.Text.Json.Utf8JsonWriter.WriteStringValue%2A> are equivalent to `WriteNull` and `WriteNullValue`.</span></span>

### <a name="write-timespan-uri-or-char-values"></a><span data-ttu-id="7d4d0-578">寫入 Timespan、Uri 或 char 值</span><span class="sxs-lookup"><span data-stu-id="7d4d0-578">Write Timespan, Uri, or char values</span></span>

<span data-ttu-id="7d4d0-579">`JsonTextWriter`提供`WriteValue` [TimeSpan](https://www.newtonsoft.com/json/help/html/M_Newtonsoft_Json_JsonTextWriter_WriteValue_18.htm)、 [Uri](https://www.newtonsoft.com/json/help/html/M_Newtonsoft_Json_JsonTextWriter_WriteValue_22.htm)和[char](https://www.newtonsoft.com/json/help/html/M_Newtonsoft_Json_JsonTextWriter_WriteValue_3.htm)值的方法。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-579">`JsonTextWriter` provides `WriteValue` methods for [TimeSpan](https://www.newtonsoft.com/json/help/html/M_Newtonsoft_Json_JsonTextWriter_WriteValue_18.htm), [Uri](https://www.newtonsoft.com/json/help/html/M_Newtonsoft_Json_JsonTextWriter_WriteValue_22.htm), and [char](https://www.newtonsoft.com/json/help/html/M_Newtonsoft_Json_JsonTextWriter_WriteValue_3.htm) values.</span></span> <span data-ttu-id="7d4d0-580">`Utf8JsonWriter`沒有對等的方法。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-580">`Utf8JsonWriter` doesn't have equivalent methods.</span></span> <span data-ttu-id="7d4d0-581">相反地，請將這些值格式化為字串`ToString()`（例如，藉由呼叫<xref:System.Text.Json.Utf8JsonWriter.WriteStringValue%2A>）並呼叫。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-581">Instead, format these values as strings (by calling `ToString()`, for example) and call <xref:System.Text.Json.Utf8JsonWriter.WriteStringValue%2A>.</span></span>

### <a name="multi-targeting"></a><span data-ttu-id="7d4d0-582">多目標</span><span class="sxs-lookup"><span data-stu-id="7d4d0-582">Multi-targeting</span></span>

<span data-ttu-id="7d4d0-583">如果您需要繼續針對特定目標`Newtonsoft.Json` framework 使用，您可以多個目標，而且有兩個執行。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-583">If you need to continue to use `Newtonsoft.Json` for certain target frameworks, you can multi-target and have two implementations.</span></span> <span data-ttu-id="7d4d0-584">不過，這並不簡單，而且需要進行`#ifdefs`一些和來源的重複。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-584">However, this is not trivial and would require some `#ifdefs` and source duplication.</span></span> <span data-ttu-id="7d4d0-585">盡可能共用程式碼的方法之一，就是在`Utf8JsonWriter`和`Newtonsoft` `JsonTextWriter`之間建立包裝函式。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-585">One way to share as much code as possible is to create a wrapper around `Utf8JsonWriter` and `Newtonsoft` `JsonTextWriter`.</span></span> <span data-ttu-id="7d4d0-586">此包裝函式會統一公用介面區，同時隔離行為差異。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-586">This wrapper would unify the public surface area while isolating the behavioral differences.</span></span> <span data-ttu-id="7d4d0-587">這可讓您將變更主要隔離到類型的結構。</span><span class="sxs-lookup"><span data-stu-id="7d4d0-587">This lets you isolate the changes mainly to the construction of the type.</span></span> <span data-ttu-id="7d4d0-588">[DependencyModel](https://www.nuget.org/packages/Microsoft.Extensions.DependencyModel/3.1.0/)程式庫如下：</span><span class="sxs-lookup"><span data-stu-id="7d4d0-588">[Microsoft.Extensions.DependencyModel](https://www.nuget.org/packages/Microsoft.Extensions.DependencyModel/3.1.0/) library follows:</span></span>

* [<span data-ttu-id="7d4d0-589">UnifiedJsonWriter.JsonTextWriter.cs</span><span class="sxs-lookup"><span data-stu-id="7d4d0-589">UnifiedJsonWriter.JsonTextWriter.cs</span></span>](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/installer/managed/Microsoft.Extensions.DependencyModel/UnifiedJsonWriter.JsonTextWriter.cs)
* [<span data-ttu-id="7d4d0-590">UnifiedJsonWriter.Utf8JsonWriter.cs</span><span class="sxs-lookup"><span data-stu-id="7d4d0-590">UnifiedJsonWriter.Utf8JsonWriter.cs</span></span>](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/installer/managed/Microsoft.Extensions.DependencyModel/UnifiedJsonWriter.Utf8JsonWriter.cs)

## <a name="additional-resources"></a><span data-ttu-id="7d4d0-591">其他資源</span><span class="sxs-lookup"><span data-stu-id="7d4d0-591">Additional resources</span></span>

<!-- * [System.Text.Json roadmap](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/libraries/System.Text.Json/roadmap/README.md)[Restore this when the roadmap is updated.]-->
* <span data-ttu-id="7d4d0-592">[System.Text.Json簡要](system-text-json-overview.md)</span><span class="sxs-lookup"><span data-stu-id="7d4d0-592">[System.Text.Json overview](system-text-json-overview.md)</span></span>
* <span data-ttu-id="7d4d0-593">[如何使用System.Text.Json](system-text-json-how-to.md)</span><span class="sxs-lookup"><span data-stu-id="7d4d0-593">[How to use System.Text.Json](system-text-json-how-to.md)</span></span>
* [<span data-ttu-id="7d4d0-594">如何撰寫自訂轉換器</span><span class="sxs-lookup"><span data-stu-id="7d4d0-594">How to write custom converters</span></span>](system-text-json-converters-how-to.md)
* <span data-ttu-id="7d4d0-595">[中的 DateTime 和 DateTimeOffset 支援System.Text.Json](../datetime/system-text-json-support.md)</span><span class="sxs-lookup"><span data-stu-id="7d4d0-595">[DateTime and DateTimeOffset support in System.Text.Json](../datetime/system-text-json-support.md)</span></span>
* <span data-ttu-id="7d4d0-596">[System.Text.JsonAPI 參考](xref:System.Text.Json)</span><span class="sxs-lookup"><span data-stu-id="7d4d0-596">[System.Text.Json API reference](xref:System.Text.Json)</span></span>
* <span data-ttu-id="7d4d0-597">[System.Text.Json.序列化 API 參考](xref:System.Text.Json.Serialization)</span><span class="sxs-lookup"><span data-stu-id="7d4d0-597">[System.Text.Json.Serialization API reference](xref:System.Text.Json.Serialization)</span></span>
