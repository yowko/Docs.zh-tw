---
title: 在 WPF 中託管 Win32 控制項
titleSuffix: ''
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
helpviewer_keywords:
- hosting Win32 control in WPF [WPF]
- Win32 code [WPF], WPF interoperation
ms.assetid: a676b1eb-fc55-4355-93ab-df840c41cea0
ms.openlocfilehash: 5185e60640c652b79bd105db54830ac3acc57129
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 03/12/2020
ms.locfileid: "79186745"
---
# <a name="walkthrough-host-a-win32-control-in-wpf"></a><span data-ttu-id="897a5-102">演練：在 WPF 中託管 Win32 控制項</span><span class="sxs-lookup"><span data-stu-id="897a5-102">Walkthrough: Host a Win32 Control in WPF</span></span>
<span data-ttu-id="897a5-103">Windows 演示基礎 （WPF） 為創建應用程式提供了豐富的環境。</span><span class="sxs-lookup"><span data-stu-id="897a5-103">Windows Presentation Foundation (WPF) provides a rich environment for creating applications.</span></span> <span data-ttu-id="897a5-104">但是，當您對 Win32 代碼進行大量投資時，在 WPF 應用程式中至少重用其中一些代碼，而不是完全重寫它可能更有效。</span><span class="sxs-lookup"><span data-stu-id="897a5-104">However, when you have a substantial investment in Win32 code, it may be more effective to reuse at least some of that code in your WPF application rather than rewrite it completely.</span></span> <span data-ttu-id="897a5-105">WPF 提供了一種在 WPF 頁面上託管 Win32 視窗的簡單機制。</span><span class="sxs-lookup"><span data-stu-id="897a5-105">WPF provides a straightforward mechanism for hosting a Win32 window, on a WPF page.</span></span>  
  
 <span data-ttu-id="897a5-106">本主題將引導您完成一個應用程式，[在 WPF 示例中託管 Win32 ListBox 控制項](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control)，該應用程式託管 Win32 清單方塊控制項。</span><span class="sxs-lookup"><span data-stu-id="897a5-106">This topic walks you through an application, [Hosting a Win32 ListBox Control in WPF Sample](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control), that hosts a Win32 list box control.</span></span> <span data-ttu-id="897a5-107">此常規過程可以擴展到託管任何 Win32 視窗。</span><span class="sxs-lookup"><span data-stu-id="897a5-107">This general procedure can be extended to hosting any Win32 window.</span></span>  

<a name="requirements"></a>
## <a name="requirements"></a><span data-ttu-id="897a5-108">需求</span><span class="sxs-lookup"><span data-stu-id="897a5-108">Requirements</span></span>  
 <span data-ttu-id="897a5-109">本主題假定對 WPF 和 Windows API 程式設計基本熟悉。</span><span class="sxs-lookup"><span data-stu-id="897a5-109">This topic assumes a basic familiarity with both WPF and Windows API programming.</span></span> <span data-ttu-id="897a5-110">有關 WPF 程式設計的基本介紹，請參閱[入門](../getting-started/index.md)。</span><span class="sxs-lookup"><span data-stu-id="897a5-110">For a basic introduction to WPF programming, see [Getting Started](../getting-started/index.md).</span></span> <span data-ttu-id="897a5-111">有關 Windows API 程式設計的介紹，請參閱有關該主題的眾多書籍中的任何一本書，特別是查理斯·佩佐德的*程式設計 Windows。*</span><span class="sxs-lookup"><span data-stu-id="897a5-111">For an introduction to Windows API programming, see any of the numerous books on the subject, in particular *Programming Windows* by Charles Petzold.</span></span>  
  
 <span data-ttu-id="897a5-112">由於本主題附帶的示例在 C# 中實現，因此它利用平台叫用服務 （PInvoke） 訪問 Windows API。</span><span class="sxs-lookup"><span data-stu-id="897a5-112">Because the sample that accompanies this topic is implemented in C#, it makes use of Platform Invocation Services (PInvoke) to access the Windows API.</span></span> <span data-ttu-id="897a5-113">對 PInvoke 的熟悉是有説明的，但不是必須的。</span><span class="sxs-lookup"><span data-stu-id="897a5-113">Some familiarity with PInvoke is helpful but not essential.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="897a5-114">本主題包含一些來自相關聯範例的程式碼範例。</span><span class="sxs-lookup"><span data-stu-id="897a5-114">This topic includes a number of code examples from the associated sample.</span></span> <span data-ttu-id="897a5-115">不過，為了方便閱讀，並未包含完整的範例程式碼。</span><span class="sxs-lookup"><span data-stu-id="897a5-115">However, for readability, it does not include the complete sample code.</span></span> <span data-ttu-id="897a5-116">您可以在[WPF 示例中從承載 Win32 ListBox 控制項](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control)獲取或查看完整的代碼。</span><span class="sxs-lookup"><span data-stu-id="897a5-116">You can obtain or view complete code from [Hosting a Win32 ListBox Control in WPF Sample](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control).</span></span>  
  
<a name="basic_procedure"></a>
## <a name="the-basic-procedure"></a><span data-ttu-id="897a5-117">基本程序</span><span class="sxs-lookup"><span data-stu-id="897a5-117">The Basic Procedure</span></span>  
 <span data-ttu-id="897a5-118">本節概述了在 WPF 頁面上託管 Win32 視窗的基本過程。</span><span class="sxs-lookup"><span data-stu-id="897a5-118">This section outlines the basic procedure for hosting a Win32 window on a WPF page.</span></span> <span data-ttu-id="897a5-119">其餘各節將說明每個步驟的詳細資料。</span><span class="sxs-lookup"><span data-stu-id="897a5-119">The remaining sections go through the details of each step.</span></span>  
  
 <span data-ttu-id="897a5-120">基本裝載程序如下︰</span><span class="sxs-lookup"><span data-stu-id="897a5-120">The basic hosting procedure is:</span></span>  
  
1. <span data-ttu-id="897a5-121">實現 WPF 頁以承載視窗。</span><span class="sxs-lookup"><span data-stu-id="897a5-121">Implement a WPF page to host the window.</span></span> <span data-ttu-id="897a5-122">一種技術是創建一<xref:System.Windows.Controls.Border>個元素來為託管視窗保留頁面的一部分。</span><span class="sxs-lookup"><span data-stu-id="897a5-122">One technique is to create a <xref:System.Windows.Controls.Border> element to reserve a section of the page for the hosted window.</span></span>  
  
2. <span data-ttu-id="897a5-123">實現類以承載從<xref:System.Windows.Interop.HwndHost>繼承的控制項。</span><span class="sxs-lookup"><span data-stu-id="897a5-123">Implement a class to host the control that inherits from <xref:System.Windows.Interop.HwndHost>.</span></span>  
  
3. <span data-ttu-id="897a5-124">在該類中<xref:System.Windows.Interop.HwndHost>，重寫類成員<xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>。</span><span class="sxs-lookup"><span data-stu-id="897a5-124">In that class, override the <xref:System.Windows.Interop.HwndHost> class member <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>.</span></span>  
  
4. <span data-ttu-id="897a5-125">將託管視窗創建為包含 WPF 頁的視窗的子視窗。</span><span class="sxs-lookup"><span data-stu-id="897a5-125">Create the hosted window as a child of the window that contains the WPF page.</span></span> <span data-ttu-id="897a5-126">儘管傳統的 WPF 程式設計不需要顯式使用它，但託管頁是具有控制碼 （HWND） 的視窗。</span><span class="sxs-lookup"><span data-stu-id="897a5-126">Although conventional WPF programming does not need to explicitly make use of it, the hosting page is a window with a handle (HWND).</span></span> <span data-ttu-id="897a5-127">通過`hwndParent`<xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>方法的參數接收頁面 HWND。</span><span class="sxs-lookup"><span data-stu-id="897a5-127">You receive the page HWND through the `hwndParent` parameter of the <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A> method.</span></span> <span data-ttu-id="897a5-128">裝載的視窗應該建立為此 HWND 的子系。</span><span class="sxs-lookup"><span data-stu-id="897a5-128">The hosted window should be created as a child of this HWND.</span></span>  
  
5. <span data-ttu-id="897a5-129">建立主視窗之後，即會傳回裝載之視窗的 HWND。</span><span class="sxs-lookup"><span data-stu-id="897a5-129">Once you have created the host window, return the HWND of the hosted window.</span></span> <span data-ttu-id="897a5-130">如果要承載一個或多個 Win32 控制項，通常創建主機視窗作為 HWND 的子級，並創建該主機視窗的控制項子級。</span><span class="sxs-lookup"><span data-stu-id="897a5-130">If you want to host one or more Win32 controls, you typically create a host window as a child of the HWND and make the controls children of that host window.</span></span> <span data-ttu-id="897a5-131">在主機視窗中包裝控制項為 WPF 頁提供了一種從控制項接收通知的簡單方法，該控制項處理 HWND 邊界通知的一些特定 Win32 問題。</span><span class="sxs-lookup"><span data-stu-id="897a5-131">Wrapping the controls in a host window provides a simple way for your WPF page to receive notifications from the controls, which deals with some particular Win32 issues with notifications across the HWND boundary.</span></span>  
  
6. <span data-ttu-id="897a5-132">處理傳送至主視窗的已選取訊息，例如來自子控制項的通知。</span><span class="sxs-lookup"><span data-stu-id="897a5-132">Handle selected messages sent to the host window, such as notifications from child controls.</span></span> <span data-ttu-id="897a5-133">做法有二種。</span><span class="sxs-lookup"><span data-stu-id="897a5-133">There are two ways to do this.</span></span>  
  
    - <span data-ttu-id="897a5-134">如果希望處理託管類中的消息，請重寫<xref:System.Windows.Interop.HwndHost.WndProc%2A><xref:System.Windows.Interop.HwndHost>類的方法。</span><span class="sxs-lookup"><span data-stu-id="897a5-134">If you prefer to handle messages in your hosting class, override the <xref:System.Windows.Interop.HwndHost.WndProc%2A> method of the <xref:System.Windows.Interop.HwndHost> class.</span></span>  
  
    - <span data-ttu-id="897a5-135">如果希望 WPF 處理消息，請處理代碼後面的<xref:System.Windows.Interop.HwndHost>類<xref:System.Windows.Interop.HwndHost.MessageHook>事件。</span><span class="sxs-lookup"><span data-stu-id="897a5-135">If you prefer to have the WPF handle the messages, handle the <xref:System.Windows.Interop.HwndHost> class <xref:System.Windows.Interop.HwndHost.MessageHook> event in your code-behind.</span></span> <span data-ttu-id="897a5-136">針對裝載的視窗接收到的每個訊息，都會發生此事件。</span><span class="sxs-lookup"><span data-stu-id="897a5-136">This event occurs for every message that is received by the hosted window.</span></span> <span data-ttu-id="897a5-137">如果選擇此選項，則仍必須重寫<xref:System.Windows.Interop.HwndHost.WndProc%2A>，但只需要最少的實現。</span><span class="sxs-lookup"><span data-stu-id="897a5-137">If you choose this option, you must still override <xref:System.Windows.Interop.HwndHost.WndProc%2A>, but you only need a minimal implementation.</span></span>  
  
7. <span data-ttu-id="897a5-138">重寫<xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A>的<xref:System.Windows.Interop.HwndHost.WndProc%2A>和<xref:System.Windows.Interop.HwndHost></span><span class="sxs-lookup"><span data-stu-id="897a5-138">Override the <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> and <xref:System.Windows.Interop.HwndHost.WndProc%2A> methods of <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="897a5-139">您必須重寫這些方法才能滿足協定，<xref:System.Windows.Interop.HwndHost>但可能只需要提供最少的實現。</span><span class="sxs-lookup"><span data-stu-id="897a5-139">You must override these methods to satisfy the <xref:System.Windows.Interop.HwndHost> contract, but you may only need to provide a minimal implementation.</span></span>  
  
8. <span data-ttu-id="897a5-140">在代碼背後的檔中，創建控制項宿主類的實例，並將其使其成為旨在承載視窗<xref:System.Windows.Controls.Border>的元素的子級。</span><span class="sxs-lookup"><span data-stu-id="897a5-140">In your code-behind file, create an instance of the control hosting class and make it a child of the <xref:System.Windows.Controls.Border> element that is intended to host the window.</span></span>  
  
9. <span data-ttu-id="897a5-141">通過向託管視窗發送 Microsoft Windows 消息並處理來自其子視窗的消息（如控制項發送的通知）與託管視窗通信。</span><span class="sxs-lookup"><span data-stu-id="897a5-141">Communicate with the hosted window by sending it Microsoft Windows messages and handling messages from its child windows, such as notifications sent by controls.</span></span>  
  
<a name="page_layout"></a>
## <a name="implement-the-page-layout"></a><span data-ttu-id="897a5-142">實作版面配置</span><span class="sxs-lookup"><span data-stu-id="897a5-142">Implement the Page Layout</span></span>  
 <span data-ttu-id="897a5-143">承載 ListBox 控制項的 WPF 頁的佈局由兩個區域組成。</span><span class="sxs-lookup"><span data-stu-id="897a5-143">The layout for the WPF page that hosts the ListBox Control consists of two regions.</span></span> <span data-ttu-id="897a5-144">頁面的左側承載多個 WPF 控制項，這些控制項提供[!INCLUDE[TLA#tla_ui](../../../../includes/tlasharptla-ui-md.md)]允許您操作 Win32 控制項的 控制項。</span><span class="sxs-lookup"><span data-stu-id="897a5-144">The left side of the page hosts several WPF controls that provide a [!INCLUDE[TLA#tla_ui](../../../../includes/tlasharptla-ui-md.md)] that allows you to manipulate the Win32 control.</span></span> <span data-ttu-id="897a5-145">頁面的右上角具有 ListBox 託管控制項的方形區域。</span><span class="sxs-lookup"><span data-stu-id="897a5-145">The upper right corner of the page has a square region for the hosted ListBox Control.</span></span>  
  
 <span data-ttu-id="897a5-146">實現此佈局的代碼非常簡單。</span><span class="sxs-lookup"><span data-stu-id="897a5-146">The code to implement this layout is quite simple.</span></span> <span data-ttu-id="897a5-147">根項目是<xref:System.Windows.Controls.DockPanel>具有兩個子項目的 。</span><span class="sxs-lookup"><span data-stu-id="897a5-147">The root element is a <xref:System.Windows.Controls.DockPanel> that has two child elements.</span></span> <span data-ttu-id="897a5-148">第一個是<xref:System.Windows.Controls.Border>承載 ListBox 控制項的元素。</span><span class="sxs-lookup"><span data-stu-id="897a5-148">The first is a <xref:System.Windows.Controls.Border> element that hosts the ListBox Control.</span></span> <span data-ttu-id="897a5-149">它會佔用頁面右上角的 200x200 方形。</span><span class="sxs-lookup"><span data-stu-id="897a5-149">It occupies a 200x200 square in the upper right corner of the page.</span></span> <span data-ttu-id="897a5-150">第二個是包含<xref:System.Windows.Controls.StackPanel>一組 WPF 控制項的元素，該控制項顯示資訊，並允許您通過設置公開的交互操作屬性來操作 ListBox 控制項。</span><span class="sxs-lookup"><span data-stu-id="897a5-150">The second is a <xref:System.Windows.Controls.StackPanel> element that contains a set of WPF controls that display information and allow you to manipulate the ListBox Control by setting exposed interoperation properties.</span></span> <span data-ttu-id="897a5-151">對於 的每個元素都是<xref:System.Windows.Controls.StackPanel>的子項目，請參閱用於詳細介紹這些元素是什麼或它們做什麼的各種元素的參考材料，這些元素列在下面的示例代碼中，但此處不會解釋（基本交互操作模型不需要其中任何元素，它們提供是為了向示例添加一些交互性）。</span><span class="sxs-lookup"><span data-stu-id="897a5-151">For each of the elements that are children of the <xref:System.Windows.Controls.StackPanel>, see the reference material for the various elements used for details on what these elements are or what they do, these are listed in the example code below but will not be explained here (the basic interoperation model does not require any of them, they are provided to add some interactivity to the sample).</span></span>  
  
 [!code-xaml[WPFHostingWin32Control#WPFUI](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml#wpfui)]  
  
<a name="host_class"></a>
## <a name="implement-a-class-to-host-the-microsoft-win32-control"></a><span data-ttu-id="897a5-152">實作類別以裝載 Microsoft Win32 控制項</span><span class="sxs-lookup"><span data-stu-id="897a5-152">Implement a Class to Host the Microsoft Win32 Control</span></span>  
 <span data-ttu-id="897a5-153">此範例的核心是實際裝載 ControlHost.cs 控制項的類別。</span><span class="sxs-lookup"><span data-stu-id="897a5-153">The core of this sample is the class that actually hosts the control, ControlHost.cs.</span></span> <span data-ttu-id="897a5-154">它繼承自 <xref:System.Windows.Interop.HwndHost>。</span><span class="sxs-lookup"><span data-stu-id="897a5-154">It inherits from <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="897a5-155">建構函式採用兩個參數，高度和寬度，對應于承載 ListBox 控制項<xref:System.Windows.Controls.Border>的元素的高度和寬度。</span><span class="sxs-lookup"><span data-stu-id="897a5-155">The constructor takes two parameters, height and width, which correspond to the height and width of the <xref:System.Windows.Controls.Border> element that hosts the ListBox control.</span></span> <span data-ttu-id="897a5-156">這些值稍後用於確保控制項的大小與<xref:System.Windows.Controls.Border>元素匹配。</span><span class="sxs-lookup"><span data-stu-id="897a5-156">These values are used later to ensure that the size of the control matches the <xref:System.Windows.Controls.Border> element.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#ControlHostClass](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#controlhostclass)]
 [!code-vb[WPFHostingWin32Control#ControlHostClass](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#controlhostclass)]  
  
 <span data-ttu-id="897a5-157">還有一組常數。</span><span class="sxs-lookup"><span data-stu-id="897a5-157">There is also a set of constants.</span></span> <span data-ttu-id="897a5-158">這些常量大部分取自 Winuser.h，允許您在調用 Win32 函數時使用傳統名稱。</span><span class="sxs-lookup"><span data-stu-id="897a5-158">These constants are largely taken from Winuser.h, and allow you to use conventional names when calling Win32 functions.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#ControlHostConstants](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#controlhostconstants)]
 [!code-vb[WPFHostingWin32Control#ControlHostConstants](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#controlhostconstants)]  
  
<a name="buildwindowcore"></a>
### <a name="override-buildwindowcore-to-create-the-microsoft-win32-window"></a><span data-ttu-id="897a5-159">覆寫 BuildWindowCore 以建立 Microsoft Win32 視窗</span><span class="sxs-lookup"><span data-stu-id="897a5-159">Override BuildWindowCore to Create the Microsoft Win32 Window</span></span>  
 <span data-ttu-id="897a5-160">重寫此方法以創建將由頁面承載的 Win32 視窗，並在視窗和頁面之間建立連接。</span><span class="sxs-lookup"><span data-stu-id="897a5-160">You override this method to create the Win32 window that will be hosted by the page, and make the connection between the window and the page.</span></span> <span data-ttu-id="897a5-161">因為此範例包含裝載 ListBox 控制項，所以會建立兩個視窗。</span><span class="sxs-lookup"><span data-stu-id="897a5-161">Because this sample involves hosting a ListBox Control, two windows are created.</span></span> <span data-ttu-id="897a5-162">第一個是 WPF 頁實際承載的視窗。</span><span class="sxs-lookup"><span data-stu-id="897a5-162">The first is the window that is actually hosted by the WPF page.</span></span> <span data-ttu-id="897a5-163">ListBox 控制項會建立為該視窗的子系。</span><span class="sxs-lookup"><span data-stu-id="897a5-163">The ListBox Control is created as a child of that window.</span></span>  
  
 <span data-ttu-id="897a5-164">此方法的原因是要簡化接收來自控制項之通知的程序。</span><span class="sxs-lookup"><span data-stu-id="897a5-164">The reason for this approach is to simplify the process of receiving notifications from the control.</span></span> <span data-ttu-id="897a5-165">該<xref:System.Windows.Interop.HwndHost>類允許您處理發送到它所承載的視窗的消息。</span><span class="sxs-lookup"><span data-stu-id="897a5-165">The <xref:System.Windows.Interop.HwndHost> class allows you to process messages sent to the window that it is hosting.</span></span> <span data-ttu-id="897a5-166">如果直接承載 Win32 控制項，則會收到發送到控制項的內部訊息迴圈的消息。</span><span class="sxs-lookup"><span data-stu-id="897a5-166">If you host a Win32 control directly, you receive the messages sent to the internal message loop of the control.</span></span> <span data-ttu-id="897a5-167">您可以顯示控制項並將訊息傳送給它，但收不到控制項傳送至其父視窗的通知。</span><span class="sxs-lookup"><span data-stu-id="897a5-167">You can display the control and send it messages, but you do not receive the notifications that the control sends to its parent window.</span></span> <span data-ttu-id="897a5-168">這表示，除此之外，您偵測不到使用者何時與控制項互動。</span><span class="sxs-lookup"><span data-stu-id="897a5-168">This means, among other things, that you have no way of detecting when the user interacts with the control.</span></span> <span data-ttu-id="897a5-169">相反地，會建立主視窗，並將控制項設定為該視窗的子系。</span><span class="sxs-lookup"><span data-stu-id="897a5-169">Instead, create a host window and make the control a child of that window.</span></span> <span data-ttu-id="897a5-170">這可讓您處理主視窗的訊息，包括控制項傳送給它的通知。</span><span class="sxs-lookup"><span data-stu-id="897a5-170">This allows you to process the messages for the host window including the notifications sent to it by the control.</span></span> <span data-ttu-id="897a5-171">為了方便起見，因為主視窗略高於控制項的簡單包裝函式，所以此套件將稱為 ListBox 控制項。</span><span class="sxs-lookup"><span data-stu-id="897a5-171">For convenience, since the host window is little more than a simple wrapper for the control, the package will be referred to as a ListBox control.</span></span>  
  
<a name="create_the_window_and_listbox"></a>
#### <a name="create-the-host-window-and-listbox-control"></a><span data-ttu-id="897a5-172">建立主視窗和 ListBox 控制項</span><span class="sxs-lookup"><span data-stu-id="897a5-172">Create the Host Window and ListBox Control</span></span>  
 <span data-ttu-id="897a5-173">可以使用 PInvoke 通過創建和註冊視窗類等為控制項創建主機視窗。</span><span class="sxs-lookup"><span data-stu-id="897a5-173">You can use PInvoke to create a host window for the control by creating and registering a window class, and so on.</span></span> <span data-ttu-id="897a5-174">不過，比較簡單的方法是使用預先定義的「靜態」視窗類別來建立視窗。</span><span class="sxs-lookup"><span data-stu-id="897a5-174">However, a much simpler approach is to create a window with the predefined "static" window class.</span></span> <span data-ttu-id="897a5-175">這提供接收來自控制項之通知所需的視窗程序，而且需要最少的程式碼。</span><span class="sxs-lookup"><span data-stu-id="897a5-175">This provides you with the window procedure you need in order to receive notifications from the control, and requires minimal coding.</span></span>  
  
 <span data-ttu-id="897a5-176">控制項的 HWND 是透過唯讀屬性所公開；因此，主頁面可以使用它，以將訊息傳送至控制項。</span><span class="sxs-lookup"><span data-stu-id="897a5-176">The HWND of the control is exposed through a read-only property, such that the host page can use it to send messages to the control.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#IntPtrProperty](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#intptrproperty)]
 [!code-vb[WPFHostingWin32Control#IntPtrProperty](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#intptrproperty)]  
  
 <span data-ttu-id="897a5-177">ListBox 控制項會建立為主視窗的子系。</span><span class="sxs-lookup"><span data-stu-id="897a5-177">The ListBox control is created as a child of the host window.</span></span> <span data-ttu-id="897a5-178">如上面所討論，這兩個視窗的高度和寬度都會設定為傳遞至建構函式的值。</span><span class="sxs-lookup"><span data-stu-id="897a5-178">The height and width of both windows are set to the values passed to the constructor, discussed above.</span></span> <span data-ttu-id="897a5-179">這確保主視窗和控制項的大小與頁面上的保留區域相同。</span><span class="sxs-lookup"><span data-stu-id="897a5-179">This ensures that the size of the host window and control is identical to the reserved area on the page.</span></span>  <span data-ttu-id="897a5-180">創建視窗後，該示例將返回包含<xref:System.Runtime.InteropServices.HandleRef>主機視窗的 HWND 的物件。</span><span class="sxs-lookup"><span data-stu-id="897a5-180">After the windows are created, the sample returns a <xref:System.Runtime.InteropServices.HandleRef> object that contains the HWND of the host window.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#BuildWindowCore](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#buildwindowcore)]
 [!code-vb[WPFHostingWin32Control#BuildWindowCore](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#buildwindowcore)]  
  
 [!code-csharp[WPFHostingWin32Control#BuildWindowCoreHelper](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#buildwindowcorehelper)]
 [!code-vb[WPFHostingWin32Control#BuildWindowCoreHelper](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#buildwindowcorehelper)]  
  
<a name="destroywindow_wndproc"></a>
### <a name="implement-destroywindow-and-wndproc"></a><span data-ttu-id="897a5-181">實作 DestroyWindow 和 WndProc</span><span class="sxs-lookup"><span data-stu-id="897a5-181">Implement DestroyWindow and WndProc</span></span>  
 <span data-ttu-id="897a5-182">除了 之外<xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>，還必須重寫 的<xref:System.Windows.Interop.HwndHost.WndProc%2A><xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A><xref:System.Windows.Interop.HwndHost>和 方法。</span><span class="sxs-lookup"><span data-stu-id="897a5-182">In addition to <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>, you must also override the <xref:System.Windows.Interop.HwndHost.WndProc%2A> and <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> methods of the <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="897a5-183">在此示例中，控制項的消息由<xref:System.Windows.Interop.HwndHost.MessageHook>處理常式處理，因此 ，<xref:System.Windows.Interop.HwndHost.WndProc%2A><xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A>的實現是最小的。</span><span class="sxs-lookup"><span data-stu-id="897a5-183">In this example, the messages for the control are handled by the <xref:System.Windows.Interop.HwndHost.MessageHook> handler, thus the implementation of <xref:System.Windows.Interop.HwndHost.WndProc%2A> and <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> is minimal.</span></span> <span data-ttu-id="897a5-184">在 中<xref:System.Windows.Interop.HwndHost.WndProc%2A>，設置為`handled``false`指示消息未處理，並返回 0。</span><span class="sxs-lookup"><span data-stu-id="897a5-184">In the case of <xref:System.Windows.Interop.HwndHost.WndProc%2A>, set `handled` to `false` to indicate that the  message was not handled and return 0.</span></span> <span data-ttu-id="897a5-185">對於<xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A>，只需破壞視窗。</span><span class="sxs-lookup"><span data-stu-id="897a5-185">For <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A>, simply destroy the window.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#WndProcDestroy](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#wndprocdestroy)]
 [!code-vb[WPFHostingWin32Control#WndProcDestroy](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#wndprocdestroy)]  
  
 [!code-csharp[WPFHostingWin32Control#WndProcDestroyHelper](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#wndprocdestroyhelper)]
 [!code-vb[WPFHostingWin32Control#WndProcDestroyHelper](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#wndprocdestroyhelper)]  
  
<a name="host_the_control"></a>
## <a name="host-the-control-on-the-page"></a><span data-ttu-id="897a5-186">在頁面上裝載控制項</span><span class="sxs-lookup"><span data-stu-id="897a5-186">Host the Control on the Page</span></span>  
 <span data-ttu-id="897a5-187">要在頁面上承載控制項，請首先創建`ControlHost`類的新實例。</span><span class="sxs-lookup"><span data-stu-id="897a5-187">To host the control on the page, you first create a new instance of the `ControlHost` class.</span></span> <span data-ttu-id="897a5-188">將包含控制項 （`ControlHostElement`） 的邊框元素的高度和寬度傳遞給`ControlHost`建構函式。</span><span class="sxs-lookup"><span data-stu-id="897a5-188">Pass the height and width of the border element that contains the control (`ControlHostElement`) to the `ControlHost` constructor.</span></span> <span data-ttu-id="897a5-189">這確保 ListBox 的大小正確。</span><span class="sxs-lookup"><span data-stu-id="897a5-189">This ensures that the ListBox is sized correctly.</span></span> <span data-ttu-id="897a5-190">然後，通過將物件`ControlHost`<xref:System.Windows.Controls.Decorator.Child%2A>分配給主機<xref:System.Windows.Controls.Border>的屬性，在頁面上託管控制項。</span><span class="sxs-lookup"><span data-stu-id="897a5-190">You then host the control on the page by assigning the `ControlHost` object to the <xref:System.Windows.Controls.Decorator.Child%2A> property of the host <xref:System.Windows.Controls.Border>.</span></span>  
  
 <span data-ttu-id="897a5-191">該示例將處理常式附加到<xref:System.Windows.Interop.HwndHost.MessageHook>`ControlHost`要從控制項接收消息的事件。</span><span class="sxs-lookup"><span data-stu-id="897a5-191">The sample attaches a handler to the <xref:System.Windows.Interop.HwndHost.MessageHook> event of the `ControlHost` to receive messages from the control.</span></span> <span data-ttu-id="897a5-192">針對傳送至裝載之視窗的每個訊息，都會引發此事件。</span><span class="sxs-lookup"><span data-stu-id="897a5-192">This event is raised for every message sent to the hosted window.</span></span> <span data-ttu-id="897a5-193">在此情況下，這些是傳送至包裝實際 ListBox 控制項之視窗的訊息，包括來自控制項的通知。</span><span class="sxs-lookup"><span data-stu-id="897a5-193">In this case, these are the messages sent to window that wraps the actual ListBox control, including notifications from the control.</span></span> <span data-ttu-id="897a5-194">此範例會呼叫 SendMessage 以從控制項取得資訊，並修改其內容。</span><span class="sxs-lookup"><span data-stu-id="897a5-194">The sample calls SendMessage to obtain information from the control and modify its contents.</span></span> <span data-ttu-id="897a5-195">下節討論頁面如何與控制項進行通訊的詳細資料。</span><span class="sxs-lookup"><span data-stu-id="897a5-195">The details of how the page communicates with the control are discussed in the next section.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="897a5-196">請注意，有兩個 PInvoke 聲明用於發送消息。</span><span class="sxs-lookup"><span data-stu-id="897a5-196">Notice that there are two PInvoke declarations for SendMessage.</span></span> <span data-ttu-id="897a5-197">這是必需的，因為一個使用`wParam`參數傳遞字串，另一個使用它傳遞整數。</span><span class="sxs-lookup"><span data-stu-id="897a5-197">This is necessary because one uses the `wParam` parameter to pass a string and the other uses it to pass an integer.</span></span> <span data-ttu-id="897a5-198">每個簽章都必須要有不同的宣告，確保正確地封送處理資料。</span><span class="sxs-lookup"><span data-stu-id="897a5-198">You need a separate declaration for each signature to ensure that the data is marshaled correctly.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#HostWindowClass](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#hostwindowclass)]
 [!code-vb[WPFHostingWin32Control#HostWindowClass](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#hostwindowclass)]  
  
 [!code-csharp[WPFHostingWin32Control#ControlMsgFilterSendMessage](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#controlmsgfiltersendmessage)]
 [!code-vb[WPFHostingWin32Control#ControlMsgFilterSendMessage](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#controlmsgfiltersendmessage)]  
  
<a name="communication"></a>
## <a name="implement-communication-between-the-control-and-the-page"></a><span data-ttu-id="897a5-199">實作控制項與頁面之間的通訊</span><span class="sxs-lookup"><span data-stu-id="897a5-199">Implement Communication Between the Control and the Page</span></span>  
 <span data-ttu-id="897a5-200">通過向控制項發送 Windows 消息來操作該控制項。</span><span class="sxs-lookup"><span data-stu-id="897a5-200">You manipulate the control by sending it Windows messages.</span></span> <span data-ttu-id="897a5-201">在使用者透過將通知傳送至其主視窗與控制項互動時，控制項就會通知您。</span><span class="sxs-lookup"><span data-stu-id="897a5-201">The control notifies you when the user interacts with it by sending notifications to its host window.</span></span> <span data-ttu-id="897a5-202">[在 WPF 示例中託管 Win32 ListBox 控制項](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control)包括一個 UI，它提供了幾個示例，說明其工作原理：</span><span class="sxs-lookup"><span data-stu-id="897a5-202">The [Hosting a Win32 ListBox Control in WPF](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control) sample includes a UI that provides several examples of how this works:</span></span>  
  
- <span data-ttu-id="897a5-203">將項目附加至清單。</span><span class="sxs-lookup"><span data-stu-id="897a5-203">Append an item to the list.</span></span>  
  
- <span data-ttu-id="897a5-204">刪除清單中選取的項目</span><span class="sxs-lookup"><span data-stu-id="897a5-204">Delete the selected item from the list</span></span>  
  
- <span data-ttu-id="897a5-205">顯示目前所選取項目的文字。</span><span class="sxs-lookup"><span data-stu-id="897a5-205">Display the text of the currently selected item.</span></span>  
  
- <span data-ttu-id="897a5-206">顯示清單中的項目數。</span><span class="sxs-lookup"><span data-stu-id="897a5-206">Display the number of items in the list.</span></span>  
  
 <span data-ttu-id="897a5-207">使用者還可以通過按一下清單方塊中的專案來選擇該專案，就像對於傳統的 Win32 應用程式一樣。</span><span class="sxs-lookup"><span data-stu-id="897a5-207">The user can also select an item in the list box by clicking on it, just as they would for a conventional Win32 application.</span></span> <span data-ttu-id="897a5-208">每次使用者透過選取、新增或附加項目來變更清單方塊的狀態時，都會更新顯示的資料。</span><span class="sxs-lookup"><span data-stu-id="897a5-208">The displayed data is updated each time the user changes the state of the list box by selecting, adding, or appending an item.</span></span>  
  
 <span data-ttu-id="897a5-209">要追加專案，請將清單方塊發送[`LB_ADDSTRING`一條消息](/windows/desktop/Controls/lb-addstring)。</span><span class="sxs-lookup"><span data-stu-id="897a5-209">To append items, send the list box an [`LB_ADDSTRING` message](/windows/desktop/Controls/lb-addstring).</span></span> <span data-ttu-id="897a5-210">要刪除專案，請[`LB_GETCURSEL`](/windows/desktop/Controls/lb-getcursel)發送以獲取當前選擇的索引，然後[`LB_DELETESTRING`](/windows/desktop/Controls/lb-deletestring)刪除該專案。</span><span class="sxs-lookup"><span data-stu-id="897a5-210">To delete items, send [`LB_GETCURSEL`](/windows/desktop/Controls/lb-getcursel) to get the index of the current selection and then [`LB_DELETESTRING`](/windows/desktop/Controls/lb-deletestring) to delete the item.</span></span> <span data-ttu-id="897a5-211">該示例還發送[`LB_GETCOUNT`](/windows/desktop/Controls/lb-getcount)，並使用返回的值更新顯示項數的顯示。</span><span class="sxs-lookup"><span data-stu-id="897a5-211">The sample also sends [`LB_GETCOUNT`](/windows/desktop/Controls/lb-getcount), and uses the returned value to update the display that shows the number of items.</span></span> <span data-ttu-id="897a5-212">這兩個實例[`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage)都使用上一節中討論的 PInvoke 聲明之一。</span><span class="sxs-lookup"><span data-stu-id="897a5-212">Both these instances of [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) use one of the PInvoke declarations discussed in the previous section.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#AppendDeleteText](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#appenddeletetext)]
 [!code-vb[WPFHostingWin32Control#AppendDeleteText](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#appenddeletetext)]  
  
 <span data-ttu-id="897a5-213">當使用者選擇專案或更改其選擇時，控制項通過向宿主視窗[`WM_COMMAND`發送消息來通知](/windows/desktop/menurc/wm-command)主機視窗，這將引發頁面<xref:System.Windows.Interop.HwndHost.MessageHook>的事件。</span><span class="sxs-lookup"><span data-stu-id="897a5-213">When the user selects an item or changes their selection, the control notifies the host window by sending it a [`WM_COMMAND` message](/windows/desktop/menurc/wm-command), which raises the <xref:System.Windows.Interop.HwndHost.MessageHook> event for the page.</span></span> <span data-ttu-id="897a5-214">處理常式會接收與主視窗的主要視窗程序相同的資訊。</span><span class="sxs-lookup"><span data-stu-id="897a5-214">The handler receives the same information as the main window procedure of the host window.</span></span> <span data-ttu-id="897a5-215">它還傳遞對布林值的`handled`引用。</span><span class="sxs-lookup"><span data-stu-id="897a5-215">It also passes a reference to a Boolean value, `handled`.</span></span> <span data-ttu-id="897a5-216">您設置為`handled``true`指示已處理消息，無需進一步處理。</span><span class="sxs-lookup"><span data-stu-id="897a5-216">You set `handled` to `true` to indicate that you have handled the message and no further processing is needed.</span></span>  
  
 <span data-ttu-id="897a5-217">[`WM_COMMAND`](/windows/desktop/menurc/wm-command)發送的原因有多種，因此您必須檢查通知 ID 以確定它是否是您希望處理的事件。</span><span class="sxs-lookup"><span data-stu-id="897a5-217">[`WM_COMMAND`](/windows/desktop/menurc/wm-command) is sent for a variety of reasons, so you must examine the notification ID to determine whether it is an event that you wish to handle.</span></span> <span data-ttu-id="897a5-218">ID 包含在`wParam`參數的高字中。</span><span class="sxs-lookup"><span data-stu-id="897a5-218">The ID is contained in the high word of the `wParam` parameter.</span></span> <span data-ttu-id="897a5-219">該示例使用位運算子提取 ID。</span><span class="sxs-lookup"><span data-stu-id="897a5-219">The sample uses bitwise operators to extract the ID.</span></span> <span data-ttu-id="897a5-220">如果使用者已創建或更改其選擇，則 ID 將為[`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange)。</span><span class="sxs-lookup"><span data-stu-id="897a5-220">If the user has made or changed their selection, the ID will be [`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange).</span></span>  
  
 <span data-ttu-id="897a5-221">收到[`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange)時，示例通過向控制項[`LB_GETCURSEL`](/windows/desktop/Controls/lb-getcursel)發送消息來獲取所選項的索引。</span><span class="sxs-lookup"><span data-stu-id="897a5-221">When [`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange) is received, the sample gets the index of the selected item by sending the control a [`LB_GETCURSEL` message](/windows/desktop/Controls/lb-getcursel).</span></span> <span data-ttu-id="897a5-222">要獲取文本，請首先創建 一<xref:System.Text.StringBuilder>個 。</span><span class="sxs-lookup"><span data-stu-id="897a5-222">To get the text, you first create a <xref:System.Text.StringBuilder>.</span></span> <span data-ttu-id="897a5-223">然後，向控制項[`LB_GETTEXT`發送消息。](/windows/desktop/Controls/lb-gettext)</span><span class="sxs-lookup"><span data-stu-id="897a5-223">You then send the control an [`LB_GETTEXT` message](/windows/desktop/Controls/lb-gettext).</span></span> <span data-ttu-id="897a5-224">將空<xref:System.Text.StringBuilder>物件作為參數傳遞`wParam`。</span><span class="sxs-lookup"><span data-stu-id="897a5-224">Pass the empty <xref:System.Text.StringBuilder> object as the `wParam` parameter.</span></span> <span data-ttu-id="897a5-225">返回[`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage)時，<xref:System.Text.StringBuilder>將包含所選項的文本。</span><span class="sxs-lookup"><span data-stu-id="897a5-225">When [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) returns, the <xref:System.Text.StringBuilder> will contain the text of the selected item.</span></span> <span data-ttu-id="897a5-226">這種使用[`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage)需要另一個 PInvoke 聲明。</span><span class="sxs-lookup"><span data-stu-id="897a5-226">This use of [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) requires yet another PInvoke declaration.</span></span>  
  
 <span data-ttu-id="897a5-227">最後，設置為`handled``true`指示消息已處理。</span><span class="sxs-lookup"><span data-stu-id="897a5-227">Finally, set `handled` to `true` to indicate that the message has been handled.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="897a5-228">另請參閱</span><span class="sxs-lookup"><span data-stu-id="897a5-228">See also</span></span>

- <xref:System.Windows.Interop.HwndHost>
- [<span data-ttu-id="897a5-229">WPF 和 Win32 交互操作</span><span class="sxs-lookup"><span data-stu-id="897a5-229">WPF and Win32 Interoperation</span></span>](wpf-and-win32-interoperation.md)
- [<span data-ttu-id="897a5-230">逐步解說：我的第一個 WPF 桌面應用程式</span><span class="sxs-lookup"><span data-stu-id="897a5-230">Walkthrough: My first WPF desktop application</span></span>](../getting-started/walkthrough-my-first-wpf-desktop-application.md)
