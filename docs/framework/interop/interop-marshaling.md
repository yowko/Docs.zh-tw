---
title: Interop 封送處理
ms.date: 03/30/2017
helpviewer_keywords:
- marshaling, COM interop
- interop marshaling
- interop marshaling, about interop marshaling
ms.assetid: 115f7a2f-d422-4605-ab36-13a8dd28142a
ms.openlocfilehash: 70514811a9d236dc485f64fc34297cdb057a1512
ms.sourcegitcommit: 559fcfbe4871636494870a8b716bf7325df34ac5
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 10/30/2019
ms.locfileid: "73124280"
---
# <a name="interop-marshaling"></a><span data-ttu-id="191cd-102">Interop 封送處理</span><span class="sxs-lookup"><span data-stu-id="191cd-102">Interop Marshaling</span></span>

<span data-ttu-id="191cd-103">Interop 封送處理會控制如何以方法引數傳遞資料，以及控制呼叫期間於 Managed 和 Unmanaged 記憶體之間的傳回值。</span><span class="sxs-lookup"><span data-stu-id="191cd-103">Interop marshaling governs how data is passed in method arguments and return values between managed and unmanaged memory during calls.</span></span> <span data-ttu-id="191cd-104">Interop 封送處理是由 Common Language Runtime 的封送處理服務所執行的執行階段活動。</span><span class="sxs-lookup"><span data-stu-id="191cd-104">Interop marshaling is a run-time activity performed by the common language runtime's marshaling service.</span></span>

<span data-ttu-id="191cd-105">大部分的資料類型在 Managed 和 Unmanaged 記憶體中有共通的表示。</span><span class="sxs-lookup"><span data-stu-id="191cd-105">Most data types have common representations in both managed and unmanaged memory.</span></span> <span data-ttu-id="191cd-106">Interop 封送處理器會為您處理這些類型。</span><span class="sxs-lookup"><span data-stu-id="191cd-106">The interop marshaler handles these types for you.</span></span> <span data-ttu-id="191cd-107">其他類型可能模稜兩可，或根本不會在 Managed 記憶體中表示。</span><span class="sxs-lookup"><span data-stu-id="191cd-107">Other types can be ambiguous or not represented at all in managed memory.</span></span>

<span data-ttu-id="191cd-108">模稜兩可的類型不是有多個對應到單一 Managed 類型的 Unmanaged 表示，就是遺漏類型資訊 (例如陣列的大小)。</span><span class="sxs-lookup"><span data-stu-id="191cd-108">An ambiguous type can have either multiple unmanaged representations that map to a single managed type, or missing type information, such as the size of an array.</span></span> <span data-ttu-id="191cd-109">針對模稜兩可的類型，封送處理器會提供預設表示，以及替代表示 (其中有多個表示)。</span><span class="sxs-lookup"><span data-stu-id="191cd-109">For ambiguous types, the marshaler provides a default representation and alternative representations where multiple representations exist.</span></span> <span data-ttu-id="191cd-110">您可以將有關如何封送處理模稜兩可類型的明確指示，提供給封送處理器。</span><span class="sxs-lookup"><span data-stu-id="191cd-110">You can supply explicit instructions to the marshaler on how it is to marshal an ambiguous type.</span></span>

## <a name="platform-invoke-and-com-interop-models"></a><span data-ttu-id="191cd-111">平台叫用和 COM Interop 模型</span><span class="sxs-lookup"><span data-stu-id="191cd-111">Platform Invoke and COM Interop Models</span></span>

<span data-ttu-id="191cd-112">Common Language Runtime 提供兩種與 Unmanaged 程式碼互通的機制：</span><span class="sxs-lookup"><span data-stu-id="191cd-112">The common language runtime provides two mechanisms for interoperating with unmanaged code:</span></span>

- <span data-ttu-id="191cd-113">平台叫用，可讓 Managed 程式碼呼叫 Unmanaged 程式庫所匯出的函式。</span><span class="sxs-lookup"><span data-stu-id="191cd-113">Platform invoke, which enables managed code to call functions exported from an unmanaged library.</span></span>
- <span data-ttu-id="191cd-114">COM Interop，可讓 Managed 程式碼透過介面與元件物件模型 (COM) 物件互動。</span><span class="sxs-lookup"><span data-stu-id="191cd-114">COM interop, which enables managed code to interact with Component Object Model (COM) objects through interfaces.</span></span>

<span data-ttu-id="191cd-115">平台叫用和 COM Interop 都會使用 Interop 封送處理，正確地將方法引數在呼叫端和被呼叫端之間來回移動 (如有必要)。</span><span class="sxs-lookup"><span data-stu-id="191cd-115">Both platform invoke and COM interop use interop marshaling to accurately move method arguments between caller and callee and back, if required.</span></span> <span data-ttu-id="191cd-116">如下圖所示，除非需要[回呼函式](callback-functions.md)，否則平台叫用方法呼叫會從 Managed 程式碼流向 Unmanaged 程式碼，但絕不會反向流回。</span><span class="sxs-lookup"><span data-stu-id="191cd-116">As the following illustration shows, a platform invoke method call flows from managed to unmanaged code and never the other way, except when [callback functions](callback-functions.md) are involved.</span></span> <span data-ttu-id="191cd-117">即使平台叫用呼叫只可以從 Managed 程式碼流向 Unmanaged 程式碼，資料還是可以當做輸入或輸出參數來進行雙向流動。</span><span class="sxs-lookup"><span data-stu-id="191cd-117">Even though platform invoke calls can flow only from managed to unmanaged code, data can flow in both directions as input or output parameters.</span></span> <span data-ttu-id="191cd-118">COM Interop 方法呼叫可以流向任一方向。</span><span class="sxs-lookup"><span data-stu-id="191cd-118">COM interop method calls can flow in either direction.</span></span>

<span data-ttu-id="191cd-119">![平台叫用](./media/interop-marshaling/interop-marshaling-invoke-and-com.png "平台叫用和 COM Interop 呼叫流程")</span><span class="sxs-lookup"><span data-stu-id="191cd-119">![Platform invoke](./media/interop-marshaling/interop-marshaling-invoke-and-com.png "Platform invoke and COM interop call flow")</span></span>

<span data-ttu-id="191cd-120">這兩種機制在最低層級會使用相同的 Interop 封送處理服務，但特定資料類型只受 COM Interop 或平台叫用支援。</span><span class="sxs-lookup"><span data-stu-id="191cd-120">At the lowest level, both mechanisms use the same interop marshaling service; however, certain data types are supported exclusively by COM interop or platform invoke.</span></span> <span data-ttu-id="191cd-121">如需詳細資料，請參閱[預設的封送處理行為](default-marshaling-behavior.md)。</span><span class="sxs-lookup"><span data-stu-id="191cd-121">For details, see [Default Marshaling Behavior](default-marshaling-behavior.md).</span></span>

## <a name="marshaling-and-com-apartments"></a><span data-ttu-id="191cd-122">封送處理和 COM Apartment</span><span class="sxs-lookup"><span data-stu-id="191cd-122">Marshaling and COM Apartments</span></span>

<span data-ttu-id="191cd-123">Interop 封送處理器會在 Common Language Runtime 堆積和 Unmanaged 堆積之間封送處理資料。</span><span class="sxs-lookup"><span data-stu-id="191cd-123">The interop marshaler marshals data between the common language runtime heap and the unmanaged heap.</span></span> <span data-ttu-id="191cd-124">當呼叫端和被呼叫端無法在相同的資料執行個體上進行操作時，就會發生封送處理。</span><span class="sxs-lookup"><span data-stu-id="191cd-124">Marshaling occurs whenever the caller and callee cannot operate on the same instance of data.</span></span> <span data-ttu-id="191cd-125">即使呼叫端和被呼叫端擁有其自己的資料複本，Interop 封送處理器還是可以讓呼叫端和被呼叫端在相同的資料上進行操作。</span><span class="sxs-lookup"><span data-stu-id="191cd-125">The interop marshaler makes it possible for the caller and callee to appear to be operating on the same data even if they have their own copy of the data.</span></span>

<span data-ttu-id="191cd-126">COM 也有一個封送處理器，可在不同的 COM Apartment 或不同的 COM 處理序之間封送處理資料。</span><span class="sxs-lookup"><span data-stu-id="191cd-126">COM also has a marshaler that marshals data between COM apartments or different COM processes.</span></span> <span data-ttu-id="191cd-127">在相同 COM Apartment 中的 Managed 和 Unmanaged 程式碼之間呼叫時，只需要 Interop 封送處理器。</span><span class="sxs-lookup"><span data-stu-id="191cd-127">When calling between managed and unmanaged code within the same COM apartment, the interop marshaler is the only marshaler involved.</span></span> <span data-ttu-id="191cd-128">在不同 COM Apartment 或不同處理序中的 Managed 程式碼和 Unmanaged 程式碼之間呼叫時，則同時需要 Interop 封送處理器和 COM 封送處理器。</span><span class="sxs-lookup"><span data-stu-id="191cd-128">When calling between managed code and unmanaged code in a different COM apartment or a different process, both the interop marshaler and the COM marshaler are involved.</span></span>

### <a name="com-clients-and-managed-servers"></a><span data-ttu-id="191cd-129">COM 用戶端和 Managed 伺服器</span><span class="sxs-lookup"><span data-stu-id="191cd-129">COM Clients and Managed Servers</span></span>

<span data-ttu-id="191cd-130">具有 [Regasm.exe (組件註冊工具)](../tools/regasm-exe-assembly-registration-tool.md) 所註冊之類型程式庫的已匯出 Managed 伺服器，會將 `ThreadingModel` 登錄項目設定為 `Both`。</span><span class="sxs-lookup"><span data-stu-id="191cd-130">An exported managed server with a type library registered by the [Regasm.exe (Assembly Registration Tool)](../tools/regasm-exe-assembly-registration-tool.md) has a `ThreadingModel` registry entry set to `Both`.</span></span> <span data-ttu-id="191cd-131">這個值表示伺服器可在單一執行緒 Apartment (STA) 或多執行緒 Apartment (MTA) 中啟動。</span><span class="sxs-lookup"><span data-stu-id="191cd-131">This value indicates that the server can be activated in a single-threaded apartment (STA) or a multithreaded apartment (MTA).</span></span> <span data-ttu-id="191cd-132">伺服器物件會建立在與其呼叫者相同的單元中，如下表所示：</span><span class="sxs-lookup"><span data-stu-id="191cd-132">The server object is created in the same apartment as its caller, as shown in the following table:</span></span>

|<span data-ttu-id="191cd-133">COM 用戶端</span><span class="sxs-lookup"><span data-stu-id="191cd-133">COM client</span></span>|<span data-ttu-id="191cd-134">.NET 伺服器</span><span class="sxs-lookup"><span data-stu-id="191cd-134">.NET server</span></span>|<span data-ttu-id="191cd-135">封送處理需求</span><span class="sxs-lookup"><span data-stu-id="191cd-135">Marshaling requirements</span></span>|
|----------------|-----------------|-----------------------------|
|<span data-ttu-id="191cd-136">STA</span><span class="sxs-lookup"><span data-stu-id="191cd-136">STA</span></span>|<span data-ttu-id="191cd-137">`Both` 變成 STA。</span><span class="sxs-lookup"><span data-stu-id="191cd-137">`Both` becomes STA.</span></span>|<span data-ttu-id="191cd-138">相同 Apartment 封送處理。</span><span class="sxs-lookup"><span data-stu-id="191cd-138">Same-apartment marshaling.</span></span>|
|<span data-ttu-id="191cd-139">MTA</span><span class="sxs-lookup"><span data-stu-id="191cd-139">MTA</span></span>|<span data-ttu-id="191cd-140">`Both` 變成 MTA。</span><span class="sxs-lookup"><span data-stu-id="191cd-140">`Both` becomes MTA.</span></span>|<span data-ttu-id="191cd-141">相同 Apartment 封送處理。</span><span class="sxs-lookup"><span data-stu-id="191cd-141">Same-apartment marshaling.</span></span>|

<span data-ttu-id="191cd-142">由於用戶端和伺服器位於相同的 Apartment 中，因此 Interop 封送處理服務會自動處理所有資料封送處理。</span><span class="sxs-lookup"><span data-stu-id="191cd-142">Because the client and server are in the same apartment, the interop marshaling service automatically handles all data marshaling.</span></span> <span data-ttu-id="191cd-143">下圖顯示 Interop 封送處理服務在相同 COM 樣式 Apartment 內的 Managed 和 Unmanaged 堆積之間進行操作。</span><span class="sxs-lookup"><span data-stu-id="191cd-143">The following illustration shows the interop marshaling service operating between managed and unmanaged heaps within the same COM-style apartment.</span></span>

<span data-ttu-id="191cd-144">![Managed 和非受控堆積之間的 Interop 封送處理](./media/interop-marshaling/interop-heaps-managed-and-unmanaged.gif "相同 Apartment 封送處理流程")</span><span class="sxs-lookup"><span data-stu-id="191cd-144">![Interop marshaling between managed and unmanaged heaps](./media/interop-marshaling/interop-heaps-managed-and-unmanaged.gif "Same-apartment marshaling process")</span></span>

<span data-ttu-id="191cd-145">如果您打算匯出 Managed 伺服器，請注意 COM 用戶端會決定伺服器的 Apartment。</span><span class="sxs-lookup"><span data-stu-id="191cd-145">If you plan to export a managed server, be aware that the COM client determines the apartment of the server.</span></span> <span data-ttu-id="191cd-146">由 MTA 中初始化的 COM 用戶端所呼叫的 Managed 伺服器必須確保執行緒安全。</span><span class="sxs-lookup"><span data-stu-id="191cd-146">A managed server called by a COM client initialized in an MTA must ensure thread safety.</span></span>

### <a name="managed-clients-and-com-servers"></a><span data-ttu-id="191cd-147">Managed 用戶端和 COM 伺服器</span><span class="sxs-lookup"><span data-stu-id="191cd-147">Managed Clients and COM Servers</span></span>

<span data-ttu-id="191cd-148">Managed 用戶端 Apartment 的預設值為 MTA；不過，.NET 用戶端的應用程式類型可以變更預設值。</span><span class="sxs-lookup"><span data-stu-id="191cd-148">The default setting for managed client apartments is MTA; however, the application type of the .NET client can change the default setting.</span></span> <span data-ttu-id="191cd-149">例如，Visual Basic 用戶端 Apartment 設定為 STA。</span><span class="sxs-lookup"><span data-stu-id="191cd-149">For example, a Visual Basic client apartment setting is STA.</span></span> <span data-ttu-id="191cd-150">您可以使用 <xref:System.STAThreadAttribute?displayProperty=nameWithType>、<xref:System.MTAThreadAttribute?displayProperty=nameWithType>、<xref:System.Threading.Thread.ApartmentState%2A?displayProperty=nameWithType> 屬性或 <xref:System.Web.UI.Page.AspCompatMode%2A?displayProperty=nameWithType> 屬性，來檢查及變更 Managed 用戶端的 Apartment 設定。</span><span class="sxs-lookup"><span data-stu-id="191cd-150">You can use the <xref:System.STAThreadAttribute?displayProperty=nameWithType>, the <xref:System.MTAThreadAttribute?displayProperty=nameWithType>, the <xref:System.Threading.Thread.ApartmentState%2A?displayProperty=nameWithType> property, or the <xref:System.Web.UI.Page.AspCompatMode%2A?displayProperty=nameWithType> property to examine and change the apartment setting of a managed client.</span></span>

<span data-ttu-id="191cd-151">元件作者會設定 COM 伺服器的執行緒相似性。</span><span class="sxs-lookup"><span data-stu-id="191cd-151">The author of the component sets the thread affinity of a COM server.</span></span> <span data-ttu-id="191cd-152">下表顯示 .NET 用戶端和 COM 伺服器的 Apartment 設定組合，</span><span class="sxs-lookup"><span data-stu-id="191cd-152">The following table shows the combinations of apartment settings for .NET clients and COM servers.</span></span> <span data-ttu-id="191cd-153">並顯示組合所產生的封送處理需求。</span><span class="sxs-lookup"><span data-stu-id="191cd-153">It also shows the resulting marshaling requirements for the combinations.</span></span>

|<span data-ttu-id="191cd-154">.NET 用戶端</span><span class="sxs-lookup"><span data-stu-id="191cd-154">.NET client</span></span>|<span data-ttu-id="191cd-155">COM 伺服器</span><span class="sxs-lookup"><span data-stu-id="191cd-155">COM server</span></span>|<span data-ttu-id="191cd-156">封送處理需求</span><span class="sxs-lookup"><span data-stu-id="191cd-156">Marshaling requirements</span></span>|
|-----------------|----------------|-----------------------------|
|<span data-ttu-id="191cd-157">MTA (預設值)</span><span class="sxs-lookup"><span data-stu-id="191cd-157">MTA (default)</span></span>|<span data-ttu-id="191cd-158">MTA</span><span class="sxs-lookup"><span data-stu-id="191cd-158">MTA</span></span><br /><br /> <span data-ttu-id="191cd-159">STA</span><span class="sxs-lookup"><span data-stu-id="191cd-159">STA</span></span>|<span data-ttu-id="191cd-160">Interop 封送處理。</span><span class="sxs-lookup"><span data-stu-id="191cd-160">Interop marshaling.</span></span><br /><br /> <span data-ttu-id="191cd-161">Interop 和 COM 封送處理。</span><span class="sxs-lookup"><span data-stu-id="191cd-161">Interop and COM marshaling.</span></span>|
|<span data-ttu-id="191cd-162">STA</span><span class="sxs-lookup"><span data-stu-id="191cd-162">STA</span></span>|<span data-ttu-id="191cd-163">MTA</span><span class="sxs-lookup"><span data-stu-id="191cd-163">MTA</span></span><br /><br /> <span data-ttu-id="191cd-164">STA</span><span class="sxs-lookup"><span data-stu-id="191cd-164">STA</span></span>|<span data-ttu-id="191cd-165">Interop 和 COM 封送處理。</span><span class="sxs-lookup"><span data-stu-id="191cd-165">Interop and COM marshaling.</span></span><br /><br /> <span data-ttu-id="191cd-166">Interop 封送處理。</span><span class="sxs-lookup"><span data-stu-id="191cd-166">Interop marshaling.</span></span>|

<span data-ttu-id="191cd-167">當 Managed 用戶端和 Unmnaged 伺服器位於相同的 Apartment 中時，Interop 封送處理服務會處理所有資料封送處理。</span><span class="sxs-lookup"><span data-stu-id="191cd-167">When a managed client and unmanaged server are in the same apartment, the interop marshaling service handles all data marshaling.</span></span> <span data-ttu-id="191cd-168">不過，當用戶端和伺服器在不同的 Apartment 中初始化時，還需要 COM 封送處理。</span><span class="sxs-lookup"><span data-stu-id="191cd-168">However, when client and server are initialized in different apartments, COM marshaling is also required.</span></span> <span data-ttu-id="191cd-169">下圖顯示跨單元呼叫的元素：</span><span class="sxs-lookup"><span data-stu-id="191cd-169">The following illustration shows the elements of a cross-apartment call:</span></span>

<span data-ttu-id="191cd-170">![COM 封送處理](./media/interop-marshaling/single-process-across-multi-apartment.gif ".NET 用戶端和 COM 物件之間的跨 Apartment 呼叫")</span><span class="sxs-lookup"><span data-stu-id="191cd-170">![COM marshaling](./media/interop-marshaling/single-process-across-multi-apartment.gif "Cross-apartment call between a .NET client and COM object")</span></span>

<span data-ttu-id="191cd-171">針對跨 Apartment 封送處理，您可以執行下列動作：</span><span class="sxs-lookup"><span data-stu-id="191cd-171">For cross-apartment marshaling, you can do the following:</span></span>

- <span data-ttu-id="191cd-172">接受跨 Apartment 封送處理的額外負荷，只有在許多呼叫跨界限時才會很明顯。</span><span class="sxs-lookup"><span data-stu-id="191cd-172">Accept the overhead of the cross-apartment marshaling, which is noticeable only when there are many calls across the boundary.</span></span> <span data-ttu-id="191cd-173">您必須註冊 COM 元件的類型程式庫，呼叫才能成功跨 Apartment 界限。</span><span class="sxs-lookup"><span data-stu-id="191cd-173">You must register the type library of the COM component for calls to successfully cross the apartment boundary.</span></span>
- <span data-ttu-id="191cd-174">將用戶端執行緒設定為 STA 或 MTA 來變更主執行緒。</span><span class="sxs-lookup"><span data-stu-id="191cd-174">Alter the main thread by setting the client thread to STA or MTA.</span></span> <span data-ttu-id="191cd-175">例如，如果您的 C# 用戶端呼叫許多 STA COM 元件，您可以將主執行緒設定為 STA，以避免跨 Apartment 封送處理。</span><span class="sxs-lookup"><span data-stu-id="191cd-175">For example, if your C# client calls many STA COM components, you can avoid cross-apartment marshaling by setting the main thread to STA.</span></span>

    > [!NOTE]
    > <span data-ttu-id="191cd-176">將 C# 用戶端的執行緒設定為 STA 之後，對 MTA COM 元件的呼叫會需要跨 Apartment 封送處理。</span><span class="sxs-lookup"><span data-stu-id="191cd-176">Once the thread of a C# client is set to STA, calls to MTA COM components will require cross-apartment marshaling.</span></span>

<span data-ttu-id="191cd-177">如需明確選取 Apartment 模型的指示，請參閱 [Managed 和 Unmanaged 執行緒處理](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/5s8ee185(v=vs.100))。</span><span class="sxs-lookup"><span data-stu-id="191cd-177">For instructions on explicitly selecting an apartment model, see [Managed and Unmanaged Threading](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/5s8ee185(v=vs.100)).</span></span>

## <a name="marshaling-remote-calls"></a><span data-ttu-id="191cd-178">封送處理遠端呼叫</span><span class="sxs-lookup"><span data-stu-id="191cd-178">Marshaling Remote Calls</span></span>

<span data-ttu-id="191cd-179">如同跨 Apartment 封送處理，當物件位於不同的處理序時，在 Managed 和 Unmanaged 程式碼之間的每一個呼叫都需要 COM 封送處理。</span><span class="sxs-lookup"><span data-stu-id="191cd-179">As with cross-apartment marshaling, COM marshaling is involved in each call between managed and unmanaged code whenever the objects reside in separate processes.</span></span> <span data-ttu-id="191cd-180">例如:</span><span class="sxs-lookup"><span data-stu-id="191cd-180">For example:</span></span>

- <span data-ttu-id="191cd-181">在遠端主機上叫用 Managed 伺服器的 COM 用戶端會使用分散式 COM (DCOM)。</span><span class="sxs-lookup"><span data-stu-id="191cd-181">A COM client that invokes a managed server on a remote host uses distributed COM (DCOM).</span></span>
- <span data-ttu-id="191cd-182">在遠端主機上叫用 COM 伺服器的 Managed 用戶端會使用 DCOM。</span><span class="sxs-lookup"><span data-stu-id="191cd-182">A managed client that invokes a COM server on a remote host uses DCOM.</span></span>

<span data-ttu-id="191cd-183">下圖顯示 interop 封送處理和 COM 封送處理如何提供跨進程和主機界限的通道：</span><span class="sxs-lookup"><span data-stu-id="191cd-183">The following illustration shows how interop marshaling and COM marshaling provide communications channels across process and host boundaries:</span></span>

<span data-ttu-id="191cd-184">![COM 封送處理](./media/interop-marshaling/interop-and-com-marshaling.gif "跨處理序封送處理")</span><span class="sxs-lookup"><span data-stu-id="191cd-184">![COM marshaling](./media/interop-marshaling/interop-and-com-marshaling.gif "Cross-process marshaling")</span></span>

### <a name="preserving-identity"></a><span data-ttu-id="191cd-185">保留識別</span><span class="sxs-lookup"><span data-stu-id="191cd-185">Preserving Identity</span></span>

<span data-ttu-id="191cd-186">Common Language Runtime 會保留 Managed 和 Unmanaged 參考的識別。</span><span class="sxs-lookup"><span data-stu-id="191cd-186">The common language runtime preserves the identity of managed and unmanaged references.</span></span> <span data-ttu-id="191cd-187">下圖顯示跨處理序和主機界限的直接 Unmanaged 參考 (上方列) 和直接 Managed 參考 (下方列) 的流程。</span><span class="sxs-lookup"><span data-stu-id="191cd-187">The following illustration shows the flow of direct unmanaged references (top row) and direct managed references (bottom row) across process and host boundaries.</span></span>

<span data-ttu-id="191cd-188">![COM 可呼叫包裝函式和執行時間可呼叫包裝函式](./media/interop-marshaling/interop-direct-ref-across-process.gif "跨處理序和主機界限傳遞的參考")</span><span class="sxs-lookup"><span data-stu-id="191cd-188">![COM callable wrapper and runtime callable wrapper](./media/interop-marshaling/interop-direct-ref-across-process.gif "Reference passing across process and host boundaries")</span></span>

<span data-ttu-id="191cd-189">在本圖中：</span><span class="sxs-lookup"><span data-stu-id="191cd-189">In this illustration:</span></span>

- <span data-ttu-id="191cd-190">Unmanaged 用戶端會從 Managed 物件取得 COM 物件的參考 (Managed 物件會從遠端主機取得這個參考)。</span><span class="sxs-lookup"><span data-stu-id="191cd-190">An unmanaged client gets a reference to a COM object from a managed object that gets this reference from a remote host.</span></span> <span data-ttu-id="191cd-191">遠端處理機制是 DCOM。</span><span class="sxs-lookup"><span data-stu-id="191cd-191">The remoting mechanism is DCOM.</span></span>
- <span data-ttu-id="191cd-192">Managed 用戶端會從 COM 物件取得 Managed 物件的參考 (COM 物件會從遠端主機取得這個參考)。</span><span class="sxs-lookup"><span data-stu-id="191cd-192">A managed client gets a reference to a managed object from a COM object that gets this reference from a remote host.</span></span> <span data-ttu-id="191cd-193">遠端處理機制是 DCOM。</span><span class="sxs-lookup"><span data-stu-id="191cd-193">The remoting mechanism is DCOM.</span></span>

    > [!NOTE]
    > <span data-ttu-id="191cd-194">必須註冊 Managed 伺服器的匯出類型程式庫。</span><span class="sxs-lookup"><span data-stu-id="191cd-194">The exported type library of the managed server must be registered.</span></span>

<span data-ttu-id="191cd-195">呼叫端和被呼叫端之間的處理序界限數目並不相關；同處理序和跨處理序呼叫會有相同的直接參考。</span><span class="sxs-lookup"><span data-stu-id="191cd-195">The number of process boundaries between caller and callee is irrelevant; the same direct referencing occurs for in-process and out-of-process calls.</span></span>

### <a name="managed-remoting"></a><span data-ttu-id="191cd-196">Managed 遠端處理</span><span class="sxs-lookup"><span data-stu-id="191cd-196">Managed Remoting</span></span>

<span data-ttu-id="191cd-197">執行階段也會提供 Managed 遠端處理，可供您用來建立跨處理序和主機界限的 Managed 物件之間的通訊通道。</span><span class="sxs-lookup"><span data-stu-id="191cd-197">The runtime also provides managed remoting, which you can use to establish a communications channel between managed objects across process and host boundaries.</span></span> <span data-ttu-id="191cd-198">受控遠端處理可以在通訊元件之間容納防火牆，如下圖所示：</span><span class="sxs-lookup"><span data-stu-id="191cd-198">Managed remoting can accommodate a firewall between the communicating components, as the following illustration shows:</span></span>

<span data-ttu-id="191cd-199">![SOAP 或 TcpChannel](./media/interop-marshaling/interop-remote-soap-or-tcp.gif "使用 SOAP 或 TcpChannel 類別跨防火牆進行遠端呼叫")使用 SOAP 或 TcpChannel 類別跨防火牆進行遠端呼叫</span><span class="sxs-lookup"><span data-stu-id="191cd-199">![SOAP or TcpChannel](./media/interop-marshaling/interop-remote-soap-or-tcp.gif "Remote calls across firewalls using SOAP or the TcpChannel class") Remote calls across firewalls using SOAP or the TcpChannel class</span></span>

<span data-ttu-id="191cd-200">某些非受控的呼叫可經由 SOAP 導引，例如 Serviced 元件和 COM 之間的呼叫。</span><span class="sxs-lookup"><span data-stu-id="191cd-200">Some unmanaged calls can be channeled through SOAP, such as the calls between serviced components and COM.</span></span>

## <a name="related-topics"></a><span data-ttu-id="191cd-201">相關主題</span><span class="sxs-lookup"><span data-stu-id="191cd-201">Related Topics</span></span>

|<span data-ttu-id="191cd-202">標題</span><span class="sxs-lookup"><span data-stu-id="191cd-202">Title</span></span>|<span data-ttu-id="191cd-203">描述</span><span class="sxs-lookup"><span data-stu-id="191cd-203">Description</span></span>|
|-----------|-----------------|
|[<span data-ttu-id="191cd-204">預設的封送處理行為</span><span class="sxs-lookup"><span data-stu-id="191cd-204">Default Marshaling Behavior</span></span>](default-marshaling-behavior.md)|<span data-ttu-id="191cd-205">描述 Interop 封送處理服務用來封送處理資料的規則。</span><span class="sxs-lookup"><span data-stu-id="191cd-205">Describes the rules that the interop marshaling service uses to marshal data.</span></span>|
|[<span data-ttu-id="191cd-206">使用平台叫用封送處理資料</span><span class="sxs-lookup"><span data-stu-id="191cd-206">Marshaling Data with Platform Invoke</span></span>](marshaling-data-with-platform-invoke.md)|<span data-ttu-id="191cd-207">描述如何宣告方法參數，以及將引數傳遞給 Unmanaged 程式庫所匯出的函式。</span><span class="sxs-lookup"><span data-stu-id="191cd-207">Describes how to declare method parameters and pass arguments to functions exported by unmanaged libraries.</span></span>|
|[<span data-ttu-id="191cd-208">使用 COM Interop 封送處理資料</span><span class="sxs-lookup"><span data-stu-id="191cd-208">Marshaling Data with COM Interop</span></span>](marshaling-data-with-com-interop.md)|<span data-ttu-id="191cd-209">描述如何自訂 COM 包裝函式來變更封送處理行為。</span><span class="sxs-lookup"><span data-stu-id="191cd-209">Describes how to customize COM wrappers to alter marshaling behavior.</span></span>|
|[<span data-ttu-id="191cd-210">如何：將 Managed 程式碼 DCOM 移轉至 WCF</span><span class="sxs-lookup"><span data-stu-id="191cd-210">How to: Migrate Managed-Code DCOM to WCF</span></span>](how-to-migrate-managed-code-dcom-to-wcf.md)|<span data-ttu-id="191cd-211">描述如何從 DCOM 移轉至 WCF。</span><span class="sxs-lookup"><span data-stu-id="191cd-211">Describes how to migrate from DCOM to WCF.</span></span>|
|[<span data-ttu-id="191cd-212">操作說明：對應 HRESULT 和例外狀況</span><span class="sxs-lookup"><span data-stu-id="191cd-212">How to: Map HRESULTs and Exceptions</span></span>](how-to-map-hresults-and-exceptions.md)|<span data-ttu-id="191cd-213">描述如何將自訂例外狀況對應到 HRESULT，並提供每一個 HRESULT 與其在 .NET Framework 中可比較的例外狀況類別之完整對應。</span><span class="sxs-lookup"><span data-stu-id="191cd-213">Describes how to map custom exceptions to HRESULTs and provides the complete mapping from each HRESULT to its comparable exception class in the .NET Framework.</span></span>|
|<span data-ttu-id="191cd-214">[使用泛型型別互通](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/ms229590(v=vs.100))</span><span class="sxs-lookup"><span data-stu-id="191cd-214">[Interoperating Using Generic Types](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/ms229590(v=vs.100))</span></span>|<span data-ttu-id="191cd-215">描述使用泛型類型來取得 COM 互通性時所支援的動作。</span><span class="sxs-lookup"><span data-stu-id="191cd-215">Describes which actions are supported when using generic types for COM interoperability.</span></span>|
|[<span data-ttu-id="191cd-216">與 Unmanaged 程式碼互通</span><span class="sxs-lookup"><span data-stu-id="191cd-216">Interoperating with Unmanaged Code</span></span>](index.md)|<span data-ttu-id="191cd-217">描述 Common Language Runtime 提供的互通性服務。</span><span class="sxs-lookup"><span data-stu-id="191cd-217">Describes interoperability services provided by the common language runtime.</span></span>|
|<span data-ttu-id="191cd-218">[進階 COM 互通性](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/bd9cdfyx(v=vs.100))</span><span class="sxs-lookup"><span data-stu-id="191cd-218">[Advanced COM Interoperability](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/bd9cdfyx(v=vs.100))</span></span>|<span data-ttu-id="191cd-219">提供有關將 COM 元件納入 .NET Framework 應用程式的詳細資訊連結。</span><span class="sxs-lookup"><span data-stu-id="191cd-219">Provides links to more information about incorporating COM components into your .NET Framework application.</span></span>|
|<span data-ttu-id="191cd-220">[互通的設計考量](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/61aax4kh(v=vs.100))</span><span class="sxs-lookup"><span data-stu-id="191cd-220">[Design Considerations for Interoperation](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/61aax4kh(v=vs.100))</span></span>|<span data-ttu-id="191cd-221">提供撰寫整合式 COM 元件的秘訣。</span><span class="sxs-lookup"><span data-stu-id="191cd-221">Provides tips for writing integrated COM components.</span></span>|

## <a name="reference"></a><span data-ttu-id="191cd-222">參考資料</span><span class="sxs-lookup"><span data-stu-id="191cd-222">Reference</span></span>

<xref:System.Runtime.InteropServices?displayProperty=nameWithType>
