---
title: 覆寫服務的身分識別以進行驗證
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
ms.assetid: d613a22b-07d7-41a4-bada-1adc653b9b5d
ms.openlocfilehash: a02e5a2f69ad1819e6ab98b0454dd64599382cd0
ms.sourcegitcommit: bc293b14af795e0e999e3304dd40c0222cf2ffe4
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 11/26/2020
ms.locfileid: "96262772"
---
# <a name="override-the-identity-of-a-service-for-authentication"></a><span data-ttu-id="25e6a-102">覆寫服務的身分識別以進行驗證</span><span class="sxs-lookup"><span data-stu-id="25e6a-102">Override the identity of a service for authentication</span></span>

<span data-ttu-id="25e6a-103">一般來說，您不需要在服務上設定身分識別，因為選擇用戶端認證類型，即表示服務中繼資料中公開的身分識別類型。</span><span class="sxs-lookup"><span data-stu-id="25e6a-103">Typically, you do not have to set the identity on a service because the selection of a client credential type dictates the type of identity exposed in the service metadata.</span></span> <span data-ttu-id="25e6a-104">例如，下列設定程式碼會使用 [\<wsHttpBinding>](../../configure-apps/file-schema/wcf/wshttpbinding.md) 元素，並將 `clientCredentialType` 屬性設定為 Windows。</span><span class="sxs-lookup"><span data-stu-id="25e6a-104">For example, the following configuration code uses the [\<wsHttpBinding>](../../configure-apps/file-schema/wcf/wshttpbinding.md) element and sets the `clientCredentialType` attribute to Windows.</span></span>  

 <span data-ttu-id="25e6a-105">下列 Web 服務描述語言 (WSDL) 片段會顯示端點先前定義的身分識別。</span><span class="sxs-lookup"><span data-stu-id="25e6a-105">The following Web Services Description Language (WSDL) fragment shows the identity for the endpoint previously defined.</span></span> <span data-ttu-id="25e6a-106">在此範例中，服務會在特定使用者帳戶下以自我裝載服務的形式執行 (username@contoso.com) ，因此使用者主體名稱 (UPN) 身分識別包含帳戶名稱。</span><span class="sxs-lookup"><span data-stu-id="25e6a-106">In this example, the service is running as a self-hosted service under a particular user account (username@contoso.com) and therefore the user principal name (UPN) identity contains the account name.</span></span> <span data-ttu-id="25e6a-107">UPN 也稱為 Windows 網域中的使用者登入名稱。</span><span class="sxs-lookup"><span data-stu-id="25e6a-107">The UPN is also known as the user sign-in name in a Windows domain.</span></span>  

 <span data-ttu-id="25e6a-108">如需示範身分識別設定的範例應用程式，請參閱 [服務識別範例](../samples/service-identity-sample.md)。</span><span class="sxs-lookup"><span data-stu-id="25e6a-108">For a sample application that demonstrates identity setting, see [Service Identity Sample](../samples/service-identity-sample.md).</span></span> <span data-ttu-id="25e6a-109">如需服務身分識別的詳細資訊，請參閱 [服務身分識別和驗證](../feature-details/service-identity-and-authentication.md)。</span><span class="sxs-lookup"><span data-stu-id="25e6a-109">For more information about service identity, see [Service Identity and Authentication](../feature-details/service-identity-and-authentication.md).</span></span>  
  
## <a name="kerberos-authentication-and-identity"></a><span data-ttu-id="25e6a-110">Kerberos 驗證和身分識別</span><span class="sxs-lookup"><span data-stu-id="25e6a-110">Kerberos Authentication and Identity</span></span>  

 <span data-ttu-id="25e6a-111">根據預設，當服務設定為使用 Windows 認證時，在 [\<identity>](../../configure-apps/file-schema/wcf/identity.md) [\<userPrincipalName>](../../configure-apps/file-schema/wcf/userprincipalname.md) [\<servicePrincipalName>](../../configure-apps/file-schema/wcf/serviceprincipalname.md) WSDL 中會產生包含或專案的元素。</span><span class="sxs-lookup"><span data-stu-id="25e6a-111">By default, when a service is configured to use a Windows credential, an [\<identity>](../../configure-apps/file-schema/wcf/identity.md) element that contains a [\<userPrincipalName>](../../configure-apps/file-schema/wcf/userprincipalname.md) or [\<servicePrincipalName>](../../configure-apps/file-schema/wcf/serviceprincipalname.md) element is generated in the WSDL.</span></span> <span data-ttu-id="25e6a-112">如果服務是在 `LocalSystem` 、 `LocalService` 或 `NetworkService` 帳戶之下執行，則預設會以形式產生服務主體名稱 (spn) ， `host/` \<*hostname*> 因為這些帳戶可以存取電腦的 SPN 資料。</span><span class="sxs-lookup"><span data-stu-id="25e6a-112">If the service is running under the `LocalSystem`, `LocalService`, or `NetworkService` account, a service principal name (SPN) is generated by default in the form of `host/`\<*hostname*> because those accounts have access to the computer's SPN data.</span></span> <span data-ttu-id="25e6a-113">如果服務是在不同的帳戶下執行，Windows Communication Foundation (WCF) 會產生 domainName 形式的 UPN \<*username*> @< *domainName* `>` 。</span><span class="sxs-lookup"><span data-stu-id="25e6a-113">If the service is running under a different account, Windows Communication Foundation (WCF) generates a UPN in the form of \<*username*>@<*domainName*`>`.</span></span> <span data-ttu-id="25e6a-114">這種情況發生的原因是 Kerberos 驗證需要對用戶端提供 UPN 或 SPN，才能驗證服務。</span><span class="sxs-lookup"><span data-stu-id="25e6a-114">This occurs because Kerberos authentication requires that a UPN or SPN be supplied to the client to authenticate the service.</span></span>  
  
 <span data-ttu-id="25e6a-115">您也可以使用 [Setspn](/previous-versions/windows/it-pro/windows-server-2008-R2-and-2008/cc731241(v=ws.10)) 工具，向網域中的服務帳戶註冊額外的 SPN。</span><span class="sxs-lookup"><span data-stu-id="25e6a-115">You can also use the [Setspn](/previous-versions/windows/it-pro/windows-server-2008-R2-and-2008/cc731241(v=ws.10)) tool to register an additional SPN with a service's account in a domain.</span></span> <span data-ttu-id="25e6a-116">您接著就可以使用 SPN 做為服務的身分識別。</span><span class="sxs-lookup"><span data-stu-id="25e6a-116">You can then use the SPN as the identity of the service.</span></span> <span data-ttu-id="25e6a-117">如需此工具的詳細資訊，請參閱 [Setspn 總覽](/previous-versions/windows/it-pro/windows-server-2003/cc773257(v=ws.10))。</span><span class="sxs-lookup"><span data-stu-id="25e6a-117">For more information about the tool, see [Setspn Overview](/previous-versions/windows/it-pro/windows-server-2003/cc773257(v=ws.10)).</span></span>  
  
> [!NOTE]
> <span data-ttu-id="25e6a-118">若不進行交涉而使用 Windows 認證類型，服務的使用者帳戶必須可以存取透過 Active Directory 網域登錄的 SPN。</span><span class="sxs-lookup"><span data-stu-id="25e6a-118">To use the Windows credential type without negotiation, the service's user account must have access to the SPN that is registered with the Active Directory domain.</span></span> <span data-ttu-id="25e6a-119">您可以透過下列方法完成這項作業：</span><span class="sxs-lookup"><span data-stu-id="25e6a-119">You can do this in the following ways:</span></span>  
  
- <span data-ttu-id="25e6a-120">使用 NetworkService 或 LocalSystem 帳戶執行服務。</span><span class="sxs-lookup"><span data-stu-id="25e6a-120">Use the NetworkService or LocalSystem account to run your service.</span></span> <span data-ttu-id="25e6a-121">因為這些帳戶可以存取電腦加入 Active Directory 網域時所建立的電腦 SPN，所以 WCF 會在服務的中繼資料中自動產生適當的 SPN 元素， (WSDL) 。</span><span class="sxs-lookup"><span data-stu-id="25e6a-121">Because those accounts have access to the machine SPN that is established when the machine joins the Active Directory domain, WCF automatically generates the proper SPN element inside the service's endpoint in the service's metadata (WSDL).</span></span>  
  
- <span data-ttu-id="25e6a-122">使用任意的 Active Directory 網域帳戶來執行服務。</span><span class="sxs-lookup"><span data-stu-id="25e6a-122">Use an arbitrary Active Directory domain account to run your service.</span></span> <span data-ttu-id="25e6a-123">在這個情況下，請建立該網域帳戶的 SPN，您可使用 Setspn.exe 公用程式工具來進行。</span><span class="sxs-lookup"><span data-stu-id="25e6a-123">In this case, establish an SPN for that domain account, which you can do by using the Setspn.exe utility tool.</span></span> <span data-ttu-id="25e6a-124">建立服務帳戶的 SPN 之後，請將 WCF 設定為透過 (WSDL) 的中繼資料，將該 SPN 發行至服務的用戶端。</span><span class="sxs-lookup"><span data-stu-id="25e6a-124">Once you create the SPN for the service's account, configure WCF to publish that SPN to the service's clients through its metadata (WSDL).</span></span> <span data-ttu-id="25e6a-125">不論是透過應用程式組態檔或程式碼，均可設定公開端點的端點身分識別以完成此作業。</span><span class="sxs-lookup"><span data-stu-id="25e6a-125">This is done by setting the endpoint identity for the exposed endpoint, either through an application configuration file or code.</span></span>  
  
 <span data-ttu-id="25e6a-126">如需 Spn、Kerberos 通訊協定和 Active Directory 的詳細資訊，請參閱 [Windows 的 Kerberos 技術補充](/previous-versions/msp-n-p/ff649429(v=pandp.10))。</span><span class="sxs-lookup"><span data-stu-id="25e6a-126">For more information about SPNs, the Kerberos protocol, and Active Directory, see [Kerberos Technical Supplement for Windows](/previous-versions/msp-n-p/ff649429(v=pandp.10)).</span></span>  
  
### <a name="when-spn-or-upn-equals-the-empty-string"></a><span data-ttu-id="25e6a-127">SPN 或 UPN 等於空字串時</span><span class="sxs-lookup"><span data-stu-id="25e6a-127">When SPN or UPN Equals the Empty String</span></span>  

 <span data-ttu-id="25e6a-128">如果您設定 SPN 或 UPN 等於空字串，那麼會發生一些不同的狀況，依使用的安全性層級和驗證模式而定：</span><span class="sxs-lookup"><span data-stu-id="25e6a-128">If you set the SPN or UPN equal to an empty string, a number of different things happen, depending on the security level and authentication mode being used:</span></span>  
  
- <span data-ttu-id="25e6a-129">如果您使用傳輸層級安全性，就會選擇 NT LanMan (NTLM) 驗證。</span><span class="sxs-lookup"><span data-stu-id="25e6a-129">If you are using transport level security, NT LanMan (NTLM) authentication is chosen.</span></span>  
  
- <span data-ttu-id="25e6a-130">如果您使用訊息層級安全性，驗證可能會失敗，依驗證模式而定：</span><span class="sxs-lookup"><span data-stu-id="25e6a-130">If you are using message level security, authentication may fail, depending on the authentication mode:</span></span>  
  
- <span data-ttu-id="25e6a-131">如果您正在使用 `spnego` 模式，且 `AllowNtlm` 屬性設定為 `false` ，則驗證會失敗。</span><span class="sxs-lookup"><span data-stu-id="25e6a-131">If you are using `spnego` mode and the `AllowNtlm` attribute is set to `false`, authentication fails.</span></span>  
  
- <span data-ttu-id="25e6a-132">如果您正在使用 `spnego` 模式，且 `AllowNtlm` 屬性設定為 `true` ，則如果 UPN 是空的，則驗證會失敗，但如果 SPN 是空的，則會成功。</span><span class="sxs-lookup"><span data-stu-id="25e6a-132">If you are using `spnego` mode and the `AllowNtlm` attribute is set to `true`, authentication fails if the UPN is empty but succeeds if the SPN is empty.</span></span>  
  
- <span data-ttu-id="25e6a-133">如果您正在直接使用 Kerberos (也就是「單次」)，則驗證失敗。</span><span class="sxs-lookup"><span data-stu-id="25e6a-133">If you are using Kerberos direct (also known as "one-shot"), authentication fails.</span></span>  
  
### <a name="use-the-identity-element-in-configuration"></a><span data-ttu-id="25e6a-134">使用 \<identity> Configuration 中的元素</span><span class="sxs-lookup"><span data-stu-id="25e6a-134">Use the \<identity> Element in Configuration</span></span>  

 <span data-ttu-id="25e6a-135">如果您在先前顯示的系結中變更用戶端認證類型 `Certificate` ，則產生的 WSDL 會包含識別值的 Base64 序列化 x.509 憑證，如下列程式碼所示。</span><span class="sxs-lookup"><span data-stu-id="25e6a-135">If you change the client credential type in the binding previously shown to `Certificate`, then the generated WSDL contains a Base64 serialized X.509 certificate for the identity value as shown in the following code.</span></span> <span data-ttu-id="25e6a-136">這是 Windows 以外所有用戶端認證類型的預設值。</span><span class="sxs-lookup"><span data-stu-id="25e6a-136">This is the default for all client credential types other than Windows.</span></span>  

 <span data-ttu-id="25e6a-137">您可以使用 [設定] 中的 [<>] 專案 `identity` 或在程式碼中設定身分識別，變更預設服務身分識別的值或變更身分識別的類型。</span><span class="sxs-lookup"><span data-stu-id="25e6a-137">You can change the value of the default service identity or change the type of the identity by using the <`identity`> element in configuration or by setting the identity in code.</span></span> <span data-ttu-id="25e6a-138">下列組態程式碼會以 `contoso.com` 這個值來設定網域名稱系統 (DNS) 身分識別。</span><span class="sxs-lookup"><span data-stu-id="25e6a-138">The following configuration code sets a domain name system (DNS) identity with the value `contoso.com`.</span></span>  

### <a name="set-identity-programmatically"></a><span data-ttu-id="25e6a-139">以程式設計方式設定身分識別</span><span class="sxs-lookup"><span data-stu-id="25e6a-139">Set Identity Programmatically</span></span>  

 <span data-ttu-id="25e6a-140">因為 WCF 會自動判斷身分識別，所以您的服務不需要明確指定身分識別。</span><span class="sxs-lookup"><span data-stu-id="25e6a-140">Your service does not have to explicitly specify an identity, because WCF automatically determines it.</span></span> <span data-ttu-id="25e6a-141">但是，WCF 可讓您在必要時指定端點上的身分識別。</span><span class="sxs-lookup"><span data-stu-id="25e6a-141">However, WCF allows you to specify an identity on an endpoint, if necessary.</span></span> <span data-ttu-id="25e6a-142">下列程式碼會新增具有特定 DNS 身分識別的新服務端點。</span><span class="sxs-lookup"><span data-stu-id="25e6a-142">The following code adds a new service endpoint with a specific DNS identity.</span></span>  
  
 [!code-csharp[C_Identity#5](../../../../samples/snippets/csharp/VS_Snippets_CFX/c_identity/cs/source.cs#5)]
 [!code-vb[C_Identity#5](../../../../samples/snippets/visualbasic/VS_Snippets_CFX/c_identity/vb/source.vb#5)]  
  
## <a name="see-also"></a><span data-ttu-id="25e6a-143">另請參閱</span><span class="sxs-lookup"><span data-stu-id="25e6a-143">See also</span></span>

- [<span data-ttu-id="25e6a-144">作法：建立自訂用戶端身分識別驗證器</span><span class="sxs-lookup"><span data-stu-id="25e6a-144">How to: Create a Custom Client Identity Verifier</span></span>](how-to-create-a-custom-client-identity-verifier.md)
- [<span data-ttu-id="25e6a-145">服務身分識別和驗證</span><span class="sxs-lookup"><span data-stu-id="25e6a-145">Service Identity and Authentication</span></span>](../feature-details/service-identity-and-authentication.md)
