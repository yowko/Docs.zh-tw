---
title: 篩選
ms.date: 03/30/2017
ms.assetid: 4002946c-e34a-4356-8cfb-e25912a4be63
ms.openlocfilehash: 7051e69f6a38d536251c26365a634d4ffbc46f1b
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: HT
ms.contentlocale: zh-TW
ms.lasthandoff: 04/23/2019
ms.locfileid: "61856696"
---
# <a name="filtering"></a><span data-ttu-id="897eb-102">篩選</span><span class="sxs-lookup"><span data-stu-id="897eb-102">Filtering</span></span>
<span data-ttu-id="897eb-103">Windows Communication Foundation (WCF) 的篩選系統可以使用宣告式篩選條件來比對訊息，並做出作業決策。</span><span class="sxs-lookup"><span data-stu-id="897eb-103">The Windows Communication Foundation (WCF) filtering system can use declarative filters to match messages and make operational decisions.</span></span> <span data-ttu-id="897eb-104">您可以使用篩選條件檢查訊息的部分，以判斷如何處理訊息。</span><span class="sxs-lookup"><span data-stu-id="897eb-104">You can use filters to determine what to do with a message by examining part of the message.</span></span> <span data-ttu-id="897eb-105">例如，佇列處理序可以使用 XPath 1.0 查詢檢查已知標頭的優先順序項目，以便決定是否要將訊息移到佇列前頭。</span><span class="sxs-lookup"><span data-stu-id="897eb-105">A queuing process, for example, can use an XPath 1.0 query to check the priority element of a known header to determine whether to move a message to the front of the queue.</span></span>  
  
 <span data-ttu-id="897eb-106">篩選系統組成一組類別會有效地判斷哪些一組篩選條件是`true`特定 WCF 訊息。</span><span class="sxs-lookup"><span data-stu-id="897eb-106">The filtering system is composed of a set of classes that can efficiently determine which of a set of filters are `true` for a particular WCF message.</span></span>  
  
 <span data-ttu-id="897eb-107">篩選系統是 WCF 傳訊; 的核心元件它可因應自如。</span><span class="sxs-lookup"><span data-stu-id="897eb-107">The filtering system is a core component of WCF messaging; it is designed to be extremely fast.</span></span> <span data-ttu-id="897eb-108">每個篩選條件實作已經過最佳化，針對特定種類的比對針對 WCF 訊息。</span><span class="sxs-lookup"><span data-stu-id="897eb-108">Each filter implementation has been optimized for a particular kind of matching against WCF messages.</span></span>  
  
 <span data-ttu-id="897eb-109">篩選系統不具備執行緒安全。</span><span class="sxs-lookup"><span data-stu-id="897eb-109">The filtering system is not thread safe.</span></span> <span data-ttu-id="897eb-110">應用程式必須處理任何鎖定語意。</span><span class="sxs-lookup"><span data-stu-id="897eb-110">The application must handle any locking semantics.</span></span> <span data-ttu-id="897eb-111">不過，它確實支援多種讀取器和單一寫入器。</span><span class="sxs-lookup"><span data-stu-id="897eb-111">It does, however, support a multi-reader, single writer.</span></span>  
  
## <a name="where-filtering-fits"></a><span data-ttu-id="897eb-112">篩選的用途</span><span class="sxs-lookup"><span data-stu-id="897eb-112">Where Filtering Fits</span></span>  
 <span data-ttu-id="897eb-113">接收到訊息之後，就會執行篩選，篩選是分派訊息至適當應用程式元件的程序一部分。</span><span class="sxs-lookup"><span data-stu-id="897eb-113">Filtering is performed after a message is received and is part of the process of dispatching the message to the proper application component.</span></span> <span data-ttu-id="897eb-114">篩選系統的設計可解決數個 WCF 子系統，包括傳訊、 路由、 安全性、 事件處理，以及系統管理的需求。</span><span class="sxs-lookup"><span data-stu-id="897eb-114">The design of the filtering system addresses the requirements of several WCF subsystems, including messaging, routing, security, event handling, and system management.</span></span>  
  
## <a name="filters"></a><span data-ttu-id="897eb-115">篩選條件</span><span class="sxs-lookup"><span data-stu-id="897eb-115">Filters</span></span>  
 <span data-ttu-id="897eb-116">篩選引擎有兩個主要元件：篩選條件和篩選資料表。</span><span class="sxs-lookup"><span data-stu-id="897eb-116">The filter engine has two primary components, filters and filter tables.</span></span> <span data-ttu-id="897eb-117">篩選條件會根據使用者指定的邏輯條件，做出有關訊息的布林值決策。</span><span class="sxs-lookup"><span data-stu-id="897eb-117">A filter makes Boolean decisions about a message based on user-specified logical conditions.</span></span> <span data-ttu-id="897eb-118">篩選條件會實作 <xref:System.ServiceModel.Dispatcher.MessageFilter> 類別。</span><span class="sxs-lookup"><span data-stu-id="897eb-118">Filters implement the <xref:System.ServiceModel.Dispatcher.MessageFilter> class.</span></span>  
  
 <span data-ttu-id="897eb-119"><xref:System.ServiceModel.Dispatcher.MessageFilter.Match%2A> 方法會用於判斷訊息是否符合篩選條件。</span><span class="sxs-lookup"><span data-stu-id="897eb-119">The <xref:System.ServiceModel.Dispatcher.MessageFilter.Match%2A> methods are used to determine if a message satisfies a filter.</span></span> <span data-ttu-id="897eb-120">其中一個方法會測試訊息的標頭，但無法檢查訊息本文。</span><span class="sxs-lookup"><span data-stu-id="897eb-120">One of the methods tests the message's header, but cannot inspect the message body.</span></span> <span data-ttu-id="897eb-121">另一種方法會採用*訊息緩衝區*做為輸入參數，並可以檢查訊息本文。</span><span class="sxs-lookup"><span data-stu-id="897eb-121">The other method takes a *message buffer* as an input parameter and can inspect the message body.</span></span>  
  
 <span data-ttu-id="897eb-122">篩選條件通常不會個別進行測試，而是以篩選資料表的一部分進行測試，篩選資料表是 <xref:System.ServiceModel.Dispatcher.MessageFilterTable%601.CreateFilterTable%2A> 方法建立的一般類別。</span><span class="sxs-lookup"><span data-stu-id="897eb-122">Filters are not usually tested individually, but as part of a filter table, which is a generic class that the <xref:System.ServiceModel.Dispatcher.MessageFilterTable%601.CreateFilterTable%2A> method creates.</span></span>  
  
 <span data-ttu-id="897eb-123">每一種篩選條件都會特製化為比對特定種類的布林值條件。</span><span class="sxs-lookup"><span data-stu-id="897eb-123">The several kinds of filters each specialize in matching on a particular kind of Boolean condition.</span></span> <span data-ttu-id="897eb-124">一旦建構篩選條件，您就無法變更篩選條件使用的準則。若要修改篩選條件的準則，請建構新的篩選條件並刪除現有的篩選條件。</span><span class="sxs-lookup"><span data-stu-id="897eb-124">Once you construct a filter, you cannot change the criteria that a filter uses; to modify a filter's criteria, construct a new one and delete the existing filter.</span></span>  
  
### <a name="action-filters"></a><span data-ttu-id="897eb-125">動作篩選條件</span><span class="sxs-lookup"><span data-stu-id="897eb-125">Action Filters</span></span>  
 <span data-ttu-id="897eb-126"><xref:System.ServiceModel.Dispatcher.ActionMessageFilter> 包含動作字串的清單。</span><span class="sxs-lookup"><span data-stu-id="897eb-126">The <xref:System.ServiceModel.Dispatcher.ActionMessageFilter> contains a list of action strings.</span></span> <span data-ttu-id="897eb-127">如果篩選條件清單中的任何動作符合訊息或訊息緩衝區中的動作標頭，`Match` 方法就會傳回 `true`。</span><span class="sxs-lookup"><span data-stu-id="897eb-127">If any of the actions in the filter’s list matches the Action header in the message or message buffer, the `Match` method returns `true`.</span></span> <span data-ttu-id="897eb-128">如果清單是空白的，則篩選條件會被視為是全部符合的篩選條件，而任何訊息或訊息緩衝區相符，並且 `Match` 會傳回 `true`。</span><span class="sxs-lookup"><span data-stu-id="897eb-128">If the list is empty, the filter is considered a match-all filter and any message or message buffer matches and `Match` returns `true`.</span></span> <span data-ttu-id="897eb-129">如果篩選條件清單中沒有任何動作符合訊息或訊息緩衝區中的動作標頭，`Match` 方法就會傳回 `false`。</span><span class="sxs-lookup"><span data-stu-id="897eb-129">If none of the actions in the filter’s list matches the Action header in the message or message buffer, `Match` returns `false`.</span></span> <span data-ttu-id="897eb-130">如果訊息中沒有任何動作，而且篩選條件的清單不是空白，則 `Match` 會傳回 `false`。</span><span class="sxs-lookup"><span data-stu-id="897eb-130">If there is no action in the message and the filter’s list is non-empty, then `Match` returns `false`.</span></span>  
  
### <a name="endpoint-address-filters"></a><span data-ttu-id="897eb-131">端點位址篩選條件</span><span class="sxs-lookup"><span data-stu-id="897eb-131">Endpoint Address Filters</span></span>  
 <span data-ttu-id="897eb-132"><xref:System.ServiceModel.Dispatcher.EndpointAddressMessageFilter> 會根據端點位址篩選訊息和訊息緩衝區 (如標頭集合中所示)。</span><span class="sxs-lookup"><span data-stu-id="897eb-132">The <xref:System.ServiceModel.Dispatcher.EndpointAddressMessageFilter> filters messages and message buffers based on an endpoint address, as represented in their header collection.</span></span> <span data-ttu-id="897eb-133">訊息若要通過這類篩選條件，必須符合下列條件：</span><span class="sxs-lookup"><span data-stu-id="897eb-133">For a message to pass such a filter, the following conditions must be met:</span></span>  
  
- <span data-ttu-id="897eb-134">篩選器位址的統一資源識別項 (URI) 必須與訊息 To 標頭中的位址 URI 相同。</span><span class="sxs-lookup"><span data-stu-id="897eb-134">The filter’s address Uniform Resource Identifier (URI) must be the same as the one in the message To header.</span></span>  
  
- <span data-ttu-id="897eb-135">篩選條件 (`address.Headers` 條件) 位址中的每個端點參數必須在要對應的訊息中找出標頭。</span><span class="sxs-lookup"><span data-stu-id="897eb-135">Each endpoint parameter in the address of the filter (`address.Headers` collection) must find a header in the message to map on.</span></span> <span data-ttu-id="897eb-136">不過，為了使符合項目維持為 `true`，仍接受訊息或訊息緩衝區中的額外標頭。</span><span class="sxs-lookup"><span data-stu-id="897eb-136">Extra headers in the message or message buffer are acceptable for the match to remain `true`.</span></span>  
  
### <a name="prefix-endpoint-address-filters"></a><span data-ttu-id="897eb-137">前置詞端點位址篩選條件</span><span class="sxs-lookup"><span data-stu-id="897eb-137">Prefix Endpoint Address Filters</span></span>  
  
1. <span data-ttu-id="897eb-138"><xref:System.ServiceModel.Dispatcher.PrefixEndpointAddressMessageFilter> 的作用就像 <xref:System.ServiceModel.Dispatcher.EndpointAddressMessageFilter> 篩選條件，只除了符合項目可以在訊息 URI 的前置詞上之外。</span><span class="sxs-lookup"><span data-stu-id="897eb-138">The <xref:System.ServiceModel.Dispatcher.PrefixEndpointAddressMessageFilter> functions just like the <xref:System.ServiceModel.Dispatcher.EndpointAddressMessageFilter> filter, except that the match can be on a prefix of the message URI.</span></span> <span data-ttu-id="897eb-139">例如，將指定位址的篩選`http://www.adatum.com`比對訊息定址到`http://www.adatum.com/userA`。</span><span class="sxs-lookup"><span data-stu-id="897eb-139">For example, a filter specifying the address `http://www.adatum.com` matches messages addressed to `http://www.adatum.com/userA`.</span></span>  
  
### <a name="xpath-message-filters"></a><span data-ttu-id="897eb-140">XPath 訊息篩選條件</span><span class="sxs-lookup"><span data-stu-id="897eb-140">XPath Message Filters</span></span>  
 <span data-ttu-id="897eb-141"><xref:System.ServiceModel.Dispatcher.XPathMessageFilter> 會使用 XPath 運算式來判斷 XML 文件是否包含特定項目、屬性、文字或其他 XML 語法結構。</span><span class="sxs-lookup"><span data-stu-id="897eb-141">An <xref:System.ServiceModel.Dispatcher.XPathMessageFilter> uses an XPath expression to determine whether an XML document contains specific elements, attributes, text, or other XML syntactic constructs.</span></span> <span data-ttu-id="897eb-142">篩選條件經過最佳化，用於 XPath 的精簡子集時能夠發揮極佳的效率。</span><span class="sxs-lookup"><span data-stu-id="897eb-142">The filter is optimized to be extremely efficient for a strict subset of XPath.</span></span> <span data-ttu-id="897eb-143">XML 路徑語言所述[W3C XML 路徑語言 1.0 規格](https://go.microsoft.com/fwlink/?LinkId=94779)。</span><span class="sxs-lookup"><span data-stu-id="897eb-143">The XML Path Language is described in the [W3C XML Path Language 1.0 specification](https://go.microsoft.com/fwlink/?LinkId=94779).</span></span>  
  
 <span data-ttu-id="897eb-144">一般而言，應用程式會在端點使用 <xref:System.ServiceModel.Dispatcher.XPathMessageFilter> 來查詢 SOAP 訊息的內容，然後根據查詢的結果採取適當的行動。</span><span class="sxs-lookup"><span data-stu-id="897eb-144">Typically, an application uses an <xref:System.ServiceModel.Dispatcher.XPathMessageFilter> at an endpoint to query the contents of a SOAP message and then takes the appropriate action based on the results of that query.</span></span> <span data-ttu-id="897eb-145">例如，佇列處理序可能會使用 XPath 查詢來檢查已知標頭的優先順序項目，以便決定是否要將訊息移到佇列前頭。</span><span class="sxs-lookup"><span data-stu-id="897eb-145">A queuing process, for example, may use an XPath query to inspect the priority element of a known header to decide whether to move a message to the front of the queue.</span></span>  
  
## <a name="filter-tables"></a><span data-ttu-id="897eb-146">篩選資料表</span><span class="sxs-lookup"><span data-stu-id="897eb-146">Filter Tables</span></span>  
 <span data-ttu-id="897eb-147">篩選資料表是用來儲存索引鍵值組，其中篩選條件是索引鍵，而一些關聯的資料為值。</span><span class="sxs-lookup"><span data-stu-id="897eb-147">Filter tables are used to store key-value pairs, where a filter is the key and some associated data is the value.</span></span> <span data-ttu-id="897eb-148">篩選資料可以用來表示當訊息符合篩選條件，並且篩選資料的型別為篩選資料表類別的泛型參數時所要採取的動作。</span><span class="sxs-lookup"><span data-stu-id="897eb-148">The filter data can be used to indicate what actions to take if a message matches the filter and the type of the filter data is the generic parameter for the filter table class.</span></span> <span data-ttu-id="897eb-149">篩選資料可以由路由規則、工作階段安全性狀態、通道上的接聽項等項目而組成。</span><span class="sxs-lookup"><span data-stu-id="897eb-149">The filter data can consist of routing rules, session security state, listeners on a channel, and so on.</span></span> <span data-ttu-id="897eb-150">此資料可以用在需要資料流程控制的位置中。</span><span class="sxs-lookup"><span data-stu-id="897eb-150">The data can be used where data flow control is necessary.</span></span>  
  
 <span data-ttu-id="897eb-151">篩選資料表會實作泛型介面 <xref:System.ServiceModel.Dispatcher.IMessageFilterTable%601>。</span><span class="sxs-lookup"><span data-stu-id="897eb-151">Filter tables implement the generic interface <xref:System.ServiceModel.Dispatcher.IMessageFilterTable%601>.</span></span>  
  
 <span data-ttu-id="897eb-152">篩選資料表具有數個方法，這些方法會根據資料表中的所有篩選條件比對訊息，並且傳回符合篩選條件或資料的未排序集合。</span><span class="sxs-lookup"><span data-stu-id="897eb-152">Filter tables have several methods that match a message against all the filters in the table and return an unordered collection of matching filters or data.</span></span> <span data-ttu-id="897eb-153">有些比對方法屬於多重比對，並且會傳回所有符合的項目。</span><span class="sxs-lookup"><span data-stu-id="897eb-153">Some of the match methods are multiple-match and return all matching items.</span></span> <span data-ttu-id="897eb-154">其他方法則是單一比對，只傳回一個項目，並且如果有一個以上的篩選條件相符，則會擲回 <xref:System.ServiceModel.Dispatcher.MultipleFilterMatchesException>。</span><span class="sxs-lookup"><span data-stu-id="897eb-154">Others are single-match, returning only one item, and throw a <xref:System.ServiceModel.Dispatcher.MultipleFilterMatchesException> if more than one filter matches.</span></span>  
  
### <a name="message-filter-table"></a><span data-ttu-id="897eb-155">訊息篩選資料表</span><span class="sxs-lookup"><span data-stu-id="897eb-155">Message Filter Table</span></span>  
 <span data-ttu-id="897eb-156"><xref:System.ServiceModel.Dispatcher.MessageFilterTable%601> 是 <xref:System.ServiceModel.Dispatcher.IMessageFilterTable%601> 最普遍的實作。</span><span class="sxs-lookup"><span data-stu-id="897eb-156">The <xref:System.ServiceModel.Dispatcher.MessageFilterTable%601> is the most general implementation of <xref:System.ServiceModel.Dispatcher.IMessageFilterTable%601>.</span></span> <span data-ttu-id="897eb-157">您可以在資料表中儲存所有類型的篩選條件。</span><span class="sxs-lookup"><span data-stu-id="897eb-157">You can store filters of all types in the table.</span></span>  
  
 <span data-ttu-id="897eb-158">您可以指派數值優先順序給篩選條件，其中最大數字表示最高優先順序。</span><span class="sxs-lookup"><span data-stu-id="897eb-158">You can assign numeric priorities to filters, where the highest priority is signified by the highest number.</span></span> <span data-ttu-id="897eb-159">多種篩選條件類型可以具有相同的優先順序。</span><span class="sxs-lookup"><span data-stu-id="897eb-159">Multiple types of filters can have the same priority.</span></span> <span data-ttu-id="897eb-160">特定類型的篩選條件可以顯示在一個以上的優先順序層級中。</span><span class="sxs-lookup"><span data-stu-id="897eb-160">A particular type of filter can appear in more than one priority level.</span></span>  
  
 <span data-ttu-id="897eb-161">比對會從最高優先順序開始進行，一旦以指定的優先順序找到符合的篩選條件，就不會檢查較低優先順序的篩選條件。</span><span class="sxs-lookup"><span data-stu-id="897eb-161">Matching is done starting with the highest priority, and once matching filters are found with a given priority, no filters with lower priorities are examined.</span></span> <span data-ttu-id="897eb-162">因此，如果您正在使用單一篩選條件比對方法，並且有一個以上的篩選條件符合訊息，但每個符合的篩選條件具有不同的優先順序，那麼就不會擲回任何例外狀況，而是會傳回具有最高優先順序的篩選條件。</span><span class="sxs-lookup"><span data-stu-id="897eb-162">Therefore, if you are using a single-filter match method, and more than one filter matches a message, but each matching filter has a different priority, then no exception is thrown and the filter with the highest priority is returned.</span></span> <span data-ttu-id="897eb-163">同樣地，多重篩選條件比對方法只會傳回具有最高優先順序的符合篩選條件。</span><span class="sxs-lookup"><span data-stu-id="897eb-163">Similarly, a multiple-filter match method returns only those matching filters with the highest priority.</span></span>  
  
### <a name="xpath-message-filter-table"></a><span data-ttu-id="897eb-164">XPath 訊息篩選資料表</span><span class="sxs-lookup"><span data-stu-id="897eb-164">XPath Message Filter Table</span></span>  
 <span data-ttu-id="897eb-165"><xref:System.ServiceModel.Dispatcher.XPathMessageFilterTable%601> 已針對宣告式 XPath 篩選條件最佳化，因此資料表索引鍵為 <xref:System.ServiceModel.Dispatcher.XPathMessageFilter>。</span><span class="sxs-lookup"><span data-stu-id="897eb-165">The <xref:System.ServiceModel.Dispatcher.XPathMessageFilterTable%601> is optimized for declarative XPath filters, so the table key is a <xref:System.ServiceModel.Dispatcher.XPathMessageFilter>.</span></span>  
  
 <span data-ttu-id="897eb-166"><xref:System.ServiceModel.Dispatcher.XPathMessageFilterTable%601> 類別會最佳化適用於大部分傳訊案例的 XPath 子集的比對，並且支援完整的 XPath 1.0 文法。</span><span class="sxs-lookup"><span data-stu-id="897eb-166">The <xref:System.ServiceModel.Dispatcher.XPathMessageFilterTable%601> class optimizes matching for a subset of XPath that covers most of the messaging scenarios and also supports the full XPath 1.0 grammar.</span></span> <span data-ttu-id="897eb-167">它具有最佳化的演算法，能夠有效地進行平行比對。</span><span class="sxs-lookup"><span data-stu-id="897eb-167">It has optimized algorithms for efficient parallel matching.</span></span>  
  
 <span data-ttu-id="897eb-168">這個資料表有數個經過特製化的 `Match` 方法，這些方法可以在 <xref:System.Xml.XPath.XPathNavigator> 和 <xref:System.ServiceModel.Dispatcher.SeekableXPathNavigator> 上操作。</span><span class="sxs-lookup"><span data-stu-id="897eb-168">This table has several specialized `Match` methods that operate over an <xref:System.Xml.XPath.XPathNavigator> and a <xref:System.ServiceModel.Dispatcher.SeekableXPathNavigator>.</span></span> <span data-ttu-id="897eb-169"><xref:System.ServiceModel.Dispatcher.SeekableXPathNavigator> 透過新增 <xref:System.Xml.XPath.XPathNavigator> 屬性來擴充 <xref:System.ServiceModel.Dispatcher.SeekableXPathNavigator.CurrentPosition%2A> 類別。</span><span class="sxs-lookup"><span data-stu-id="897eb-169">A <xref:System.ServiceModel.Dispatcher.SeekableXPathNavigator> extends the <xref:System.Xml.XPath.XPathNavigator> class by adding a <xref:System.ServiceModel.Dispatcher.SeekableXPathNavigator.CurrentPosition%2A> property.</span></span> <span data-ttu-id="897eb-170">這個屬性允許儲存 XML 文件中的位置並迅速將其載入，而不需要複製 (Clone) 導覽；此類作業對 <xref:System.Xml.XPath.XPathNavigator> 而言，是非常耗費資源的記憶體配置。</span><span class="sxs-lookup"><span data-stu-id="897eb-170">This property allows positions within the XML document to be saved and loaded quickly without having to clone the navigator, an expensive memory allocation that the <xref:System.Xml.XPath.XPathNavigator> requires for such an operation.</span></span> <span data-ttu-id="897eb-171">WCF XPath 引擎必須經常記錄游標在 XML 文件，執行查詢過程中的位置而<xref:System.ServiceModel.Dispatcher.SeekableXPathNavigator>提供重要的最佳化訊息處理。</span><span class="sxs-lookup"><span data-stu-id="897eb-171">The WCF XPath engine must frequently record the position of the cursor in the course of executing queries on XML documents, so the <xref:System.ServiceModel.Dispatcher.SeekableXPathNavigator> provides an important optimization for message processing.</span></span>  
  
## <a name="customer-scenarios"></a><span data-ttu-id="897eb-172">客戶案例</span><span class="sxs-lookup"><span data-stu-id="897eb-172">Customer Scenarios</span></span>  
 <span data-ttu-id="897eb-173">您可以視訊息中包含的資料而定，隨時使用篩選將訊息傳送到不同的處理模組。</span><span class="sxs-lookup"><span data-stu-id="897eb-173">You can use filtering any time you want to send a message to different processing modules depending on data contained in the message.</span></span> <span data-ttu-id="897eb-174">根據訊息的動作程式碼路由訊息，以及根據訊息的端點位址分離訊息資料流的信號，即為兩個典型的案例。</span><span class="sxs-lookup"><span data-stu-id="897eb-174">Two typical scenarios are routing a message based on its action code and de-multiplexing a stream of messages based on the messages' endpoint address.</span></span>  
  
### <a name="routing"></a><span data-ttu-id="897eb-175">路由</span><span class="sxs-lookup"><span data-stu-id="897eb-175">Routing</span></span>  
 <span data-ttu-id="897eb-176">端點的接聽項會接聽訊息 SOAP 標頭中有一或多個動作程式碼的訊息。</span><span class="sxs-lookup"><span data-stu-id="897eb-176">The listener of an endpoint listens for messages that have one or more action codes in the message's SOAP header.</span></span> <span data-ttu-id="897eb-177">透過將包含動作程式碼的陣列傳遞至其建構函式以建立 <xref:System.ServiceModel.Dispatcher.ActionMessageFilter>，即可實作此動作。</span><span class="sxs-lookup"><span data-stu-id="897eb-177">You implement this by creating an <xref:System.ServiceModel.Dispatcher.ActionMessageFilter> by passing an array that contains the action codes to its constructor.</span></span> <span data-ttu-id="897eb-178">它會使用該篩選條件註冊 `ListenerFactory`，因此，只有動作符合篩選條件的其中一個動作的訊息，才會到達該特定端點。</span><span class="sxs-lookup"><span data-stu-id="897eb-178">It uses that filter to register with the `ListenerFactory`, so only messages whose action matches one of those in the filter get to that particular endpoint.</span></span>  
  
### <a name="de-multiplexing"></a><span data-ttu-id="897eb-179">分離信號</span><span class="sxs-lookup"><span data-stu-id="897eb-179">De-multiplexing</span></span>  
 <span data-ttu-id="897eb-180">當多個端點從網路外的相同 `ServiceListener` 上散開時，要分離訊息信號並確認這些端點是否屬於特定端點位址的唯一方法就是使用 <xref:System.ServiceModel.Dispatcher.EndpointAddressMessageFilter>，它會查詢儲存在標頭中的資訊，藉此選取送往已註冊之端點的訊息。</span><span class="sxs-lookup"><span data-stu-id="897eb-180">When multiple endpoints fan out from the same `ServiceListener` off the wire, the only way to de-multiplex messages and know whether they belong to a certain endpoint address, is to use <xref:System.ServiceModel.Dispatcher.EndpointAddressMessageFilter>s, which select messages toward the registered endpoints by performing a lookup on the information stored in the headers.</span></span> <span data-ttu-id="897eb-181">在這些篩選條件中，只有通過的訊息具有對應至下列兩者的所有必要標頭：</span><span class="sxs-lookup"><span data-stu-id="897eb-181">In these filters, only those messages that pass have all necessary headers that correspond to both:</span></span>  
  
- <span data-ttu-id="897eb-182">`EndpointAddress` 中的 URI。</span><span class="sxs-lookup"><span data-stu-id="897eb-182">The URI in the `EndpointAddress`.</span></span>  
  
- <span data-ttu-id="897eb-183">如 `EndpointAddress` 所指定，<xref:System.ServiceModel.Dispatcher.EndpointAddressMessageFilter> 中的其餘端點參數。</span><span class="sxs-lookup"><span data-stu-id="897eb-183">The rest of the endpoint parameters in the `EndpointAddress` as specified in the <xref:System.ServiceModel.Dispatcher.EndpointAddressMessageFilter>.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="897eb-184">另請參閱</span><span class="sxs-lookup"><span data-stu-id="897eb-184">See also</span></span>

- [<span data-ttu-id="897eb-185">資料傳輸與序列化</span><span class="sxs-lookup"><span data-stu-id="897eb-185">Data Transfer and Serialization</span></span>](../../../../docs/framework/wcf/feature-details/data-transfer-and-serialization.md)
