---
title: 基本 HTTP 服務
ms.date: 03/30/2017
ms.assetid: 27048b43-8a54-4f2a-9952-594bbfab10ad
ms.openlocfilehash: 914ad5f04d980fd53cd07251461367356f00b4cc
ms.sourcegitcommit: 2eceb05f1a5bb261291a1f6a91c5153727ac1c19
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 09/04/2018
ms.locfileid: "43516623"
---
# <a name="basic-http-service"></a><span data-ttu-id="e6a03-102">基本 HTTP 服務</span><span class="sxs-lookup"><span data-stu-id="e6a03-102">Basic HTTP Service</span></span>
<span data-ttu-id="e6a03-103">此範例示範如何實作 HTTP、 RPC 的服務，也就是一般稱為"POX"(Plain Old XML) 服務 – 使用 Windows Communication Foundation (WCF) REST 程式設計模型。</span><span class="sxs-lookup"><span data-stu-id="e6a03-103">This sample demonstrates how to implement an HTTP-based, RPC-based service - popularly referred to as "POX" (Plain Old XML) service – using the Windows Communication Foundation (WCF) REST Programming model.</span></span> <span data-ttu-id="e6a03-104">這個範例是由兩個元件所組成： 自我裝載的 WCF HTTP 服務 (Service.cs) 和主控台應用程式 (Program.cs) 建立服務並給它的呼叫。</span><span class="sxs-lookup"><span data-stu-id="e6a03-104">This sample consists of two components: a self-hosted WCF HTTP service (Service.cs) and a console application (Program.cs) that creates the service and makes calls to it.</span></span>  
  
## <a name="sample-details"></a><span data-ttu-id="e6a03-105">範例詳細資料</span><span class="sxs-lookup"><span data-stu-id="e6a03-105">Sample Details</span></span>  
 <span data-ttu-id="e6a03-106">WCF 服務會公開 2 個作業，`EchoWithGet`和`EchoWithPost`，它會傳回當做輸入傳遞的字串。</span><span class="sxs-lookup"><span data-stu-id="e6a03-106">The WCF service exposes 2 operations, `EchoWithGet` and `EchoWithPost`, which returns the string that was passed as input.</span></span>  
  
 <span data-ttu-id="e6a03-107">`EchoWithGet` 作業是以 <xref:System.ServiceModel.Web.WebGetAttribute> 加上附註，表示此作業會處理 HTTP `GET` 要求。</span><span class="sxs-lookup"><span data-stu-id="e6a03-107">The `EchoWithGet` operation is annotated with <xref:System.ServiceModel.Web.WebGetAttribute>, which indicates that the operation processes HTTP `GET` requests.</span></span> <span data-ttu-id="e6a03-108"><xref:System.ServiceModel.Web.WebGetAttribute> 不會明確地指定 <xref:System.UriTemplate>，因此，此作業預期使用查詢字串參數和名稱 `s` 來傳入輸入字串。</span><span class="sxs-lookup"><span data-stu-id="e6a03-108">Because the <xref:System.ServiceModel.Web.WebGetAttribute> does not explicitly specify a <xref:System.UriTemplate>, the operation expects the input string to be passed in using a query string parameter with name `s`.</span></span> <span data-ttu-id="e6a03-109">請注意，此服務預期的 URI 格式可以使用 <xref:System.ServiceModel.Web.WebGetAttribute.UriTemplate%2A> 屬性自訂。</span><span class="sxs-lookup"><span data-stu-id="e6a03-109">Note that the format of the URI that the service expects can be customized using the <xref:System.ServiceModel.Web.WebGetAttribute.UriTemplate%2A> property.</span></span>  
  
 <span data-ttu-id="e6a03-110">`EchoWithPost` 作業是以 <xref:System.ServiceModel.Web.WebInvokeAttribute> 加上附註，表示它不是 `GET` 作業 (它有副作用)。</span><span class="sxs-lookup"><span data-stu-id="e6a03-110">The `EchoWithPost` operation is annotated with <xref:System.ServiceModel.Web.WebInvokeAttribute>, which indicates it is not a `GET` operation (it has side effects).</span></span> <span data-ttu-id="e6a03-111"><xref:System.ServiceModel.Web.WebInvokeAttribute> 不會明確地指定 `Method`，因此，此作業會處理 HTTP `POST` 要求，而且這些要求在要求主體中擁有字串 (例如，以 XML 格式)。</span><span class="sxs-lookup"><span data-stu-id="e6a03-111">Because the <xref:System.ServiceModel.Web.WebInvokeAttribute> does not explicitly specify a `Method`, the operation processes HTTP `POST` requests that have the string in the request body (in the XML format, for instance).</span></span> <span data-ttu-id="e6a03-112">請注意，HTTP 方法和要求的 URI 格式可以分別使用 <xref:System.ServiceModel.Web.WebInvokeAttribute.Method%2A> 和 <xref:System.ServiceModel.Web.WebInvokeAttribute.UriTemplate> 屬性自訂。</span><span class="sxs-lookup"><span data-stu-id="e6a03-112">Note that the HTTP method and the format of the URI for the request can be customized using the <xref:System.ServiceModel.Web.WebInvokeAttribute.Method%2A> and <xref:System.ServiceModel.Web.WebInvokeAttribute.UriTemplate> properties respectively.</span></span>  
  
 <span data-ttu-id="e6a03-113">App.config 檔案會以預設的 <xref:System.ServiceModel.Description.WebHttpEndpoint> 設定 WCF 服務，且 <xref:System.ServiceModel.Description.WebHttpEndpoint.HelpEnabled%2A> 屬性設定為 `true`。</span><span class="sxs-lookup"><span data-stu-id="e6a03-113">The App.config file configures the WCF service with a default <xref:System.ServiceModel.Description.WebHttpEndpoint> that has the <xref:System.ServiceModel.Description.WebHttpEndpoint.HelpEnabled%2A> property set to `true`.</span></span> <span data-ttu-id="e6a03-114">如此一來，WCF 基礎結構會建立自動 HTML 說明頁`http://localhost:8000/Customers/help`，提供如何建構服務的 HTTP 要求以及如何取用服務之 HTTP 回應的相關資訊。</span><span class="sxs-lookup"><span data-stu-id="e6a03-114">As a result, the WCF infrastructure creates an automatic HTML based help page at `http://localhost:8000/Customers/help` that provides information about how to construct HTTP requests to the service and how to consume the service’s HTTP response.</span></span>  
  
 <span data-ttu-id="e6a03-115">Program.cs 會示範如何使用 WCF 通道處理站，才能呼叫服務以及處理回應。</span><span class="sxs-lookup"><span data-stu-id="e6a03-115">Program.cs demonstrates how a WCF channel factory can be used to make calls to the service and process responses.</span></span> <span data-ttu-id="e6a03-116">請注意，這只是存取 WCF 服務的其中一種方式。</span><span class="sxs-lookup"><span data-stu-id="e6a03-116">Note that this is just one way to access a WCF service.</span></span> <span data-ttu-id="e6a03-117">您也可以使用其他 .NET Framework 類別 (例如 <xref:System.Net.HttpWebRequest> 和 <xref:System.Net.WebClient>) 來存取服務。</span><span class="sxs-lookup"><span data-stu-id="e6a03-117">It is also possible to access the service using other .NET Framework classes like <xref:System.Net.HttpWebRequest> and <xref:System.Net.WebClient>.</span></span> <span data-ttu-id="e6a03-118">SDK 中的其他範例 (例如[自動格式選取](../../../../docs/framework/wcf/samples/automatic-format-selection.md)範例並[基本資源服務](../../../../docs/framework/wcf/samples/basic-resource-service.md)範例) 示範如何使用這些類別與 WCF 服務進行通訊。</span><span class="sxs-lookup"><span data-stu-id="e6a03-118">Other samples in the SDK (such as the [Automatic Format Selection](../../../../docs/framework/wcf/samples/automatic-format-selection.md) sample and [Basic Resource Service](../../../../docs/framework/wcf/samples/basic-resource-service.md) sample) show how to use these classes to communicate with a WCF service.</span></span>  
  
 <span data-ttu-id="e6a03-119">此範例包含同時在主控台應用程式中執行的自我裝載服務和用戶端。</span><span class="sxs-lookup"><span data-stu-id="e6a03-119">The sample consists a self-hosted service and a client that both run within a console application.</span></span> <span data-ttu-id="e6a03-120">當主控台應用程式執行時，用戶端會對服務發出要求，然後將相關的資訊從回應寫入至主控台視窗。</span><span class="sxs-lookup"><span data-stu-id="e6a03-120">As the console application runs, the client makes requests to the service and writes the pertinent information from the responses to the console window.</span></span>  
  
#### <a name="to-use-this-sample"></a><span data-ttu-id="e6a03-121">若要使用這個範例</span><span class="sxs-lookup"><span data-stu-id="e6a03-121">To use this sample</span></span>  
  
1.  <span data-ttu-id="e6a03-122">開啟基本 Http 服務範例的方案。</span><span class="sxs-lookup"><span data-stu-id="e6a03-122">Open the solution for the Basic Http Service Sample.</span></span> <span data-ttu-id="e6a03-123">啟動 [!INCLUDE[vs_current_long](../../../../includes/vs-current-long-md.md)] 時，您必須以系統管理員身分執行，才能讓範例成功執行。</span><span class="sxs-lookup"><span data-stu-id="e6a03-123">When launching [!INCLUDE[vs_current_long](../../../../includes/vs-current-long-md.md)], you must run as an administrator for the sample to execute successfully.</span></span> <span data-ttu-id="e6a03-124">執行這項操作，以滑鼠右鍵按一下[!INCLUDE[vs_current_long](../../../../includes/vs-current-long-md.md)]圖示，然後選取**系統管理員身分執行**從內容功能表。</span><span class="sxs-lookup"><span data-stu-id="e6a03-124">Do this by right-clicking the [!INCLUDE[vs_current_long](../../../../includes/vs-current-long-md.md)] icon and selecting **Run as Administrator** from the context menu.</span></span>  
  
2.  <span data-ttu-id="e6a03-125">按下 CTRL+SHIFT+B 建置方案，然後按下 Ctrl+F5，在不偵錯的情況下執行主控台應用程式。</span><span class="sxs-lookup"><span data-stu-id="e6a03-125">Press CTRL+SHIFT+B to build the solution and then press Ctrl+F5 to run the console application without debugging.</span></span> <span data-ttu-id="e6a03-126">主控台視窗隨即出現，並提供執行中服務的 URI，以及執行中服務之 HTML 說明頁的 URI。</span><span class="sxs-lookup"><span data-stu-id="e6a03-126">The console window appears and provides the URI of the running service and the URI of the HTML help page for the running service.</span></span> <span data-ttu-id="e6a03-127">您可以隨時在瀏覽器中輸入說明頁的 URI 來檢視 HTML 說明頁。</span><span class="sxs-lookup"><span data-stu-id="e6a03-127">At any point in time you can view the HTML help page by typing the URI of the help page in a browser.</span></span> <span data-ttu-id="e6a03-128">當範例執行時，用戶端會寫入目前活動的狀態。</span><span class="sxs-lookup"><span data-stu-id="e6a03-128">As the sample runs, the client writes the status of the current activity.</span></span>  
  
3.  <span data-ttu-id="e6a03-129">按下任何按鍵可終止此範例。</span><span class="sxs-lookup"><span data-stu-id="e6a03-129">Press any key to terminate the sample.</span></span>  
  
> [!IMPORTANT]
>  <span data-ttu-id="e6a03-130">這些範例可能已安裝在您的電腦上。</span><span class="sxs-lookup"><span data-stu-id="e6a03-130">The samples may already be installed on your machine.</span></span> <span data-ttu-id="e6a03-131">請先檢查下列 (預設) 目錄，然後再繼續。</span><span class="sxs-lookup"><span data-stu-id="e6a03-131">Check for the following (default) directory before continuing.</span></span>  
>   
>  `<InstallDrive>:\WF_WCF_Samples`  
>   
>  <span data-ttu-id="e6a03-132">如果此目錄不存在，請移至[Windows Communication Foundation (WCF) 和.NET Framework 4 的 Windows Workflow Foundation (WF) 範例](https://go.microsoft.com/fwlink/?LinkId=150780)以下載所有 Windows Communication Foundation (WCF) 和[!INCLUDE[wf1](../../../../includes/wf1-md.md)]範例。</span><span class="sxs-lookup"><span data-stu-id="e6a03-132">If this directory does not exist, go to [Windows Communication Foundation (WCF) and Windows Workflow Foundation (WF) Samples for .NET Framework 4](https://go.microsoft.com/fwlink/?LinkId=150780) to download all Windows Communication Foundation (WCF) and [!INCLUDE[wf1](../../../../includes/wf1-md.md)] samples.</span></span> <span data-ttu-id="e6a03-133">此範例位於下列目錄。</span><span class="sxs-lookup"><span data-stu-id="e6a03-133">This sample is located in the following directory.</span></span>  
>   
>  `<InstallDrive>:\WF_WCF_Samples\WCF\Basic\Web\BasicHttpService`  
  
## <a name="see-also"></a><span data-ttu-id="e6a03-134">另請參閱</span><span class="sxs-lookup"><span data-stu-id="e6a03-134">See Also</span></span>  
 [<span data-ttu-id="e6a03-135">自動格式選取</span><span class="sxs-lookup"><span data-stu-id="e6a03-135">Automatic Format Selection</span></span>](../../../../docs/framework/wcf/samples/automatic-format-selection.md)  
 [<span data-ttu-id="e6a03-136">基本資源服務</span><span class="sxs-lookup"><span data-stu-id="e6a03-136">Basic Resource Service</span></span>](../../../../docs/framework/wcf/samples/basic-resource-service.md)
