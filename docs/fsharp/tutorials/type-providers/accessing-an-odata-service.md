---
title: "逐步解說：使用型別提供者存取 OData 服務 (F#)"
description: "了解如何使用 F # 3.0 的 F # ODataService 類型提供者來產生 OData 服務的用戶端類型，並提供資料摘要之服務的查詢。"
keywords: "Visual F#, F#, 函式程式設計"
author: cartermp
ms.author: phcart
ms.date: 05/16/2016
ms.topic: language-reference
ms.prod: .net
ms.technology: devlang-fsharp
ms.devlang: fsharp
ms.assetid: 0adae84c-b0fa-455f-994b-274ecdc6df30
ms.openlocfilehash: 750407c36a989cece30c0c0654ff905c8eee3b33
ms.sourcegitcommit: 655fd4f78741967f80c409cef98347fdcf77857d
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 02/28/2018
---
# <a name="walkthrough-accessing-an-odata-service-by-using-type-providers"></a><span data-ttu-id="fba36-104">逐步解說：使用型別提供者存取 OData 服務</span><span class="sxs-lookup"><span data-stu-id="fba36-104">Walkthrough: Accessing an OData Service by Using Type Providers</span></span>

> [!NOTE]
<span data-ttu-id="fba36-105">本指南針對 F # 3.0 所撰寫，而且會更新。</span><span class="sxs-lookup"><span data-stu-id="fba36-105">This guide was written for F# 3.0 and will be updated.</span></span>  <span data-ttu-id="fba36-106">請參閱 [FSharp.Data](https://fsharp.github.io/FSharp.Data/) 以取得最新的跨平台型別提供者。</span><span class="sxs-lookup"><span data-stu-id="fba36-106">See [FSharp.Data](https://fsharp.github.io/FSharp.Data/) for up-to-date, cross-platform type providers.</span></span>

> [!NOTE]
<span data-ttu-id="fba36-107">應用程式開發介面參考連結將帶您到 MSDN。</span><span class="sxs-lookup"><span data-stu-id="fba36-107">The API reference links will take you to MSDN.</span></span>  <span data-ttu-id="fba36-108">docs.microsoft.com API 參考不完整。</span><span class="sxs-lookup"><span data-stu-id="fba36-108">The docs.microsoft.com API reference is not complete.</span></span>

<span data-ttu-id="fba36-109">OData，這表示開放式資料通訊協定，是透過網際網路傳輸資料的通訊協定。</span><span class="sxs-lookup"><span data-stu-id="fba36-109">OData, meaning Open Data Protocol, is a protocol for transferring data over the Internet.</span></span> <span data-ttu-id="fba36-110">許多資料提供者所發行的 OData web 服務公開其資料的存取權。</span><span class="sxs-lookup"><span data-stu-id="fba36-110">Many data providers expose access to their data by publishing an OData web service.</span></span> <span data-ttu-id="fba36-111">您可以從 F # 3.0 中使用的資料類型自動產生的任何 OData 來源存取資料`ODataService`型別提供者。</span><span class="sxs-lookup"><span data-stu-id="fba36-111">You can access data from any OData source in F# 3.0 using data types that are automatically generated by the `ODataService` type provider.</span></span> <span data-ttu-id="fba36-112">如需有關 OData 的詳細資訊，請參閱 https://msdn.microsoft.com/library/da3380cc-f6da-4c6c-bdb2-bb86afa59d62。</span><span class="sxs-lookup"><span data-stu-id="fba36-112">For more information about OData, see https://msdn.microsoft.com/library/da3380cc-f6da-4c6c-bdb2-bb86afa59d62.</span></span>

<span data-ttu-id="fba36-113">本逐步解說會示範如何使用 F # ODataService 類型提供者來產生 OData 服務的用戶端類型，並提供資料摘要之服務的查詢。</span><span class="sxs-lookup"><span data-stu-id="fba36-113">This walkthrough shows you how to use the F# ODataService type provider to generate client types for an OData service and query data feeds that the service provides.</span></span>

<span data-ttu-id="fba36-114">這個逐步解說將說明下列工作，您應該按照這個逐步解說的順序執行才能成功：</span><span class="sxs-lookup"><span data-stu-id="fba36-114">This walkthrough illustrates the following tasks, which you should perform in this order for the walkthrough to succeed:</span></span>


- <span data-ttu-id="fba36-115">設定 OData 服務的用戶端專案</span><span class="sxs-lookup"><span data-stu-id="fba36-115">Configuring a client project for an OData service</span></span>
<br />

- <span data-ttu-id="fba36-116">存取 OData 型別</span><span class="sxs-lookup"><span data-stu-id="fba36-116">Accessing OData types</span></span>
<br />

- <span data-ttu-id="fba36-117">查詢 OData 服務</span><span class="sxs-lookup"><span data-stu-id="fba36-117">Querying an OData service</span></span>
<br />

- <span data-ttu-id="fba36-118">驗證 OData 要求</span><span class="sxs-lookup"><span data-stu-id="fba36-118">Verifying the OData requests</span></span>
<br />


## <a name="configuring-a-client-project-for-an-odata-service"></a><span data-ttu-id="fba36-119">設定 OData 服務的用戶端專案</span><span class="sxs-lookup"><span data-stu-id="fba36-119">Configuring a client project for an OData service</span></span>
<span data-ttu-id="fba36-120">在此步驟中，您將設定專案使用 OData 型別提供者。</span><span class="sxs-lookup"><span data-stu-id="fba36-120">In this step, you set up a project to use an OData type provider.</span></span>


#### <a name="to-configure-a-client-project-for-an-odata-service"></a><span data-ttu-id="fba36-121">若要設定的 OData 服務的用戶端專案</span><span class="sxs-lookup"><span data-stu-id="fba36-121">To configure a client project for an OData service</span></span>

- <span data-ttu-id="fba36-122">開啟 F # 主控台應用程式專案，然後再將參考加入`System.Data.Services.Client`Framework 組件。</span><span class="sxs-lookup"><span data-stu-id="fba36-122">Open an F# Console Application project, and then add a reference to the `System.Data.Services.Client` Framework assembly.</span></span>
<br />

- <span data-ttu-id="fba36-123">在下`Extensions`，將參考加入`FSharp.Data.TypeProviders`組件。</span><span class="sxs-lookup"><span data-stu-id="fba36-123">Under `Extensions`, add a reference to the `FSharp.Data.TypeProviders` assembly.</span></span>
<br />

## <a name="accessing-odata-types"></a><span data-ttu-id="fba36-124">存取 OData 型別</span><span class="sxs-lookup"><span data-stu-id="fba36-124">Accessing OData types</span></span>
<span data-ttu-id="fba36-125">在此步驟中，您可以建立提供 OData 服務的存取權的類型和資料型別提供者。</span><span class="sxs-lookup"><span data-stu-id="fba36-125">In this step, you create a type provider that provides access to the types and data for an OData service.</span></span>


#### <a name="to-access-odata-types"></a><span data-ttu-id="fba36-126">若要存取 OData 類型</span><span class="sxs-lookup"><span data-stu-id="fba36-126">To access OData types</span></span>

- <span data-ttu-id="fba36-127">在程式碼編輯器中，開啟 F # 原始程式檔，並輸入下列程式碼。</span><span class="sxs-lookup"><span data-stu-id="fba36-127">In the Code Editor, open an F# source file, and enter the following code.</span></span>
<br />

```fsharp
open Microsoft.FSharp.Data.TypeProviders


type Northwind = ODataService<"https://services.odata.org/Northwind/Northwind.svc/">

let db = Northwind.GetDataContext()
let fullContext = Northwind.ServiceTypes.NorthwindEntities()
```

<span data-ttu-id="fba36-128">在此範例中，您已叫用 F # 類型提供者，而且所指示的位置，以便建立一組會根據您指定的 OData URI 的類型。</span><span class="sxs-lookup"><span data-stu-id="fba36-128">In this example, you have invoked the F# type provider and instructed it to create a set of types that are based on the OData URI that you specified.</span></span> <span data-ttu-id="fba36-129">兩個物件都包含資料; 的相關資訊其中一個是簡化的資料內容，`db`在範例中。</span><span class="sxs-lookup"><span data-stu-id="fba36-129">Two objects are available that contain information about the data; one is a simplified data context, `db` in the example.</span></span> <span data-ttu-id="fba36-130">此物件只包含資料類型相關聯的資料庫，包括對於資料表或摘要的型別。</span><span class="sxs-lookup"><span data-stu-id="fba36-130">This object contains only the data types that are associated with the database, which include types for tables or feeds.</span></span> <span data-ttu-id="fba36-131">另一個物件，`fullContext`在此範例中，是的執行個體`System.Data.Linq.DataContext`且包含許多其他屬性、 方法和事件。</span><span class="sxs-lookup"><span data-stu-id="fba36-131">The other object, `fullContext` in this example, is an instance of `System.Data.Linq.DataContext` and contains many additional properties, methods, and events.</span></span>
<br />


## <a name="querying-an-odata-service"></a><span data-ttu-id="fba36-132">查詢 OData 服務</span><span class="sxs-lookup"><span data-stu-id="fba36-132">Querying an OData service</span></span>
<span data-ttu-id="fba36-133">在此步驟中，您可以使用 F # 查詢運算式來查詢 OData 服務。</span><span class="sxs-lookup"><span data-stu-id="fba36-133">In this step, you use F# query expressions to query the OData service.</span></span>


#### <a name="to-query-an-odata-service"></a><span data-ttu-id="fba36-134">若要查詢的 OData 服務</span><span class="sxs-lookup"><span data-stu-id="fba36-134">To query an OData service</span></span>

1. <span data-ttu-id="fba36-135">既然您已經設定類型提供者，您可以查詢 OData 服務。</span><span class="sxs-lookup"><span data-stu-id="fba36-135">Now that you've set up the type provider, you can query an OData service.</span></span>
<br />  <span data-ttu-id="fba36-136">OData 支援只有一個可用的查詢作業的子集。</span><span class="sxs-lookup"><span data-stu-id="fba36-136">OData supports only a subset of the available query operations.</span></span> <span data-ttu-id="fba36-137">支援下列作業和其相對應的關鍵字：</span><span class="sxs-lookup"><span data-stu-id="fba36-137">The following operations and their corresponding keywords are supported:</span></span>
<br />
  - <span data-ttu-id="fba36-138">投影 (`select`)</span><span class="sxs-lookup"><span data-stu-id="fba36-138">projection (`select`)</span></span>
<br />

  - <span data-ttu-id="fba36-139">篩選 (`where`、 藉由使用字串和日期的作業)</span><span class="sxs-lookup"><span data-stu-id="fba36-139">filtering (`where`, by using string and date operations)</span></span>
<br />

  - <span data-ttu-id="fba36-140">分頁 (`skip`， `take`)</span><span class="sxs-lookup"><span data-stu-id="fba36-140">paging (`skip`, `take`)</span></span>
<br />

  - <span data-ttu-id="fba36-141">排序 (`orderBy`， `thenBy`)</span><span class="sxs-lookup"><span data-stu-id="fba36-141">ordering (`orderBy`, `thenBy`)</span></span>
<br />

  - <span data-ttu-id="fba36-142">`AddQueryOption` 和`Expand`，這是 OData 特定作業</span><span class="sxs-lookup"><span data-stu-id="fba36-142">`AddQueryOption` and `Expand`, which are OData-specific operations</span></span>
<br />

  <span data-ttu-id="fba36-143">如需詳細資訊，請參閱[LINQ 考量 &#40;WCF Data Services &#41;](https://msdn.microsoft.com/library/ee622463.aspx).</span><span class="sxs-lookup"><span data-stu-id="fba36-143">For more information, see [LINQ Considerations &#40;WCF Data Services&#41;](https://msdn.microsoft.com/library/ee622463.aspx).</span></span>
<br />  <span data-ttu-id="fba36-144">如果您想要將所有摘要或資料表中的項目，使用簡單的形式的查詢運算式，如下列程式碼所示：</span><span class="sxs-lookup"><span data-stu-id="fba36-144">If you want all of the entries in a feed or table, use the simplest form of the query expression, as in the following code:</span></span>
<br />

```fsharp
query {
  for customer in db.Customers do
  select customer
} |> Seq.iter (fun customer ->
                  printfn "ID: %s\nCompany: %s" customer.CustomerID customer.CompanyName
                  printfn "Contact: %s\nAddress: %s" customer.ContactName customer.Address
                  printfn "         %s, %s %s" customer.City customer.Region customer.PostalCode
                  printfn "%s\n" customer.Phone)
```

2. <span data-ttu-id="fba36-145">指定欄位或資料行，您想要使用的 select 關鍵字後的 tuple。</span><span class="sxs-lookup"><span data-stu-id="fba36-145">Specify the fields or columns that you want by using a tuple after the select keyword.</span></span>
<br />

```fsharp
  query { 
    for cat in db.Categories do
    select (cat.CategoryID, cat.CategoryName, cat.Description)
  } |> Seq.iter (fun (id, name, description) ->
                    printfn "ID: %d\nCategory: %s\nDescription: %s\n" id name description)
```

3. <span data-ttu-id="fba36-146">使用指定的條件`where`子句。</span><span class="sxs-lookup"><span data-stu-id="fba36-146">Specify conditions by using a `where` clause.</span></span>
<br />

```fsharp
query {
  for employee in db.Employees do
  where (employee.EmployeeID = 9)
  select employee
} |> Seq.iter (fun employee ->
                  printfn "Name: %s ID: %d" (employee.FirstName + " " + employee.LastName) (employee.EmployeeID))
```

4. <span data-ttu-id="fba36-147">使用指定的子字串條件查詢`System.String.Contains(System.String)`方法。</span><span class="sxs-lookup"><span data-stu-id="fba36-147">Specify a substring condition to the query by using the `System.String.Contains(System.String)` method.</span></span> <span data-ttu-id="fba36-148">下列查詢會傳回名稱中有"Chef"的所有產品。</span><span class="sxs-lookup"><span data-stu-id="fba36-148">The following query returns all products that have "Chef" in their names.</span></span> <span data-ttu-id="fba36-149">也請注意，使用`System.Nullable<'T>.GetValueOrDefault()`。</span><span class="sxs-lookup"><span data-stu-id="fba36-149">Also notice the use of `System.Nullable<'T>.GetValueOrDefault()`.</span></span> <span data-ttu-id="fba36-150">`UnitPrice`是可為 null 的值，因此您必須使用來取得值`Value`屬性，或您必須呼叫`System.Nullable<'T>.GetValueOrDefault()`。</span><span class="sxs-lookup"><span data-stu-id="fba36-150">The `UnitPrice` is a nullable value, so you must either get the value by using the `Value` property, or you must call `System.Nullable<'T>.GetValueOrDefault()`.</span></span>
<br />

```fsharp
query { 
  for product in db.Products do
  where (product.ProductName.Contains("Chef"))
  select product
} |> Seq.iter (fun product ->
                  printfn "ID: %d Product: %s" product.ProductID product.ProductName
                  printfn "Price: %M\n" (product.UnitPrice.GetValueOrDefault()))
```

5. <span data-ttu-id="fba36-151">使用`System.String.EndsWith(System.String)`方法，以指定的字串是以特定子結束。</span><span class="sxs-lookup"><span data-stu-id="fba36-151">Use the `System.String.EndsWith(System.String)` method to specify that a string ends with a certain substring.</span></span>
<br />

```fsharp
query {
  for product in db.Products do
  where (product.ProductName.EndsWith("u"))
  select product
} |> Seq.iter (fun product ->
                  printfn "ID: %d Product: %s" product.ProductID product.ProductName
                  printfn "Price: %M\n" (product.UnitPrice.GetValueOrDefault()))
```

6. <span data-ttu-id="fba36-152">結合條件，在 where 子句使用`&&`運算子。</span><span class="sxs-lookup"><span data-stu-id="fba36-152">Combine conditions in a where clause by using the `&&` operator.</span></span>
<br />

```fsharp
// Open this module to use the nullable operators ?> and ?<.
open Microsoft.FSharp.Linq.NullableOperators

let salesIn1997 = query { 
  for sales in db.Category_Sales_for_1997 do
  where (sales.CategorySales ?> 50000.00M && sales.CategorySales ?< 60000.0M)
  select sales }

salesIn1997
|> Seq.iter (fun sales ->
                printfn "Category: %s Sales: %M" sales.CategoryName (sales.CategorySales.GetValueOrDefault()))
```

<span data-ttu-id="fba36-153">運算子`?>`和`?<`都是可為 null 的運算子。</span><span class="sxs-lookup"><span data-stu-id="fba36-153">The operators `?>` and `?<` are nullable operators.</span></span> <span data-ttu-id="fba36-154">您可以使用一組完整的可為 null 的等式和比較運算子。</span><span class="sxs-lookup"><span data-stu-id="fba36-154">You can use a full set of nullable equality and comparison operators.</span></span> <span data-ttu-id="fba36-155">如需詳細資訊，請參閱[Linq.NullableOperators 模組](https://msdn.microsoft.com/visualfsharpdocs/conceptual/linq.nullableoperators-module-%5bfsharp%5d)。</span><span class="sxs-lookup"><span data-stu-id="fba36-155">For more information, see [Linq.NullableOperators Module](https://msdn.microsoft.com/visualfsharpdocs/conceptual/linq.nullableoperators-module-%5bfsharp%5d).</span></span>
<br />

7. <span data-ttu-id="fba36-156">使用`sortBy`查詢運算子以指定順序，並且使用`thenBy`指定的順序的另一個層級。</span><span class="sxs-lookup"><span data-stu-id="fba36-156">Use the `sortBy` query operator to specify ordering, and use `thenBy` to specify another level of ordering.</span></span> <span data-ttu-id="fba36-157">另請注意使用查詢的 select 部分中的 tuple。</span><span class="sxs-lookup"><span data-stu-id="fba36-157">Notice also the use of a tuple in the select part of the query.</span></span> <span data-ttu-id="fba36-158">因此，查詢具有 tuple 做為項目類型。</span><span class="sxs-lookup"><span data-stu-id="fba36-158">Therefore, the query has a tuple as an element type.</span></span>
<br />

```fsharp
printfn "Freight for some orders: "

query { 
  for order in db.Orders do
  sortBy (order.OrderDate.Value)
  thenBy (order.OrderID)
  select (order.OrderDate, order.OrderID, order.Customer.CompanyName)
} |> Seq.iter (fun (orderDate, orderID, company) ->
                  printfn "OrderDate: %s" (orderDate.GetValueOrDefault().ToString())
                  printfn "OrderID: %d Company: %s\n" orderID company)
```

8. <span data-ttu-id="fba36-159">忽略指定的記錄數目使用 skip 運算子，並使用 take 運算子來指定要傳回的記錄數目。</span><span class="sxs-lookup"><span data-stu-id="fba36-159">Ignore a specified number of records by using the skip operator, and use the take operator to specify a number of records to return.</span></span> <span data-ttu-id="fba36-160">如此一來，您可以在資料摘要上實作分頁。</span><span class="sxs-lookup"><span data-stu-id="fba36-160">In this way, you can implement paging on data feeds.</span></span>
<br />

```fsharp
printfn "Get the first page of 2 employees."

query { 
  for employee in db.Employees do
  take 2
  select employee
} |> Seq.iter (fun employee ->
                  printfn "Name: %s ID: %d" (employee.FirstName + " " + employee.LastName) (employee.EmployeeID)) 

printfn "Get the next 2 employees."

query { 
  for employee in db.Employees do
  skip 2
  take 2
  select employee
} |> Seq.iter (fun employee ->
                  printfn "Name: %s ID: %d" (employee.FirstName + " " + employee.LastName) (employee.EmployeeID))
```

## <a name="verifying-the-odata-request"></a><span data-ttu-id="fba36-161">驗證 OData 要求</span><span class="sxs-lookup"><span data-stu-id="fba36-161">Verifying the OData request</span></span>
<span data-ttu-id="fba36-162">每一個 OData 查詢會轉譯成特定的 OData 要求 URI。</span><span class="sxs-lookup"><span data-stu-id="fba36-162">Every OData query is translated into a specific OData request URI.</span></span> <span data-ttu-id="fba36-163">您可以確認，URI，可能是用於偵錯用途，加入事件處理常式到`SendingRequest`完整的資料物件上的事件。</span><span class="sxs-lookup"><span data-stu-id="fba36-163">You can verify that URI, perhaps for debugging purposes, by adding an event handler to the `SendingRequest` event on the full data context object.</span></span>


#### <a name="to-verify-the-odata-request"></a><span data-ttu-id="fba36-164">若要驗證 OData 要求</span><span class="sxs-lookup"><span data-stu-id="fba36-164">To verify the OData request</span></span>

- <span data-ttu-id="fba36-165">若要驗證 OData 要求 URI，請使用下列程式碼：</span><span class="sxs-lookup"><span data-stu-id="fba36-165">To verify the OData request URI, use the following code:</span></span>
<br />

```fsharp
// The DataContext property returns the full data context.
db.DataContext.SendingRequest.Add (fun eventArgs -> printfn "Requesting %A" eventArgs.Request.RequestUri)
```

<span data-ttu-id="fba36-166">先前的程式碼的輸出為：</span><span class="sxs-lookup"><span data-stu-id="fba36-166">The output of the previous code is:</span></span>
<br />`requesting https://services.odata.org/Northwind/Northwind.svc/Orders()?$orderby=ShippedDate&amp;$select=OrderID,ShippedDate`


## <a name="see-also"></a><span data-ttu-id="fba36-167">請參閱</span><span class="sxs-lookup"><span data-stu-id="fba36-167">See Also</span></span>
[<span data-ttu-id="fba36-168">查詢運算式</span><span class="sxs-lookup"><span data-stu-id="fba36-168">Query Expressions</span></span>](../../language-reference/query-expressions.md)

[<span data-ttu-id="fba36-169">LINQ 考量 (WCF Data Services)</span><span class="sxs-lookup"><span data-stu-id="fba36-169">LINQ Considerations (WCF Data Services)</span></span>](https://msdn.microsoft.com/library/ee622463.aspx)

[<span data-ttu-id="fba36-170">ODataService 類型提供者 （F #）</span><span class="sxs-lookup"><span data-stu-id="fba36-170">ODataService Type Provider (F#)</span></span>](https://msdn.microsoft.com/visualfsharpdocs/conceptual/odataservice-type-provider-%5bfsharp%5d)
