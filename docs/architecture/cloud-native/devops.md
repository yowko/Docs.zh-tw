---
title: 雲端原生 DevOps
description: 架構適用于 Azure 的雲端原生 .NET 應用程式 |雲端原生 DevOps
ms.date: 06/30/2019
ms.openlocfilehash: a056da833d7c6da11ab956337b77deab5e9bd159
ms.sourcegitcommit: 55f438d4d00a34b9aca9eedaac3f85590bb11565
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 09/23/2019
ms.locfileid: "71183186"
---
# <a name="cloud-native-devops"></a><span data-ttu-id="69d32-103">雲端原生 DevOps</span><span class="sxs-lookup"><span data-stu-id="69d32-103">Cloud Native DevOps</span></span>

[!INCLUDE [book-preview](../../../includes/book-preview.md)]

<span data-ttu-id="69d32-104">軟體顧問的最愛口號是回答「It 相依」的問題。</span><span class="sxs-lookup"><span data-stu-id="69d32-104">The favorite mantra of software consultants is to answer "It depends" to any question posed.</span></span> <span data-ttu-id="69d32-105">這不是因為軟體顧問喜歡不接受某一個位置。</span><span class="sxs-lookup"><span data-stu-id="69d32-105">It isn't because software consultants are fond of not taking a position.</span></span> <span data-ttu-id="69d32-106">這是因為軟體中的任何問題都沒有真正的解答。</span><span class="sxs-lookup"><span data-stu-id="69d32-106">It's because there's no one true answer to any questions in software.</span></span> <span data-ttu-id="69d32-107">絕對不會有任何權利和錯誤，而是相反之間的平衡。</span><span class="sxs-lookup"><span data-stu-id="69d32-107">There's no absolute right and wrong, but rather a balance between opposites.</span></span>

<span data-ttu-id="69d32-108">比方說，開發 web 應用程式的兩個主要學校：單一頁面應用程式（Spa）與伺服器端應用程式。</span><span class="sxs-lookup"><span data-stu-id="69d32-108">Take, for instance, the two major schools of developing web applications: Single Page Applications (SPAs) versus server-side applications.</span></span> <span data-ttu-id="69d32-109">一方面，使用者經驗傾向于 Spa，而網頁伺服器的流量可能會最小化，讓您能夠將它們裝載在簡單的靜態裝載上。</span><span class="sxs-lookup"><span data-stu-id="69d32-109">On the one hand, the user experience tends to be better with SPAs and the amount of traffic to the web server can be minimized making it possible to host them on something as simple as static hosting.</span></span> <span data-ttu-id="69d32-110">另一方面，Spa 的開發速度較慢，而且更容易測試。</span><span class="sxs-lookup"><span data-stu-id="69d32-110">On the other hand, SPAs tend to be slower to develop and more difficult to test.</span></span> <span data-ttu-id="69d32-111">哪一個是正確的選擇？</span><span class="sxs-lookup"><span data-stu-id="69d32-111">Which one is the right choice?</span></span> <span data-ttu-id="69d32-112">這也取決於您的情況。</span><span class="sxs-lookup"><span data-stu-id="69d32-112">Well, it depends on your situation.</span></span>

<span data-ttu-id="69d32-113">雲端原生應用程式不會受限於相同的二分法。</span><span class="sxs-lookup"><span data-stu-id="69d32-113">Cloud-native applications aren't immune to that same dichotomy.</span></span> <span data-ttu-id="69d32-114">它們在開發、穩定性和擴充性方面都有清楚的優勢，但管理起來可能會很困難。</span><span class="sxs-lookup"><span data-stu-id="69d32-114">They have clear advantages in terms of speed of development, stability, and scalability, but managing them can be quite a bit more difficult.</span></span>

<span data-ttu-id="69d32-115">幾年前，將應用程式從開發移至生產環境，再花上一個月，甚至更多的過程並不罕見。</span><span class="sxs-lookup"><span data-stu-id="69d32-115">Years ago, it wasn't uncommon for the process of moving an application from development to production to take a month, or even more.</span></span> <span data-ttu-id="69d32-116">公司以6個月或甚至每年的速度發行軟體。</span><span class="sxs-lookup"><span data-stu-id="69d32-116">Companies released software on a 6-month or even every year cadence.</span></span> <span data-ttu-id="69d32-117">除了 Microsoft Windows 以外，您還需要在 Windows 10 的過去一天內，瞭解發行的步調。</span><span class="sxs-lookup"><span data-stu-id="69d32-117">One needs to look no further than Microsoft Windows to get an idea for the cadence of releases that were acceptable before the ever-green days of Windows 10.</span></span> <span data-ttu-id="69d32-118">Windows XP 與 Vista 之間有五年的時間，Vista 與 Windows 7 之間有一個進一步的3個。</span><span class="sxs-lookup"><span data-stu-id="69d32-118">Five years passed between Windows XP and Vista, a further 3 between Vista and Windows 7.</span></span>

<span data-ttu-id="69d32-119">現在已妥善建立，可以快速發行軟體，讓更快速的公司在其 sloth 的競爭對手方面有相當大的市場優勢。</span><span class="sxs-lookup"><span data-stu-id="69d32-119">It's now fairly well established that being able to release software rapidly gives fast-moving companies a huge market advantage over their more sloth-like competitors.</span></span> <span data-ttu-id="69d32-120">這是因為 Windows 10 的重大更新現在大約每六個月。</span><span class="sxs-lookup"><span data-stu-id="69d32-120">It's for that reason that major updates to Windows 10 are now approximately every six months.</span></span>

<span data-ttu-id="69d32-121">可讓您更快、更可靠的版本提供價值給企業的模式和實務，統稱為 DevOps。</span><span class="sxs-lookup"><span data-stu-id="69d32-121">The patterns and practices that enable faster, more reliable releases to deliver value to the business are collectively known as DevOps.</span></span> <span data-ttu-id="69d32-122">其中包含跨越整個軟體發展生命週期的各種想法，從指定應用程式一直到傳遞和操作該應用程式都是如此。</span><span class="sxs-lookup"><span data-stu-id="69d32-122">They consist of a wide range of ideas spanning the entire software development life cycle from specifying an application all the way up to delivering and operating that application.</span></span>

<span data-ttu-id="69d32-123">DevOps 會在微服務之前出現，而且很可能會在沒有 DevOps 的情況下，讓您更輕鬆地在生產環境中釋出和操作一個但多個應用程式，而不會有更適合的目的服務。</span><span class="sxs-lookup"><span data-stu-id="69d32-123">DevOps emerged before microservices and it's likely that the movement towards smaller, more fit to purpose services wouldn't have been possible without DevOps to make releasing and operating not just one but many applications in production easier.</span></span> 

![圖11-0 搜尋趨勢顯示，微服務的成長不會開始，直到 DevOps 是一個建立良好的想法為止。](./media/microservices-vs-devops.png)

<span data-ttu-id="69d32-125">透過良好的 DevOps 實務，可以在實際操作應用程式的山地工作下，實現雲端原生應用程式的優點，而不需要 suffocating。</span><span class="sxs-lookup"><span data-stu-id="69d32-125">Through good DevOps practices, it's possible to realize the advantages of cloud-native applications without suffocating under a mountain of work actually operating the applications.</span></span>

<span data-ttu-id="69d32-126">DevOps 時沒有黃金 hammer。</span><span class="sxs-lookup"><span data-stu-id="69d32-126">There's no golden hammer when it comes to DevOps.</span></span> <span data-ttu-id="69d32-127">沒有人可以銷售一套完整且全面的解決方案來發行和操作高品質的應用程式。</span><span class="sxs-lookup"><span data-stu-id="69d32-127">Nobody can sell a complete and all-encompassing solution for releasing and operating high-quality applications.</span></span> <span data-ttu-id="69d32-128">這是因為每個應用程式的差異與所有其他應用程式截然不同。</span><span class="sxs-lookup"><span data-stu-id="69d32-128">This is because each application is wildly different from all others.</span></span> <span data-ttu-id="69d32-129">不過，有些工具可以讓 DevOps 的工作更不艱巨。</span><span class="sxs-lookup"><span data-stu-id="69d32-129">However, there are tools that can make DevOps a far less daunting proposition.</span></span> <span data-ttu-id="69d32-130">其中一個工具稱為 Azure DevOps。</span><span class="sxs-lookup"><span data-stu-id="69d32-130">One of these tools is known as Azure DevOps.</span></span>

## <a name="azure-devops"></a><span data-ttu-id="69d32-131">Azure DevOps</span><span class="sxs-lookup"><span data-stu-id="69d32-131">Azure DevOps</span></span>

<span data-ttu-id="69d32-132">Azure DevOps 具有長歷史。</span><span class="sxs-lookup"><span data-stu-id="69d32-132">Azure DevOps has a long pedigree.</span></span> <span data-ttu-id="69d32-133">當 Team Foundation Server 第一次在線上移動，以及透過各種名稱變更時，它可以將其根重新追蹤至：Visual Studio Online 和 Visual Studio Team Services。</span><span class="sxs-lookup"><span data-stu-id="69d32-133">It can trace its roots back to when Team Foundation Server first moved online and through the various name changes: Visual Studio Online and Visual Studio Team Services.</span></span> <span data-ttu-id="69d32-134">不過，過去幾年來，它就會變得遠超過其前身。</span><span class="sxs-lookup"><span data-stu-id="69d32-134">Through the years, however, it has become far more than its predecessors.</span></span>

<span data-ttu-id="69d32-135">Azure DevOps 分成五個主要元件：</span><span class="sxs-lookup"><span data-stu-id="69d32-135">Azure DevOps is divided into five major components:</span></span>

![圖 11-1 Azure DevOps 的五個主要區域](./media/devops-components.png)

<span data-ttu-id="69d32-137">**Azure Boards** -提供問題和工作專案追蹤工具，致力於讓使用者挑選最適合他們的工作流程。</span><span class="sxs-lookup"><span data-stu-id="69d32-137">**Azure Boards** - Provides an issue and work item tracking tool that strives to allow users to pick the workflows that work best for them.</span></span> <span data-ttu-id="69d32-138">其中包含一些預先設定的範本，其中包括支援 SCRUM 和看式開發的樣式。</span><span class="sxs-lookup"><span data-stu-id="69d32-138">It comes with a number of pre-configured templates including ones to support SCRUM and Kanban styles of development.</span></span> 

<span data-ttu-id="69d32-139">支援早期 Team Foundation 版本控制（TFVC）和業界最愛 git 的**Azure Repos**原始碼管理。</span><span class="sxs-lookup"><span data-stu-id="69d32-139">**Azure Repos** - Source code management that supports the venerable Team Foundation Version Control (TFVC) and the industry favorite git.</span></span> <span data-ttu-id="69d32-140">提取要求提供了一種方式，可讓您在進行變更時進行討論，藉以啟用社交編碼。</span><span class="sxs-lookup"><span data-stu-id="69d32-140">Pull requests provide a way to enable social coding by fostering discussion of changes as they're made.</span></span>

<span data-ttu-id="69d32-141">**Azure Pipelines** -支援與 Azure 緊密整合的組建和發行管理系統。</span><span class="sxs-lookup"><span data-stu-id="69d32-141">**Azure Pipelines** - A build and release management system that supports tight integration with Azure.</span></span> <span data-ttu-id="69d32-142">組建可以在各種不同的平臺上執行，從 Windows 到 Linux，再到 MacOS。</span><span class="sxs-lookup"><span data-stu-id="69d32-142">Builds can be run on a variety of platforms from Windows to Linux to MacOS.</span></span> <span data-ttu-id="69d32-143">組建代理程式可能會布建在雲端或內部部署中。</span><span class="sxs-lookup"><span data-stu-id="69d32-143">Build agents may be provisioned in the cloud or on-premises.</span></span>

<span data-ttu-id="69d32-144">**Azure Test Plans** -Test Plans 功能提供的測試管理和探勘測試支援不會留下任何 QA 人員。</span><span class="sxs-lookup"><span data-stu-id="69d32-144">**Azure Test Plans** - No QA person will be left behind with the test management and exploratory testing support offered by the Test Plans feature.</span></span>

<span data-ttu-id="69d32-145">**Azure Artifacts** -一種成品摘要，可讓公司建立自己、內部、版本的 NuGet、npm 及其他專案。</span><span class="sxs-lookup"><span data-stu-id="69d32-145">**Azure Artifacts** - An artifact feed that allows companies to create their own, internal, versions of NuGet, npm, and others.</span></span> <span data-ttu-id="69d32-146">如果集中式存放庫失敗，它會以雙重目的作為上游套件的快取。</span><span class="sxs-lookup"><span data-stu-id="69d32-146">It serves a double purpose of acting as a cache of upstream packages if there's a failure of a centralized repository.</span></span>

<span data-ttu-id="69d32-147">Azure DevOps 中的最上層組織單位稱為「專案」（Project）。</span><span class="sxs-lookup"><span data-stu-id="69d32-147">The top-level organizational unit in Azure DevOps is known as a Project.</span></span> <span data-ttu-id="69d32-148">在每個專案中，可以開啟和關閉各種元件（例如 Azure Artifacts）。</span><span class="sxs-lookup"><span data-stu-id="69d32-148">Within each project the various components, such as Azure Artifacts, can be turned on and off.</span></span> <span data-ttu-id="69d32-149">如果使用者想要在 GitHub 中管理自己的原始程式碼，但仍然利用 Azure Pipelines，那麼就可以做到這一點。</span><span class="sxs-lookup"><span data-stu-id="69d32-149">If users want to manage their source code in GitHub but still take advantage of Azure Pipelines, then that's perfectly possible.</span></span> <span data-ttu-id="69d32-150">事實上，許多開放原始碼專案會利用 Azure DevOps 所提供的[免費組建](https://azure.microsoft.com/blog/announcing-azure-pipelines-with-unlimited-ci-cd-minutes-for-open-source/)，同時將其原始程式碼保留在 GitHub 上。</span><span class="sxs-lookup"><span data-stu-id="69d32-150">In fact, many open-source projects leverage the [free builds](https://azure.microsoft.com/blog/announcing-azure-pipelines-with-unlimited-ci-cd-minutes-for-open-source/) offered by Azure DevOps while keeping their source code on GitHub.</span></span> <span data-ttu-id="69d32-151">某些重要的開放原始碼專案（例如[Visual Studio Code](https://code.visualstudio.com/)、 [yarn](https://yarnpkg.com/en/)、 [gulp](https://gulpjs.com/)和[NumPy](https://www.numpy.org/) ）已進行轉換。</span><span class="sxs-lookup"><span data-stu-id="69d32-151">Some significant open-source projects such as [Visual Studio Code](https://code.visualstudio.com/), [yarn](https://yarnpkg.com/en/), [gulp](https://gulpjs.com/), and [NumPy](https://www.numpy.org/) have made the transition.</span></span>

<span data-ttu-id="69d32-152">每個元件都提供雲端原生應用程式的一些優點，但最有用的三個是原始檔控制、面板和管線。</span><span class="sxs-lookup"><span data-stu-id="69d32-152">Each of these components provides some advantages for cloud-native applications, but the three most useful are the source control, boards and pipelines.</span></span>  

## <a name="source-control"></a><span data-ttu-id="69d32-153">原始檔控制</span><span class="sxs-lookup"><span data-stu-id="69d32-153">Source control</span></span>

<span data-ttu-id="69d32-154">為雲端原生應用程式組織程式碼可能是一項挑戰。</span><span class="sxs-lookup"><span data-stu-id="69d32-154">Organizing the code for a cloud-native application can be challenging.</span></span> <span data-ttu-id="69d32-155">雲端原生應用程式通常是由彼此通訊的小型應用程式 web 所組成，而不是單一的大型應用程式。</span><span class="sxs-lookup"><span data-stu-id="69d32-155">Instead of a single giant application, the cloud-native applications tend to be made up of a web of smaller applications that talk with one another.</span></span> <span data-ttu-id="69d32-156">就像計算中的所有專案一樣，程式碼的最佳排列仍然是開放的問題。</span><span class="sxs-lookup"><span data-stu-id="69d32-156">As with all things in computing, the best arrangement of code remains an open question.</span></span> <span data-ttu-id="69d32-157">有一些使用不同版面配置的成功應用程式範例，但兩個變體似乎最受歡迎。</span><span class="sxs-lookup"><span data-stu-id="69d32-157">There are examples of successful applications using different kinds of layouts, but two variants seem to have the most popularity.</span></span>

<span data-ttu-id="69d32-158">在進入實際的原始檔控制本身之前，您可能值得決定適當的專案數目。</span><span class="sxs-lookup"><span data-stu-id="69d32-158">Before getting down into the actual source control itself, it's probably worth deciding on how many projects are appropriate.</span></span> <span data-ttu-id="69d32-159">在單一專案中，支援多個存放庫和組建管線。</span><span class="sxs-lookup"><span data-stu-id="69d32-159">Within a single project, there's support for multiple repositories, and build pipelines.</span></span> <span data-ttu-id="69d32-160">面板有點複雜，但是工作也可以輕鬆地指派給單一專案中的多個小組。</span><span class="sxs-lookup"><span data-stu-id="69d32-160">Boards are a little more complicated but there too the tasks can easily be assigned to multiple teams within a single project.</span></span> <span data-ttu-id="69d32-161">從單一 Azure DevOps 專案中，您肯定可以支援數百個甚至數千個開發人員。</span><span class="sxs-lookup"><span data-stu-id="69d32-161">It's certainly possible to support hundreds, even thousands of developers, out of a single Azure DevOps project.</span></span> <span data-ttu-id="69d32-162">這麼做可能是最佳的方法，因為它提供單一位置，讓所有開發人員都能使用，並減少當開發人員不確定該應用程式所在的專案時，發現它的混淆。</span><span class="sxs-lookup"><span data-stu-id="69d32-162">Doing so is likely the best approach as it provides a single place for all developer to work out of and reduces the confusion of finding that one application when developers are unsure in which project in which it resides.</span></span>

<span data-ttu-id="69d32-163">分割 Azure DevOps 專案中的微服務程式代碼可能會有更大的挑戰。</span><span class="sxs-lookup"><span data-stu-id="69d32-163">Splitting up code for microservices within the Azure DevOps project can be slightly more challenging.</span></span>

![圖11-2 單一與多個存放庫](./media/single-repository-vs-multiple.png)

### <a name="repository-per-microservice"></a><span data-ttu-id="69d32-165">每個微服務的存放庫</span><span class="sxs-lookup"><span data-stu-id="69d32-165">Repository per microservice</span></span>

<span data-ttu-id="69d32-166">乍看之下，這似乎是分割原始程式碼以進行微服務的最邏輯方法。</span><span class="sxs-lookup"><span data-stu-id="69d32-166">At first glance, this seems like the most logical approach to splitting up the source code for microservices.</span></span> <span data-ttu-id="69d32-167">每個存放庫都可以包含建立一個微服務所需的程式碼。</span><span class="sxs-lookup"><span data-stu-id="69d32-167">Each repository can contain the code needed to build the one microservice.</span></span> <span data-ttu-id="69d32-168">這種方法的優點很容易看到：</span><span class="sxs-lookup"><span data-stu-id="69d32-168">The advantages to this approach are readily visible:</span></span>

1. <span data-ttu-id="69d32-169">建立和維護應用程式的指示可以新增至每個存放庫根目錄中的讀我檔案。</span><span class="sxs-lookup"><span data-stu-id="69d32-169">Instructions for building and maintaining the application can be added to a README file at the root of each repository.</span></span> <span data-ttu-id="69d32-170">當您透過存放庫進行翻轉時，很容易就能找到這些指示，縮短開發人員的加速時間。</span><span class="sxs-lookup"><span data-stu-id="69d32-170">When flipping through the repositories, it's easy to find these instructions, reducing spin-up time for developers.</span></span>
2. <span data-ttu-id="69d32-171">每個服務都位於邏輯位置，只要知道服務的名稱，就能輕鬆找到。</span><span class="sxs-lookup"><span data-stu-id="69d32-171">Every service is located in a logical place, easily found by knowing the name of the service.</span></span>
3. <span data-ttu-id="69d32-172">您可以輕鬆地設定組建，使其只有在對擁有存放庫進行變更時才會觸發。</span><span class="sxs-lookup"><span data-stu-id="69d32-172">Builds can easily be set up such that they're only triggered when a change is made to the owning repository.</span></span>
4. <span data-ttu-id="69d32-173">進入存放庫的變更數僅限於處理專案的少數開發人員。</span><span class="sxs-lookup"><span data-stu-id="69d32-173">The number of changes coming into a repository is limited to the small number of developers working on the project.</span></span>
5. <span data-ttu-id="69d32-174">藉由限制開發人員擁有讀取和寫入權限的儲存機制，可輕鬆設定安全性。</span><span class="sxs-lookup"><span data-stu-id="69d32-174">Security is easy to set up by restricting the repositories to which developers have read and write permissions.</span></span>
6. <span data-ttu-id="69d32-175">主控小組可以變更存放庫層級設定，與其他人的討論最少。</span><span class="sxs-lookup"><span data-stu-id="69d32-175">Repository level settings can be changed by the owning team with a minimum of discussion with others.</span></span>

<span data-ttu-id="69d32-176">微服務背後的其中一個重要概念是，服務應該區分開來，並彼此區隔。</span><span class="sxs-lookup"><span data-stu-id="69d32-176">One of the key ideas behind microservices is that services should be siloed and separated from each other.</span></span> <span data-ttu-id="69d32-177">使用網域導向設計來決定服務的界限時，服務會作為交易界限。</span><span class="sxs-lookup"><span data-stu-id="69d32-177">When using Domain Driven Design to decide on the boundaries for services the services act as transactional boundaries.</span></span> <span data-ttu-id="69d32-178">資料庫更新不應跨越多個服務。</span><span class="sxs-lookup"><span data-stu-id="69d32-178">Database updates shouldn't span multiple services.</span></span> <span data-ttu-id="69d32-179">這項相關資料的集合稱為「系結內容」。</span><span class="sxs-lookup"><span data-stu-id="69d32-179">This collection of related data is referred to as a bounded context.</span></span>  <span data-ttu-id="69d32-180">這項想法的反映是將微服務資料隔離到資料庫，並獨立于其餘的服務。</span><span class="sxs-lookup"><span data-stu-id="69d32-180">This idea is reflected by the isolation of microservice data to a database separate and autonomous from the rest of the services.</span></span> <span data-ttu-id="69d32-181">這讓您有很大的想法，可以將這種作法帶到原始程式碼。</span><span class="sxs-lookup"><span data-stu-id="69d32-181">It makes a great deal of sense to carry this idea all the way through to the source code.</span></span>

<span data-ttu-id="69d32-182">不過，這種方法並不會有問題。</span><span class="sxs-lookup"><span data-stu-id="69d32-182">However, this approach isn't without its issues.</span></span> <span data-ttu-id="69d32-183">我們時間的其中一個比較 gnarly 的開發問題是管理相依性。</span><span class="sxs-lookup"><span data-stu-id="69d32-183">One of the more gnarly development problems of our time is managing dependencies.</span></span> <span data-ttu-id="69d32-184">請考慮組成平均`node_modules`目錄的檔案數目。</span><span class="sxs-lookup"><span data-stu-id="69d32-184">Consider the number of files that make up the average `node_modules` directory.</span></span> <span data-ttu-id="69d32-185">全新安裝（例如`create-react-app` ）可能會攜帶數以千計的套件。</span><span class="sxs-lookup"><span data-stu-id="69d32-185">A fresh install of something like `create-react-app` is likely to bring with it thousands of packages.</span></span> <span data-ttu-id="69d32-186">如何管理這些相依性的問題，是一項很棘手的問題。</span><span class="sxs-lookup"><span data-stu-id="69d32-186">The question of how to manage these dependencies is a difficult one.</span></span> 

<span data-ttu-id="69d32-187">如果相依性已更新，下游套件也必須更新此相依性。</span><span class="sxs-lookup"><span data-stu-id="69d32-187">If a dependency is updated, then downstream packages must also update this dependency.</span></span> <span data-ttu-id="69d32-188">可惜的是，這會讓開發工作順利進行`node_modules` ，因此，目錄最後會有單一封裝的多個版本，每個套件的相依性會稍有不同。</span><span class="sxs-lookup"><span data-stu-id="69d32-188">Unfortunately, that takes development work so, invariably, the `node_modules` directory ends up with multiple versions of a single package, each one a dependency of some other package that is versioned at a slightly different cadence.</span></span> <span data-ttu-id="69d32-189">部署應用程式時，應該使用哪個版本的相依性？</span><span class="sxs-lookup"><span data-stu-id="69d32-189">When deploying an application, which version of a dependency should be used?</span></span> <span data-ttu-id="69d32-190">目前在生產環境中的版本？</span><span class="sxs-lookup"><span data-stu-id="69d32-190">The version that is currently in production?</span></span> <span data-ttu-id="69d32-191">目前在搶鮮版（Beta）中，但在取用者進入生產階段時可能會在生產環境中的版本？</span><span class="sxs-lookup"><span data-stu-id="69d32-191">The version that is currently in Beta but is likely to be in production by the time the consumer makes it to production?</span></span> <span data-ttu-id="69d32-192">只使用微服務不會解決的困難問題。</span><span class="sxs-lookup"><span data-stu-id="69d32-192">Difficult problems that aren't resolved by just using microservices.</span></span>

<span data-ttu-id="69d32-193">有各種不同的專案相依的程式庫。</span><span class="sxs-lookup"><span data-stu-id="69d32-193">There are libraries that are depended upon by a wide variety of projects.</span></span> <span data-ttu-id="69d32-194">藉由將微服務除以每個存放庫中的其中一個，可使用內部存放庫（Azure Artifacts）來最佳解析內部相依性。</span><span class="sxs-lookup"><span data-stu-id="69d32-194">By dividing the microservices up with one in each repository the internal dependencies can best be resolved by using the internal repository, Azure Artifacts.</span></span> <span data-ttu-id="69d32-195">程式庫的組建會將其最新版本推送至 Azure Artifacts 以供內部使用。</span><span class="sxs-lookup"><span data-stu-id="69d32-195">Builds for libraries will push their latest versions into Azure Artifacts for internal consumption.</span></span> <span data-ttu-id="69d32-196">下游專案仍然必須手動更新，才能依賴新更新的套件。</span><span class="sxs-lookup"><span data-stu-id="69d32-196">The downstream project must still be manually updated to take a dependency on the newly updated packages.</span></span>

<span data-ttu-id="69d32-197">在服務之間移動程式碼時，有另一個缺點會自我呈現。</span><span class="sxs-lookup"><span data-stu-id="69d32-197">Another disadvantage presents itself when moving code between services.</span></span> <span data-ttu-id="69d32-198">雖然相信將應用程式的第一個部門分割成微服務是 100% 正確，但事實上，我們很少會 prescient 為不會產生服務部門錯誤。</span><span class="sxs-lookup"><span data-stu-id="69d32-198">Although it would be nice to believe that the first division of an application into microservices is 100% correct, the reality is that rarely we're so prescient as to make no service division mistakes.</span></span> <span data-ttu-id="69d32-199">因此，功能和驅動它的程式碼必須從服務到服務：存放庫移至存放庫。</span><span class="sxs-lookup"><span data-stu-id="69d32-199">Thus, functionality and the code that drives it will need to move from service to service: repository to repository.</span></span> <span data-ttu-id="69d32-200">當您從某個存放庫大幅新增了至另一個儲存機制時，程式碼會失去其歷程記錄。</span><span class="sxs-lookup"><span data-stu-id="69d32-200">When leaping from one repository to another, the code loses its history.</span></span> <span data-ttu-id="69d32-201">在許多情況下，尤其是在 audit 的事件中，有一段程式碼的完整歷程記錄非常寶貴。</span><span class="sxs-lookup"><span data-stu-id="69d32-201">There are many cases, especially in the event of an audit, where having full history on a piece of code is invaluable.</span></span>

<span data-ttu-id="69d32-202">最後也可能是最重要的缺點，就是協調變更。</span><span class="sxs-lookup"><span data-stu-id="69d32-202">The final and perhaps most important disadvantage is coordinating changes.</span></span> <span data-ttu-id="69d32-203">在真正的微服務應用程式中，服務之間不應該有任何部署相依性。</span><span class="sxs-lookup"><span data-stu-id="69d32-203">In a true microservices application, there should be no deployment dependencies between services.</span></span> <span data-ttu-id="69d32-204">您應該依任何順序部署服務 A、B 和 C，因為它們的結合性不盡相同。</span><span class="sxs-lookup"><span data-stu-id="69d32-204">It should be possible to deploy services A, B, and C in any order as they have loose coupling.</span></span> <span data-ttu-id="69d32-205">不過事實上，有時候會想要同時進行跨多個存放庫的變更。</span><span class="sxs-lookup"><span data-stu-id="69d32-205">In reality, however, there are times when it's desirable to make a change that crosses multiple repositories at the same time.</span></span> <span data-ttu-id="69d32-206">部分範例包括更新程式庫以關閉安全性漏洞，或變更所有服務所使用的通訊協定。</span><span class="sxs-lookup"><span data-stu-id="69d32-206">Some examples include updating a library to close a security hole or changing a communication protocol used by all services.</span></span>

<span data-ttu-id="69d32-207">若要進行跨存放庫變更，必須將每個存放庫的認可連續進行。</span><span class="sxs-lookup"><span data-stu-id="69d32-207">To do a cross-repository change requires a commit to each repository be made in succession.</span></span> <span data-ttu-id="69d32-208">每個存放庫中的每個變更都必須個別提取要求和檢查。</span><span class="sxs-lookup"><span data-stu-id="69d32-208">Each change in each repository will need to be pull-requested and reviewed separately.</span></span> <span data-ttu-id="69d32-209">這可能很容易協調，而且通常會令人困擾。</span><span class="sxs-lookup"><span data-stu-id="69d32-209">This can be difficult to coordinate and generally annoying to do.</span></span>

<span data-ttu-id="69d32-210">使用許多存放庫的另一種方式，是將所有原始程式碼放在一個巨大、所有的單一存放庫中。</span><span class="sxs-lookup"><span data-stu-id="69d32-210">An alternative to using many repositories is to put all the source code together in a giant, all knowing, single repository.</span></span>

### <a name="single-repository"></a><span data-ttu-id="69d32-211">單一存放庫</span><span class="sxs-lookup"><span data-stu-id="69d32-211">Single repository</span></span>

<span data-ttu-id="69d32-212">在此方法中，有時稱為「 [monorepository](https://danluu.com/monorepo/)」，每個服務的所有原始程式碼都會放入相同的儲存機制中。</span><span class="sxs-lookup"><span data-stu-id="69d32-212">In this approach, sometimes referred to as a [monorepository](https://danluu.com/monorepo/), all the source code for every service is put into the same repository.</span></span> <span data-ttu-id="69d32-213">一開始，這似乎是很可怕的概念，可能會讓您難以處理原始程式碼。</span><span class="sxs-lookup"><span data-stu-id="69d32-213">At first, this seems like a terrible idea likely to make dealing with source code unwieldy.</span></span> <span data-ttu-id="69d32-214">不過，有一些標示的優點可讓您以這種方式運作。</span><span class="sxs-lookup"><span data-stu-id="69d32-214">There are, however, some marked advantages to working this way.</span></span>

<span data-ttu-id="69d32-215">第一個優點是，管理專案之間的相依性比較容易。</span><span class="sxs-lookup"><span data-stu-id="69d32-215">The first advantage is that it's easier to manage dependencies between projects.</span></span> <span data-ttu-id="69d32-216">專案可以直接彼此匯入，而不是依賴某些外部成品摘要。</span><span class="sxs-lookup"><span data-stu-id="69d32-216">Instead of relying on some external artifact feed, projects can directly import one another.</span></span> <span data-ttu-id="69d32-217">這表示更新是即時的，而且在編譯時期可能會在開發人員工作站上找到衝突的版本。</span><span class="sxs-lookup"><span data-stu-id="69d32-217">This means that updates are instant, and conflicting versions are likely to be found at compile time on the developer's workstation.</span></span> <span data-ttu-id="69d32-218">實際上，將一些整合測試轉移到左邊。</span><span class="sxs-lookup"><span data-stu-id="69d32-218">In effect, shifting some of the integration testing left.</span></span>

<span data-ttu-id="69d32-219">在專案之間移動程式碼時，您現在可以更輕鬆地保留歷程記錄，因為檔案會被偵測為已移動，而不是重寫。</span><span class="sxs-lookup"><span data-stu-id="69d32-219">When moving code between projects, it's now easier to preserve the history as the files will be detected as having been moved rather than being rewritten.</span></span>

<span data-ttu-id="69d32-220">另一個優點是跨服務界限的廣泛變更可以在單一認可中進行。</span><span class="sxs-lookup"><span data-stu-id="69d32-220">Another advantage is that wide ranging changes that cross service boundaries can be made in a single commit.</span></span> <span data-ttu-id="69d32-221">這可減少可能會個別審查變更的額外負荷。</span><span class="sxs-lookup"><span data-stu-id="69d32-221">This reduces the overhead of having potentially dozens of changes to review individually.</span></span>

<span data-ttu-id="69d32-222">有許多工具可執行程式碼的靜態分析，以偵測不安全的程式設計做法或有問題地使用 Api。</span><span class="sxs-lookup"><span data-stu-id="69d32-222">There are many tools that can perform static analysis of code to detect insecure programming practices or problematic use of APIs.</span></span> <span data-ttu-id="69d32-223">在多存放庫的世界中，每個存放庫都必須反復查看，才能找出問題。</span><span class="sxs-lookup"><span data-stu-id="69d32-223">In a multi-repository world, each repository will need to be iterated over to find the problems in them.</span></span> <span data-ttu-id="69d32-224">單一存放庫可讓您在同一處執行分析。</span><span class="sxs-lookup"><span data-stu-id="69d32-224">The single repository allows running the analysis all in one place.</span></span>

<span data-ttu-id="69d32-225">單一存放庫方法也有許多缺點。</span><span class="sxs-lookup"><span data-stu-id="69d32-225">There are also many disadvantages to the single repository approach.</span></span> <span data-ttu-id="69d32-226">其中一個最重要的是，讓單一存放庫引發安全性考慮。</span><span class="sxs-lookup"><span data-stu-id="69d32-226">One of the most worrying ones is that having a single repository raises security concerns.</span></span> <span data-ttu-id="69d32-227">如果存放庫的內容在每個服務模型的存放庫中流失，則遺失的程式碼數量最少。</span><span class="sxs-lookup"><span data-stu-id="69d32-227">If the contents of a repository are leaked in a repository per service model, the amount of code lost is minimal.</span></span> <span data-ttu-id="69d32-228">使用單一存放庫，公司擁有的所有專案都可能會遺失。</span><span class="sxs-lookup"><span data-stu-id="69d32-228">With a single repository, everything the company owns could be lost.</span></span> <span data-ttu-id="69d32-229">在過去發生這種情況時，有很多範例會 derailing 整個遊戲開發工作。</span><span class="sxs-lookup"><span data-stu-id="69d32-229">There have been many examples in the past of this happening and derailing entire game development efforts.</span></span> <span data-ttu-id="69d32-230">擁有多個存放庫會公開較少的介面區，這是大部分安全性作法中非常理想的特性。</span><span class="sxs-lookup"><span data-stu-id="69d32-230">Having multiple repositories exposes less surface area, which is a very desirable trait in most security practices.</span></span>

<span data-ttu-id="69d32-231">單一存放庫的大小可能很快就會變得難以管理。</span><span class="sxs-lookup"><span data-stu-id="69d32-231">The size of the single repository is likely to become unmanageable rapidly.</span></span> <span data-ttu-id="69d32-232">這會帶來一些有趣的效能影響。</span><span class="sxs-lookup"><span data-stu-id="69d32-232">This presents some interesting performance implications.</span></span> <span data-ttu-id="69d32-233">您可能需要使用特定的工具，例如[適用于 Git 的虛擬檔案系統](https://vfsforgit.org/)，其原本是設計來改善開發人員對 Windows 小組的體驗。</span><span class="sxs-lookup"><span data-stu-id="69d32-233">It may become necessary to use specialized tools such as [Virtual File System for Git](https://vfsforgit.org/), which was originally designed to improve the experience for developers on the Windows team.</span></span>

<span data-ttu-id="69d32-234">使用單一存放庫的引數通常會向下細分為 Facebook 或 Google 使用此方法進行原始程式碼安排的引數。</span><span class="sxs-lookup"><span data-stu-id="69d32-234">Frequently the argument for using a single repository boils down to an argument that Facebook or Google use this method for source code arrangement.</span></span> <span data-ttu-id="69d32-235">如果此方法對這些公司而言夠好，那麼就一定是適用于所有公司的正確方法。</span><span class="sxs-lookup"><span data-stu-id="69d32-235">If the approach is good enough for these companies, then, surely, it's the correct approach for all companies.</span></span> <span data-ttu-id="69d32-236">事實上，非常少的公司會針對 Facebook 或 Google 的規模等任何專案進行操作。</span><span class="sxs-lookup"><span data-stu-id="69d32-236">The truth of the matter is that very few companies operate on anything like the scale of Facebook or Google.</span></span> <span data-ttu-id="69d32-237">這些規模所發生的問題與大部分的開發人員會面臨的不同。</span><span class="sxs-lookup"><span data-stu-id="69d32-237">The problems that occur at those scales are different from those most developers will face.</span></span> <span data-ttu-id="69d32-238">適用于 goose 的好處可能不適合看。</span><span class="sxs-lookup"><span data-stu-id="69d32-238">What is good for the goose may not be good for the gander.</span></span>

<span data-ttu-id="69d32-239">最後，這兩個解決方案都可以用來裝載微服務的原始程式碼。</span><span class="sxs-lookup"><span data-stu-id="69d32-239">In the end, either solution can be used to host the source code for microservices.</span></span> <span data-ttu-id="69d32-240">不過，在大多數情況下，在單一存放庫中操作的管理和工程負荷，並不值得 meager 的優點。</span><span class="sxs-lookup"><span data-stu-id="69d32-240">However, in most cases, the management and engineering overhead of operating in a single repository isn't worth the meager advantages.</span></span> <span data-ttu-id="69d32-241">將程式碼分割到多個存放庫，鼓勵更好的顧慮區隔，並鼓勵開發團隊之間的自主性。</span><span class="sxs-lookup"><span data-stu-id="69d32-241">Splitting code up over multiple repositories encourages better separation of concerns and encourages autonomy among development teams.</span></span>  

### <a name="standard-directory-structure"></a><span data-ttu-id="69d32-242">標準目錄結構</span><span class="sxs-lookup"><span data-stu-id="69d32-242">Standard directory structure</span></span>

<span data-ttu-id="69d32-243">無論單一或多個存放庫都爭論，每個服務都有自己的目錄。</span><span class="sxs-lookup"><span data-stu-id="69d32-243">Regardless of the single versus multiple repositories debate each service will have its own directory.</span></span> <span data-ttu-id="69d32-244">讓開發人員快速地跨專案進行的最佳優化之一，是維護標準目錄結構。</span><span class="sxs-lookup"><span data-stu-id="69d32-244">One of the best optimizations to allow developers to cross between projects quickly is to maintain a standard directory structure.</span></span>

![圖11-3 電子郵件和登入服務的標準目錄結構](./media/dir-struct.png)

<span data-ttu-id="69d32-246">每當建立新的專案時，就應該使用放置正確結構的範本。</span><span class="sxs-lookup"><span data-stu-id="69d32-246">Whenever a new project is created, a template that puts in place the correct structure should be used.</span></span> <span data-ttu-id="69d32-247">此範本也可以將這類有用的專案包含為基本架構自述`azure-pipelines.yml`檔和。</span><span class="sxs-lookup"><span data-stu-id="69d32-247">This template can also include such useful items as a skeleton README file and an `azure-pipelines.yml`.</span></span> <span data-ttu-id="69d32-248">在任何微服務架構中，專案之間的高度變異數會使服務的大量作業變得更棘手。</span><span class="sxs-lookup"><span data-stu-id="69d32-248">In any microservice architecture, a high degree of variance between projects makes bulk operations against the services more difficult.</span></span>

<span data-ttu-id="69d32-249">有許多工具可針對整個目錄提供範本化，其中包含數個原始程式碼目錄。</span><span class="sxs-lookup"><span data-stu-id="69d32-249">There are many tools that can provide templating for an entire directory, containing several source code directories.</span></span> <span data-ttu-id="69d32-250">[Yeoman](https://yeoman.io/)在 JavaScript 世界中很熱門，GitHub 有最近發行的存放[庫範本](https://github.blog/2019-06-06-generate-new-repositories-with-repository-templates/)，可提供許多相同的功能。</span><span class="sxs-lookup"><span data-stu-id="69d32-250">[Yeoman](https://yeoman.io/) is popular in the JavaScript world and GitHub have recently released [Repository Templates](https://github.blog/2019-06-06-generate-new-repositories-with-repository-templates/), which provide much of the same functionality.</span></span>

## <a name="task-management"></a><span data-ttu-id="69d32-251">任務管理</span><span class="sxs-lookup"><span data-stu-id="69d32-251">Task management</span></span>

<span data-ttu-id="69d32-252">管理任何專案中的工作可能會很棘手。</span><span class="sxs-lookup"><span data-stu-id="69d32-252">Managing tasks in any project can be difficult.</span></span> <span data-ttu-id="69d32-253">在前面，有無數的問題需要回答，以確保開發人員能以最佳的生產力進行設定。</span><span class="sxs-lookup"><span data-stu-id="69d32-253">Up front there are countless questions to be answered about the sort of workflows to set up to ensure optimal developer productivity.</span></span>

<span data-ttu-id="69d32-254">雲端原生應用程式通常會比傳統軟體產品小，或至少會分成較小的服務。</span><span class="sxs-lookup"><span data-stu-id="69d32-254">Cloud-native applications tend to be smaller than traditional software products or at least they're divided into smaller services.</span></span> <span data-ttu-id="69d32-255">追蹤與這些服務相關的問題或工作，與任何其他軟體專案一樣重要。</span><span class="sxs-lookup"><span data-stu-id="69d32-255">Tracking of issues or tasks related to these services remains as important as with any other software project.</span></span> <span data-ttu-id="69d32-256">任何人都不想要追蹤某個工作專案，或向客戶說明未正確記錄其問題。</span><span class="sxs-lookup"><span data-stu-id="69d32-256">Nobody wants to lose track of some work item or explain to a customer that their issue wasn't properly logged.</span></span> <span data-ttu-id="69d32-257">面板會在專案層級進行設定，但在每個專案中，可以定義區域。</span><span class="sxs-lookup"><span data-stu-id="69d32-257">Boards are configured at the project level but within each project, areas can be defined.</span></span> <span data-ttu-id="69d32-258">這些可讓您細分數個元件的問題。</span><span class="sxs-lookup"><span data-stu-id="69d32-258">These allow breaking down issues across several components.</span></span> <span data-ttu-id="69d32-259">將整個應用程式的所有工作都放在同一個位置的優點是，您可以輕鬆地將工作專案從一個小組移到另一個團隊，因為他們已獲得更好的瞭解。</span><span class="sxs-lookup"><span data-stu-id="69d32-259">The advantage to keeping all the work for the entire application in one place is that it's easy to move work items from one team to another as they're understood better.</span></span>

<span data-ttu-id="69d32-260">Azure DevOps 隨附一些預先設定的熱門範本。</span><span class="sxs-lookup"><span data-stu-id="69d32-260">Azure DevOps comes with a number of popular templates pre-configured.</span></span> <span data-ttu-id="69d32-261">在最基本的設定中，您只需要知道待處理專案中的內容、人員的工作，以及完成的動作。</span><span class="sxs-lookup"><span data-stu-id="69d32-261">In the most basic configuration, all that is needed to know is what's in the backlog, what people are working on, and what's done.</span></span> <span data-ttu-id="69d32-262">請務必讓您瞭解建立軟體的程式，以便將工作設定為優先處理，並向客戶回報已完成的作業。</span><span class="sxs-lookup"><span data-stu-id="69d32-262">It's important to have this visibility into the process of building software, so that work can be prioritized and completed tasks reported to the customer.</span></span> <span data-ttu-id="69d32-263">當然，非常少的軟體專案會以`to do`、 `doing`和`done`等方式，來處理簡單的程式。</span><span class="sxs-lookup"><span data-stu-id="69d32-263">Of course, very few software projects stick to a process as simple as `to do`, `doing`, and `done`.</span></span> <span data-ttu-id="69d32-264">人們不需要花很長的時間，就能`QA`開始`Detailed Specification`將或之類的步驟新增至程式。</span><span class="sxs-lookup"><span data-stu-id="69d32-264">It doesn't take long for people to start adding steps like `QA` or `Detailed Specification` to the process.</span></span>

<span data-ttu-id="69d32-265">Agile 方法的其中一個比較重要的部分，是固定間隔的自我自我檢查。</span><span class="sxs-lookup"><span data-stu-id="69d32-265">One of the more important parts of Agile methodologies is self-introspection at regular intervals.</span></span> <span data-ttu-id="69d32-266">這些評論的目的是要讓您深入瞭解小組面臨的問題，以及如何改善。</span><span class="sxs-lookup"><span data-stu-id="69d32-266">These reviews are meant to provide insight into what problems the team is facing and how they can be improved.</span></span> <span data-ttu-id="69d32-267">這通常表示透過開發程式來變更問題和功能的流程。</span><span class="sxs-lookup"><span data-stu-id="69d32-267">Frequently, this means changing the flow of issues and features through the development process.</span></span> <span data-ttu-id="69d32-268">因此，以額外的階段擴展面板的版面配置，會是完美的狀況。</span><span class="sxs-lookup"><span data-stu-id="69d32-268">So, it's perfectly healthy to expand the layouts of the boards with additional stages.</span></span>

<span data-ttu-id="69d32-269">面板中的階段並不是唯一的組織工具。</span><span class="sxs-lookup"><span data-stu-id="69d32-269">The stages in the boards aren't the only organizational tool.</span></span> <span data-ttu-id="69d32-270">視面板的設定而定，有一個工作專案階層。</span><span class="sxs-lookup"><span data-stu-id="69d32-270">Depending on the configuration of the board, there's a hierarchy of work items.</span></span> <span data-ttu-id="69d32-271">最細微的專案可以出現在面板上，這是一項任務。</span><span class="sxs-lookup"><span data-stu-id="69d32-271">The most granular item that can appear on a board is a task.</span></span> <span data-ttu-id="69d32-272">根據預設，工作包含標題、描述、優先順序、剩餘工作量，以及連結至其他工作專案或開發專案（分支、認可、提取要求、組建等等）之能力的欄位。</span><span class="sxs-lookup"><span data-stu-id="69d32-272">Out of the box a task contains fields for a title, description, a priority, an estimate of the amount of work remaining and the ability to link to other work items or development items (branches, commits, pull requests, builds, and so forth).</span></span> <span data-ttu-id="69d32-273">工作專案可以分類到應用程式的不同區域和不同的反復專案（短期衝刺），以便更輕鬆地找到它們。</span><span class="sxs-lookup"><span data-stu-id="69d32-273">Work items can be classified into different areas of the application and different iterations (sprints) to make finding them easier.</span></span>

![圖 11-4 Azure DevOps 中的範例工作](./media/task-details.png)

<span data-ttu-id="69d32-275">[描述] 欄位支援您預期的一般樣式（粗體、斜體底線和刪除線），以及插入影像的能力。</span><span class="sxs-lookup"><span data-stu-id="69d32-275">The description field supports the normal styles you'd expect (bold, italic underscore and strike through) and the ability to insert images.</span></span> <span data-ttu-id="69d32-276">這讓它成為一種功能強大的工具，可在指定工作或 bug 時使用。</span><span class="sxs-lookup"><span data-stu-id="69d32-276">This makes it a very powerful tool for use when specifying work or bugs.</span></span>

<span data-ttu-id="69d32-277">工作可以匯總成功能，以定義較大的工作單位。</span><span class="sxs-lookup"><span data-stu-id="69d32-277">Tasks can be rolled up into features, which define a larger unit of work.</span></span> <span data-ttu-id="69d32-278">接著，您可以將功能[匯總到 epics](https://docs.microsoft.com/azure/devops/boards/backlogs/define-features-epics?view=azure-devops)。</span><span class="sxs-lookup"><span data-stu-id="69d32-278">Features, in turn, can be [rolled up into epics](https://docs.microsoft.com/azure/devops/boards/backlogs/define-features-epics?view=azure-devops).</span></span> <span data-ttu-id="69d32-279">將此階層中的工作分類，可讓您更輕鬆地瞭解如何關閉大型功能。</span><span class="sxs-lookup"><span data-stu-id="69d32-279">Classifying tasks in this hierarchy makes it much easier to understand how close a large feature is to rolling out.</span></span>

![圖11-5 基本流程範本中預設設定的工作專案類型](./media/board-issue-types.png)

<span data-ttu-id="69d32-281">Azure Boards 中的問題有不同類型的觀點。</span><span class="sxs-lookup"><span data-stu-id="69d32-281">There are different kinds of views into the issues in Azure Boards.</span></span> <span data-ttu-id="69d32-282">尚未排程的專案會出現在待處理專案中。</span><span class="sxs-lookup"><span data-stu-id="69d32-282">Items that aren't yet scheduled appear in the backlog.</span></span> <span data-ttu-id="69d32-283">您可以從該處將它們指派給短期衝刺。</span><span class="sxs-lookup"><span data-stu-id="69d32-283">From there, they can be assigned to a sprint.</span></span> <span data-ttu-id="69d32-284">短期衝刺是一個時間方塊，在這段期間內，將會完成一些工作數量。</span><span class="sxs-lookup"><span data-stu-id="69d32-284">A sprint is a time box during which it's expected some quantity of work will be completed.</span></span> <span data-ttu-id="69d32-285">這項工作可以包含工作，但也包括票證的解決方式。</span><span class="sxs-lookup"><span data-stu-id="69d32-285">This work can include tasks but also the resolution of tickets.</span></span> <span data-ttu-id="69d32-286">之後，就可以從 [短期衝刺面板] 區段管理整個短期衝刺。</span><span class="sxs-lookup"><span data-stu-id="69d32-286">Once there, the entire sprint can be managed from the Sprint board section.</span></span> <span data-ttu-id="69d32-287">此視圖會顯示工作的進度，並包含 [向下燒錄] 圖表，以便在短期衝刺成功時，提供更新的評估。</span><span class="sxs-lookup"><span data-stu-id="69d32-287">This view shows how work is progressing and includes a burn down chart to give an ever-updating estimate of if the sprint will be successful.</span></span>

![圖11-6 已定義短期衝刺的面板](./media/sprint-board.png)

<span data-ttu-id="69d32-289">現在，Azure DevOps 的面板中應該會有大量的電源。</span><span class="sxs-lookup"><span data-stu-id="69d32-289">By now, it should be apparent that there's a great deal of power in the Boards in Azure DevOps.</span></span> <span data-ttu-id="69d32-290">對於開發人員而言，會有很簡單的觀點來處理。</span><span class="sxs-lookup"><span data-stu-id="69d32-290">For developers, there are easy views of what is being worked on.</span></span> <span data-ttu-id="69d32-291">專案經理可以查看即將推出的工作，以及現有工作的總覽。</span><span class="sxs-lookup"><span data-stu-id="69d32-291">For project managers views into upcoming work as well as an overview of existing work.</span></span> <span data-ttu-id="69d32-292">對於管理員而言，有許多關於「空間」和「容量」的報告。</span><span class="sxs-lookup"><span data-stu-id="69d32-292">For managers, there are plenty of reports about resourcing and capacity.</span></span> <span data-ttu-id="69d32-293">可惜的是，雲端原生應用程式沒有任何神奇，因此不需要追蹤工作。</span><span class="sxs-lookup"><span data-stu-id="69d32-293">Unfortunately, there's nothing magical about cloud-native applications that eliminate the need to track work.</span></span> <span data-ttu-id="69d32-294">但是，如果您必須追蹤工作，有幾個地方的經驗優於 Azure DevOps。</span><span class="sxs-lookup"><span data-stu-id="69d32-294">But if you must track work, there are a few places where the experience is better than in Azure DevOps.</span></span>

## <a name="cicd-pipelines"></a><span data-ttu-id="69d32-295">CI/CD 管線</span><span class="sxs-lookup"><span data-stu-id="69d32-295">CI/CD pipelines</span></span>

<span data-ttu-id="69d32-296">軟體發展生命週期幾乎不會有任何改變，因為持續整合（CI）和持續傳遞（CD）的問世。</span><span class="sxs-lookup"><span data-stu-id="69d32-296">Almost no change in the software development life cycle has been so revolutionary as the advent of continuous integration (CI) and continuous delivery (CD).</span></span> <span data-ttu-id="69d32-297">一旦簽入變更時，立即針對專案的原始程式碼建立和執行自動化測試，會提早攔截錯誤。</span><span class="sxs-lookup"><span data-stu-id="69d32-297">Building and running automated tests against the source code of a project as soon as a change is checked in catches mistakes early.</span></span> <span data-ttu-id="69d32-298">在持續整合組建的出現之前，從存放庫提取程式碼，並找出它未通過測試或甚至無法建立的情況並不常見。</span><span class="sxs-lookup"><span data-stu-id="69d32-298">Prior to the advent of continuous integration builds, it wouldn't be uncommon to pull code from the repository and find that it didn't pass tests or couldn't even be built.</span></span> <span data-ttu-id="69d32-299">這會導致大量追蹤破壞的來源。</span><span class="sxs-lookup"><span data-stu-id="69d32-299">This resulted in a lot of tracking down the source of the breakage.</span></span>

<span data-ttu-id="69d32-300">傳統上，將軟體運送到生產環境需要大量的檔和步驟清單。</span><span class="sxs-lookup"><span data-stu-id="69d32-300">Traditionally shipping software to the production environment required extensive documentation and a list of steps.</span></span> <span data-ttu-id="69d32-301">這些步驟中的每一個都需要以手動方式在容易出錯的程式中完成。</span><span class="sxs-lookup"><span data-stu-id="69d32-301">Each one of these steps needed to be manually completed in a very error prone process.</span></span>

![圖11-7 檢查清單](./media/checklist.png)

<span data-ttu-id="69d32-303">持續整合的備用是持續傳遞，其中全新建立的封裝會部署至環境。</span><span class="sxs-lookup"><span data-stu-id="69d32-303">The sister of continuous integration is continuous delivery in which the freshly built packages are deployed to an environment.</span></span> <span data-ttu-id="69d32-304">手動程式無法調整以符合開發的速度，因此自動化變得更重要。</span><span class="sxs-lookup"><span data-stu-id="69d32-304">The manual process can't scale to match the speed of development so automation becomes more important.</span></span> <span data-ttu-id="69d32-305">檢查清單是由腳本所取代，可以更快速且更精確地執行相同的工作。</span><span class="sxs-lookup"><span data-stu-id="69d32-305">Checklists are replaced by scripts that can execute the same tasks faster and more accurately than any human.</span></span>

<span data-ttu-id="69d32-306">持續傳遞所提供的環境可能是測試環境，或由許多主要技術公司所完成，可能是生產環境。</span><span class="sxs-lookup"><span data-stu-id="69d32-306">The environment to which continuous delivery delivers might be a test environment or, as is being done by many major technology companies, it could be the production environment.</span></span> <span data-ttu-id="69d32-307">後者需要投資高品質的測試，讓您確信變更不會中斷使用者的生產環境。</span><span class="sxs-lookup"><span data-stu-id="69d32-307">The latter requires an investment in high-quality tests that can give confidence that a change isn't going to break production for users.</span></span> <span data-ttu-id="69d32-308">與持續整合程式碼早期持續傳遞中的問題相同，會及早攔截部署程式中的問題。</span><span class="sxs-lookup"><span data-stu-id="69d32-308">In the same way that continuous integration caught issues in the code early continuous delivery catches issues in the deployment process early.</span></span>

<span data-ttu-id="69d32-309">將組建和傳遞程式自動化的重要性，是由雲端原生應用程式所 accentuated。</span><span class="sxs-lookup"><span data-stu-id="69d32-309">The importance of automating the build and delivery process is accentuated by cloud-native applications.</span></span> <span data-ttu-id="69d32-310">部署會更頻繁且更多環境，因此在不可能的情況中手動部署框線。</span><span class="sxs-lookup"><span data-stu-id="69d32-310">Deployments happen more frequently and to more environments so manually deploying borders on impossible.</span></span>

### <a name="azure-builds"></a><span data-ttu-id="69d32-311">Azure 組建</span><span class="sxs-lookup"><span data-stu-id="69d32-311">Azure Builds</span></span>

<span data-ttu-id="69d32-312">Azure DevOps 提供一組工具，讓持續整合和部署比以往更容易。</span><span class="sxs-lookup"><span data-stu-id="69d32-312">Azure DevOps provides a set of tools to make continuous integration and deployment easier than ever.</span></span> <span data-ttu-id="69d32-313">這些工具位於 Azure Pipelines 之下。</span><span class="sxs-lookup"><span data-stu-id="69d32-313">These tools are located under Azure Pipelines.</span></span> <span data-ttu-id="69d32-314">第一個是 Azure 組建，這是一種可大規模執行以 YAML 為基礎之組建定義的工具。</span><span class="sxs-lookup"><span data-stu-id="69d32-314">The first of them is Azure Builds, which is a tool for running YAML-based build definitions at scale.</span></span> <span data-ttu-id="69d32-315">使用者可以攜帶自己的組建電腦（適用于組建需要精心的環境），或使用來自 Azure 託管虛擬機器之經常重新整理集區的機器。</span><span class="sxs-lookup"><span data-stu-id="69d32-315">Users can either bring their own build machines (great for if the build requires a meticulously set up environment) or use a machine from a constantly refreshed pool of Azure hosted virtual machines.</span></span> <span data-ttu-id="69d32-316">這些裝載的組建代理程式已預先安裝了各種不同的開發工具，不僅適用于 .NET 開發，還可用於從 JAVA 到 Python 到 iPhone 開發的所有專案。</span><span class="sxs-lookup"><span data-stu-id="69d32-316">These hosted build agents come pre-installed with a wide range of development tools for not just .NET development but for everything from Java to Python to iPhone development.</span></span>

<span data-ttu-id="69d32-317">DevOps 包含各種現成的組建定義，可以針對任何組建進行自訂。</span><span class="sxs-lookup"><span data-stu-id="69d32-317">DevOps includes a wide range of out of the box build definitions that can be customized for any build.</span></span> <span data-ttu-id="69d32-318">組建定義是在名`azure-pipelines.yml`為的檔案中定義，並簽入到存放庫，因此可以與原始程式碼一起建立版本。</span><span class="sxs-lookup"><span data-stu-id="69d32-318">The build definitions are defined in a file called `azure-pipelines.yml` and checked into the repository so they can be versioned along with the source code.</span></span> <span data-ttu-id="69d32-319">這可讓您更輕鬆地對分支中的組建管線進行變更，因為可以將變更簽入到該分支中。</span><span class="sxs-lookup"><span data-stu-id="69d32-319">This makes it much easier to make changes to the build pipeline in a branch as the changes can be checked into just that branch.</span></span> <span data-ttu-id="69d32-320">如圖`azure-pipelines.yml` 11-8 所示，在完整架構上建立 ASP.NET web 應用程式的範例。</span><span class="sxs-lookup"><span data-stu-id="69d32-320">An example `azure-pipelines.yml` for building an ASP.NET web application on full framework is show in Figure 11-8.</span></span>

```yml
name: $(rev:r)

variables:
  version: 9.2.0.$(Build.BuildNumber)
  solution: Portals.sln
  artifactName: drop
  buildPlatform: any cpu
  buildConfiguration: release
  
pool:
  name: Hosted VS2017
  demands:
  - msbuild
  - visualstudio
  - vstest

steps:
- task: NuGetToolInstaller@0
  displayName: 'Use NuGet 4.4.1'
  inputs:
    versionSpec: 4.4.1

- task: NuGetCommand@2
  displayName: 'NuGet restore'
  inputs:
    restoreSolution: '$(solution)'
    
- task: VSBuild@1
  displayName: 'Build solution'
  inputs:
    solution: '$(solution)'
    msbuildArgs: '/p:DeployOnBuild=true /p:WebPublishMethod=Package /p:PackageAsSingleFile=true /p:SkipInvalidConfigurations=true /p:PackageLocation="$(build.artifactstagingdirectory)\\"'
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'

- task: VSTest@2
  displayName: 'Test Assemblies'
  inputs:
    testAssemblyVer2: |
     **\$(buildConfiguration)\**\*test*.dll
     !**\obj\**
     !**\*testadapter.dll
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'

- task: CopyFiles@2
  displayName: 'Copy UI Test Files to: $(build.artifactstagingdirectory)'
  inputs:
    SourceFolder: UITests
    TargetFolder: '$(build.artifactstagingdirectory)/uitests'

- task: PublishBuildArtifacts@1
  displayName: 'Publish Artifact'
  inputs:
    PathtoPublish: '$(build.artifactstagingdirectory)'
    ArtifactName: '$(artifactName)'
  condition: succeededOrFailed()
```

<span data-ttu-id="69d32-321">**圖 11-8** -範例 azure-pipelines。 yml</span><span class="sxs-lookup"><span data-stu-id="69d32-321">**Figure 11-8** - A sample azure-pipelines.yml</span></span>

<span data-ttu-id="69d32-322">此組建定義會使用一些內建工作，讓建立組建的程式變得簡單，就像建立一組（比巨型 Millennium Falcon 更簡單）。</span><span class="sxs-lookup"><span data-stu-id="69d32-322">This build definition uses a number of built-in tasks that make creating builds as simple as building a Lego set (simpler than the giant Millennium Falcon).</span></span> <span data-ttu-id="69d32-323">例如，NuGet 工作會還原 NuGet 套件，而 VSBuild 工作會呼叫 Visual Studio 組建工具來執行實際的編譯。</span><span class="sxs-lookup"><span data-stu-id="69d32-323">For instance, the NuGet task restores NuGet packages, while the VSBuild task calls the Visual Studio build tools to perform the actual compilation.</span></span> <span data-ttu-id="69d32-324">Azure DevOps 中有數百種不同的工作可供使用，其中有數以千計的社區維護。</span><span class="sxs-lookup"><span data-stu-id="69d32-324">There are hundreds of different tasks available in Azure DevOps, with thousands more that are maintained by the community.</span></span> <span data-ttu-id="69d32-325">無論您想要執行的組建工作為何，都有可能是有人已經建立的工作。</span><span class="sxs-lookup"><span data-stu-id="69d32-325">It's likely that no matter what build tasks you're looking to run, somebody has built one already.</span></span>

<span data-ttu-id="69d32-326">組建可以透過簽入、排程或完成另一個組建來手動觸發。</span><span class="sxs-lookup"><span data-stu-id="69d32-326">Builds can be triggered manually, by a check-in, on a schedule, or by the completion of another build.</span></span> <span data-ttu-id="69d32-327">在大多數情況下，都需要在每個簽入上建立。</span><span class="sxs-lookup"><span data-stu-id="69d32-327">In most cases, building on every check-in is desirable.</span></span> <span data-ttu-id="69d32-328">您可以篩選組建，讓不同的組建針對存放庫的不同部分或針對不同的分支執行。</span><span class="sxs-lookup"><span data-stu-id="69d32-328">Builds can be filtered so that different builds run against different parts of the repository or against different branches.</span></span> <span data-ttu-id="69d32-329">這可讓您在執行快速組建的案例中，減少對提取要求的測試，並在夜間針對主幹執行完整的回歸套件。</span><span class="sxs-lookup"><span data-stu-id="69d32-329">This allows for scenarios like running fast builds with reduced testing on pull requests and running a full regression suite against the trunk on a nightly basis.</span></span>

<span data-ttu-id="69d32-330">組建的最後結果是稱為「組建成品」的檔案集合。</span><span class="sxs-lookup"><span data-stu-id="69d32-330">The end result of a build is a collection of files known as build artifacts.</span></span> <span data-ttu-id="69d32-331">這些成品可以傳遞至組建程式中的下一個步驟，或新增至 Azure 成品摘要，讓其他組建可以使用這些成品。</span><span class="sxs-lookup"><span data-stu-id="69d32-331">These artifacts can be passed along to the next step in the build process or added to an Azure Artifact feed, so they can be consumed by other builds.</span></span>

### <a name="azure-devops-releases"></a><span data-ttu-id="69d32-332">Azure DevOps 版本</span><span class="sxs-lookup"><span data-stu-id="69d32-332">Azure DevOps releases</span></span>

<span data-ttu-id="69d32-333">組建會負責將軟體編譯成可交付的套件，但仍需將成品推送至測試環境，才能完成持續傳遞。</span><span class="sxs-lookup"><span data-stu-id="69d32-333">Builds take care of compiling the software into a shippable package, but the artifacts still need to be pushed out to a testing environment to complete continuous delivery.</span></span> <span data-ttu-id="69d32-334">為此，Azure DevOps 使用稱為「發行」的個別工具。</span><span class="sxs-lookup"><span data-stu-id="69d32-334">For this, Azure DevOps uses a separate tool called Releases.</span></span> <span data-ttu-id="69d32-335">發行會使用與組建相同的工作程式庫，但會引進「階段」的概念。</span><span class="sxs-lookup"><span data-stu-id="69d32-335">Releases make use of the same tasks' library that were available to the Build but introduce a concept of "stages".</span></span> <span data-ttu-id="69d32-336">階段是安裝套件的隔離環境。</span><span class="sxs-lookup"><span data-stu-id="69d32-336">A stage is an isolated environment into which the package is installed.</span></span> <span data-ttu-id="69d32-337">例如，產品可能會使用開發、QA 和生產環境。</span><span class="sxs-lookup"><span data-stu-id="69d32-337">For instance, a product might make use of a development, a QA, and a production environment.</span></span> <span data-ttu-id="69d32-338">程式碼會持續傳遞至開發環境，讓您可以對其執行自動化測試。</span><span class="sxs-lookup"><span data-stu-id="69d32-338">Code is continuously delivered into the development environment where automated tests can be run against it.</span></span> <span data-ttu-id="69d32-339">一旦這些測試通過，發行就會移至 QA 環境進行手動測試。</span><span class="sxs-lookup"><span data-stu-id="69d32-339">Once those tests pass the release moves onto the QA environment for manual testing.</span></span> <span data-ttu-id="69d32-340">最後，程式碼會推送至生產環境，讓每個人都可以看到它。</span><span class="sxs-lookup"><span data-stu-id="69d32-340">Finally, the code is pushed to production where it's visible to everybody.</span></span>

![圖11-9 具有開發、QA 和生產階段的範例發行管線](./media/release-pipeline.png)

<span data-ttu-id="69d32-342">在上一個階段完成後，組建中的每個階段都可以自動觸發。</span><span class="sxs-lookup"><span data-stu-id="69d32-342">Each stage in the build can be automatically triggered by the completion of the previous phase.</span></span> <span data-ttu-id="69d32-343">不過，在許多情況下，這並不是理想的做法。</span><span class="sxs-lookup"><span data-stu-id="69d32-343">In many cases, however, this isn't desirable.</span></span> <span data-ttu-id="69d32-344">將程式碼移至生產環境可能需要有人核准。</span><span class="sxs-lookup"><span data-stu-id="69d32-344">Moving code into production might require approval from somebody.</span></span> <span data-ttu-id="69d32-345">版本藉由允許發行管線的每個步驟中的核准者，來支援這項功能。</span><span class="sxs-lookup"><span data-stu-id="69d32-345">Releases supports this by allowing approvers at each step of the release pipeline.</span></span> <span data-ttu-id="69d32-346">您可以設定規則，讓特定人員或人員群組必須在發行進入生產環境之前先登出。</span><span class="sxs-lookup"><span data-stu-id="69d32-346">Rules can be set up such that a specific person or group of people must sign off on a release before it makes into production.</span></span> <span data-ttu-id="69d32-347">這些閘道允許手動執行品質檢查，同時也會遵循與控制要進入生產環境的相關法規需求。</span><span class="sxs-lookup"><span data-stu-id="69d32-347">These gates allow for manual quality checks and also for compliance with any regulatory requirements related to control what goes into production.</span></span>

### <a name="everybody-gets-a-build-pipeline"></a><span data-ttu-id="69d32-348">每個人都會取得組建管線</span><span class="sxs-lookup"><span data-stu-id="69d32-348">Everybody gets a build pipeline</span></span>

<span data-ttu-id="69d32-349">設定許多組建管線並不會產生任何費用，因此，每個微服務至少有一個組建管線是有利的。</span><span class="sxs-lookup"><span data-stu-id="69d32-349">There's no cost to configuring many build pipelines, so it's advantageous to have at least one build pipeline per microservice.</span></span> <span data-ttu-id="69d32-350">在理想的情況下，微服務可獨立部署到任何環境，讓每一個都能透過自己的管線發行，而不需要釋出不相關的程式碼。</span><span class="sxs-lookup"><span data-stu-id="69d32-350">Ideally, microservices are independently deployable to any environment so having each one able to be released via its own pipeline without releasing a mass of unrelated code is perfect.</span></span> <span data-ttu-id="69d32-351">每個管線可以有自己的一組核准，以允許每個服務的組建程式變體。</span><span class="sxs-lookup"><span data-stu-id="69d32-351">Each pipeline can have its own set of approvals allowing for variations in build process for each service.</span></span>

### <a name="versioning-releases"></a><span data-ttu-id="69d32-352">版本控制版本</span><span class="sxs-lookup"><span data-stu-id="69d32-352">Versioning releases</span></span>

<span data-ttu-id="69d32-353">使用發行功能的一個缺點是，它無法在已簽入`azure-pipelines.yml`的檔案中定義。</span><span class="sxs-lookup"><span data-stu-id="69d32-353">One drawback to using the Releases functionality is that it can't be defined in a checked-in `azure-pipelines.yml` file.</span></span> <span data-ttu-id="69d32-354">有許多原因您可能想要從每個分支版本定義中，將發行基本架構包含在專案範本中。</span><span class="sxs-lookup"><span data-stu-id="69d32-354">There are many reasons you might want to do that from having per-branch release definitions to including a release skeleton in your project template.</span></span> <span data-ttu-id="69d32-355">幸好，工作正在將一些階段支援轉移到組建元件中。</span><span class="sxs-lookup"><span data-stu-id="69d32-355">Fortunately, work is ongoing to shift some of the stages support into the Build component.</span></span> <span data-ttu-id="69d32-356">這就是所謂的多階段組建，而[第一個版本現在已可供使用](https://devblogs.microsoft.com/devops/whats-new-with-azure-pipelines/)！</span><span class="sxs-lookup"><span data-stu-id="69d32-356">This will be known as multi-stage build and the [first version is available now](https://devblogs.microsoft.com/devops/whats-new-with-azure-pipelines/)!</span></span>

>[!div class="step-by-step"]
><span data-ttu-id="69d32-357">[上一頁](azure-security.md)
>[下一頁](infrastructure-as-code.md)</span><span class="sxs-lookup"><span data-stu-id="69d32-357">[Previous](azure-security.md)
[Next](infrastructure-as-code.md)</span></span>
