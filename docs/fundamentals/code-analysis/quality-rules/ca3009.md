---
title: CA3009： (程式碼分析) 的 XML 插入式弱點審查程式碼
description: 瞭解程式碼分析規則 CA3009：檢查 XML 插入式弱點的程式碼
ms.date: 07/21/2020
ms.topic: reference
author: dotpaul
ms.author: paulming
dev_langs:
- CSharp
- VB
ms.openlocfilehash: f0e1fb6558d2898ec8a90079afb84f46c8f57a29
ms.sourcegitcommit: e301979e3049ce412d19b094c60ed95b316a8f8c
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 12/16/2020
ms.locfileid: "97595316"
---
# <a name="ca3009-review-code-for-xml-injection-vulnerabilities"></a><span data-ttu-id="d6f67-103">CA3009：檢閱程式碼是否有 XML 插入式攻擊弱點</span><span class="sxs-lookup"><span data-stu-id="d6f67-103">CA3009: Review code for XML injection vulnerabilities</span></span>

| | <span data-ttu-id="d6f67-104">值</span><span class="sxs-lookup"><span data-stu-id="d6f67-104">Value</span></span> |
|-|-|
| <span data-ttu-id="d6f67-105">**規則識別碼**</span><span class="sxs-lookup"><span data-stu-id="d6f67-105">**Rule ID**</span></span> |<span data-ttu-id="d6f67-106">CA3009</span><span class="sxs-lookup"><span data-stu-id="d6f67-106">CA3009</span></span>|
| <span data-ttu-id="d6f67-107">**類別**</span><span class="sxs-lookup"><span data-stu-id="d6f67-107">**Category**</span></span> |<span data-ttu-id="d6f67-108">Microsoft.Security</span><span class="sxs-lookup"><span data-stu-id="d6f67-108">Microsoft.Security</span></span>|
| <span data-ttu-id="d6f67-109">**修正程式中斷或未中斷**</span><span class="sxs-lookup"><span data-stu-id="d6f67-109">**Fix is breaking or non-breaking**</span></span> |<span data-ttu-id="d6f67-110">非中斷</span><span class="sxs-lookup"><span data-stu-id="d6f67-110">Non-breaking</span></span>|

## <a name="cause"></a><span data-ttu-id="d6f67-111">原因</span><span class="sxs-lookup"><span data-stu-id="d6f67-111">Cause</span></span>

<span data-ttu-id="d6f67-112">可能不受信任的 HTTP 要求輸入會達到原始 XML 輸出。</span><span class="sxs-lookup"><span data-stu-id="d6f67-112">Potentially untrusted HTTP request input reaches raw XML output.</span></span>

<span data-ttu-id="d6f67-113">根據預設，此規則會分析整個程式碼基底，但這是 [可](#configure-code-to-analyze)設定的。</span><span class="sxs-lookup"><span data-stu-id="d6f67-113">By default, this rule analyzes the entire codebase, but this is [configurable](#configure-code-to-analyze).</span></span>

## <a name="rule-description"></a><span data-ttu-id="d6f67-114">規則描述</span><span class="sxs-lookup"><span data-stu-id="d6f67-114">Rule description</span></span>

<span data-ttu-id="d6f67-115">使用不受信任的輸入時，請注意 XML 插入式攻擊。</span><span class="sxs-lookup"><span data-stu-id="d6f67-115">When working with untrusted input, be mindful of XML injection attacks.</span></span> <span data-ttu-id="d6f67-116">攻擊者可以使用 XML 插入，將特殊字元插入 XML 檔中，使檔不正確 XML。</span><span class="sxs-lookup"><span data-stu-id="d6f67-116">An attacker can use XML injection to insert special characters into an XML document, making the document invalid XML.</span></span> <span data-ttu-id="d6f67-117">或者，攻擊者可能會惡意地插入其選擇的 XML 節點。</span><span class="sxs-lookup"><span data-stu-id="d6f67-117">Or, an attacker could maliciously insert XML nodes of their choosing.</span></span>

<span data-ttu-id="d6f67-118">此規則會嘗試從 HTTP 要求中找出到達原始 XML 寫入的輸入。</span><span class="sxs-lookup"><span data-stu-id="d6f67-118">This rule attempts to find input from HTTP requests reaching a raw XML write.</span></span>

> [!NOTE]
> <span data-ttu-id="d6f67-119">此規則無法追蹤元件之間的資料。</span><span class="sxs-lookup"><span data-stu-id="d6f67-119">This rule can't track data across assemblies.</span></span> <span data-ttu-id="d6f67-120">例如，如果某個元件讀取 HTTP 要求輸入，然後將它傳遞給另一個寫入原始 XML 的元件，則此規則不會產生警告。</span><span class="sxs-lookup"><span data-stu-id="d6f67-120">For example, if one assembly reads the HTTP request input and then passes it to another assembly that writes raw XML, this rule won't produce a warning.</span></span>

> [!NOTE]
> <span data-ttu-id="d6f67-121">這項規則會在方法呼叫之間分析資料流的深度，提供可設定的限制。</span><span class="sxs-lookup"><span data-stu-id="d6f67-121">There is a configurable limit to how deep this rule will analyze data flow across method calls.</span></span> <span data-ttu-id="d6f67-122">請參閱 [分析器](https://github.com/dotnet/roslyn-analyzers/blob/master/docs/Analyzer%20Configuration.md#dataflow-analysis) 設定，以瞭解如何在 EditorConfig 檔中設定限制。</span><span class="sxs-lookup"><span data-stu-id="d6f67-122">See [Analyzer Configuration](https://github.com/dotnet/roslyn-analyzers/blob/master/docs/Analyzer%20Configuration.md#dataflow-analysis) for how to configure the limit in an EditorConfig file.</span></span>

## <a name="how-to-fix-violations"></a><span data-ttu-id="d6f67-123">如何修正違規</span><span class="sxs-lookup"><span data-stu-id="d6f67-123">How to fix violations</span></span>

<span data-ttu-id="d6f67-124">請勿寫入原始 XML。</span><span class="sxs-lookup"><span data-stu-id="d6f67-124">Don't write raw XML.</span></span> <span data-ttu-id="d6f67-125">相反地，請使用對其輸入進行 XML 編碼的方法或屬性。</span><span class="sxs-lookup"><span data-stu-id="d6f67-125">Instead, use methods or properties that XML-encode their input.</span></span>

<span data-ttu-id="d6f67-126">或者，在寫入原始 XML 之前，XML 編碼輸入。</span><span class="sxs-lookup"><span data-stu-id="d6f67-126">Or, XML-encode input before writing raw XML.</span></span>

<span data-ttu-id="d6f67-127">或者，使用 sanitizers 來驗證使用者輸入，以進行基本類型轉換和 XML 編碼。</span><span class="sxs-lookup"><span data-stu-id="d6f67-127">Or, validate user input by using sanitizers for primitive type conversion and XML encoding.</span></span>

## <a name="when-to-suppress-warnings"></a><span data-ttu-id="d6f67-128">隱藏警告的時機</span><span class="sxs-lookup"><span data-stu-id="d6f67-128">When to suppress warnings</span></span>

<span data-ttu-id="d6f67-129">請勿隱藏此規則的警告。</span><span class="sxs-lookup"><span data-stu-id="d6f67-129">Don't suppress warnings from this rule.</span></span>

## <a name="configure-code-to-analyze"></a><span data-ttu-id="d6f67-130">設定要分析的程式碼</span><span class="sxs-lookup"><span data-stu-id="d6f67-130">Configure code to analyze</span></span>

<span data-ttu-id="d6f67-131">使用下列選項來設定您的程式碼基底要執行此規則的部分。</span><span class="sxs-lookup"><span data-stu-id="d6f67-131">Use the following options to configure which parts of your codebase to run this rule on.</span></span>

- [<span data-ttu-id="d6f67-132">排除特定符號</span><span class="sxs-lookup"><span data-stu-id="d6f67-132">Exclude specific symbols</span></span>](#exclude-specific-symbols)
- [<span data-ttu-id="d6f67-133">排除特定類型及其衍生類型</span><span class="sxs-lookup"><span data-stu-id="d6f67-133">Exclude specific types and their derived types</span></span>](#exclude-specific-types-and-their-derived-types)

<span data-ttu-id="d6f67-134">您可以只針對此規則、所有規則或此類別中的所有規則設定這些選項， (安全性) 。</span><span class="sxs-lookup"><span data-stu-id="d6f67-134">You can configure these options for just this rule, for all rules, or for all rules in this category (Security).</span></span> <span data-ttu-id="d6f67-135">如需詳細資訊，請參閱程式 [代碼品質規則配置選項](../code-quality-rule-options.md)。</span><span class="sxs-lookup"><span data-stu-id="d6f67-135">For more information, see [Code quality rule configuration options](../code-quality-rule-options.md).</span></span>

[!INCLUDE[excluded-symbol-names](~/includes/code-analysis/excluded-symbol-names.md)]

[!INCLUDE[excluded-type-names-with-derived-types](~/includes/code-analysis/excluded-type-names-with-derived-types.md)]

## <a name="pseudo-code-examples"></a><span data-ttu-id="d6f67-136">虛擬程式碼範例</span><span class="sxs-lookup"><span data-stu-id="d6f67-136">Pseudo-code examples</span></span>

### <a name="violation"></a><span data-ttu-id="d6f67-137">違規</span><span class="sxs-lookup"><span data-stu-id="d6f67-137">Violation</span></span>

```csharp
using System;
using System.Xml;

public partial class WebForm : System.Web.UI.Page
{
    protected void Page_Load(object sender, EventArgs e)
    {
        string input = Request.Form["in"];
        XmlDocument d = new XmlDocument();
        XmlElement root = d.CreateElement("root");
        d.AppendChild(root);

        XmlElement allowedUser = d.CreateElement("allowedUser");
        root.AppendChild(allowedUser);

        allowedUser.InnerXml = "alice";

        // If an attacker uses this for input:
        //     some text<allowedUser>oscar</allowedUser>
        // Then the XML document will be:
        //     <root>some text<allowedUser>oscar</allowedUser></root>
        root.InnerXml = input;
    }
}
```

```vb
Imports System
Imports System.Xml

Public Partial Class WebForm
    Inherits System.Web.UI.Page

    Sub Page_Load(sender As Object, e As EventArgs)
        Dim input As String = Request.Form("in")
        Dim d As XmlDocument = New XmlDocument()
        Dim root As XmlElement = d.CreateElement("root")
        d.AppendChild(root)

        Dim allowedUser As XmlElement = d.CreateElement("allowedUser")
        root.AppendChild(allowedUser)

        allowedUser.InnerXml = "alice"

        ' If an attacker uses this for input:
        '     some text<allowedUser>oscar</allowedUser>
        ' Then the XML document will be:
        '     <root>some text<allowedUser>oscar</allowedUser></root>
        root.InnerXml = input
    End Sub
End Class
```

### <a name="solution"></a><span data-ttu-id="d6f67-138">解決方案</span><span class="sxs-lookup"><span data-stu-id="d6f67-138">Solution</span></span>

```csharp
using System;
using System.Xml;

public partial class WebForm : System.Web.UI.Page
{
    protected void Page_Load(object sender, EventArgs e)
    {
        string input = Request.Form["in"];
        XmlDocument d = new XmlDocument();
        XmlElement root = d.CreateElement("root");
        d.AppendChild(root);

        XmlElement allowedUser = d.CreateElement("allowedUser");
        root.AppendChild(allowedUser);

        allowedUser.InnerText = "alice";

        // If an attacker uses this for input:
        //     some text<allowedUser>oscar</allowedUser>
        // Then the XML document will be:
        //     <root>&lt;allowedUser&gt;oscar&lt;/allowedUser&gt;some text<allowedUser>alice</allowedUser></root>
        root.InnerText = input;
    }
}
```

```vb
Imports System
Imports System.Xml

Public Partial Class WebForm
    Inherits System.Web.UI.Page

    Sub Page_Load(sender As Object, e As EventArgs)
        Dim input As String = Request.Form("in")
        Dim d As XmlDocument = New XmlDocument()
        Dim root As XmlElement = d.CreateElement("root")
        d.AppendChild(root)

        Dim allowedUser As XmlElement = d.CreateElement("allowedUser")
        root.AppendChild(allowedUser)

        allowedUser.InnerText = "alice"

        ' If an attacker uses this for input:
        '     some text<allowedUser>oscar</allowedUser>
        ' Then the XML document will be:
        '     <root>&lt;allowedUser&gt;oscar&lt;/allowedUser&gt;some text<allowedUser>alice</allowedUser></root>
        root.InnerText = input
    End Sub
End Class
```
