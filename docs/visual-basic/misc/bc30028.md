---
title: '&#39;#Else&#39;之前必須搭配相對應的&#39;#If&#39;或&#39;#ElseIf&#39;'
ms.date: 07/20/2015
f1_keywords:
- vbc30028
- bc30028
helpviewer_keywords:
- BC30028
ms.assetid: c6ed34de-d6ed-4227-9880-538055aff20a
ms.openlocfilehash: 62e454a9a480008e96ff1feb07c3654d43d09320
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 05/04/2018
ms.locfileid: "33603791"
---
# <a name="39else39-must-be-preceded-by-a-matching-39if39-or-39elseif39"></a><span data-ttu-id="a7a7f-102">&#39;#Else&#39;之前必須搭配相對應的&#39;#If&#39;或&#39;#ElseIf&#39;</span><span class="sxs-lookup"><span data-stu-id="a7a7f-102">&#39;#Else&#39; must be preceded by a matching &#39;#If&#39; or &#39;#ElseIf&#39;</span></span>
<span data-ttu-id="a7a7f-103">`#Else` 是條件式編譯指示詞。</span><span class="sxs-lookup"><span data-stu-id="a7a7f-103">`#Else` is a conditional compilation directive.</span></span> <span data-ttu-id="a7a7f-104">`#Else` 指示詞的前面沒有對應的 `#If` 或 `#ElseIf` 指示詞。</span><span class="sxs-lookup"><span data-stu-id="a7a7f-104">An `#Else` directive is not preceded by a corresponding `#If` or `#ElseIf` directive.</span></span>  
  
 <span data-ttu-id="a7a7f-105">**錯誤 ID︰** BC30028</span><span class="sxs-lookup"><span data-stu-id="a7a7f-105">**Error ID:** BC30028</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="a7a7f-106">更正這個錯誤</span><span class="sxs-lookup"><span data-stu-id="a7a7f-106">To correct this error</span></span>  
  
1.  <span data-ttu-id="a7a7f-107">請檢查是否未使用介入條件式編譯區塊將前面的 `#If` 或 `#ElseIf` 與這個 `#Else` 隔開，或不正確地放置 `#End If`。</span><span class="sxs-lookup"><span data-stu-id="a7a7f-107">Check that a preceding `#If` or `#ElseIf` is not separated from this `#Else` by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
2.  <span data-ttu-id="a7a7f-108">請檢查 `#Else` 之前是否有另一個 `#Else` 指示詞。</span><span class="sxs-lookup"><span data-stu-id="a7a7f-108">Check that `#Else` is preceded by another `#Else` directive.</span></span> <span data-ttu-id="a7a7f-109">如果是，請移除 `#Else` ，或將它變更為 `#ElseIf`。</span><span class="sxs-lookup"><span data-stu-id="a7a7f-109">If it is, either remove `#Else` or change it to an `#ElseIf`.</span></span>  
  
3.  <span data-ttu-id="a7a7f-110">如果一切狀況良好，請將 `#If` 指示詞加入條件式編譯區塊的開頭。</span><span class="sxs-lookup"><span data-stu-id="a7a7f-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="a7a7f-111">另請參閱</span><span class="sxs-lookup"><span data-stu-id="a7a7f-111">See Also</span></span>  
 [<span data-ttu-id="a7a7f-112">#If...Then...#Else 指示詞</span><span class="sxs-lookup"><span data-stu-id="a7a7f-112">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
