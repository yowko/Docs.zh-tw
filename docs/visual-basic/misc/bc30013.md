---
title: "'#ElseIf'、'#Else' 或 '#End If' 之前必須搭配相對應的 '#If'"
ms.date: 07/20/2015
f1_keywords:
- vbc30013
- bc30013
helpviewer_keywords:
- BC30013
ms.assetid: 8fe2d23c-8b8f-46d8-90f2-7f8857ea43bb
ms.openlocfilehash: 1b26a3b219d39bd7311f7d976c4b471e4c713403
ms.sourcegitcommit: 558d78d2a68acd4c95ef23231c8b4e4c7bac3902
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 04/09/2019
ms.locfileid: "59300290"
---
# <a name="elseif-else-or-end-if-must-be-preceded-by-a-matching-if"></a><span data-ttu-id="7770b-102">'#ElseIf'、'#Else' 或 '#End If' 之前必須搭配相對應的 '#If'</span><span class="sxs-lookup"><span data-stu-id="7770b-102">'#ElseIf', '#Else', or '#End If' must be preceded by a matching '#If'</span></span>
`#ElseIf`<span data-ttu-id="7770b-103">、 `#Else`和 `#End If` 都是條件式編譯指示詞。</span><span class="sxs-lookup"><span data-stu-id="7770b-103">, `#Else`, and `#End If` are conditional compilation directives.</span></span> <span data-ttu-id="7770b-104">`#ElseIf`、 `#Else`或 `#End If` 的前面沒有對應的 `#If` 指示詞。</span><span class="sxs-lookup"><span data-stu-id="7770b-104">The `#ElseIf`, `#Else`, or `#End If` is not preceded by a corresponding `#If` directive.</span></span>  
  
 <span data-ttu-id="7770b-105">**錯誤 ID:** BC30013</span><span class="sxs-lookup"><span data-stu-id="7770b-105">**Error ID:** BC30013</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="7770b-106">更正這個錯誤</span><span class="sxs-lookup"><span data-stu-id="7770b-106">To correct this error</span></span>  
  
1. <span data-ttu-id="7770b-107">檢查是否未使用介入條件式編譯區塊將預期的 `#If` 與問題中的子句隔開，或不正確地放置 `#End If`。</span><span class="sxs-lookup"><span data-stu-id="7770b-107">Check that the intended `#If` is not separated from the clause in question by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="7770b-108">每個 `#Else` 區塊中只能有一個 `#If` ，因此兩個連續的 `#Else` 指示詞會造成這個錯誤。</span><span class="sxs-lookup"><span data-stu-id="7770b-108">Only one `#Else` is permitted in each `#If` block, so two successive `#Else` directives cause this error.</span></span>  
  
2. <span data-ttu-id="7770b-109">檢查舊版 `#` 指示詞中未遺漏前置 `#If` 。</span><span class="sxs-lookup"><span data-stu-id="7770b-109">Check that the leading `#` is not missing from an earlier `#If` directive.</span></span>  
  
3. <span data-ttu-id="7770b-110">如果一切狀況良好，請將 `#If` 指示詞加入條件式編譯區塊的開頭。</span><span class="sxs-lookup"><span data-stu-id="7770b-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="7770b-111">另請參閱</span><span class="sxs-lookup"><span data-stu-id="7770b-111">See also</span></span>

- [<span data-ttu-id="7770b-112">#If...Then...#Else 指示詞</span><span class="sxs-lookup"><span data-stu-id="7770b-112">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
