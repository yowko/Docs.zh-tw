---
title: 這個第一個陳述式&#39;Sub New&#39;必須明確呼叫&#39;MyBase.New&#39;或&#39;MyClass.New&#39;因為&#39; &lt;constructorname&gt; &#39;基底類別中&#39;&lt;基&gt;&#39;的&#39;&lt;衍生類別名稱&gt;&#39;標記為過時。
ms.date: 07/20/2015
f1_keywords:
- vbc30919
- bc30919
helpviewer_keywords:
- BC30919
ms.assetid: 437e3204-8ddc-45d3-b9b4-c66d53a61a6d
ms.openlocfilehash: 24d861ef57983d801d11094d784c01d326f56de8
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 05/04/2018
---
# <a name="first-statement-of-this-39sub-new39-must-be-an-explicit-call-to-39mybasenew39-or-39myclassnew39-because-the-39ltconstructornamegt39-in-the-base-class-39ltbaseclassnamegt39-of-39ltderivedclassnamegt39-is-marked-obsolete"></a><span data-ttu-id="e1e4d-102">這個第一個陳述式&#39;Sub New&#39;必須明確呼叫&#39;MyBase.New&#39;或&#39;MyClass.New&#39;因為&#39; &lt;constructorname&gt; &#39;基底類別中&#39;&lt;基&gt;&#39;的&#39;&lt;衍生類別名稱&gt;&#39;標記為過時。</span><span class="sxs-lookup"><span data-stu-id="e1e4d-102">First statement of this &#39;Sub New&#39; must be an explicit call to &#39;MyBase.New&#39; or &#39;MyClass.New&#39; because the &#39;&lt;constructorname&gt;&#39; in the base class &#39;&lt;baseclassname&gt;&#39; of &#39;&lt;derivedclassname&gt;&#39; is marked obsolete.</span></span>
<span data-ttu-id="e1e4d-103">類別建構函式未明確地呼叫基底類別建構函式，而且隱含的基底類別建構函式已使用 <xref:System.ObsoleteAttribute> 屬性和指示詞標記，以將其視為錯誤。</span><span class="sxs-lookup"><span data-stu-id="e1e4d-103">A class constructor does not explicitly call a base class constructor, and the implicit base class constructor is marked with the <xref:System.ObsoleteAttribute> attribute and the directive to treat it as an error.</span></span>  
  
 <span data-ttu-id="e1e4d-104">當在衍生的類別建構函式未呼叫基底類別建構函式時，Visual Basic 會嘗試產生對無參數基底類別建構函式的隱含呼叫。</span><span class="sxs-lookup"><span data-stu-id="e1e4d-104">When a derived class constructor does not call a base class constructor, Visual Basic attempts to generate an implicit call to a parameterless base class constructor.</span></span> <span data-ttu-id="e1e4d-105">如果可以不使用引數呼叫的基底類別中沒有任何可存取的建構函式，Visual Basic 無法產生隱含的呼叫。</span><span class="sxs-lookup"><span data-stu-id="e1e4d-105">If there is no accessible constructor in the base class that can be called without arguments, Visual Basic cannot generate an implicit call.</span></span> <span data-ttu-id="e1e4d-106">在此情況下，必要的建構函式會標示<xref:System.ObsoleteAttribute>屬性，讓 Visual Basic 無法呼叫它。</span><span class="sxs-lookup"><span data-stu-id="e1e4d-106">In this case, the required constructor is marked with the <xref:System.ObsoleteAttribute> attribute, so Visual Basic cannot call it.</span></span>  
  
 <span data-ttu-id="e1e4d-107">您可以將任何程式設計項目標記為不再使用，方法是對其套用 <xref:System.ObsoleteAttribute> 。</span><span class="sxs-lookup"><span data-stu-id="e1e4d-107">You can mark any programming element as being no longer in use by applying <xref:System.ObsoleteAttribute> to it.</span></span> <span data-ttu-id="e1e4d-108">如果您這麼做，則可以將屬性 (attribute) 的 <xref:System.ObsoleteAttribute.IsError%2A> 屬性 (property) 設定為 `True` 或 `False`。</span><span class="sxs-lookup"><span data-stu-id="e1e4d-108">If you do this, you can set the attribute's <xref:System.ObsoleteAttribute.IsError%2A> property to either `True` or `False`.</span></span> <span data-ttu-id="e1e4d-109">如果您將它設定為 `True`，則編譯器會將使用這個項目的嘗試視為錯誤。</span><span class="sxs-lookup"><span data-stu-id="e1e4d-109">If you set it to `True`, the compiler treats an attempt to use the element as an error.</span></span> <span data-ttu-id="e1e4d-110">如果您將它設定為 `False`，或讓它預設為 `False`，則在嘗試使用該項目時，編譯器會發出警告。</span><span class="sxs-lookup"><span data-stu-id="e1e4d-110">If you set it to `False`, or let it default to `False`, the compiler issues a warning if there is an attempt to use the element.</span></span>  
  
 <span data-ttu-id="e1e4d-111">**錯誤 ID︰** BC30919</span><span class="sxs-lookup"><span data-stu-id="e1e4d-111">**Error ID:** BC30919</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="e1e4d-112">更正這個錯誤</span><span class="sxs-lookup"><span data-stu-id="e1e4d-112">To correct this error</span></span>  
  
-   <span data-ttu-id="e1e4d-113">包含 `MyBase.New()` 或 `MyClass.New()` 的呼叫作為衍生類別中 `Sub New` 的第一個陳述式。</span><span class="sxs-lookup"><span data-stu-id="e1e4d-113">Include a call to `MyBase.New()` or `MyClass.New()` as the first statement of the `Sub New` in the derived class.</span></span>