---
title: 疑難排解互通性 (Visual Basic)
ms.date: 07/20/2015
helpviewer_keywords:
- interop, deploying assemblies
- assemblies [Visual Basic]
- interop, installing assemblies that share components
- COM objects, troubleshooting
- interop, sharing components
- troubleshooting interoperability [Visual Basic]
- interoperability, troubleshooting
- COM interop [Visual Basic], troubleshooting
- assemblies [Visual Basic], deploying
- troubleshooting Visual Basic, interoperability
- interop assemblies
- interoperability, sharing components
- shared components, using with assemblies
ms.assetid: b324cc1e-b03c-4f39-aea6-6a6d5bfd0e37
ms.openlocfilehash: 65005dbbe42f52b3159c8e595972dace3064f986
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 05/04/2018
ms.locfileid: "33643701"
---
# <a name="troubleshooting-interoperability-visual-basic"></a><span data-ttu-id="ce9bb-102">疑難排解互通性 (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="ce9bb-102">Troubleshooting Interoperability (Visual Basic)</span></span>
<span data-ttu-id="ce9bb-103">當您 COM 和 managed 程式碼之間的交互操作[!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)]，您可能會遇到一或多個下列常見的問題。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-103">When you interoperate between COM and the managed code of the [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)], you may encounter one or more of the following common issues.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor1"></a> <span data-ttu-id="ce9bb-104">Interop 封送處理</span><span class="sxs-lookup"><span data-stu-id="ce9bb-104">Interop Marshaling</span></span>  
 <span data-ttu-id="ce9bb-105">有時候，您可能要使用資料型別不屬於[!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)]。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-105">At times, you may have to use data types that are not part of the [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)].</span></span> <span data-ttu-id="ce9bb-106">Interop 組件處理大部分的 COM 物件的工作，但您可能要控制受管理的物件會公開至 COM 時所使用的資料型別</span><span class="sxs-lookup"><span data-stu-id="ce9bb-106">Interop assemblies handle most of the work for COM objects, but you may have to control the data types that are used when managed objects are exposed to COM.</span></span> <span data-ttu-id="ce9bb-107">例如，類別庫中的結構必須指定`BStr`unmanaged 字串傳送至 Visual Basic 6.0 及舊版所建立的 COM 物件的類型。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-107">For example, structures in class libraries must specify the `BStr` unmanaged type on strings sent to COM objects created by Visual Basic 6.0 and earlier versions.</span></span> <span data-ttu-id="ce9bb-108">在這種情況下，您可以使用<xref:System.Runtime.InteropServices.MarshalAsAttribute>造成到即將公開為 unmanaged 類型的 managed 的類型的屬性。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-108">In such cases, you can use the <xref:System.Runtime.InteropServices.MarshalAsAttribute> attribute to cause managed types to be exposed as unmanaged types.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor2"></a> <span data-ttu-id="ce9bb-109">匯出至 Unmanaged 程式碼的固定長度字串</span><span class="sxs-lookup"><span data-stu-id="ce9bb-109">Exporting Fixed-Length Strings to Unmanaged Code</span></span>  
 <span data-ttu-id="ce9bb-110">在 Visual Basic 6.0 及舊版中，字串會匯出至 COM 物件為不含結束 null 字元的位元組序列。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-110">In Visual Basic 6.0 and earlier versions, strings are exported to COM objects as sequences of bytes without a null termination character.</span></span> <span data-ttu-id="ce9bb-111">與其他語言的相容性，Visual Basic.NET 包含終止字元匯出字串時。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-111">For compatibility with other languages, Visual Basic .NET includes a termination character when exporting strings.</span></span> <span data-ttu-id="ce9bb-112">若要解決此不相容，最好是將缺少的終止字元陣列的字串匯出`Byte`或`Char`。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-112">The best way to address this incompatibility is to export strings that lack the termination character as arrays of `Byte` or `Char`.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor3"></a> <span data-ttu-id="ce9bb-113">匯出的繼承階層</span><span class="sxs-lookup"><span data-stu-id="ce9bb-113">Exporting Inheritance Hierarchies</span></span>  
 <span data-ttu-id="ce9bb-114">Managed 的類別階層壓平合併時，公開為 COM 物件時。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-114">Managed class hierarchies flatten out when exposed as COM objects.</span></span> <span data-ttu-id="ce9bb-115">例如，如果您是成員，來定義基底類別，則會繼承基底類別會公開為 COM 物件的衍生類別中使用衍生的類別中的 COM 物件的用戶端將無法使用繼承的成員。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-115">For example, if you define a base class with a member, and then inherit the base class in a derived class that is exposed as a COM object, clients that use the derived class in the COM object will not be able to use the inherited members.</span></span> <span data-ttu-id="ce9bb-116">可以從 COM 物件存取基底類別成員，只能當做基底類別的執行個體，然後才在基底類別也會建立為 COM 物件。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-116">Base class members can be accessed from COM objects only as instances of a base class, and then only if the base class is also created as a COM object.</span></span>  
  
## <a name="overloaded-methods"></a><span data-ttu-id="ce9bb-117">多載的方法</span><span class="sxs-lookup"><span data-stu-id="ce9bb-117">Overloaded Methods</span></span>  
 <span data-ttu-id="ce9bb-118">雖然您可以使用 Visual Basic 中建立多載的方法，並不支援供 com 使用。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-118">Although you can create overloaded methods with Visual Basic, they are not supported by COM.</span></span> <span data-ttu-id="ce9bb-119">包含多載的方法的類別會公開為 COM 物件，都會產生新的方法名稱的多載的方法。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-119">When a class that contains overloaded methods is exposed as a COM object, new method names are generated for the overloaded methods.</span></span>  
  
 <span data-ttu-id="ce9bb-120">例如，假設有一個類別具有兩個多載`Synch`方法。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-120">For example, consider a class that has two overloads of the `Synch` method.</span></span> <span data-ttu-id="ce9bb-121">當類別為 COM 物件公開時，可能是新產生的方法名稱`Synch`和`Synch_2`。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-121">When the class is exposed as a COM object, the new generated method names could be `Synch` and `Synch_2`.</span></span>  
  
 <span data-ttu-id="ce9bb-122">重新命名可能會造成兩個問題的 COM 物件的取用者。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-122">The renaming can cause two problems for consumers of the COM object.</span></span>  
  
1.  <span data-ttu-id="ce9bb-123">用戶端可能不會產生的方法名稱。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-123">Clients might not expect the generated method names.</span></span>  
  
2.  <span data-ttu-id="ce9bb-124">新的多載加入至類別或其基底類別時，可以變更公開為 COM 物件的類別中產生的方法名稱。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-124">The generated method names in the class exposed as a COM object can change when new overloads are added to the class or its base class.</span></span> <span data-ttu-id="ce9bb-125">這可能會造成版本控制問題。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-125">This can cause versioning problems.</span></span>  
  
 <span data-ttu-id="ce9bb-126">若要解決這兩個問題，請為每一種方法唯一的名稱，而不是使用多載中，當您開發即將公開為 COM 物件的物件。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-126">To solve both problems, give each method a unique name, instead of using overloading, when you develop objects that will be exposed as COM objects.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor4"></a> <span data-ttu-id="ce9bb-127">使用 COM 物件，透過 Interop 組件</span><span class="sxs-lookup"><span data-stu-id="ce9bb-127">Use of COM Objects Through Interop Assemblies</span></span>  
 <span data-ttu-id="ce9bb-128">您在幾乎如同它們是 managed 程式碼取代所代表的 COM 物件使用 interop 組件。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-128">You use interop assemblies almost as if they are managed code replacements for the COM objects they represent.</span></span> <span data-ttu-id="ce9bb-129">不過，因為它們是包裝函式並不是實際的 COM 物件時，會使用 interop 組件和標準的組件之間的一些差異。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-129">However, because they are wrappers and not actual COM objects, there are some differences between using interop assemblies and standard assemblies.</span></span> <span data-ttu-id="ce9bb-130">這些差異的部分包括公開的類別，以及資料型別參數和傳回值。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-130">These areas of difference include the exposure of classes, and data types for parameters and return values.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor5"></a> <span data-ttu-id="ce9bb-131">公開為這兩個介面的類別和類別</span><span class="sxs-lookup"><span data-stu-id="ce9bb-131">Classes Exposed as Both Interfaces and Classes</span></span>  
 <span data-ttu-id="ce9bb-132">不同於標準的組件中的類別，COM 類別會公開介面和類別，表示在 COM 類別的 interop 組件中。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-132">Unlike classes in standard assemblies, COM classes are exposed in interop assemblies as both an interface and a class that represents the COM class.</span></span> <span data-ttu-id="ce9bb-133">有相同的 COM 類別的介面名稱。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-133">The interface's name is identical to that of the COM class.</span></span> <span data-ttu-id="ce9bb-134">Interop 的類別名稱是原始的 COM 類別相同，但是字附加 「 類別 」。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-134">The name of the interop class is the same as that of the original COM class, but with the word "Class" appended.</span></span> <span data-ttu-id="ce9bb-135">例如，假設您有在專案中使用 interop 組件以取得 COM 物件的參考。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-135">For example, suppose you have a project with a reference to an interop assembly for a COM object.</span></span> <span data-ttu-id="ce9bb-136">如果 COM 類別的名稱為`MyComClass`，IntelliSense 和物件瀏覽器會顯示名為介面`MyComClass`和類別，名為`MyComClassClass`。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-136">If the COM class is named `MyComClass`, IntelliSense and the Object Browser show an interface named `MyComClass` and a class named `MyComClassClass`.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor6"></a> <span data-ttu-id="ce9bb-137">建立.NET Framework 類別的執行個體</span><span class="sxs-lookup"><span data-stu-id="ce9bb-137">Creating Instances of a .NET Framework Class</span></span>  
 <span data-ttu-id="ce9bb-138">一般而言，您建立的執行個體[!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)]類別使用`New`陳述式的類別名稱。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-138">Generally, you create an instance of a [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)] class using the `New` statement with a class name.</span></span> <span data-ttu-id="ce9bb-139">Interop 組件所表示之 COM 類別是一種情況，您可以使用`New`陳述式的介面。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-139">Having a COM class represented by an interop assembly is the one case in which you can use the `New` statement with an interface.</span></span> <span data-ttu-id="ce9bb-140">除非您正在使用的 COM 類別`Inherits`陳述式，就像類別一樣，您可以使用的介面。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-140">Unless you are using the COM class with an `Inherits` statement, you can use the interface just as you would a class.</span></span> <span data-ttu-id="ce9bb-141">下列程式碼示範如何建立`Command`擁有 Microsoft ActiveX 資料物件 2.8 程式庫 COM 物件參考的專案中的物件：</span><span class="sxs-lookup"><span data-stu-id="ce9bb-141">The following code demonstrates how to create a `Command` object in a project that has a reference to the Microsoft ActiveX Data Objects 2.8 Library COM object:</span></span>  
  
 [!code-vb[VbVbalrInterop#20](../../../visual-basic/programming-guide/com-interop/codesnippet/VisualBasic/troubleshooting-interoperability_1.vb)]  
  
 <span data-ttu-id="ce9bb-142">不過，如果您使用之 COM 類別作為基底之衍生類別，您必須使用 interop 類別代表 COM 類別，如下列程式碼所示：</span><span class="sxs-lookup"><span data-stu-id="ce9bb-142">However, if you are using the COM class as the base for a derived class, you must use the interop class that represents the COM class, as in the following code:</span></span>  
  
 [!code-vb[VbVbalrInterop#21](../../../visual-basic/programming-guide/com-interop/codesnippet/VisualBasic/troubleshooting-interoperability_2.vb)]  
  
> [!NOTE]
>  <span data-ttu-id="ce9bb-143">Interop 組件隱含實作介面來代表 COM 類別。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-143">Interop assemblies implicitly implement interfaces that represent COM classes.</span></span> <span data-ttu-id="ce9bb-144">您不應該嘗試使用`Implements`會造成陳述式來實作這些介面或錯誤。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-144">You should not try to use the `Implements` statement to implement these interfaces or an error will result.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor7"></a> <span data-ttu-id="ce9bb-145">資料型別參數和傳回值</span><span class="sxs-lookup"><span data-stu-id="ce9bb-145">Data Types for Parameters and Return Values</span></span>  
 <span data-ttu-id="ce9bb-146">不同於標準的組件的成員，interop 組件成員可能具有不同於原始物件宣告中所使用的資料類型。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-146">Unlike members of standard assemblies, interop assembly members may have data types that differ from those used in the  original object declaration.</span></span> <span data-ttu-id="ce9bb-147">雖然隱含 interop 組件會將 COM 類型轉換成相容的 common language runtime 類型中，您應該注意兩方用來避免在執行階段錯誤的資料類型。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-147">Although interop assemblies implicitly convert COM types to compatible common language runtime types, you should pay attention to the data types that are used by both sides to prevent runtime errors.</span></span> <span data-ttu-id="ce9bb-148">例如，在 Visual Basic 6.0 及舊版中，類型的值中建立的 COM 物件`Integer`假設[!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)]對等型别， `Short`。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-148">For example, in COM objects created in Visual Basic 6.0 and earlier versions, values of type `Integer` assume the [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)] equivalent type, `Short`.</span></span> <span data-ttu-id="ce9bb-149">建議您使用物件瀏覽器，來檢查已匯入成員的特性，才使用。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-149">It is recommended that you use the Object Browser to examine the characteristics of imported members before you use them.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor8"></a> <span data-ttu-id="ce9bb-150">模組層級 COM 方法</span><span class="sxs-lookup"><span data-stu-id="ce9bb-150">Module level COM methods</span></span>  
 <span data-ttu-id="ce9bb-151">大部分的 COM 物件可用來建立 COM 類別使用的執行個體`New`關鍵字，然後再呼叫物件的方法。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-151">Most COM objects are used by creating an instance of a COM class using the `New` keyword and then calling methods of the object.</span></span> <span data-ttu-id="ce9bb-152">這項規則有一個例外狀況包含 COM 物件，包含`AppObj`或`GlobalMultiUse`COM 類別。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-152">One exception to this rule involves COM objects that contain `AppObj` or `GlobalMultiUse` COM classes.</span></span> <span data-ttu-id="ce9bb-153">這種類別類似於 Visual Basic.NET 類別中的模組層級方法。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-153">Such classes resemble module level methods in Visual Basic .NET classes.</span></span> <span data-ttu-id="ce9bb-154">Visual Basic 6.0 及舊版隱含地建立這類物件的執行個體為您第一次，讓您呼叫其方法的其中一個。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-154">Visual Basic 6.0 and earlier versions implicitly create instances of such objects for you the first time that you call one of their methods.</span></span> <span data-ttu-id="ce9bb-155">例如，Visual Basic 6.0 中您可以加入 Microsoft DAO 3.6 物件程式庫並呼叫參考`DBEngine`方法，而不先建立執行個體：</span><span class="sxs-lookup"><span data-stu-id="ce9bb-155">For example, in Visual Basic 6.0 you can add a reference to the Microsoft DAO 3.6 Object Library and call the `DBEngine` method without first creating an instance:</span></span>  
  
```vb  
Dim db As DAO.Database  
' Open the database.  
Set db = DBEngine.OpenDatabase("C:\nwind.mdb")  
' Use the database object.  
```  
  
 <span data-ttu-id="ce9bb-156">Visual Basic.NET，您必須一律建立 COM 物件的執行個體才能使用它們的方法。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-156">Visual Basic .NET requires that you always create instances of COM objects before you can use their methods.</span></span> <span data-ttu-id="ce9bb-157">若要在 Visual Basic 中使用這些方法，將變數宣告為所需的類別，並使用新的關鍵字來將物件指派給物件變數。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-157">To use these methods in Visual Basic, declare a variable of the desired class and use the new keyword to assign the object to the object variable.</span></span> <span data-ttu-id="ce9bb-158">`Shared`可以使用關鍵字，當您想要確定建立類別，只有一個執行個體。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-158">The `Shared` keyword can be used when you want to make sure that only one instance of the class is created.</span></span>  
  
 [!code-vb[VbVbalrInterop#23](../../../visual-basic/programming-guide/com-interop/codesnippet/VisualBasic/troubleshooting-interoperability_3.vb)]  
  
##  <a name="vbconinteroperabilitymarshalinganchor9"></a> <span data-ttu-id="ce9bb-159">事件處理常式中的未處理的錯誤</span><span class="sxs-lookup"><span data-stu-id="ce9bb-159">Unhandled Errors in Event Handlers</span></span>  
 <span data-ttu-id="ce9bb-160">一個常見的 interop 問題包含處理 COM 物件所引發的事件的事件處理常式中的錯誤。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-160">One common interop problem involves errors in event handlers that handle events raised by COM objects.</span></span> <span data-ttu-id="ce9bb-161">這類錯誤會被忽略，除非您特別要檢查是否有錯誤使用`On Error`或`Try...Catch...Finally`陳述式。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-161">Such errors are ignored unless you specifically check for errors using `On Error` or `Try...Catch...Finally` statements.</span></span> <span data-ttu-id="ce9bb-162">例如，下列範例是從 Visual Basic.NET 專案具有 Microsoft ActiveX 資料物件 2.8 程式庫 COM 物件的參考。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-162">For example, the following example is from a Visual Basic .NET project that has a reference to the Microsoft ActiveX Data Objects 2.8 Library COM object.</span></span>  
  
 [!code-vb[VbVbalrInterop#24](../../../visual-basic/programming-guide/com-interop/codesnippet/VisualBasic/troubleshooting-interoperability_4.vb)]  
  
 <span data-ttu-id="ce9bb-163">此範例中會如預期般引發錯誤。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-163">This example raises an error as expected.</span></span> <span data-ttu-id="ce9bb-164">不過，如果您嘗試不相同的範例`Try...Catch...Finally`區塊中，錯誤會被忽略，方式就如同您使用`OnError Resume Next`陳述式。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-164">However, if you try the same example without the `Try...Catch...Finally` block, the error is ignored as if you used the `OnError Resume Next` statement.</span></span> <span data-ttu-id="ce9bb-165">沒有錯誤處理，將 「 除以零以無訊息模式失敗。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-165">Without error handling, the division by zero silently fails.</span></span> <span data-ttu-id="ce9bb-166">因為這類錯誤永遠不會引發未處理例外狀況的錯誤，很重要，您會使用某種形式的事件處理常式處理事件從 COM 物件的例外狀況處理。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-166">Because such errors never raise unhandled exception errors, it is important that you use some form of exception handling in event handlers that handle events from COM objects.</span></span>  
  
### <a name="understanding-com-interop-errors"></a><span data-ttu-id="ce9bb-167">了解 COM interop 錯誤</span><span class="sxs-lookup"><span data-stu-id="ce9bb-167">Understanding COM interop errors</span></span>  
 <span data-ttu-id="ce9bb-168">不需要錯誤處理 interop 呼叫通常會產生錯誤，提供一些資訊。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-168">Without error handling, interop calls often generate errors that provide little information.</span></span> <span data-ttu-id="ce9bb-169">您應該盡可能使用結構化處理，以提供問題的相關資訊，所發生的錯誤。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-169">Whenever possible, use structured error handling to provide more information about problems when they occur.</span></span> <span data-ttu-id="ce9bb-170">當您偵錯應用程式時，這可以是特別有用。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-170">This can be especially helpful when you debug applications.</span></span> <span data-ttu-id="ce9bb-171">例如: </span><span class="sxs-lookup"><span data-stu-id="ce9bb-171">For example:</span></span>  
  
 [!code-vb[VbVbalrInterop#25](../../../visual-basic/programming-guide/com-interop/codesnippet/VisualBasic/troubleshooting-interoperability_5.vb)]  
  
 <span data-ttu-id="ce9bb-172">您可以檢查例外狀況物件的內容來找到錯誤描述、 HRESULT，以及 COM 錯誤的來源等資訊。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-172">You can find information such as the error description, HRESULT, and the source of COM errors by examining the contents of the exception object.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor10"></a> <span data-ttu-id="ce9bb-173">ActiveX 控制項問題</span><span class="sxs-lookup"><span data-stu-id="ce9bb-173">ActiveX Control Issues</span></span>  
 <span data-ttu-id="ce9bb-174">大部分的 ActiveX 控制項可搭配 Visual Basic 6.0 使用 Visual Basic.NET 運作時發生問題。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-174">Most ActiveX controls that work with Visual Basic 6.0 work with Visual Basic .NET without trouble.</span></span> <span data-ttu-id="ce9bb-175">主要的例外狀況是容器控制項中或以視覺化方式包含其他控制項的控制項。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-175">The main exceptions are container controls, or controls that visually contain other controls.</span></span> <span data-ttu-id="ce9bb-176">使用 Visual Studio 無法正確運作的舊版控制項的一些範例如下所示：</span><span class="sxs-lookup"><span data-stu-id="ce9bb-176">Some examples of older controls that do not work correctly with Visual Studio are as follows:</span></span>  
  
-   <span data-ttu-id="ce9bb-177">Microsoft Forms 2.0 框架控制項</span><span class="sxs-lookup"><span data-stu-id="ce9bb-177">Microsoft Forms 2.0 Frame control</span></span>  
  
-   <span data-ttu-id="ce9bb-178">上下按鈕控制項，也就是微調控制項</span><span class="sxs-lookup"><span data-stu-id="ce9bb-178">Up-Down control, also known as the spin control</span></span>  
  
-   <span data-ttu-id="ce9bb-179">Sheridan 索引標籤控制項</span><span class="sxs-lookup"><span data-stu-id="ce9bb-179">Sheridan Tab Control</span></span>  
  
 <span data-ttu-id="ce9bb-180">有只有少數的因應措施不支援的 ActiveX 控制項問題。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-180">There are only a few workarounds for unsupported ActiveX control problems.</span></span> <span data-ttu-id="ce9bb-181">如果您擁有原始的原始程式碼，您可以將現有的控制項移轉到 Visual Studio 中。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-181">You can migrate existing controls to Visual Studio if you own the original source code.</span></span> <span data-ttu-id="ce9bb-182">否則，您可以洽詢軟體廠商更新。ActiveX 控制項不支援的網路相容版本的控制項來取代。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-182">Otherwise, you can check with software vendors for updated .NET-compatible versions of controls to replace unsupported ActiveX controls.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor11"></a> <span data-ttu-id="ce9bb-183">傳遞的控制項 ByRef 的 ReadOnly 屬性</span><span class="sxs-lookup"><span data-stu-id="ce9bb-183">Passing ReadOnly Properties of Controls ByRef</span></span>  
 <span data-ttu-id="ce9bb-184">Visual Basic.NET 有時會引發 COM 錯誤，例如，「 錯誤 0x800A017F CTL_E_SETNOTSUPPORTED"，當您將傳遞`ReadOnly`的一些較舊的 ActiveX 控制項做為屬性`ByRef`其他程序的參數。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-184">Visual Basic .NET sometimes raises COM errors such as, "Error 0x800A017F CTL_E_SETNOTSUPPORTED", when you pass `ReadOnly` properties of some older ActiveX controls as `ByRef` parameters to other procedures.</span></span> <span data-ttu-id="ce9bb-185">從 Visual Basic 6.0 的類似程序呼叫不會引發錯誤，而且參數會視為以值傳遞。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-185">Similar procedure calls from Visual Basic 6.0 do not raise an error, and the parameters are treated as if you passed them by value.</span></span> <span data-ttu-id="ce9bb-186">Visual Basic.NET 錯誤訊息指出您要變更此屬性沒有屬性，`Set`程序。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-186">The Visual Basic .NET error message indicates that you are trying to change a property that does not have a property `Set` procedure.</span></span>  
  
 <span data-ttu-id="ce9bb-187">如果您有存取呼叫程序，您可以使用來避免這個錯誤`ByVal`關鍵字來宣告參數接受`ReadOnly`屬性。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-187">If you have access to the procedure being called, you can prevent this error by using the `ByVal` keyword to declare parameters that accept `ReadOnly` properties.</span></span> <span data-ttu-id="ce9bb-188">例如: </span><span class="sxs-lookup"><span data-stu-id="ce9bb-188">For example:</span></span>  
  
 [!code-vb[VbVbalrInterop#26](../../../visual-basic/programming-guide/com-interop/codesnippet/VisualBasic/troubleshooting-interoperability_6.vb)]  
  
 <span data-ttu-id="ce9bb-189">如果您沒有存取原始碼所呼叫的程序，您可以強制以傳值加上一組額外的括號括住的呼叫程序的屬性。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-189">If you do not have access to the source code for the procedure being called, you can force the property to be passed by value by adding an extra set of brackets around the calling procedure.</span></span> <span data-ttu-id="ce9bb-190">例如，在具有 Microsoft ActiveX 資料物件 2.8 程式庫 COM 物件的參考專案中，您可以使用：</span><span class="sxs-lookup"><span data-stu-id="ce9bb-190">For example, in a project that has a reference to the Microsoft ActiveX Data Objects 2.8 Library COM object, you can use:</span></span>  
  
 [!code-vb[VbVbalrInterop#27](../../../visual-basic/programming-guide/com-interop/codesnippet/VisualBasic/troubleshooting-interoperability_7.vb)]  
  
##  <a name="vbconinteroperabilitymarshalinganchor12"></a> <span data-ttu-id="ce9bb-191">部署 Interop 公開 （expose） 的組件</span><span class="sxs-lookup"><span data-stu-id="ce9bb-191">Deploying Assemblies That Expose Interop</span></span>  
 <span data-ttu-id="ce9bb-192">部署的 COM 介面公開 （expose） 的組件提供唯一的一些挑戰。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-192">Deploying assemblies that expose COM interfaces presents some unique challenges.</span></span> <span data-ttu-id="ce9bb-193">例如，個別應用程式參考相同的 COM 組件時，就會發生潛在的問題。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-193">For example, a potential problem occurs when separate applications reference the same COM assembly.</span></span> <span data-ttu-id="ce9bb-194">安裝新版的組件和另一個應用程式仍在使用舊版本的組件時，常會使用這種情況。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-194">This situation is common when a new version of an assembly is installed and another application is still using the old version of the assembly.</span></span> <span data-ttu-id="ce9bb-195">如果您解除安裝共用的 DLL 的組件，您會無意間造成它無法使用其他組件。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-195">If you uninstall an assembly that shares a DLL, you can unintentionally make it unavailable to the other assemblies.</span></span>  
  
 <span data-ttu-id="ce9bb-196">若要避免這個問題，您應該將共用的組件安裝到全域組件快取 (GAC) 中，並使用元件的合併模組。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-196">To avoid this problem, you should install shared assemblies to the Global Assembly Cache (GAC) and use a MergeModule for the component.</span></span> <span data-ttu-id="ce9bb-197">如果您無法在 GAC 中安裝應用程式，它應該會安裝至 CommonFilesFolder 版本特定子目錄中。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-197">If you cannot install the application in the GAC, it should be installed to CommonFilesFolder in a version-specific subdirectory.</span></span>  
  
 <span data-ttu-id="ce9bb-198">不共用的組件應該位於與呼叫的應用程式目錄中的並排顯示。</span><span class="sxs-lookup"><span data-stu-id="ce9bb-198">Assemblies that are not shared should be located side by side in the directory with the calling application.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="ce9bb-199">另請參閱</span><span class="sxs-lookup"><span data-stu-id="ce9bb-199">See Also</span></span>  
 <xref:System.Runtime.InteropServices.MarshalAsAttribute>  
 [<span data-ttu-id="ce9bb-200">COM Interop</span><span class="sxs-lookup"><span data-stu-id="ce9bb-200">COM Interop</span></span>](../../../visual-basic/programming-guide/com-interop/index.md)  
 [<span data-ttu-id="ce9bb-201">Tlbimp.exe (類型程式庫匯入工具)</span><span class="sxs-lookup"><span data-stu-id="ce9bb-201">Tlbimp.exe (Type Library Importer)</span></span>](../../../framework/tools/tlbimp-exe-type-library-importer.md)  
 [<span data-ttu-id="ce9bb-202">Tlbexp.exe (類型程式庫匯出工具)</span><span class="sxs-lookup"><span data-stu-id="ce9bb-202">Tlbexp.exe (Type Library Exporter)</span></span>](http://msdn.microsoft.com/library/a487d61b-d166-467b-a7ca-d8b52fbff42d)  
 [<span data-ttu-id="ce9bb-203">逐步解說：實作 COM 物件的繼承</span><span class="sxs-lookup"><span data-stu-id="ce9bb-203">Walkthrough: Implementing Inheritance with COM Objects</span></span>](../../../visual-basic/programming-guide/com-interop/walkthrough-implementing-inheritance-with-com-objects.md)  
 [<span data-ttu-id="ce9bb-204">Inherits 陳述式</span><span class="sxs-lookup"><span data-stu-id="ce9bb-204">Inherits Statement</span></span>](../../../visual-basic/language-reference/statements/inherits-statement.md)  
 [<span data-ttu-id="ce9bb-205">全域組件快取</span><span class="sxs-lookup"><span data-stu-id="ce9bb-205">Global Assembly Cache</span></span>](../../../framework/app-domains/gac.md)
