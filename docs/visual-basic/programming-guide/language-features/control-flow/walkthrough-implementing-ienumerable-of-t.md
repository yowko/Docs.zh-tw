---
title: 在 Visual Basic 中實作 IEnumerable
ms.custom: ''
ms.date: 07/20/2015
ms.prod: .net
ms.reviewer: ''
ms.suite: ''
ms.technology:
- devlang-visual-basic
ms.topic: article
helpviewer_keywords:
- control flow [Visual Basic]
- enumerable interfaces
- loop structures [Visual Basic], optimizing performance
- control flow [Visual Basic]
ms.assetid: c60d7589-51f2-4463-a2d5-22506bbc1554
caps.latest.revision: 15
author: dotnet-bot
ms.author: dotnetcontent
ms.openlocfilehash: 4645153f9c830bc96b7ec55367f46f09098eb78d
ms.sourcegitcommit: 86adcc06e35390f13c1e372c36d2e044f1fc31ef
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 04/26/2018
---
# <a name="walkthrough-implementing-ienumerableof-t-in-visual-basic"></a><span data-ttu-id="8eb53-102">逐步解說：在 Visual Basic 中實作 IEnumerable(Of T)</span><span class="sxs-lookup"><span data-stu-id="8eb53-102">Walkthrough: Implementing IEnumerable(Of T) in Visual Basic</span></span>
<span data-ttu-id="8eb53-103"><xref:System.Collections.Generic.IEnumerable%601>可以一次傳回一個項目值序列的類別會實作介面。</span><span class="sxs-lookup"><span data-stu-id="8eb53-103">The <xref:System.Collections.Generic.IEnumerable%601> interface is implemented by classes that can return a sequence of values one item at a time.</span></span> <span data-ttu-id="8eb53-104">傳回的資料一次的一個項目是您沒有將一組完整的資料載入記憶體，才能使用它的優點。</span><span class="sxs-lookup"><span data-stu-id="8eb53-104">The advantage of returning data one item at a time is that you do not have to load the complete set of data into memory to work with it.</span></span> <span data-ttu-id="8eb53-105">您只需要使用足夠的記憶體載入資料的單一項目。</span><span class="sxs-lookup"><span data-stu-id="8eb53-105">You only have to use sufficient memory to load a single item from the data.</span></span> <span data-ttu-id="8eb53-106">類別可實作`IEnumerable(T)`介面可以搭配`For Each`迴圈或 LINQ 查詢。</span><span class="sxs-lookup"><span data-stu-id="8eb53-106">Classes that implement the `IEnumerable(T)` interface can be used with `For Each` loops or LINQ queries.</span></span>  
  
 <span data-ttu-id="8eb53-107">例如，請考慮必須讀取大型文字檔，並傳回每一行從符合特定的搜尋準則的檔案的應用程式。</span><span class="sxs-lookup"><span data-stu-id="8eb53-107">For example, consider an application that must read a large text file and return each line from the file that matches particular search criteria.</span></span> <span data-ttu-id="8eb53-108">應用程式會使用 LINQ 查詢，傳回符合指定的準則的檔案的行。</span><span class="sxs-lookup"><span data-stu-id="8eb53-108">The application uses a LINQ query to return lines from the file that match the specified criteria.</span></span> <span data-ttu-id="8eb53-109">若要使用 LINQ 查詢來查詢檔案的內容，應用程式無法載入檔案的內容是陣列或集合。</span><span class="sxs-lookup"><span data-stu-id="8eb53-109">To query the contents of the file by using a LINQ query, the application could load the contents of the file into an array or a collection.</span></span> <span data-ttu-id="8eb53-110">不過，將整個檔案載入到陣列或集合，會消耗更多超過所需的記憶體。</span><span class="sxs-lookup"><span data-stu-id="8eb53-110">However, loading the whole file into an array or collection would consume far more memory than is required.</span></span> <span data-ttu-id="8eb53-111">LINQ 查詢無法使用的可列舉的類別，傳回符合搜尋條件的值，改為查詢的檔案內容。</span><span class="sxs-lookup"><span data-stu-id="8eb53-111">The LINQ query could instead query the file contents by using an enumerable class, returning only values that match the search criteria.</span></span> <span data-ttu-id="8eb53-112">傳回只有少數的查詢相符的值，會消耗更少的記憶體。</span><span class="sxs-lookup"><span data-stu-id="8eb53-112">Queries that return only a few matching values would consume far less memory.</span></span>  
  
 <span data-ttu-id="8eb53-113">您可以建立一個類別，實作<xref:System.Collections.Generic.IEnumerable%601>介面，以公開為可列舉的資料來源資料。</span><span class="sxs-lookup"><span data-stu-id="8eb53-113">You can create a class that implements the <xref:System.Collections.Generic.IEnumerable%601> interface to expose source data as enumerable data.</span></span> <span data-ttu-id="8eb53-114">類別可實作`IEnumerable(T)`介面則需要另一個類別可實作<xref:System.Collections.Generic.IEnumerator%601>來逐一查看來源資料的介面。</span><span class="sxs-lookup"><span data-stu-id="8eb53-114">Your class that implements the `IEnumerable(T)` interface will require another class that implements the <xref:System.Collections.Generic.IEnumerator%601> interface to iterate through the source data.</span></span> <span data-ttu-id="8eb53-115">這兩種類別可讓您依序為特定類型傳回資料的項目。</span><span class="sxs-lookup"><span data-stu-id="8eb53-115">These two classes enable you to return items of data sequentially as a specific type.</span></span>  
  
 <span data-ttu-id="8eb53-116">在本逐步解說中，您將建立一個類別，實作`IEnumerable(Of String)`介面和類別可實作`IEnumerator(Of String)`介面，以讀取一次的文字檔案一行。</span><span class="sxs-lookup"><span data-stu-id="8eb53-116">In this walkthrough, you will create a class that implements the `IEnumerable(Of String)` interface and a class that implements the `IEnumerator(Of String)` interface to read a text file one line at a time.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="creating-the-enumerable-class"></a><span data-ttu-id="8eb53-117">建立可列舉類別</span><span class="sxs-lookup"><span data-stu-id="8eb53-117">Creating the Enumerable Class</span></span>  
  
|<span data-ttu-id="8eb53-118">若要建立可列舉類別專案</span><span class="sxs-lookup"><span data-stu-id="8eb53-118">To create the enumerable class project</span></span>|  
|---|  
|<span data-ttu-id="8eb53-119">1.在 Visual Basic 中，在**檔案**功能表上，指向**新增**，然後按一下 **專案**。</span><span class="sxs-lookup"><span data-stu-id="8eb53-119">1.  In Visual Basic, on the **File** menu, point to **New** and then click **Project**.</span></span><br /><span data-ttu-id="8eb53-120">2.在 [新增專案] 對話方塊的 [專案類型] 窗格中，確認已選取 [Windows]。</span><span class="sxs-lookup"><span data-stu-id="8eb53-120">2.  In the **New Project** dialog box, in the **Project Types** pane, make sure that **Windows** is selected.</span></span> <span data-ttu-id="8eb53-121">在 [範本] 窗格中，選取 [類別庫]。</span><span class="sxs-lookup"><span data-stu-id="8eb53-121">Select **Class Library** in the **Templates** pane.</span></span> <span data-ttu-id="8eb53-122">在 [名稱] 方塊中，輸入 `StreamReaderEnumerable` 並按一下 [確定]。</span><span class="sxs-lookup"><span data-stu-id="8eb53-122">In the **Name** box, type `StreamReaderEnumerable`, and then click **OK**.</span></span> <span data-ttu-id="8eb53-123">新的專案隨即顯示。</span><span class="sxs-lookup"><span data-stu-id="8eb53-123">The new project is displayed.</span></span><br /><span data-ttu-id="8eb53-124">3.在**方案總管 中**，Class1.vb 檔案上按一下滑鼠右鍵，然後按一下**重新命名**。</span><span class="sxs-lookup"><span data-stu-id="8eb53-124">3.  In **Solution Explorer**, right-click the Class1.vb file and click **Rename**.</span></span> <span data-ttu-id="8eb53-125">將檔案重新命名為 `StreamReaderEnumerable.vb`，然後按 ENTER。</span><span class="sxs-lookup"><span data-stu-id="8eb53-125">Rename the file to `StreamReaderEnumerable.vb` and press ENTER.</span></span> <span data-ttu-id="8eb53-126">重新命名檔案時，也會將類別重新命名為 `StreamReaderEnumerable`。</span><span class="sxs-lookup"><span data-stu-id="8eb53-126">Renaming the file will also rename the class to `StreamReaderEnumerable`.</span></span> <span data-ttu-id="8eb53-127">此類別會實作 `IEnumerable(Of String)` 介面。</span><span class="sxs-lookup"><span data-stu-id="8eb53-127">This class will implement the `IEnumerable(Of String)` interface.</span></span><br /><span data-ttu-id="8eb53-128">4.StreamReaderEnumerable 專案上按一下滑鼠右鍵，指向**新增**，然後按一下 **新項目**。</span><span class="sxs-lookup"><span data-stu-id="8eb53-128">4.  Right-click the StreamReaderEnumerable project, point to **Add**, and then click **New Item**.</span></span> <span data-ttu-id="8eb53-129">選取**類別**範本。</span><span class="sxs-lookup"><span data-stu-id="8eb53-129">Select the **Class** template.</span></span> <span data-ttu-id="8eb53-130">在**名稱**方塊中，輸入`StreamReaderEnumerator.vb`按一下**確定**。</span><span class="sxs-lookup"><span data-stu-id="8eb53-130">In the **Name** box, type `StreamReaderEnumerator.vb` and click **OK**.</span></span>|  
  
 <span data-ttu-id="8eb53-131">此專案中的第一個類別的可列舉的類別，而且會實作`IEnumerable(Of String)`介面。</span><span class="sxs-lookup"><span data-stu-id="8eb53-131">The first class in this project is the enumerable class and will implement the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="8eb53-132">這個泛型介面實作<xref:System.Collections.IEnumerable>介面與這個類別的取用者可以存取做為輸入值保證`String`。</span><span class="sxs-lookup"><span data-stu-id="8eb53-132">This generic interface implements the <xref:System.Collections.IEnumerable> interface and guarantees that consumers of this class can access values typed as `String`.</span></span>  
  
|<span data-ttu-id="8eb53-133">若要加入程式碼來實作 IEnumerable</span><span class="sxs-lookup"><span data-stu-id="8eb53-133">To add the code to implement IEnumerable</span></span>|  
|---|  
|<span data-ttu-id="8eb53-134">1.開啟 StreamReaderEnumerable.vb 檔案。</span><span class="sxs-lookup"><span data-stu-id="8eb53-134">1.  Open the StreamReaderEnumerable.vb file.</span></span><br /><span data-ttu-id="8eb53-135">2.之後的行上`Public Class StreamReaderEnumerable`中，輸入下列命令，然後按 ENTER。</span><span class="sxs-lookup"><span data-stu-id="8eb53-135">2.  On the line after `Public Class StreamReaderEnumerable`, type the following and press ENTER.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#1](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_1.vb)]<br />     <span data-ttu-id="8eb53-136">Visual Basic 會自動填入所需的成員類別`IEnumerable(Of String)`介面。</span><span class="sxs-lookup"><span data-stu-id="8eb53-136">Visual Basic automatically populates the class with the members that are required by the `IEnumerable(Of String)` interface.</span></span><br /><span data-ttu-id="8eb53-137">3.這個列舉的類別會從文字檔案一行讀取行一次。</span><span class="sxs-lookup"><span data-stu-id="8eb53-137">3.  This enumerable class will read lines from a text file one line at a time.</span></span> <span data-ttu-id="8eb53-138">將下列程式碼加入至要公開 （expose） 會將檔案路徑，做為輸入參數的公用建構函式的類別。</span><span class="sxs-lookup"><span data-stu-id="8eb53-138">Add the following code to the class to expose a public constructor that takes a file path as an input parameter.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#2](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_2.vb)]<br /><span data-ttu-id="8eb53-139">4.實作<xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A>方法`IEnumerable(Of String)`介面會傳回的新執行個體`StreamReaderEnumerator`類別。</span><span class="sxs-lookup"><span data-stu-id="8eb53-139">4.  Your implementation of the <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> method of the `IEnumerable(Of String)` interface will return a new instance of the `StreamReaderEnumerator` class.</span></span> <span data-ttu-id="8eb53-140">實作`GetEnumerator`方法`IEnumerable`介面可以成為`Private`，因為您不必只成員公開`IEnumerable(Of String)`介面。</span><span class="sxs-lookup"><span data-stu-id="8eb53-140">The implementation of the `GetEnumerator` method of the `IEnumerable` interface can be made `Private`, because you have to expose only members of the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="8eb53-141">Visual Basic 為產生的程式碼取代`GetEnumerator`為下列程式碼的方法。</span><span class="sxs-lookup"><span data-stu-id="8eb53-141">Replace the code that Visual Basic generated for the `GetEnumerator` methods with the following code.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#3](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_3.vb)]|  
  
|<span data-ttu-id="8eb53-142">若要加入程式碼來實作 IEnumerator</span><span class="sxs-lookup"><span data-stu-id="8eb53-142">To add the code to implement IEnumerator</span></span>|  
|---|  
|<span data-ttu-id="8eb53-143">1.開啟 StreamReaderEnumerator.vb 檔案。</span><span class="sxs-lookup"><span data-stu-id="8eb53-143">1.  Open the StreamReaderEnumerator.vb file.</span></span><br /><span data-ttu-id="8eb53-144">2.之後的行上`Public Class StreamReaderEnumerator`中，輸入下列命令，然後按 ENTER。</span><span class="sxs-lookup"><span data-stu-id="8eb53-144">2.  On the line after `Public Class StreamReaderEnumerator`, type the following and press ENTER.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#4](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_4.vb)]<br />     <span data-ttu-id="8eb53-145">Visual Basic 會自動填入所需的成員類別`IEnumerator(Of String)`介面。</span><span class="sxs-lookup"><span data-stu-id="8eb53-145">Visual Basic automatically populates the class with the members that are required by the `IEnumerator(Of String)` interface.</span></span><br /><span data-ttu-id="8eb53-146">3.列舉值類別會開啟文字檔案，並執行檔案 I/O 以從檔案讀取的行。</span><span class="sxs-lookup"><span data-stu-id="8eb53-146">3.  The enumerator class opens the text file and performs the file I/O to read the lines from the file.</span></span> <span data-ttu-id="8eb53-147">將下列程式碼加入至開啟文字檔來讀取和公開的公用建構函式接受檔案路徑，做為輸入參數的類別。</span><span class="sxs-lookup"><span data-stu-id="8eb53-147">Add the following code to the class to expose a public constructor that takes a file path as an input parameter and open the text file for reading.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#5](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_5.vb)]<br /><span data-ttu-id="8eb53-148">4.`Current`兩個屬性`IEnumerator(Of String)`和`IEnumerator`介面從文字檔案，以傳回目前的項目`String`。</span><span class="sxs-lookup"><span data-stu-id="8eb53-148">4.  The `Current` properties for both the `IEnumerator(Of String)` and `IEnumerator` interfaces return the current item from the text file as a `String`.</span></span> <span data-ttu-id="8eb53-149">實作`Current`屬性`IEnumerator`介面可以成為`Private`，因為您不必只成員公開`IEnumerator(Of String)`介面。</span><span class="sxs-lookup"><span data-stu-id="8eb53-149">The implementation of the `Current` property of the `IEnumerator` interface can be made `Private`, because you have to expose only members of the `IEnumerator(Of String)` interface.</span></span> <span data-ttu-id="8eb53-150">Visual Basic 為產生的程式碼取代`Current`為下列程式碼的屬性。</span><span class="sxs-lookup"><span data-stu-id="8eb53-150">Replace the code that Visual Basic generated for the `Current` properties with the following code.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#6](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_6.vb)]<br /><span data-ttu-id="8eb53-151">5.`MoveNext`方法`IEnumerator`介面瀏覽到文字檔案中的下一個項目，並更新所傳回的值`Current`屬性。</span><span class="sxs-lookup"><span data-stu-id="8eb53-151">5.  The `MoveNext` method of the `IEnumerator` interface navigates to the next item in the text file and updates the value that is returned by the `Current` property.</span></span> <span data-ttu-id="8eb53-152">如果沒有更多的項目，若要閱讀，`MoveNext`方法會傳回`False`，否則為`MoveNext`方法會傳回`True`。</span><span class="sxs-lookup"><span data-stu-id="8eb53-152">If there are no more items to read, the `MoveNext` method returns `False`; otherwise the `MoveNext` method returns `True`.</span></span> <span data-ttu-id="8eb53-153">將下列程式碼加入至 `MoveNext` 方法。</span><span class="sxs-lookup"><span data-stu-id="8eb53-153">Add the following code to the `MoveNext` method.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#7](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_7.vb)]<br /><span data-ttu-id="8eb53-154">6.`Reset`方法`IEnumerator`介面會指示以指向文字檔案開頭的迭代器，並且清除目前的項目值。</span><span class="sxs-lookup"><span data-stu-id="8eb53-154">6.  The `Reset` method of the `IEnumerator` interface directs the iterator to point to the start of the text file and clears the current item value.</span></span> <span data-ttu-id="8eb53-155">將下列程式碼加入至 `Reset` 方法。</span><span class="sxs-lookup"><span data-stu-id="8eb53-155">Add the following code to the `Reset` method.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#8](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_8.vb)]<br /><span data-ttu-id="8eb53-156">7.`Dispose`方法`IEnumerator`介面可確保提供的迭代器終結之前，並釋出所有 unmanaged 的資源。</span><span class="sxs-lookup"><span data-stu-id="8eb53-156">7.  The `Dispose` method of the `IEnumerator` interface guarantees that all unmanaged resources are released before the iterator is destroyed.</span></span> <span data-ttu-id="8eb53-157">檔案控制代碼，以供`StreamReader`物件是 unmanaged 的資源，而且必須先關閉然後終結迭代器執行個體。</span><span class="sxs-lookup"><span data-stu-id="8eb53-157">The file handle that is used by the `StreamReader` object is an unmanaged resource and must be closed before the iterator instance is destroyed.</span></span> <span data-ttu-id="8eb53-158">Visual Basic 為產生的程式碼取代`Dispose`方法取代下列程式碼。</span><span class="sxs-lookup"><span data-stu-id="8eb53-158">Replace the code that Visual Basic generated for the `Dispose` method with the following code.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#9](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_9.vb)]|  
  
## <a name="using-the-sample-iterator"></a><span data-ttu-id="8eb53-159">使用範例迭代器</span><span class="sxs-lookup"><span data-stu-id="8eb53-159">Using the Sample Iterator</span></span>  
 <span data-ttu-id="8eb53-160">您可以搭配需要實作的物件的控制結構的程式碼中使用的可列舉類別`IEnumerable`，例如`For Next`迴圈或 LINQ 查詢。</span><span class="sxs-lookup"><span data-stu-id="8eb53-160">You can use an enumerable class in your code together with control structures that require an object that implements `IEnumerable`, such as a `For Next` loop or a LINQ query.</span></span> <span data-ttu-id="8eb53-161">下列範例所示`StreamReaderEnumerable`LINQ 查詢中。</span><span class="sxs-lookup"><span data-stu-id="8eb53-161">The following example shows the `StreamReaderEnumerable` in a LINQ query.</span></span>  
  
 [!code-vb[VbVbalrIteratorWalkthrough#10](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_10.vb)]  
  
## <a name="see-also"></a><span data-ttu-id="8eb53-162">另請參閱</span><span class="sxs-lookup"><span data-stu-id="8eb53-162">See Also</span></span>  
 [<span data-ttu-id="8eb53-163">Visual Basic 中的 LINQ 簡介</span><span class="sxs-lookup"><span data-stu-id="8eb53-163">Introduction to LINQ in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/linq/introduction-to-linq.md)  
 [<span data-ttu-id="8eb53-164">控制流程</span><span class="sxs-lookup"><span data-stu-id="8eb53-164">Control Flow</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/index.md)  
 [<span data-ttu-id="8eb53-165">迴圈結構</span><span class="sxs-lookup"><span data-stu-id="8eb53-165">Loop Structures</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/loop-structures.md)  
 [<span data-ttu-id="8eb53-166">For Each...Next 陳述式</span><span class="sxs-lookup"><span data-stu-id="8eb53-166">For Each...Next Statement</span></span>](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)
