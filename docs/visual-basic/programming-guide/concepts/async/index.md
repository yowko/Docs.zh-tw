---
title: 使用 Async 和 Await 進行非同步程式設計 (Visual Basic)
ms.date: 07/20/2015
ms.assetid: bd7e462b-583b-4395-9c36-45aa9e61072c
ms.openlocfilehash: 9632ee7d275e6641bcd334aa12cded44920d2fda
ms.sourcegitcommit: 9c3a4f2d3babca8919a1e490a159c1500ba7a844
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 10/12/2019
ms.locfileid: "72291664"
---
# <a name="asynchronous-programming-with-async-and-await-visual-basic"></a><span data-ttu-id="eb792-102">使用 Async 和 Await 進行非同步程式設計（Visual Basic）</span><span class="sxs-lookup"><span data-stu-id="eb792-102">Asynchronous programming with Async and Await (Visual Basic)</span></span>

<span data-ttu-id="eb792-103">您可以使用非同步程式設計，避免發生效能瓶頸並增強應用程式的整體回應性。</span><span class="sxs-lookup"><span data-stu-id="eb792-103">You can avoid performance bottlenecks and enhance the overall responsiveness of your application by using asynchronous programming.</span></span> <span data-ttu-id="eb792-104">不過，撰寫非同步應用程式的傳統技術可能很複雜，因而難以撰寫、偵錯和維護。</span><span class="sxs-lookup"><span data-stu-id="eb792-104">However, traditional techniques for writing asynchronous applications can be complicated, making them difficult to write, debug, and maintain.</span></span>

<span data-ttu-id="eb792-105">Visual Studio 2012 引進了一種簡化的方法 (非同步程式設計)，來運用 .NET Framework 4.5 和更新版本以及 Windows 執行階段中的非同步支援。</span><span class="sxs-lookup"><span data-stu-id="eb792-105">Visual Studio 2012 introduced a simplified approach, async programming, that leverages asynchronous support in the .NET Framework 4.5 and higher as well as  in the Windows Runtime.</span></span> <span data-ttu-id="eb792-106">編譯器會代替開發人員處理過去經常要處理的困難工作，而您的應用程式仍保有類似同步程式碼的邏輯結構。</span><span class="sxs-lookup"><span data-stu-id="eb792-106">The compiler does the difficult work that the developer used to do, and your application retains a logical structure that resembles synchronous code.</span></span> <span data-ttu-id="eb792-107">因此，您可以輕鬆擁有非同步程式設計的所有優點。</span><span class="sxs-lookup"><span data-stu-id="eb792-107">As a result, you get all the advantages of asynchronous programming with a fraction of the effort.</span></span>

<span data-ttu-id="eb792-108">本主題提供使用非同步程式設計的時機和使用方式的概觀，並加入包含詳細資料及範例的支援主題連結。</span><span class="sxs-lookup"><span data-stu-id="eb792-108">This topic provides an overview of when and how to use async programming and includes links to support topics that contain details and examples.</span></span>

## <a name="BKMK_WhentoUseAsynchrony"></a> <span data-ttu-id="eb792-109">非同步可改善回應性</span><span class="sxs-lookup"><span data-stu-id="eb792-109">Async improves responsiveness</span></span>

<span data-ttu-id="eb792-110">非同步對於可能在像是應用程式存取 Web 時會進行封鎖的活動而言相當重要。</span><span class="sxs-lookup"><span data-stu-id="eb792-110">Asynchrony is essential for activities that are potentially blocking, such as when your application accesses the web.</span></span> <span data-ttu-id="eb792-111">存取 Web 資源的速度有時會變慢或延遲。</span><span class="sxs-lookup"><span data-stu-id="eb792-111">Access to a web resource sometimes is slow or delayed.</span></span> <span data-ttu-id="eb792-112">如果這類活動在同步處理序中遭到封鎖，整個應用程式就必須等候。</span><span class="sxs-lookup"><span data-stu-id="eb792-112">If such an activity is blocked within a synchronous process, the entire application must wait.</span></span> <span data-ttu-id="eb792-113">在非同步處理序中，應用程式可以繼續處理其他與 Web 資源不相關的工作，直到可能的封鎖工作完成。</span><span class="sxs-lookup"><span data-stu-id="eb792-113">In an asynchronous process, the application can continue with other work that doesn't depend on the web resource until the potentially blocking task finishes.</span></span>

<span data-ttu-id="eb792-114">下表顯示非同步程式設計一般會改善回應速度的部分。</span><span class="sxs-lookup"><span data-stu-id="eb792-114">The following table shows typical areas where asynchronous programming improves responsiveness.</span></span> <span data-ttu-id="eb792-115">從 .NET Framework 4.5 和 Windows 執行階段列出的 API 包含支援非同步程式設計的方法。</span><span class="sxs-lookup"><span data-stu-id="eb792-115">The listed APIs from the  .NET Framework 4.5 and the Windows Runtime contain methods that support async programming.</span></span>

|<span data-ttu-id="eb792-116">應用程式區域</span><span class="sxs-lookup"><span data-stu-id="eb792-116">Application area</span></span>|<span data-ttu-id="eb792-117">包含非同步方法的支援 API</span><span class="sxs-lookup"><span data-stu-id="eb792-117">Supporting APIs that contain async methods</span></span>|
|----------------------|------------------------------------------------|
|<span data-ttu-id="eb792-118">Web 存取</span><span class="sxs-lookup"><span data-stu-id="eb792-118">Web access</span></span>|<span data-ttu-id="eb792-119"><xref:System.Net.Http.HttpClient>、 <xref:Windows.Web.Syndication.SyndicationClient></span><span class="sxs-lookup"><span data-stu-id="eb792-119"><xref:System.Net.Http.HttpClient>, <xref:Windows.Web.Syndication.SyndicationClient></span></span>|
|<span data-ttu-id="eb792-120">處理檔案</span><span class="sxs-lookup"><span data-stu-id="eb792-120">Working with files</span></span>|<span data-ttu-id="eb792-121"><xref:Windows.Storage.StorageFile>, <xref:System.IO.StreamWriter>, <xref:System.IO.StreamReader>, <xref:System.Xml.XmlReader></span><span class="sxs-lookup"><span data-stu-id="eb792-121"><xref:Windows.Storage.StorageFile>, <xref:System.IO.StreamWriter>, <xref:System.IO.StreamReader>, <xref:System.Xml.XmlReader></span></span>|
|<span data-ttu-id="eb792-122">處理影像</span><span class="sxs-lookup"><span data-stu-id="eb792-122">Working with images</span></span>|<span data-ttu-id="eb792-123"><xref:Windows.Media.Capture.MediaCapture>, <xref:Windows.Graphics.Imaging.BitmapEncoder>, <xref:Windows.Graphics.Imaging.BitmapDecoder></span><span class="sxs-lookup"><span data-stu-id="eb792-123"><xref:Windows.Media.Capture.MediaCapture>, <xref:Windows.Graphics.Imaging.BitmapEncoder>, <xref:Windows.Graphics.Imaging.BitmapDecoder></span></span>|
|<span data-ttu-id="eb792-124">WCF 程式設計</span><span class="sxs-lookup"><span data-stu-id="eb792-124">WCF programming</span></span>|[<span data-ttu-id="eb792-125">同步和非同步作業</span><span class="sxs-lookup"><span data-stu-id="eb792-125">Synchronous and Asynchronous Operations</span></span>](../../../../framework/wcf/synchronous-and-asynchronous-operations.md)|
|||

<span data-ttu-id="eb792-126">非同步對於存取 UI 執行緒的應用程式而言確實特別有用，因為所有 UI 相關活動通常都會共用一個執行緒。</span><span class="sxs-lookup"><span data-stu-id="eb792-126">Asynchrony proves especially valuable for applications that access the UI thread because all UI-related activity usually shares one thread.</span></span> <span data-ttu-id="eb792-127">如果同步應用程式中有任何處理序遭到封鎖，所有處理序都會遭到封鎖。</span><span class="sxs-lookup"><span data-stu-id="eb792-127">If any process is blocked in a synchronous application, all are blocked.</span></span> <span data-ttu-id="eb792-128">您的應用程式會停止回應，而且您可能會認為應用程式失敗，但實際上只是在等候。</span><span class="sxs-lookup"><span data-stu-id="eb792-128">Your application stops responding, and you might conclude that it has failed when instead it's just waiting.</span></span>

<span data-ttu-id="eb792-129">當您使用非同步方法時，應用程式會繼續回應 UI。</span><span class="sxs-lookup"><span data-stu-id="eb792-129">When you use asynchronous methods, the application continues to respond to the UI.</span></span> <span data-ttu-id="eb792-130">例如，您可以調整視窗大小或將視窗縮到最小，如果不想要等待應用程式完成，也可以將它關閉。</span><span class="sxs-lookup"><span data-stu-id="eb792-130">You can resize or minimize a window, for example, or you can close the application if you don't want to wait for it to finish.</span></span>

<span data-ttu-id="eb792-131">非同步方法會在設計非同步作業時，於選項清單中加入自動傳輸的對等項目讓您選擇。</span><span class="sxs-lookup"><span data-stu-id="eb792-131">The async-based approach adds the equivalent of an automatic transmission to the list of options that you can choose from when designing asynchronous operations.</span></span> <span data-ttu-id="eb792-132">也就是說，除了擁有傳統非同步程式設計的所有優點之外，開發人員所需投入的時間也大為減少。</span><span class="sxs-lookup"><span data-stu-id="eb792-132">That is, you get all the benefits of traditional asynchronous programming but with much less effort from the developer.</span></span>

## <a name="BKMK_HowtoWriteanAsyncMethod"></a> <span data-ttu-id="eb792-133">非同步方法比較容易撰寫</span><span class="sxs-lookup"><span data-stu-id="eb792-133">Async methods are easier to write</span></span>

<span data-ttu-id="eb792-134">Visual Basic 中的 [Async](../../../../visual-basic/language-reference/modifiers/async.md) 和 [Await](../../../../visual-basic/language-reference/operators/await-operator.md) 關鍵字都是非同步程式設計的核心。</span><span class="sxs-lookup"><span data-stu-id="eb792-134">The [Async](../../../../visual-basic/language-reference/modifiers/async.md) and [Await](../../../../visual-basic/language-reference/operators/await-operator.md) keywords in Visual Basic are the heart of async programming.</span></span> <span data-ttu-id="eb792-135">您可以使用這兩個關鍵字，使用 .NET Framework 或 Windows 執行階段中的資源來建立非同步方法，幾乎就像建立同步方法一樣容易。</span><span class="sxs-lookup"><span data-stu-id="eb792-135">By using those two keywords, you can use resources in the .NET Framework or the Windows Runtime to create an asynchronous method almost as easily as you create a synchronous method.</span></span> <span data-ttu-id="eb792-136">使用 `Async` 和 `Await` 定義的非同步方法就稱為非同步方法。</span><span class="sxs-lookup"><span data-stu-id="eb792-136">Asynchronous methods that you define by using `Async` and `Await` are referred to as async methods.</span></span>

<span data-ttu-id="eb792-137">下列範例將示範非同步方法。</span><span class="sxs-lookup"><span data-stu-id="eb792-137">The following example shows an async method.</span></span> <span data-ttu-id="eb792-138">程式碼中的一切對您而言應該幾乎完全熟悉。</span><span class="sxs-lookup"><span data-stu-id="eb792-138">Almost everything in the code should look completely familiar to you.</span></span> <span data-ttu-id="eb792-139">註解會標註您加入以建立非同步的功能。</span><span class="sxs-lookup"><span data-stu-id="eb792-139">The comments call out the features that you add to create the asynchrony.</span></span>

<span data-ttu-id="eb792-140">您可以在本主題結尾找到完整的 Windows Presentation Foundation (WPF) 範例檔案，並且可從[非同步範例：＜使用 Async 和 Await 進行非同步程式設計＞中的範例](https://docs.microsoft.com/samples/dotnet/samples/async-and-await-vb/) \(英文\) 下載範例。</span><span class="sxs-lookup"><span data-stu-id="eb792-140">You can find a complete Windows Presentation Foundation (WPF) example file at the end of this topic, and you can download the sample from [Async Sample: Example from "Asynchronous Programming with Async and Await"](https://docs.microsoft.com/samples/dotnet/samples/async-and-await-vb/).</span></span>

```vb
' Three things to note about writing an Async Function:
'  - The function has an Async modifier. 
'  - Its return type is Task or Task(Of T). (See "Return Types" section.)
'  - As a matter of convention, its name ends in "Async".
Async Function AccessTheWebAsync() As Task(Of Integer)
    Using client As New HttpClient()
        ' Call and await separately. 
        '  - AccessTheWebAsync can do other things while GetStringAsync is also running.
        '  - getStringTask stores the task we get from the call to GetStringAsync. 
        '  - Task(Of String) means it is a task which returns a String when it is done.
        Dim getStringTask As Task(Of String) =
            client.GetStringAsync("https://docs.microsoft.com/dotnet")
        ' You can do other work here that doesn't rely on the string from GetStringAsync.
        DoIndependentWork()
        ' The Await operator suspends AccessTheWebAsync.
        '  - AccessTheWebAsync does not continue until getStringTask is complete.
        '  - Meanwhile, control returns to the caller of AccessTheWebAsync.
        '  - Control resumes here when getStringTask is complete.
        '  - The Await operator then retrieves the String result from getStringTask.
        Dim urlContents As String = Await getStringTask
        ' The Return statement specifies an Integer result.
        ' A method which awaits AccessTheWebAsync receives the Length value.
        Return urlContents.Length
        
    End Using
    
End Function
```

<span data-ttu-id="eb792-141">如果 `AccessTheWebAsync` 在呼叫 `GetStringAsync` 與等候其完成之間沒有任何可以執行的工作，您可以在下列單一陳述式中呼叫和等候，以簡化程式碼。</span><span class="sxs-lookup"><span data-stu-id="eb792-141">If `AccessTheWebAsync` doesn't have any work that it can do between calling `GetStringAsync` and awaiting its completion, you can simplify your code by calling and awaiting in the following single statement.</span></span>

```vb
Dim urlContents As String = Await client.GetStringAsync()
```

 <span data-ttu-id="eb792-142">下列特性摘要說明上述範例的非同步方法：</span><span class="sxs-lookup"><span data-stu-id="eb792-142">The following characteristics summarize what makes the previous example an async method:</span></span>

- <span data-ttu-id="eb792-143">方法簽章包含 `Async` 修飾詞。</span><span class="sxs-lookup"><span data-stu-id="eb792-143">The method signature includes an `Async` modifier.</span></span>
- <span data-ttu-id="eb792-144">按照慣例，非同步方法的名稱是以 "Async" 後置字元為結尾。</span><span class="sxs-lookup"><span data-stu-id="eb792-144">The name of an async method, by convention, ends with an "Async" suffix.</span></span>
- <span data-ttu-id="eb792-145">傳回類型是下列其中一種類型：</span><span class="sxs-lookup"><span data-stu-id="eb792-145">The return type is one of the following types:</span></span>

  - <span data-ttu-id="eb792-146">如果方法的 return 陳述式中運算元的類型為 TResult，則為 <xref:System.Threading.Tasks.Task%601>。</span><span class="sxs-lookup"><span data-stu-id="eb792-146"><xref:System.Threading.Tasks.Task%601> if your method has a return statement in which the operand has type TResult.</span></span>
  - <span data-ttu-id="eb792-147">如果方法沒有 return 陳述式或是 return 陳述式沒有運算元，則為 <xref:System.Threading.Tasks.Task>。</span><span class="sxs-lookup"><span data-stu-id="eb792-147"><xref:System.Threading.Tasks.Task> if your method has no return statement or has a return statement with no operand.</span></span>
  - <span data-ttu-id="eb792-148">如果您撰寫的是非同步事件處理常式，則為 [Sub](../../../../visual-basic/programming-guide/language-features/procedures/sub-procedures.md)。</span><span class="sxs-lookup"><span data-stu-id="eb792-148">[Sub](../../../../visual-basic/programming-guide/language-features/procedures/sub-procedures.md) if you're writing an async event handler.</span></span>

  <span data-ttu-id="eb792-149">如需詳細資訊，請參閱本主題後段的＜傳回型別和參數＞。</span><span class="sxs-lookup"><span data-stu-id="eb792-149">For more information, see "Return Types and Parameters" later in this topic.</span></span>

- <span data-ttu-id="eb792-150">方法通常至少包含一個 await 運算式，表示方法在等候的非同步作業完成後才能繼續的點。</span><span class="sxs-lookup"><span data-stu-id="eb792-150">The method usually includes at least one await expression, which marks a point where the method can't continue until the awaited asynchronous operation is complete.</span></span> <span data-ttu-id="eb792-151">此時，方法已暫停，而且控制權返回到方法的呼叫端。</span><span class="sxs-lookup"><span data-stu-id="eb792-151">In the meantime, the method is suspended, and control returns to the method's caller.</span></span> <span data-ttu-id="eb792-152">本主題的下一節將說明暫停點會發生什麼情況。</span><span class="sxs-lookup"><span data-stu-id="eb792-152">The next section of this topic illustrates what happens at the suspension point.</span></span>

<span data-ttu-id="eb792-153">在非同步方法中，您會使用提供的關鍵字和類型表示您想要執行的工作，而編譯器會完成其餘的部分，包括追蹤控制權返回已暫停方法中的等候點時必須進行的作業。</span><span class="sxs-lookup"><span data-stu-id="eb792-153">In async methods, you use the provided keywords and types to indicate what you want to do, and the compiler does the rest, including keeping track of what must happen when control returns to an await point in a suspended method.</span></span> <span data-ttu-id="eb792-154">某些常式處理序像是迴圈和例外狀況處理，在傳統非同步程式碼中可能不容易處理。</span><span class="sxs-lookup"><span data-stu-id="eb792-154">Some routine processes, such as loops and exception handling, can be difficult to handle in traditional asynchronous code.</span></span> <span data-ttu-id="eb792-155">在非同步方法中，您可以像在同步方案中一樣撰寫這些項目，如此就可以解決這個問題了。</span><span class="sxs-lookup"><span data-stu-id="eb792-155">In an async method, you write these elements much as you would in a synchronous solution, and the problem is solved.</span></span>

<span data-ttu-id="eb792-156">如需舊版 .NET Framework 中非同步的詳細資訊，請參閱 [TPL 和傳統 .NET Framework 非同步程式設計](../../../../standard/parallel-programming/tpl-and-traditional-async-programming.md)。</span><span class="sxs-lookup"><span data-stu-id="eb792-156">For more information about asynchrony in previous versions of the .NET Framework, see [TPL and Traditional .NET Framework Asynchronous Programming](../../../../standard/parallel-programming/tpl-and-traditional-async-programming.md).</span></span>

## <a name="BKMK_WhatHappensUnderstandinganAsyncMethod"></a><span data-ttu-id="eb792-157">非同步方法中發生的情況</span><span class="sxs-lookup"><span data-stu-id="eb792-157">What happens in an Async method</span></span>

<span data-ttu-id="eb792-158">在非同步程式設計中要了解的最重要事情，就是控制流程如何在方法之間移動。</span><span class="sxs-lookup"><span data-stu-id="eb792-158">The most important thing to understand in asynchronous programming is how the control flow moves from method to method.</span></span> <span data-ttu-id="eb792-159">下圖將引導您了解整個程序：</span><span class="sxs-lookup"><span data-stu-id="eb792-159">The following diagram leads you through the process:</span></span>

![顯示追蹤非同步程式的圖表。](./media/index/navigation-trace-async-program.png)

<span data-ttu-id="eb792-161">圖表中的數位會對應到下列步驟：</span><span class="sxs-lookup"><span data-stu-id="eb792-161">The numbers in the diagram correspond to the following steps:</span></span>

1. <span data-ttu-id="eb792-162">事件處理常式會呼叫並等候 @no__t 0 的非同步方法。</span><span class="sxs-lookup"><span data-stu-id="eb792-162">An event handler calls and awaits the `AccessTheWebAsync` async method.</span></span>

2. <span data-ttu-id="eb792-163">`AccessTheWebAsync` 會建立 <xref:System.Net.Http.HttpClient> 執行個體並呼叫 <xref:System.Net.Http.HttpClient.GetStringAsync%2A> 非同步方法，將網站的內容當做字串下載。</span><span class="sxs-lookup"><span data-stu-id="eb792-163">`AccessTheWebAsync` creates an <xref:System.Net.Http.HttpClient> instance and calls the <xref:System.Net.Http.HttpClient.GetStringAsync%2A> asynchronous method to download the contents of a website as a string.</span></span>

3. <span data-ttu-id="eb792-164">`GetStringAsync` 中發生了導致進度暫停的一些狀況。</span><span class="sxs-lookup"><span data-stu-id="eb792-164">Something happens in `GetStringAsync` that suspends its progress.</span></span> <span data-ttu-id="eb792-165">可能必須等待網站下載或其他封鎖活動。</span><span class="sxs-lookup"><span data-stu-id="eb792-165">Perhaps it must wait for a website to download or some other blocking activity.</span></span> <span data-ttu-id="eb792-166">為了避免封鎖資源，`GetStringAsync` 會將控制權遞交 (Yield) 給它的呼叫端 `AccessTheWebAsync`。</span><span class="sxs-lookup"><span data-stu-id="eb792-166">To avoid blocking resources, `GetStringAsync` yields control to its caller, `AccessTheWebAsync`.</span></span>

     <span data-ttu-id="eb792-167">`GetStringAsync` 會傳回 <xref:System.Threading.Tasks.Task%601> (其中 TResult 是字串)，而 `AccessTheWebAsync` 則會將工作指派給 `getStringTask` 變數。</span><span class="sxs-lookup"><span data-stu-id="eb792-167">`GetStringAsync` returns a <xref:System.Threading.Tasks.Task%601> where TResult is a string, and `AccessTheWebAsync` assigns the task to the `getStringTask` variable.</span></span> <span data-ttu-id="eb792-168">工作代表對 `GetStringAsync` 之呼叫的進行中程序，並承諾會在工作完成時產生實際字串值。</span><span class="sxs-lookup"><span data-stu-id="eb792-168">The task represents the ongoing process for the call to `GetStringAsync`, with a commitment to produce an actual string value when the work is complete.</span></span>

4. <span data-ttu-id="eb792-169">因為尚未等候 `getStringTask`，所以 `AccessTheWebAsync` 可以繼續進行其他不相依於 `GetStringAsync` 之最終結果的其他工作。</span><span class="sxs-lookup"><span data-stu-id="eb792-169">Because `getStringTask` hasn't been awaited yet, `AccessTheWebAsync` can continue with other work that doesn't depend on the final result from `GetStringAsync`.</span></span> <span data-ttu-id="eb792-170">這項工作是由對同步方法 `DoIndependentWork` 的呼叫來表示。</span><span class="sxs-lookup"><span data-stu-id="eb792-170">That work is represented by a call to the synchronous method `DoIndependentWork`.</span></span>

5. <span data-ttu-id="eb792-171">`DoIndependentWork` 是完成其工作並傳回其呼叫端的同步方法。</span><span class="sxs-lookup"><span data-stu-id="eb792-171">`DoIndependentWork` is a synchronous method that does its work and returns to its caller.</span></span>

6. <span data-ttu-id="eb792-172">`AccessTheWebAsync` 已完成所有可處理的工作，但未取得來自 `getStringTask` 的結果。</span><span class="sxs-lookup"><span data-stu-id="eb792-172">`AccessTheWebAsync` has run out of work that it can do without a result from `getStringTask`.</span></span> <span data-ttu-id="eb792-173">`AccessTheWebAsync` 接著要計算和傳回下載字串的長度，但是方法必須等到有字串時才能計算該值。</span><span class="sxs-lookup"><span data-stu-id="eb792-173">`AccessTheWebAsync` next wants to calculate and return the length of the downloaded string, but the method can't calculate that value until the method has the string.</span></span>

     <span data-ttu-id="eb792-174">因此，`AccessTheWebAsync` 會使用 await 運算子暫停其進度，並將控制權遞交 (Yield) 給呼叫 `AccessTheWebAsync` 的方法。</span><span class="sxs-lookup"><span data-stu-id="eb792-174">Therefore, `AccessTheWebAsync` uses an await operator to suspend its progress and to yield control to the method that called `AccessTheWebAsync`.</span></span> <span data-ttu-id="eb792-175">`AccessTheWebAsync` 會將 `Task<int>` (在 Visual Basic 中為 `Task(Of Integer)`) 傳回呼叫端。</span><span class="sxs-lookup"><span data-stu-id="eb792-175">`AccessTheWebAsync` returns a `Task<int>` (`Task(Of Integer)` in Visual Basic) to the caller.</span></span> <span data-ttu-id="eb792-176">這項工作代表承諾會產生相當於下載字串長度的整數結果。</span><span class="sxs-lookup"><span data-stu-id="eb792-176">The task represents a promise to produce an integer result that's the length of the downloaded string.</span></span>

    > [!NOTE]
    > <span data-ttu-id="eb792-177">如果 `GetStringAsync` (和 `getStringTask`) 在 `AccessTheWebAsync` 等候它之前先完成，控制權仍會留在 `AccessTheWebAsync`。</span><span class="sxs-lookup"><span data-stu-id="eb792-177">If `GetStringAsync` (and therefore `getStringTask`) is complete before `AccessTheWebAsync` awaits it, control remains in `AccessTheWebAsync`.</span></span> <span data-ttu-id="eb792-178">如果呼叫的非同步處理序 (`AccessTheWebAsync`) 已完成，而 AccessTheWebSync 無需等候最終結果時，那麼暫停然後再返回 `getStringTask` 就是不必要的。</span><span class="sxs-lookup"><span data-stu-id="eb792-178">The expense of suspending and then returning to `AccessTheWebAsync` would be wasted if the called asynchronous process (`getStringTask`) has already completed and AccessTheWebSync doesn't have to wait for the final result.</span></span>

     <span data-ttu-id="eb792-179">在呼叫端 (在這個範例中是事件處理常式) 內，處理模式會持續進行。</span><span class="sxs-lookup"><span data-stu-id="eb792-179">Inside the caller (the event handler in this example), the processing pattern continues.</span></span> <span data-ttu-id="eb792-180">呼叫端可能會在等候結果之前執行其他不取決於 `AccessTheWebAsync` 之結果的工作，或者呼叫端可能立即等候。</span><span class="sxs-lookup"><span data-stu-id="eb792-180">The caller might do other work that doesn't depend on the result from `AccessTheWebAsync` before awaiting that result, or the caller might await immediately.</span></span>   <span data-ttu-id="eb792-181">事件處理常式會等候 `AccessTheWebAsync`，而 `AccessTheWebAsync` 會等候 `GetStringAsync`。</span><span class="sxs-lookup"><span data-stu-id="eb792-181">The event handler is waiting for `AccessTheWebAsync`, and `AccessTheWebAsync` is waiting for `GetStringAsync`.</span></span>

7. <span data-ttu-id="eb792-182">`GetStringAsync` 完成並產生字串結果。</span><span class="sxs-lookup"><span data-stu-id="eb792-182">`GetStringAsync` completes and produces a string result.</span></span> <span data-ttu-id="eb792-183">字串結果不會依照您預期的方式透過呼叫 `GetStringAsync` 來傳回</span><span class="sxs-lookup"><span data-stu-id="eb792-183">The string result isn't returned by the call to `GetStringAsync` in the way that you might expect.</span></span> <span data-ttu-id="eb792-184">(請記住，這個方法已在步驟 3 傳回工作)。字串結果會改為儲存在表示方法 `getStringTask` 完成的工作中。</span><span class="sxs-lookup"><span data-stu-id="eb792-184">(Remember that the method already returned a task in step 3.) Instead, the string result is stored in the task that represents the completion of the method, `getStringTask`.</span></span> <span data-ttu-id="eb792-185">await 運算子會從 `getStringTask` 擷取結果。</span><span class="sxs-lookup"><span data-stu-id="eb792-185">The await operator retrieves the result from `getStringTask`.</span></span> <span data-ttu-id="eb792-186">指派陳述式會將擷取的結果指派給 `urlContents`。</span><span class="sxs-lookup"><span data-stu-id="eb792-186">The assignment statement assigns the retrieved result to `urlContents`.</span></span>

8. <span data-ttu-id="eb792-187">當 `AccessTheWebAsync` 擁有字串結果時，方法就可以計算字串的長度。</span><span class="sxs-lookup"><span data-stu-id="eb792-187">When `AccessTheWebAsync` has the string result, the method can calculate the length of the string.</span></span> <span data-ttu-id="eb792-188">然後 `AccessTheWebAsync` 的工作也已完成，而且等候事件處理常式可以繼續執行。</span><span class="sxs-lookup"><span data-stu-id="eb792-188">Then the work of `AccessTheWebAsync` is also complete, and the waiting event handler can resume.</span></span> <span data-ttu-id="eb792-189">在本主題最後的完整範例中，您可以確認事件處理常式會擷取並列印長度結果的值。</span><span class="sxs-lookup"><span data-stu-id="eb792-189">In the full example at the end of the topic, you can confirm that the event handler retrieves and prints the value of the length result.</span></span>

<span data-ttu-id="eb792-190">如果您不熟悉非同步程式設計，請花一分鐘思考同步和非同步行為之間的差異。</span><span class="sxs-lookup"><span data-stu-id="eb792-190">If you are new to asynchronous programming, take a minute to consider the difference between synchronous and asynchronous behavior.</span></span> <span data-ttu-id="eb792-191">同步方法會在其工作完成時傳回 (步驟 5)，而非同步方法則會在其工作暫停時傳回工作值 (步驟 3 和步驟 6)。</span><span class="sxs-lookup"><span data-stu-id="eb792-191">A synchronous method returns when its work is complete (step 5), but an async method returns a task value when its work is suspended (steps 3 and 6).</span></span> <span data-ttu-id="eb792-192">當非同步方法最後完成其工作時，工作會標示為已完成，而結果 (如果有的話) 會儲存在工作中。</span><span class="sxs-lookup"><span data-stu-id="eb792-192">When the async method eventually completes its work, the task is marked as completed and the result, if any, is stored in the task.</span></span>

<span data-ttu-id="eb792-193">如需控制流程的詳細資訊，請參閱[非同步程式中的控制流程 (Visual Basic)](control-flow-in-async-programs.md)。</span><span class="sxs-lookup"><span data-stu-id="eb792-193">For more information about control flow, see [Control Flow in Async Programs (Visual Basic)](control-flow-in-async-programs.md).</span></span>

## <a name="BKMK_APIAsyncMethods"></a> <span data-ttu-id="eb792-194">API 非同步方法</span><span class="sxs-lookup"><span data-stu-id="eb792-194">API Async Methods</span></span>

<span data-ttu-id="eb792-195">您可能會想知道哪裡可以找到支援非同步程式設計的方法，例如 `GetStringAsync`。</span><span class="sxs-lookup"><span data-stu-id="eb792-195">You might be wondering where to find methods such as `GetStringAsync` that support async programming.</span></span> <span data-ttu-id="eb792-196">.NET Framework 4.5 或更高版本包含許多可搭配 `Async` 和 `Await` 的成員。</span><span class="sxs-lookup"><span data-stu-id="eb792-196">The .NET Framework 4.5 or higher contains many members that work with `Async` and `Await`.</span></span> <span data-ttu-id="eb792-197">您可以依據附加至成員名稱的 "Async" 尾碼，以及傳回類型 <xref:System.Threading.Tasks.Task> 或 <xref:System.Threading.Tasks.Task%601> 來辨識這些成員。</span><span class="sxs-lookup"><span data-stu-id="eb792-197">You can recognize these members by the "Async" suffix that's attached to the member name and a return type of <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601>.</span></span> <span data-ttu-id="eb792-198">例如，相對於同步方法 <xref:System.IO.Stream.CopyTo%2A>、<xref:System.IO.Stream.Read%2A> 和 <xref:System.IO.Stream.Write%2A>，`System.IO.Stream` 類別也包含一些方法，例如 <xref:System.IO.Stream.CopyToAsync%2A>、<xref:System.IO.Stream.ReadAsync%2A> 和 <xref:System.IO.Stream.WriteAsync%2A>。</span><span class="sxs-lookup"><span data-stu-id="eb792-198">For example, the `System.IO.Stream` class contains methods such as <xref:System.IO.Stream.CopyToAsync%2A>, <xref:System.IO.Stream.ReadAsync%2A>, and <xref:System.IO.Stream.WriteAsync%2A> alongside the synchronous methods <xref:System.IO.Stream.CopyTo%2A>, <xref:System.IO.Stream.Read%2A>, and <xref:System.IO.Stream.Write%2A>.</span></span>

<span data-ttu-id="eb792-199">Windows 執行階段也包含許多您可以在 Windows 應用程式中與 `Async` 和 `Await` 搭配使用的方法。</span><span class="sxs-lookup"><span data-stu-id="eb792-199">The Windows Runtime also contains many methods that you can use with `Async` and `Await` in Windows apps.</span></span> <span data-ttu-id="eb792-200">如需詳細資訊和範例方法，請參閱[在中C#呼叫非同步 api 或 Visual Basic](/windows/uwp/threading-async/call-asynchronous-apis-in-csharp-or-visual-basic)[非同步程式設計（Windows 執行階段應用程式）](https://docs.microsoft.com/previous-versions/windows/apps/hh464924(v=win.10))，以及 @no__t 3WhenAny：在 .NET Framework 和 Windows 執行階段 @ no__t-0 之間銜接。</span><span class="sxs-lookup"><span data-stu-id="eb792-200">For more information and example methods, see [Call asynchronous APIs in C# or Visual Basic](/windows/uwp/threading-async/call-asynchronous-apis-in-csharp-or-visual-basic), [Asynchronous programming (Windows Runtime apps)](https://docs.microsoft.com/previous-versions/windows/apps/hh464924(v=win.10)), and [WhenAny: Bridging between the .NET Framework and the Windows Runtime](https://docs.microsoft.com/previous-versions/visualstudio/visual-studio-2013/jj635140(v=vs.120)).</span></span>

## <a name="BKMK_Threads"></a> <span data-ttu-id="eb792-201">執行緒</span><span class="sxs-lookup"><span data-stu-id="eb792-201">Threads</span></span>

<span data-ttu-id="eb792-202">非同步方法主要做為非封鎖作業使用。</span><span class="sxs-lookup"><span data-stu-id="eb792-202">Async methods are intended to be non-blocking operations.</span></span> <span data-ttu-id="eb792-203">當等候的工作正在執行時，非同步方法中的 @no__t 0 運算式不會封鎖目前的執行緒。</span><span class="sxs-lookup"><span data-stu-id="eb792-203">An `Await` expression in an async method doesn't block the current thread while the awaited task is running.</span></span> <span data-ttu-id="eb792-204">運算式會改為註冊方法的其餘部分做為接續，並將控制權交還給非同步方法的呼叫端。</span><span class="sxs-lookup"><span data-stu-id="eb792-204">Instead, the expression signs up the rest of the method as a continuation and returns control to the caller of the async method.</span></span>

<span data-ttu-id="eb792-205">`Async` 和 `Await` 關鍵字不會導致建立其他執行緒。</span><span class="sxs-lookup"><span data-stu-id="eb792-205">The `Async` and `Await` keywords don't cause additional threads to be created.</span></span> <span data-ttu-id="eb792-206">非同步方法不需要多執行緒處理，因為非同步方法不會在自己的執行緒上執行。</span><span class="sxs-lookup"><span data-stu-id="eb792-206">Async methods don't require multi-threading because an async method doesn't run on its own thread.</span></span> <span data-ttu-id="eb792-207">方法會在目前的同步處理內容執行，而且只有在方法為作用中時才會在執行緒上花費時間。</span><span class="sxs-lookup"><span data-stu-id="eb792-207">The method runs on the current synchronization context and uses time on the thread only when the method is active.</span></span> <span data-ttu-id="eb792-208">您可以使用 <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> 將受限於 CPU 的工作移到背景執行緒，但是背景執行緒無法協助處理正在等待結果產生的處理序。</span><span class="sxs-lookup"><span data-stu-id="eb792-208">You can use <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> to move CPU-bound work to a background thread, but a background thread doesn't help with a process that's just waiting for results to become available.</span></span>

<span data-ttu-id="eb792-209">非同步程式設計的非同步方法幾乎是所有案例的現有方法當中較好的方法。</span><span class="sxs-lookup"><span data-stu-id="eb792-209">The async-based approach to asynchronous programming is preferable to existing approaches in almost every case.</span></span> <span data-ttu-id="eb792-210">特別是，此方法優於 i/o 系結作業的 <xref:System.ComponentModel.BackgroundWorker>，因為程式碼較簡單，而且您不需要防範競爭條件。</span><span class="sxs-lookup"><span data-stu-id="eb792-210">In particular, this approach is better than <xref:System.ComponentModel.BackgroundWorker> for I/O-bound operations because the code is simpler and you don't have to guard against race conditions.</span></span> <span data-ttu-id="eb792-211">與 <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> 結合時，非同步程式設計會比受限於 CPU 之作業的 <xref:System.ComponentModel.BackgroundWorker> 還要好，因為非同步程式設計會將執行程式碼的協調工作細節，從 `Task.Run` 傳輸至執行緒集區的工作中分出來。</span><span class="sxs-lookup"><span data-stu-id="eb792-211">In combination with <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType>, async programming is better than <xref:System.ComponentModel.BackgroundWorker> for CPU-bound operations because async programming separates the coordination details of running your code from the work that `Task.Run` transfers to the threadpool.</span></span>

## <a name="BKMK_AsyncandAwait"></a> <span data-ttu-id="eb792-212">Async 和 Await</span><span class="sxs-lookup"><span data-stu-id="eb792-212">Async and Await</span></span>

<span data-ttu-id="eb792-213">如果您使用 [Async](../../../../visual-basic/language-reference/modifiers/async.md) 修飾詞來將方法指定為非同步方法，就會啟用下列兩項功能。</span><span class="sxs-lookup"><span data-stu-id="eb792-213">If you specify that a method is an async method by using an [Async](../../../../visual-basic/language-reference/modifiers/async.md) modifier, you enable the following two capabilities.</span></span>

- <span data-ttu-id="eb792-214">標記的非同步方法可以使用 [Await](../../../../visual-basic/language-reference/operators/await-operator.md) 來指定暫停點。</span><span class="sxs-lookup"><span data-stu-id="eb792-214">The marked async method can use [Await](../../../../visual-basic/language-reference/operators/await-operator.md) to designate suspension points.</span></span> <span data-ttu-id="eb792-215">await 運算子會告知編譯器，非同步方法只有在等候的非同步處理序完成後，才能繼續通過該點。</span><span class="sxs-lookup"><span data-stu-id="eb792-215">The await operator tells the compiler that the async method can't continue past that point until the awaited asynchronous process is complete.</span></span> <span data-ttu-id="eb792-216">同時，控制權會返回非同步方法的呼叫端。</span><span class="sxs-lookup"><span data-stu-id="eb792-216">In the meantime, control returns to the caller of the async method.</span></span>

  <span data-ttu-id="eb792-217">非同步方法在 @no__t 0 的運算式暫止不會構成從方法結束，而且不會執行 @no__t 1 區塊。</span><span class="sxs-lookup"><span data-stu-id="eb792-217">The suspension of an async method at an `Await` expression doesn't constitute an exit from the method, and `Finally` blocks don't run.</span></span>

- <span data-ttu-id="eb792-218">標記的非同步方法本身可以做為其呼叫方法的等候目標。</span><span class="sxs-lookup"><span data-stu-id="eb792-218">The marked async method can itself be awaited by methods that call it.</span></span>

<span data-ttu-id="eb792-219">非同步方法通常包含一或多個 `Await` 運算子的出現，但缺少 @no__t 1 運算式不會造成編譯器錯誤。</span><span class="sxs-lookup"><span data-stu-id="eb792-219">An async method typically contains one or more occurrences of an `Await` operator, but the absence of `Await` expressions doesn't cause a compiler error.</span></span> <span data-ttu-id="eb792-220">如果非同步方法未使用 `Await` 運算子來標記暫停點，則即使 `Async` 修飾詞，方法還是會當做同步方法來執行。</span><span class="sxs-lookup"><span data-stu-id="eb792-220">If an async method doesn't use an `Await` operator to mark a suspension point, the method executes as a synchronous method does, despite the `Async` modifier.</span></span> <span data-ttu-id="eb792-221">編譯器將對這類方法發出警告。</span><span class="sxs-lookup"><span data-stu-id="eb792-221">The compiler issues a warning for such methods.</span></span>

<span data-ttu-id="eb792-222">`Async` 和 `Await` 都是內容關鍵字。</span><span class="sxs-lookup"><span data-stu-id="eb792-222">`Async` and `Await` are contextual keywords.</span></span> <span data-ttu-id="eb792-223">如需詳細資訊和範例，請參閱下列主題：</span><span class="sxs-lookup"><span data-stu-id="eb792-223">For more information and examples, see the following topics:</span></span>

- [<span data-ttu-id="eb792-224">Async</span><span class="sxs-lookup"><span data-stu-id="eb792-224">Async</span></span>](../../../../visual-basic/language-reference/modifiers/async.md)
- [<span data-ttu-id="eb792-225">Await 運算子</span><span class="sxs-lookup"><span data-stu-id="eb792-225">Await Operator</span></span>](../../../../visual-basic/language-reference/operators/await-operator.md)

## <a name="BKMK_ReturnTypesandParameters"></a> <span data-ttu-id="eb792-226">傳回型別和參數</span><span class="sxs-lookup"><span data-stu-id="eb792-226">Return types and parameters</span></span>

<span data-ttu-id="eb792-227">在 .NET Framework 程式設計中，非同步方法通常會傳回 <xref:System.Threading.Tasks.Task> 或 <xref:System.Threading.Tasks.Task%601>。</span><span class="sxs-lookup"><span data-stu-id="eb792-227">In .NET Framework programming, an async method typically returns a <xref:System.Threading.Tasks.Task> or a <xref:System.Threading.Tasks.Task%601>.</span></span> <span data-ttu-id="eb792-228">在非同步方法內，會將 `Await` 運算子套用到呼叫另一個非同步方法所傳回的工作。</span><span class="sxs-lookup"><span data-stu-id="eb792-228">Inside an async method, an `Await` operator is applied to a task that's returned from a call to another async method.</span></span>

<span data-ttu-id="eb792-229">如果方法包含指定 `TResult` 類型運算元的 [Return](../../../../visual-basic/language-reference/statements/return-statement.md) 陳述式，請指定 <xref:System.Threading.Tasks.Task%601> 作為傳回型別。</span><span class="sxs-lookup"><span data-stu-id="eb792-229">You specify <xref:System.Threading.Tasks.Task%601> as the return type if the method contains a [Return](../../../../visual-basic/language-reference/statements/return-statement.md) statement that specifies an operand of type `TResult`.</span></span>

<span data-ttu-id="eb792-230">如果方法沒有 return 陳述式，或者方法的 return 陳述式不會傳回運算元，請使用 `Task` 做為傳回類型。</span><span class="sxs-lookup"><span data-stu-id="eb792-230">You use `Task` as the return type if the method has no return statement or has a return statement that doesn't return an operand.</span></span>

<span data-ttu-id="eb792-231">下列範例顯示如何宣告和呼叫傳回 <xref:System.Threading.Tasks.Task%601> 或 <xref:System.Threading.Tasks.Task> 的方法：</span><span class="sxs-lookup"><span data-stu-id="eb792-231">The following example shows how you declare and call a method that returns a <xref:System.Threading.Tasks.Task%601> or a <xref:System.Threading.Tasks.Task>:</span></span>

```vb
' Signature specifies Task(Of Integer)
Async Function TaskOfTResult_MethodAsync() As Task(Of Integer)

    Dim hours As Integer
    ' . . .
    ' Return statement specifies an integer result.
    Return hours
End Function

' Calls to TaskOfTResult_MethodAsync
Dim returnedTaskTResult As Task(Of Integer) = TaskOfTResult_MethodAsync()
Dim intResult As Integer = Await returnedTaskTResult
' or, in a single statement
Dim intResult As Integer = Await TaskOfTResult_MethodAsync()

' Signature specifies Task
Async Function Task_MethodAsync() As Task

    ' . . .
    ' The method has no return statement.
End Function

' Calls to Task_MethodAsync
Task returnedTask = Task_MethodAsync()
Await returnedTask
' or, in a single statement
Await Task_MethodAsync()
```

<span data-ttu-id="eb792-232">每項傳回的工作都代表進行中的工作。</span><span class="sxs-lookup"><span data-stu-id="eb792-232">Each returned task represents ongoing work.</span></span> <span data-ttu-id="eb792-233">工作會封裝這個非同步處理序狀態的相關資訊，以及處理序的最終結果，或是處理序不成功時，則會封裝處理序引發的例外狀況。</span><span class="sxs-lookup"><span data-stu-id="eb792-233">A task encapsulates information about the state of the asynchronous process and, eventually, either the final result from the process or the exception that the process raises if it doesn't succeed.</span></span>

<span data-ttu-id="eb792-234">非同步方法也可以是 `Sub` 方法。</span><span class="sxs-lookup"><span data-stu-id="eb792-234">An async method can also be a `Sub` method.</span></span> <span data-ttu-id="eb792-235">這個傳回型別主要用於定義需要傳回型別的事件處理常式。</span><span class="sxs-lookup"><span data-stu-id="eb792-235">This return type is used primarily to define event handlers, where a return type is required.</span></span> <span data-ttu-id="eb792-236">非同步事件處理常式通常做為非同步程式的起點。</span><span class="sxs-lookup"><span data-stu-id="eb792-236">Async event handlers often serve as the starting point for async programs.</span></span>

<span data-ttu-id="eb792-237">不能等候為 @no__t 0 程式的非同步方法，而且呼叫端無法攔截方法擲回的任何例外狀況。</span><span class="sxs-lookup"><span data-stu-id="eb792-237">An async method that's a `Sub` procedure can't be awaited, and the caller can't catch any exceptions that the method throws.</span></span>

<span data-ttu-id="eb792-238">非同步方法無法宣告 [ByRef](../../../../visual-basic/language-reference/modifiers/byref.md) 參數，但是此方法可以呼叫具有這類參數的方法。</span><span class="sxs-lookup"><span data-stu-id="eb792-238">An async method can't declare [ByRef](../../../../visual-basic/language-reference/modifiers/byref.md) parameters, but the method can call methods that have such parameters.</span></span>

<span data-ttu-id="eb792-239">如需詳細資訊和範例，請參閱[非同步傳回型別 (Visual Basic)](async-return-types.md)。</span><span class="sxs-lookup"><span data-stu-id="eb792-239">For more information and examples, see [Async Return Types (Visual Basic)](async-return-types.md).</span></span> <span data-ttu-id="eb792-240">如需如何在非同步方法中攔截例外狀況的詳細資訊，請參閱 [Try...Catch...Finally 陳述式](../../../../visual-basic/language-reference/statements/try-catch-finally-statement.md)。</span><span class="sxs-lookup"><span data-stu-id="eb792-240">For more information about how to catch exceptions in async methods, see [Try...Catch...Finally Statement](../../../../visual-basic/language-reference/statements/try-catch-finally-statement.md).</span></span>

<span data-ttu-id="eb792-241">Windows 執行階段程式設計中的非同步 API 具有下列其中一種傳回型別 (類似於工作)：</span><span class="sxs-lookup"><span data-stu-id="eb792-241">Asynchronous APIs in Windows Runtime programming have one of the following return types, which are similar to tasks:</span></span>

- <span data-ttu-id="eb792-242"><xref:Windows.Foundation.IAsyncOperation%601>，對應至 <xref:System.Threading.Tasks.Task%601></span><span class="sxs-lookup"><span data-stu-id="eb792-242"><xref:Windows.Foundation.IAsyncOperation%601>, which corresponds to <xref:System.Threading.Tasks.Task%601></span></span>
- <span data-ttu-id="eb792-243"><xref:Windows.Foundation.IAsyncAction>，對應至 <xref:System.Threading.Tasks.Task></span><span class="sxs-lookup"><span data-stu-id="eb792-243"><xref:Windows.Foundation.IAsyncAction>, which corresponds to <xref:System.Threading.Tasks.Task></span></span>
- <xref:Windows.Foundation.IAsyncActionWithProgress%601>
- <xref:Windows.Foundation.IAsyncOperationWithProgress%602>

<span data-ttu-id="eb792-244">如需詳細資訊和範例，請參閱[在或 Visual Basic C#中呼叫非同步 api](/windows/uwp/threading-async/call-asynchronous-apis-in-csharp-or-visual-basic)。</span><span class="sxs-lookup"><span data-stu-id="eb792-244">For more information and an example, see [Call asynchronous APIs in C# or Visual Basic](/windows/uwp/threading-async/call-asynchronous-apis-in-csharp-or-visual-basic).</span></span>

## <a name="BKMK_NamingConvention"></a> <span data-ttu-id="eb792-245">命名慣例</span><span class="sxs-lookup"><span data-stu-id="eb792-245">Naming convention</span></span>

<span data-ttu-id="eb792-246">依照慣例，您會將 "Async" 附加至具有 `Async` 修飾詞的方法名稱。</span><span class="sxs-lookup"><span data-stu-id="eb792-246">By convention, you append "Async" to the names of methods that have an `Async` modifier.</span></span>

<span data-ttu-id="eb792-247">當事件、基底類別或介面合約採用不同的名稱時，您可以忽略慣例。</span><span class="sxs-lookup"><span data-stu-id="eb792-247">You can ignore the convention where an event, base class, or interface contract suggests a different name.</span></span> <span data-ttu-id="eb792-248">例如，您不應該重新命名一般事件處理常式，例如 `Button1_Click`。</span><span class="sxs-lookup"><span data-stu-id="eb792-248">For example, you shouldn't rename common event handlers, such as `Button1_Click`.</span></span>

## <a name="BKMK_RelatedTopics"></a> <span data-ttu-id="eb792-249">相關主題和範例 (Visual Studio)</span><span class="sxs-lookup"><span data-stu-id="eb792-249">Related topics and samples (Visual Studio)</span></span>

|<span data-ttu-id="eb792-250">標題</span><span class="sxs-lookup"><span data-stu-id="eb792-250">Title</span></span>|<span data-ttu-id="eb792-251">說明</span><span class="sxs-lookup"><span data-stu-id="eb792-251">Description</span></span>|<span data-ttu-id="eb792-252">範例</span><span class="sxs-lookup"><span data-stu-id="eb792-252">Sample</span></span>|
|-----------|-----------------|------------|
|[<span data-ttu-id="eb792-253">逐步解說：使用 Async 和 Await 存取 Web （Visual Basic） </span><span class="sxs-lookup"><span data-stu-id="eb792-253">Walkthrough: Accessing the Web by Using Async and Await (Visual Basic)</span></span>](walkthrough-accessing-the-web-by-using-async-and-await.md)|<span data-ttu-id="eb792-254">顯示如何將同步 WPF 方案轉換為非同步 WPF 方案。</span><span class="sxs-lookup"><span data-stu-id="eb792-254">Shows how to convert a synchronous WPF solution to an asynchronous WPF solution.</span></span> <span data-ttu-id="eb792-255">應用程式會下載一系列的網站。</span><span class="sxs-lookup"><span data-stu-id="eb792-255">The application downloads a series of websites.</span></span>|<span data-ttu-id="eb792-256">[非同步範例：存取 Web 逐步解說](https://code.msdn.microsoft.com/Async-Sample-Accessing-the-9c10497f) \(英文\)</span><span class="sxs-lookup"><span data-stu-id="eb792-256">[Async Sample: Accessing the Web Walkthrough](https://code.msdn.microsoft.com/Async-Sample-Accessing-the-9c10497f)</span></span>|
|<span data-ttu-id="eb792-257">[如何：使用 System.threading.tasks.task.whenall （Visual Basic） ](how-to-extend-the-async-walkthrough-by-using-task-whenall.md) 擴充非同步逐步解說</span><span class="sxs-lookup"><span data-stu-id="eb792-257">[How to: Extend the Async Walkthrough by Using Task.WhenAll (Visual Basic)](how-to-extend-the-async-walkthrough-by-using-task-whenall.md)</span></span>|<span data-ttu-id="eb792-258">將 <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType> 加入至前一個逐步解說。</span><span class="sxs-lookup"><span data-stu-id="eb792-258">Adds <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType> to the previous walkthrough.</span></span> <span data-ttu-id="eb792-259">使用 `WhenAll` 會同時開始進行所有的下載。</span><span class="sxs-lookup"><span data-stu-id="eb792-259">The use of `WhenAll` starts all the downloads at the same time.</span></span>||
|[<span data-ttu-id="eb792-260">如何：使用 Async 和 Await （Visual Basic），同時發出多個 Web 要求 </span><span class="sxs-lookup"><span data-stu-id="eb792-260">How to: Make Multiple Web Requests in Parallel by Using Async and Await (Visual Basic)</span></span>](how-to-make-multiple-web-requests-in-parallel-by-using-async-and-await.md)|<span data-ttu-id="eb792-261">示範如何同時啟動數個工作。</span><span class="sxs-lookup"><span data-stu-id="eb792-261">Demonstrates how to start several tasks at the same time.</span></span>|[<span data-ttu-id="eb792-262">非同步範例：以平行方式提出多個 Web 要求</span><span class="sxs-lookup"><span data-stu-id="eb792-262">Async Sample: Make Multiple Web Requests in Parallel</span></span>](https://code.msdn.microsoft.com/Async-Make-Multiple-Web-49adb82e)|
|[<span data-ttu-id="eb792-263">非同步方法的傳回型別 (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="eb792-263">Async Return Types (Visual Basic)</span></span>](async-return-types.md)|<span data-ttu-id="eb792-264">說明非同步方法可以傳回的類型，並解釋每種類型的適用時機。</span><span class="sxs-lookup"><span data-stu-id="eb792-264">Illustrates the types that async methods can return and explains when each type is appropriate.</span></span>||
|[<span data-ttu-id="eb792-265">非同步程式中的控制流程 (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="eb792-265">Control Flow in Async Programs (Visual Basic)</span></span>](control-flow-in-async-programs.md)|<span data-ttu-id="eb792-266">在非同步程式中詳細追蹤一連串 await 運算式的控制流程。</span><span class="sxs-lookup"><span data-stu-id="eb792-266">Traces in detail the flow of control through a succession of await expressions in an asynchronous program.</span></span>|[<span data-ttu-id="eb792-267">非同步範例：非同步程式中的控制流程</span><span class="sxs-lookup"><span data-stu-id="eb792-267">Async Sample: Control Flow in Async Programs</span></span>](https://code.msdn.microsoft.com/Async-Sample-Control-Flow-5c804fc0)|
|[<span data-ttu-id="eb792-268">微調非同步應用程式 (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="eb792-268">Fine-Tuning Your Async Application (Visual Basic)</span></span>](fine-tuning-your-async-application.md)|<span data-ttu-id="eb792-269">顯示如何將下列功能加入至您的非同步方案：</span><span class="sxs-lookup"><span data-stu-id="eb792-269">Shows how to add the following functionality to your async solution:</span></span><br /><br /> <span data-ttu-id="eb792-270">- [取消一項非同步工作或工作清單 (Visual Basic)](../../../../visual-basic/programming-guide/concepts/async/cancel-an-async-task-or-a-list-of-tasks.md)</span><span class="sxs-lookup"><span data-stu-id="eb792-270">- [Cancel an Async Task or a List of Tasks (Visual Basic)](../../../../visual-basic/programming-guide/concepts/async/cancel-an-async-task-or-a-list-of-tasks.md)</span></span><br /><span data-ttu-id="eb792-271">- [在一段時間後取消非同步工作 (Visual Basic)](cancel-async-tasks-after-a-period-of-time.md)</span><span class="sxs-lookup"><span data-stu-id="eb792-271">- [Cancel Async Tasks after a Period of Time (Visual Basic)](cancel-async-tasks-after-a-period-of-time.md)</span></span><br /><span data-ttu-id="eb792-272">- [當其中一項工作完成時，取消剩餘的非同步工作 (Visual Basic)](cancel-remaining-async-tasks-after-one-is-complete.md)</span><span class="sxs-lookup"><span data-stu-id="eb792-272">- [Cancel Remaining Async Tasks after One Is Complete (Visual Basic)](cancel-remaining-async-tasks-after-one-is-complete.md)</span></span><br /><span data-ttu-id="eb792-273">- [啟動多項非同步工作並在它們完成時進行處理 (Visual Basic)](start-multiple-async-tasks-and-process-them-as-they-complete.md)</span><span class="sxs-lookup"><span data-stu-id="eb792-273">- [Start Multiple Async Tasks and Process Them As They Complete (Visual Basic)](start-multiple-async-tasks-and-process-them-as-they-complete.md)</span></span>|[<span data-ttu-id="eb792-274">非同步範例：微調您的應用程式</span><span class="sxs-lookup"><span data-stu-id="eb792-274">Async Sample: Fine Tuning Your Application</span></span>](https://code.msdn.microsoft.com/Async-Fine-Tuning-Your-a676abea)|
|[<span data-ttu-id="eb792-275">處理非同步應用程式中的重新進入 (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="eb792-275">Handling Reentrancy in Async Apps (Visual Basic)</span></span>](handling-reentrancy-in-async-apps.md)|<span data-ttu-id="eb792-276">示範如何處理使用中的非同步作業在執行時重新開機的情況。</span><span class="sxs-lookup"><span data-stu-id="eb792-276">Shows how to handle cases in which an active asynchronous operation is restarted while it's running.</span></span>||
|<span data-ttu-id="eb792-277">[WhenAny：銜接 .NET Framework 和 Windows 執行階段](https://docs.microsoft.com/previous-versions/visualstudio/visual-studio-2013/jj635140(v=vs.120))</span><span class="sxs-lookup"><span data-stu-id="eb792-277">[WhenAny: Bridging between the .NET Framework and the Windows Runtime](https://docs.microsoft.com/previous-versions/visualstudio/visual-studio-2013/jj635140(v=vs.120))</span></span>|<span data-ttu-id="eb792-278">顯示如何在 Windows 執行階段中進行 .NET Framework 與 IAsyncOperations 之工作型別之間的橋接，讓您可以搭配使用 <xref:System.Threading.Tasks.Task.WhenAny%2A> 與 Windows 執行階段方法。</span><span class="sxs-lookup"><span data-stu-id="eb792-278">Shows how to bridge between Task types in the .NET Framework and IAsyncOperations in the Windows Runtime so that you can use <xref:System.Threading.Tasks.Task.WhenAny%2A> with a Windows Runtime method.</span></span>|<span data-ttu-id="eb792-279">[非同步範例：銜接 .NET 和 Windows 執行階段 (AsTask 和 WhenAny)](https://docs.microsoft.com/previous-versions/visualstudio/visual-studio-2013/jj635140(v=vs.120)) \(英文\)</span><span class="sxs-lookup"><span data-stu-id="eb792-279">[Async Sample: Bridging between .NET and Windows Runtime (AsTask and WhenAny)](https://docs.microsoft.com/previous-versions/visualstudio/visual-studio-2013/jj635140(v=vs.120))</span></span>|
|<span data-ttu-id="eb792-280">非同步取消：銜接 .NET Framework 和 Windows 執行階段</span><span class="sxs-lookup"><span data-stu-id="eb792-280">Async Cancellation: Bridging between the .NET Framework and the Windows Runtime</span></span>|<span data-ttu-id="eb792-281">顯示如何在 Windows 執行階段中進行 .NET Framework 與 IAsyncOperations 之工作型別之間的橋接，讓您可以搭配使用 <xref:System.Threading.CancellationTokenSource> 與 Windows 執行階段方法。</span><span class="sxs-lookup"><span data-stu-id="eb792-281">Shows how to bridge between Task types in the .NET Framework and IAsyncOperations in the Windows Runtime so that you can use <xref:System.Threading.CancellationTokenSource> with a Windows Runtime method.</span></span>|[<span data-ttu-id="eb792-282">非同步範例：銜接 .NET 和 Windows 執行階段 (AsTask & Cancellation) (英文)</span><span class="sxs-lookup"><span data-stu-id="eb792-282">Async Sample: Bridging between .NET and Windows Runtime (AsTask & Cancellation)</span></span>](https://code.msdn.microsoft.com/Async-Sample-Bridging-9479eca3)|
|[<span data-ttu-id="eb792-283">使用非同步方式存取檔案 (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="eb792-283">Using Async for File Access (Visual Basic)</span></span>](using-async-for-file-access.md)|<span data-ttu-id="eb792-284">列出並示範使用 async 和 await 存取檔案的優點。</span><span class="sxs-lookup"><span data-stu-id="eb792-284">Lists and demonstrates the benefits of using async and await to access files.</span></span>||
|[<span data-ttu-id="eb792-285">工作式非同步模式 (TAP)</span><span class="sxs-lookup"><span data-stu-id="eb792-285">Task-based Asynchronous Pattern (TAP)</span></span>](../../../../standard/asynchronous-programming-patterns/task-based-asynchronous-pattern-tap.md)|<span data-ttu-id="eb792-286">描述 .NET Framework 中非同步的新模式。</span><span class="sxs-lookup"><span data-stu-id="eb792-286">Describes a new pattern for asynchrony in the .NET Framework.</span></span> <span data-ttu-id="eb792-287">這個模式是根據 <xref:System.Threading.Tasks.Task> 和 <xref:System.Threading.Tasks.Task%601> 類型。</span><span class="sxs-lookup"><span data-stu-id="eb792-287">The pattern is based on the <xref:System.Threading.Tasks.Task> and <xref:System.Threading.Tasks.Task%601> types.</span></span>||
|[<span data-ttu-id="eb792-288">Channel 9 上的非同步影片</span><span class="sxs-lookup"><span data-stu-id="eb792-288">Async Videos on Channel 9</span></span>](https://channel9.msdn.com/search?term=async+&type=All)|<span data-ttu-id="eb792-289">提供有關非同步程式設計的各種不同視訊連結。</span><span class="sxs-lookup"><span data-stu-id="eb792-289">Provides links to a variety of videos about async programming.</span></span>||

## <a name="BKMK_CompleteExample"></a> <span data-ttu-id="eb792-290">完整範例</span><span class="sxs-lookup"><span data-stu-id="eb792-290">Complete Example</span></span>

<span data-ttu-id="eb792-291">下列程式碼是本主題討論之 Windows Presentation Foundation (WPF) 應用程式中的 MainWindow.xaml.vb 檔案。</span><span class="sxs-lookup"><span data-stu-id="eb792-291">The following code is the MainWindow.xaml.vb file from the Windows Presentation Foundation (WPF) application that this topic discusses.</span></span> <span data-ttu-id="eb792-292">您可以從[非同步範例：＜使用 Async 和 Await 進行非同步程式設計＞中的範例](https://docs.microsoft.com/samples/dotnet/samples/async-and-await-vb/) \(英文\) 下載範例。</span><span class="sxs-lookup"><span data-stu-id="eb792-292">You can download the sample from [Async Sample: Example from "Asynchronous Programming with Async and Await"](https://docs.microsoft.com/samples/dotnet/samples/async-and-await-vb/).</span></span>

[!code-vb[async](~/samples/async/async-and-await/vb/MainWindow.xaml.vb)]

## <a name="see-also"></a><span data-ttu-id="eb792-293">另請參閱</span><span class="sxs-lookup"><span data-stu-id="eb792-293">See also</span></span>

- [<span data-ttu-id="eb792-294">Await 運算子</span><span class="sxs-lookup"><span data-stu-id="eb792-294">Await Operator</span></span>](../../../../visual-basic/language-reference/operators/await-operator.md)
- [<span data-ttu-id="eb792-295">Async</span><span class="sxs-lookup"><span data-stu-id="eb792-295">Async</span></span>](../../../../visual-basic/language-reference/modifiers/async.md)
