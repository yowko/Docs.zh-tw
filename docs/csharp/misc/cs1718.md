---
title: "編譯器警告 (層級 3) CS1718"
ms.date: 07/20/2015
ms.prod: .net
ms.technology:
- devlang-csharp
ms.topic: article
f1_keywords:
- CS1718
helpviewer_keywords:
- CS1718
ms.assetid: 7c1c11fd-4f91-482d-b8f7-efe2a361634e
caps.latest.revision: 
author: BillWagner
ms.author: wiwagn
ms.openlocfilehash: 6a49a97470e6a53d8fe4c8931da87ba25f7e3696
ms.sourcegitcommit: 83dd5ec003e788ccb3e33f3412a7af39ae347646
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 03/15/2018
---
# <a name="compiler-warning-level-3-cs1718"></a><span data-ttu-id="63cf4-102">編譯器警告 (層級 3) CS1718</span><span class="sxs-lookup"><span data-stu-id="63cf4-102">Compiler Warning (level 3) CS1718</span></span>
<span data-ttu-id="63cf4-103">對同一個變數進行比較; 您是否想要比較別的東西?</span><span class="sxs-lookup"><span data-stu-id="63cf4-103">Comparison made to same variable; did you mean to compare something else?</span></span>  
  
 <span data-ttu-id="63cf4-104">如果您想要與其他項目相比較，您應該只要更正陳述式。</span><span class="sxs-lookup"><span data-stu-id="63cf4-104">If you meant to compare to something else, then you should simply correct the statement.</span></span>  
  
 <span data-ttu-id="63cf4-105">但另一種可能性是您在測試為 true 或 false，並使用陳述式例如 `if (a == a) (true)` 或 `if (a < a) (false)`進行。</span><span class="sxs-lookup"><span data-stu-id="63cf4-105">But another possibility is that you were testing for true or false, and were doing so by statements such as `if (a == a) (true)` or `if (a < a) (false)`.</span></span> <span data-ttu-id="63cf4-106">最好只使用 `if (true)` 或 `if (false)`。</span><span class="sxs-lookup"><span data-stu-id="63cf4-106">It is better to simply say `if (true)` or `if (false)`.</span></span> <span data-ttu-id="63cf4-107">不這麼做的原因有兩個：</span><span class="sxs-lookup"><span data-stu-id="63cf4-107">There are two reasons for this:</span></span>  
  
-   <span data-ttu-id="63cf4-108">它比較簡單：只說您心中所指的永遠比較清楚。</span><span class="sxs-lookup"><span data-stu-id="63cf4-108">It is simpler: it is always clearer to simply say what you mean.</span></span>  
  
-   <span data-ttu-id="63cf4-109">有助於避免混淆：C# 2.0 的一項新功能是可為 Null 的實值類型，類似於 T-SQL (SQL Server 所使用的程式設計語言) 中的 `null` 值。</span><span class="sxs-lookup"><span data-stu-id="63cf4-109">It helps avoid confusion: a new feature of C# 2.0 is nullable value types, which are analogous to the value `null` in T-SQL, the programming language used by SQL Server.</span></span> <span data-ttu-id="63cf4-110">熟悉 T-SQL 的開發人員可能會擔心可為 Null 的類型對例如 `if (a == a)`的運算式有什麼影響，因為在 T-SQL 中使用三元邏輯。</span><span class="sxs-lookup"><span data-stu-id="63cf4-110">Developers familiar with T-SQL might be concerned about the effect of nullable types on expressions such as `if (a == a)`, because of the use of ternary logic in T-SQL.</span></span> <span data-ttu-id="63cf4-111">如果您使用 `true` 或 `false`，即可避免可能的混淆。</span><span class="sxs-lookup"><span data-stu-id="63cf4-111">If you use `true` or `false`, you avoid this possible confusion.</span></span>  
  
## <a name="example"></a><span data-ttu-id="63cf4-112">範例</span><span class="sxs-lookup"><span data-stu-id="63cf4-112">Example</span></span>  
 <span data-ttu-id="63cf4-113">下列程式碼會產生警告 CS1718：</span><span class="sxs-lookup"><span data-stu-id="63cf4-113">The following code generates warning CS1718.</span></span>  
  
```csharp  
// CS1718.cs  
using System;  
public class Tester   
{  
    public static void Main()   
    {   
        int i = 0;  
        if (i == i) { // CS1718.cs  
        //if (true) {   
            i++;  
        }  
    }  
}  
```
