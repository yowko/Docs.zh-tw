---
title: "編譯器警告 (層級 3) CS1717"
ms.date: 07/20/2015
ms.prod: .net
ms.technology: devlang-csharp
ms.topic: article
f1_keywords: CS1717
helpviewer_keywords: CS1717
ms.assetid: 5b150a2c-5d61-4cd8-b4d4-e6c2b93b52c6
caps.latest.revision: "14"
author: BillWagner
ms.author: wiwagn
ms.openlocfilehash: 72ded024603e425b68ede6593fceee723e26a865
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 10/18/2017
---
# <a name="compiler-warning-level-3-cs1717"></a><span data-ttu-id="c461e-102">編譯器警告 (層級 3) CS1717</span><span class="sxs-lookup"><span data-stu-id="c461e-102">Compiler Warning (level 3) CS1717</span></span>
<span data-ttu-id="c461e-103">對同一個變數進行指派; 您是否想要指派別的東西?</span><span class="sxs-lookup"><span data-stu-id="c461e-103">Assignment made to same variable; did you mean to assign something else?</span></span>  
  
 <span data-ttu-id="c461e-104">當您將變數指派給它本身時，例如 `a = a`，就會出現這個警告。</span><span class="sxs-lookup"><span data-stu-id="c461e-104">This warning occurs when you assign a variable to itself, such as `a = a`.</span></span>  
  
 <span data-ttu-id="c461e-105">幾個常見的錯誤都會產生這個警告：</span><span class="sxs-lookup"><span data-stu-id="c461e-105">Several common mistakes can generate this warning:</span></span>  
  
-   <span data-ttu-id="c461e-106">將 `a = a` 寫成 **if** 陳述式的條件，例如 `if (a = a)`，就會出現這個警告。</span><span class="sxs-lookup"><span data-stu-id="c461e-106">Writing `a = a` as the condition of an **if** statement, such as `if (a = a)`.</span></span> <span data-ttu-id="c461e-107">您的意思可能是 `if (a == a)`，這一律為 true，所以您可以寫成更簡潔的 `if (true)`。</span><span class="sxs-lookup"><span data-stu-id="c461e-107">You probably meant to say `if (a == a)`, which is always true, so you could write this more concisely as `if (true)`.</span></span>  
  
-   <span data-ttu-id="c461e-108">拼錯字。</span><span class="sxs-lookup"><span data-stu-id="c461e-108">Mistyping.</span></span> <span data-ttu-id="c461e-109">您的意思可能是 `a = b`。</span><span class="sxs-lookup"><span data-stu-id="c461e-109">You probably meant to say `a = b`.</span></span>  
  
-   <span data-ttu-id="c461e-110">在參數和欄位同名的建構函式中，不要使用 **this** 關鍵字：您的意思可能是 `this.a = a`，就會出現這個警告。</span><span class="sxs-lookup"><span data-stu-id="c461e-110">In a constructor where the parameter has the same name as the field, not using the **this** keyword: you probably meant to say `this.a = a`.</span></span>  
  
## <a name="example"></a><span data-ttu-id="c461e-111">範例</span><span class="sxs-lookup"><span data-stu-id="c461e-111">Example</span></span>  
 <span data-ttu-id="c461e-112">下列範例會產生 CS1717。</span><span class="sxs-lookup"><span data-stu-id="c461e-112">The following sample generates CS1717.</span></span>  
  
```  
// CS1717.cs  
// compile with: /W:3  
public class Test  
{  
   public static void Main()  
   {  
      int x = 0;  
      x = x;   // CS1717  
   }  
}  
```
