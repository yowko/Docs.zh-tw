---
description: 編譯器警告 (層級 2) CS0278
title: 編譯器警告 (層級 2) CS0278
ms.date: 07/20/2015
f1_keywords:
- CS0278
helpviewer_keywords:
- CS0278
ms.assetid: 5418cbbe-bcec-4379-a6f6-410987beb96a
ms.openlocfilehash: 9ecb413746d940b4f3ba44a289d0709aa56f30a5
ms.sourcegitcommit: d579fb5e4b46745fd0f1f8874c94c6469ce58604
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 08/30/2020
ms.locfileid: "89143268"
---
# <a name="compiler-warning-level-2-cs0278"></a><span data-ttu-id="dbffa-103">編譯器警告 (層級 2) CS0278</span><span class="sxs-lookup"><span data-stu-id="dbffa-103">Compiler Warning (level 2) CS0278</span></span>
<span data-ttu-id="dbffa-104">'type' 未實作 'pattern name' 模式。</span><span class="sxs-lookup"><span data-stu-id="dbffa-104">'type' does not implement the 'pattern name' pattern.</span></span> <span data-ttu-id="dbffa-105">'method name' 與 'method name' 模稜兩可。</span><span class="sxs-lookup"><span data-stu-id="dbffa-105">'method name' is ambiguous with 'method name'.</span></span>  
  
<span data-ttu-id="dbffa-106">C# 中有幾個依賴已定義模式的陳述式，例如 `foreach` 和 `using`。</span><span class="sxs-lookup"><span data-stu-id="dbffa-106">There are several statements in C# that rely on defined patterns, such as `foreach` and `using`.</span></span> <span data-ttu-id="dbffa-107">例如， [ `foreach` 語句](../language-reference/keywords/foreach-in.md)依賴實作為「可列舉」模式的集合類別。</span><span class="sxs-lookup"><span data-stu-id="dbffa-107">For example, the [`foreach` statement](../language-reference/keywords/foreach-in.md) relies on the collection class implementing the "enumerable" pattern.</span></span>
  
<span data-ttu-id="dbffa-108">如果編譯器因為模稜兩可而無法比對，就可能發生 CS0278。</span><span class="sxs-lookup"><span data-stu-id="dbffa-108">CS0278 can occur if the compiler is unable to make the match due to ambiguities.</span></span> <span data-ttu-id="dbffa-109">比方說，「可列舉」模式要求具有稱為 `MoveNext`的方法，而您的程式碼可能包含稱為 `MoveNext`的兩個方法。</span><span class="sxs-lookup"><span data-stu-id="dbffa-109">For example, the "enumerable" pattern requires that there be a method called `MoveNext`, and your code might contain two methods called `MoveNext`.</span></span> <span data-ttu-id="dbffa-110">編譯器會嘗試尋找一個介面來使用，但仍然建議您判斷並解決模稜兩可的原因。</span><span class="sxs-lookup"><span data-stu-id="dbffa-110">The compiler will attempt to find an interface to use, but it is recommended that you determine and resolve the cause of the ambiguity.</span></span>  
  
## <a name="example"></a><span data-ttu-id="dbffa-111">範例</span><span class="sxs-lookup"><span data-stu-id="dbffa-111">Example</span></span>  
 <span data-ttu-id="dbffa-112">下列範例會產生 CS0278：</span><span class="sxs-lookup"><span data-stu-id="dbffa-112">The following sample generates CS0278.</span></span>  
  
```csharp  
// CS0278.cs  
using System.Collections.Generic;  
public class myTest
{  
   public static void TestForeach<W>(W w)
      where W: IEnumerable<int>, IEnumerable<string>  
   {  
      foreach (int i in w) {}   // CS0278  
   }  
}  
```
