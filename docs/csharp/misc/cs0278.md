---
description: 編譯器警告 (層級 2) CS0278
title: 編譯器警告 (層級 2) CS0278
ms.date: 07/20/2015
f1_keywords:
- CS0278
helpviewer_keywords:
- CS0278
ms.assetid: 5418cbbe-bcec-4379-a6f6-410987beb96a
ms.openlocfilehash: 4536b143a39f5640dec9b2a5a4a781d810b28fb2
ms.sourcegitcommit: 5b475c1855b32cf78d2d1bbb4295e4c236f39464
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 09/24/2020
ms.locfileid: "91177198"
---
# <a name="compiler-warning-level-2-cs0278"></a><span data-ttu-id="5ca1b-103">編譯器警告 (層級 2) CS0278</span><span class="sxs-lookup"><span data-stu-id="5ca1b-103">Compiler Warning (level 2) CS0278</span></span>

<span data-ttu-id="5ca1b-104">'type' 未實作 'pattern name' 模式。</span><span class="sxs-lookup"><span data-stu-id="5ca1b-104">'type' does not implement the 'pattern name' pattern.</span></span> <span data-ttu-id="5ca1b-105">'method name' 與 'method name' 模稜兩可。</span><span class="sxs-lookup"><span data-stu-id="5ca1b-105">'method name' is ambiguous with 'method name'.</span></span>  
  
<span data-ttu-id="5ca1b-106">C# 中有幾個依賴已定義模式的陳述式，例如 `foreach` 和 `using`。</span><span class="sxs-lookup"><span data-stu-id="5ca1b-106">There are several statements in C# that rely on defined patterns, such as `foreach` and `using`.</span></span> <span data-ttu-id="5ca1b-107">例如， [ `foreach` 語句](../language-reference/keywords/foreach-in.md)依賴實作為「可列舉」模式的集合類別。</span><span class="sxs-lookup"><span data-stu-id="5ca1b-107">For example, the [`foreach` statement](../language-reference/keywords/foreach-in.md) relies on the collection class implementing the "enumerable" pattern.</span></span>
  
<span data-ttu-id="5ca1b-108">如果編譯器因為模稜兩可而無法比對，就可能發生 CS0278。</span><span class="sxs-lookup"><span data-stu-id="5ca1b-108">CS0278 can occur if the compiler is unable to make the match due to ambiguities.</span></span> <span data-ttu-id="5ca1b-109">比方說，「可列舉」模式要求具有稱為 `MoveNext`的方法，而您的程式碼可能包含稱為 `MoveNext`的兩個方法。</span><span class="sxs-lookup"><span data-stu-id="5ca1b-109">For example, the "enumerable" pattern requires that there be a method called `MoveNext`, and your code might contain two methods called `MoveNext`.</span></span> <span data-ttu-id="5ca1b-110">編譯器會嘗試尋找一個介面來使用，但仍然建議您判斷並解決模稜兩可的原因。</span><span class="sxs-lookup"><span data-stu-id="5ca1b-110">The compiler will attempt to find an interface to use, but it is recommended that you determine and resolve the cause of the ambiguity.</span></span>  
  
## <a name="example"></a><span data-ttu-id="5ca1b-111">範例</span><span class="sxs-lookup"><span data-stu-id="5ca1b-111">Example</span></span>  

 <span data-ttu-id="5ca1b-112">下列範例會產生 CS0278：</span><span class="sxs-lookup"><span data-stu-id="5ca1b-112">The following sample generates CS0278.</span></span>  
  
```csharp  
// CS0278.cs  
using System.Collections.Generic;  
public class myTest
{  
   public static void TestForeach<W>(W w)
      where W: IEnumerable<int>, IEnumerable<string>  
   {  
      foreach (int i in w) {}   // CS0278  
   }  
}  
```
