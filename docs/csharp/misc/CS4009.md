---
title: 編譯器錯誤 CS4009
ms.date: 08/25/2018
f1_keywords:
- CS4009
helpviewer_keywords:
- CS4009
ms.openlocfilehash: 176ed2ed64fdcf50b7822a44be25e01c658e105a
ms.sourcegitcommit: 559fcfbe4871636494870a8b716bf7325df34ac5
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 10/30/2019
ms.locfileid: "73100927"
---
# <a name="compiler-error-cs4009"></a><span data-ttu-id="d1b55-102">編譯器錯誤 CS4009</span><span class="sxs-lookup"><span data-stu-id="d1b55-102">Compiler Error CS4009</span></span>

<span data-ttu-id="d1b55-103">' Type. Method '：進入點不能以 `async` 修飾詞標記。</span><span class="sxs-lookup"><span data-stu-id="d1b55-103">'Type.Method': an entry point cannot be marked with the `async` modifier.</span></span>

<span data-ttu-id="d1b55-104">您不能在應用程式進入點（通常是 `Main` 方法）中使用 `async` 關鍵字。</span><span class="sxs-lookup"><span data-stu-id="d1b55-104">You cannot use the `async` keyword in the application entry point (typically the `Main` method).</span></span>

> [!IMPORTANT]
> <span data-ttu-id="d1b55-105">從C# 7.1 開始，`Main` 方法可以有 `async` 修飾詞。</span><span class="sxs-lookup"><span data-stu-id="d1b55-105">Starting with C# 7.1, the `Main` method can have an `async` modifier.</span></span> <span data-ttu-id="d1b55-106">如需詳細資訊，請參閱[7.1 的新功能文章C#中](../whats-new/csharp-7-1.md)的[Async main](../whats-new/csharp-7-1.md#async-main)一節。</span><span class="sxs-lookup"><span data-stu-id="d1b55-106">For more information, see the [Async main](../whats-new/csharp-7-1.md#async-main) section of the [What's new in C# 7.1](../whats-new/csharp-7-1.md) article.</span></span> <span data-ttu-id="d1b55-107">如需有關如何選取C#語言版本的詳細資訊，請參閱[選取C#語言版本](../language-reference/configure-language-version.md)一文。</span><span class="sxs-lookup"><span data-stu-id="d1b55-107">For information about how to select the C# language version, see the [Select the C# language version](../language-reference/configure-language-version.md) article.</span></span>

## <a name="example"></a><span data-ttu-id="d1b55-108">範例</span><span class="sxs-lookup"><span data-stu-id="d1b55-108">Example</span></span>

<span data-ttu-id="d1b55-109">下列範例會產生 CS4009：</span><span class="sxs-lookup"><span data-stu-id="d1b55-109">The following example produces CS4009:</span></span>

```csharp
using System;
using System.Threading.Tasks;

public class Example
{
    public static async void Main()
    {
        Console.WriteLine("About to wait two seconds");
        await WaitTwoSeconds();
        Console.WriteLine("About to exit the program");
    }

    private static async Task WaitTwoSeconds()
    {
        await Task.Delay(2000);
        Console.WriteLine("Returning from an asynchronous method");
    } 
}
```

## <a name="to-correct-this-error"></a><span data-ttu-id="d1b55-110">更正這個錯誤</span><span class="sxs-lookup"><span data-stu-id="d1b55-110">To correct this error</span></span>

<span data-ttu-id="d1b55-111">將專案[所使用的C#語言版本更新](../language-reference/configure-language-version.md)為7.1 或更高版本。</span><span class="sxs-lookup"><span data-stu-id="d1b55-111">[Update the C# language version](../language-reference/configure-language-version.md) used by the project to 7.1 or higher.</span></span>

<span data-ttu-id="d1b55-112">如果您使用C# 7.0 或更低版本，請從應用程式進入點的簽章中移除 `async` 關鍵字。</span><span class="sxs-lookup"><span data-stu-id="d1b55-112">If you use C# 7.0 or lower, remove the `async` keyword from the signature of the application entry point.</span></span> <span data-ttu-id="d1b55-113">同時移除您在應用程式進入點中用來等待非同步方法的任何 `await` 關鍵字。</span><span class="sxs-lookup"><span data-stu-id="d1b55-113">Also remove any `await` keywords you've used to await asynchronous methods in your application entry point.</span></span> 

<span data-ttu-id="d1b55-114">不過，您仍然必須等待非同步方法完成，您的進入點才會繼續執行。</span><span class="sxs-lookup"><span data-stu-id="d1b55-114">However, you still have to wait for the asynchronous method to complete before your entry point resumes execution.</span></span> <span data-ttu-id="d1b55-115">否則，編譯會產生編譯器警告 CS4014，而應用程式會在非同步作業完成之前終止。</span><span class="sxs-lookup"><span data-stu-id="d1b55-115">Otherwise, compilation generates compiler warning CS4014, and the application will terminate before the asynchronous operation completes.</span></span> <span data-ttu-id="d1b55-116">下列範例說明此問題：</span><span class="sxs-lookup"><span data-stu-id="d1b55-116">The following example illustrates this problem:</span></span>

[!code-csharp[CS4009](~/samples/snippets/csharp/misc/cs4009-1.cs)]

<span data-ttu-id="d1b55-117">若要等候傳回 <xref:System.Threading.Tasks.Task>的方法，請呼叫它的 <xref:System.Threading.Tasks.Task.Wait%2A> 方法，如下列範例所示：</span><span class="sxs-lookup"><span data-stu-id="d1b55-117">To await a method that returns a <xref:System.Threading.Tasks.Task>, call its <xref:System.Threading.Tasks.Task.Wait%2A> method, as the following example illustrates:</span></span>

[!code-csharp[CS4009](~/samples/snippets/csharp/misc/cs4009-2.cs)]

<span data-ttu-id="d1b55-118">若要等候傳回 <xref:System.Threading.Tasks.Task%601>的方法，請取出其 <xref:System.Threading.Tasks.Task%601.Result> 屬性的值，如下列範例所示：</span><span class="sxs-lookup"><span data-stu-id="d1b55-118">To await a method that returns a <xref:System.Threading.Tasks.Task%601>, retrieve the value of its <xref:System.Threading.Tasks.Task%601.Result> property, as the following example does:</span></span>

[!code-csharp[CS4009](~/samples/snippets/csharp/misc/cs4009-3.cs)]

## <a name="see-also"></a><span data-ttu-id="d1b55-119">請參閱</span><span class="sxs-lookup"><span data-stu-id="d1b55-119">See also</span></span>

- [<span data-ttu-id="d1b55-120">`async` 關鍵字</span><span class="sxs-lookup"><span data-stu-id="d1b55-120">`async` keyword</span></span>](../language-reference/keywords/async.md)
- [<span data-ttu-id="d1b55-121">`await` 運算子</span><span class="sxs-lookup"><span data-stu-id="d1b55-121">`await` operator</span></span>](../language-reference/operators/await.md)
