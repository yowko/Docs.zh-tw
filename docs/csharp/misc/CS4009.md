---
title: 編譯器錯誤 CS4009
ms.date: 08/25/2018
f1_keywords:
- CS4009
helpviewer_keywords:
- CS4009
ms.openlocfilehash: 4c171997845e1e14aeb1193c9db1c7ef4adc40f2
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 03/12/2020
ms.locfileid: "79173324"
---
# <a name="compiler-error-cs4009"></a><span data-ttu-id="e6f16-102">編譯器錯誤 CS4009</span><span class="sxs-lookup"><span data-stu-id="e6f16-102">Compiler Error CS4009</span></span>

<span data-ttu-id="e6f16-103">"類型.方法"：進入點不能用`async`修飾符標記。</span><span class="sxs-lookup"><span data-stu-id="e6f16-103">'Type.Method': an entry point cannot be marked with the `async` modifier.</span></span>

<span data-ttu-id="e6f16-104">不能在`async`應用程式進入點（通常是`Main`方法）中使用關鍵字。</span><span class="sxs-lookup"><span data-stu-id="e6f16-104">You cannot use the `async` keyword in the application entry point (typically the `Main` method).</span></span>

> [!IMPORTANT]
> <span data-ttu-id="e6f16-105">從 C# 7.1`Main`開始，該方法可以`async`具有修改器。</span><span class="sxs-lookup"><span data-stu-id="e6f16-105">Starting with C# 7.1, the `Main` method can have an `async` modifier.</span></span> <span data-ttu-id="e6f16-106">有關詳細資訊，請參閱[C# 7.1 文章中新增功能的](../whats-new/csharp-7-1.md) [Async 主](../whats-new/csharp-7-1.md#async-main)部分。</span><span class="sxs-lookup"><span data-stu-id="e6f16-106">For more information, see the [Async main](../whats-new/csharp-7-1.md#async-main) section of the [What's new in C# 7.1](../whats-new/csharp-7-1.md) article.</span></span> <span data-ttu-id="e6f16-107">有關如何選擇 C# 語言版本的資訊，請參閱[選擇 C# 語言版本](../language-reference/configure-language-version.md)一文。</span><span class="sxs-lookup"><span data-stu-id="e6f16-107">For information about how to select the C# language version, see the [Select the C# language version](../language-reference/configure-language-version.md) article.</span></span>

## <a name="example"></a><span data-ttu-id="e6f16-108">範例</span><span class="sxs-lookup"><span data-stu-id="e6f16-108">Example</span></span>

<span data-ttu-id="e6f16-109">以下示例生成 CS4009：</span><span class="sxs-lookup"><span data-stu-id="e6f16-109">The following example produces CS4009:</span></span>

```csharp
using System;
using System.Threading.Tasks;

public class Example
{
    public static async void Main()
    {
        Console.WriteLine("About to wait two seconds");
        await WaitTwoSeconds();
        Console.WriteLine("About to exit the program");
    }

    private static async Task WaitTwoSeconds()
    {
        await Task.Delay(2000);
        Console.WriteLine("Returning from an asynchronous method");
    }
}
```

## <a name="to-correct-this-error"></a><span data-ttu-id="e6f16-110">更正這個錯誤</span><span class="sxs-lookup"><span data-stu-id="e6f16-110">To correct this error</span></span>

<span data-ttu-id="e6f16-111">將專案使用的[C# 語言版本更新](../language-reference/configure-language-version.md)為 7.1 或更高版本。</span><span class="sxs-lookup"><span data-stu-id="e6f16-111">[Update the C# language version](../language-reference/configure-language-version.md) used by the project to 7.1 or higher.</span></span>

<span data-ttu-id="e6f16-112">如果使用 C# 7.0 或更低，請`async`從應用程式進入點的簽名中刪除關鍵字。</span><span class="sxs-lookup"><span data-stu-id="e6f16-112">If you use C# 7.0 or lower, remove the `async` keyword from the signature of the application entry point.</span></span> <span data-ttu-id="e6f16-113">還要刪除應用程式`await`進入點中用於等待非同步方法的任何關鍵字。</span><span class="sxs-lookup"><span data-stu-id="e6f16-113">Also remove any `await` keywords you've used to await asynchronous methods in your application entry point.</span></span>

<span data-ttu-id="e6f16-114">但是，在進入點恢復執行之前，您仍必須等待非同步方法完成。</span><span class="sxs-lookup"><span data-stu-id="e6f16-114">However, you still have to wait for the asynchronous method to complete before your entry point resumes execution.</span></span> <span data-ttu-id="e6f16-115">否則，編譯將生成編譯器警告 CS4014，應用程式將在非同步作業完成之前終止。</span><span class="sxs-lookup"><span data-stu-id="e6f16-115">Otherwise, compilation generates compiler warning CS4014, and the application will terminate before the asynchronous operation completes.</span></span> <span data-ttu-id="e6f16-116">以下示例說明了此問題：</span><span class="sxs-lookup"><span data-stu-id="e6f16-116">The following example illustrates this problem:</span></span>

[!code-csharp[CS4009](~/samples/snippets/csharp/misc/cs4009-1.cs)]

<span data-ttu-id="e6f16-117">要等待返回<xref:System.Threading.Tasks.Task>的方法，請調用其<xref:System.Threading.Tasks.Task.Wait%2A>方法，如下例所示：</span><span class="sxs-lookup"><span data-stu-id="e6f16-117">To await a method that returns a <xref:System.Threading.Tasks.Task>, call its <xref:System.Threading.Tasks.Task.Wait%2A> method, as the following example illustrates:</span></span>

[!code-csharp[CS4009](~/samples/snippets/csharp/misc/cs4009-2.cs)]

<span data-ttu-id="e6f16-118">若要等待返回 的方法<xref:System.Threading.Tasks.Task%601>，檢索其<xref:System.Threading.Tasks.Task%601.Result>屬性的值，如下例所示：</span><span class="sxs-lookup"><span data-stu-id="e6f16-118">To await a method that returns a <xref:System.Threading.Tasks.Task%601>, retrieve the value of its <xref:System.Threading.Tasks.Task%601.Result> property, as the following example does:</span></span>

[!code-csharp[CS4009](~/samples/snippets/csharp/misc/cs4009-3.cs)]

## <a name="see-also"></a><span data-ttu-id="e6f16-119">另請參閱</span><span class="sxs-lookup"><span data-stu-id="e6f16-119">See also</span></span>

- [<span data-ttu-id="e6f16-120">`async`關鍵 字</span><span class="sxs-lookup"><span data-stu-id="e6f16-120">`async` keyword</span></span>](../language-reference/keywords/async.md)
- [<span data-ttu-id="e6f16-121">`await`運算元</span><span class="sxs-lookup"><span data-stu-id="e6f16-121">`await` operator</span></span>](../language-reference/operators/await.md)
