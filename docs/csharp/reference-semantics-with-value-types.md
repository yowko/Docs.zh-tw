---
title: 具備實值型別的參考語意
description: 了解最小化安全地複製結構的語言功能
ms.date: 11/10/2017
ms.custom: mvc
ms.openlocfilehash: f241219994d7a03192a4aea69b912bf1ac5ed29c
ms.sourcegitcommit: 5bbfe34a9a14e4ccb22367e57b57585c208cf757
ms.translationtype: HT
ms.contentlocale: zh-TW
ms.lasthandoff: 09/18/2018
ms.locfileid: "45964693"
---
# <a name="reference-semantics-with-value-types"></a><span data-ttu-id="df285-103">具備實值型別的參考語意</span><span class="sxs-lookup"><span data-stu-id="df285-103">Reference semantics with value types</span></span>

<span data-ttu-id="df285-104">使用實值型別的優點是他們通常可避免堆積配置。</span><span class="sxs-lookup"><span data-stu-id="df285-104">An advantage to using value types is that they often avoid heap allocations.</span></span>
<span data-ttu-id="df285-105">相對地，缺點則是他們根據實值複製。</span><span class="sxs-lookup"><span data-stu-id="df285-105">The disadvantage is that they are copied by value.</span></span> <span data-ttu-id="df285-106">這種兩難的情況使得最佳化針對大量資料進行運作的演算法，變得更加困難。</span><span class="sxs-lookup"><span data-stu-id="df285-106">This tradeoff makes it harder to optimize algorithms that operate on large amounts of data.</span></span> <span data-ttu-id="df285-107">C# 7.2 中新的語言功能提供一項新的機制，可對實值型別使用利用參考傳遞的語意。</span><span class="sxs-lookup"><span data-stu-id="df285-107">New language features in C# 7.2 provide mechanisms that enable pass-by-reference semantics with value types.</span></span> <span data-ttu-id="df285-108">若能善用這些功能，即可同時最小化配置及複製作業。</span><span class="sxs-lookup"><span data-stu-id="df285-108">Use these features wisely to minimize both allocations and copy operations.</span></span> <span data-ttu-id="df285-109">本文會探討這些新功能。</span><span class="sxs-lookup"><span data-stu-id="df285-109">This article explores those new features.</span></span>

<span data-ttu-id="df285-110">文中有許多程式碼範例示範有 C# 7.2 的新增功能。</span><span class="sxs-lookup"><span data-stu-id="df285-110">Much of the sample code in this article demonstrates features added in C# 7.2.</span></span> <span data-ttu-id="df285-111">若要使用這些功能，必須在您的專專案設為使用 C# 7.2 或更新版本。</span><span class="sxs-lookup"><span data-stu-id="df285-111">In order to use those features, you must configure your project to use C# 7.2 or later.</span></span> <span data-ttu-id="df285-112">如需設定語言版本的詳細資訊，請參閱[設定語言版本](language-reference/configure-language-version.md)。</span><span class="sxs-lookup"><span data-stu-id="df285-112">For more information on setting the language version see [configure the language version](language-reference/configure-language-version.md).</span></span>

## <a name="passing-arguments-by-readonly-reference"></a><span data-ttu-id="df285-113">以唯讀參考傳遞引數</span><span class="sxs-lookup"><span data-stu-id="df285-113">Passing arguments by readonly reference</span></span>

<span data-ttu-id="df285-114">C# 7.2 新增了 `in` 關鍵字來補充現有的 `ref` 與 `out` 關鍵字，以傳遞依照參考的引數。</span><span class="sxs-lookup"><span data-stu-id="df285-114">C# 7.2 adds the `in` keyword to complement the existing `ref` and `out` keywords to pass arguments by reference.</span></span> <span data-ttu-id="df285-115">`in` 關鍵字會根據參考指定傳遞引數，但呼叫的方法不會修改值。</span><span class="sxs-lookup"><span data-stu-id="df285-115">The `in` keyword specifies passing the argument by reference, but the called method does not modify the value.</span></span> 

<span data-ttu-id="df285-116">該新增功能提供完整的詞彙能表達您的設計目的。</span><span class="sxs-lookup"><span data-stu-id="df285-116">This addition provides a full vocabulary to express your design intent.</span></span> <span data-ttu-id="df285-117">當您未在下列方法簽章中指定下列任一修飾詞時，會在傳遞至呼叫的方法時，複製實值型別。</span><span class="sxs-lookup"><span data-stu-id="df285-117">Value types are copied when passed to a called method when you don't specify any of the following modifiers in the method signature.</span></span> <span data-ttu-id="df285-118">每個修飾詞皆會指定要利用參考傳遞實值型別，避免複製。</span><span class="sxs-lookup"><span data-stu-id="df285-118">Each of these modifiers specifies that a value type is passed by reference, avoiding the copy.</span></span> <span data-ttu-id="df285-119">每個修飾詞皆表示不同之目的：</span><span class="sxs-lookup"><span data-stu-id="df285-119">Each modifier expresses a different intent:</span></span>

- <span data-ttu-id="df285-120">`out`：此方法會設定用作為此參數的引數值。</span><span class="sxs-lookup"><span data-stu-id="df285-120">`out`: This method sets the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="df285-121">`ref`：此方法會設定用作為此參數的引數值。</span><span class="sxs-lookup"><span data-stu-id="df285-121">`ref`: This method may set the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="df285-122">`in`：此方法不會修改用作為此參數的引數值。</span><span class="sxs-lookup"><span data-stu-id="df285-122">`in`: This method does not modify the value of the argument used as this parameter.</span></span>

<span data-ttu-id="df285-123">當您新增 `in` 修飾詞來利用參考傳遞引數時，即表明您的設計目的是利用參考傳遞引數，來避免不必要的複製。</span><span class="sxs-lookup"><span data-stu-id="df285-123">Add the `in` modifier to pass an argument by reference and declare your design intent to pass arguments by reference to avoid unnecessary copying.</span></span> <span data-ttu-id="df285-124">您並不想修改用作為該引數的物件。</span><span class="sxs-lookup"><span data-stu-id="df285-124">You do not intend to modify the object used as that argument.</span></span> <span data-ttu-id="df285-125">下列程式碼示範計算 3D 空間中不同兩點間距離的方法。</span><span class="sxs-lookup"><span data-stu-id="df285-125">The following code shows an example of a method that calculates the distance between two points in 3D space.</span></span> 

[!code-csharp[InArgument](../../samples/csharp/reference-semantics/Program.cs#InArgument "Specifying an In argument")]

<span data-ttu-id="df285-126">引數為雙結構，每個結構皆包含三個雙精度浮點數。</span><span class="sxs-lookup"><span data-stu-id="df285-126">The arguments are two structures that each contain three doubles.</span></span> <span data-ttu-id="df285-127">一個雙精度浮點數是 8 個位元組，因此每個引數是 24 個位元組。</span><span class="sxs-lookup"><span data-stu-id="df285-127">A double is 8 bytes, so each argument is 24 bytes.</span></span> <span data-ttu-id="df285-128">透過指定 `in` 修飾詞，將 4 或 8 個位元組參考傳遞到這些引數，位元組大小取決於電腦的架構。</span><span class="sxs-lookup"><span data-stu-id="df285-128">By specifying the `in` modifier, you pass a 4-byte or 8-byte reference to those arguments, depending on the architecture of the machine.</span></span> <span data-ttu-id="df285-129">位元組大小的差異很小，但是當您的應用程式使用許多不同的值在緊密迴圈中呼叫此方法時，差異會快速加大。</span><span class="sxs-lookup"><span data-stu-id="df285-129">The difference in size is small, but it can quickly add up when your application calls this method in a tight loop using many different values.</span></span>
 
<span data-ttu-id="df285-130">`in` 修飾詞也可於其他方面補足 `out` 和 `ref`。</span><span class="sxs-lookup"><span data-stu-id="df285-130">The `in` modifier complements `out` and `ref` in other ways as well.</span></span> <span data-ttu-id="df285-131">您無法為差異處只有是否出現 `in`、`out` 或 `ref` 的方法，建立多載。</span><span class="sxs-lookup"><span data-stu-id="df285-131">You cannot create overloads of a method that differ only in the presence of `in`, `out`, or `ref`.</span></span> <span data-ttu-id="df285-132">這些新規則沿用一直以來為 `out` 和 `ref` 參數所定義的相同行為。</span><span class="sxs-lookup"><span data-stu-id="df285-132">These new rules extend the same behavior that had always been defined for `out` and `ref` parameters.</span></span>

<span data-ttu-id="df285-133">`in` 修飾詞可套用至任何使用下列參數的成員：方法、委派、Lambda、區域函式、索引子、運算子。</span><span class="sxs-lookup"><span data-stu-id="df285-133">The `in` modifier may be applied to any member that takes parameters: methods, delegates, lambdas, local functions, indexers, operators.</span></span>

<span data-ttu-id="df285-134">不同於 `ref` 和 `out` 引數，您可以對 `in` 參數的引數使用常值或常數。</span><span class="sxs-lookup"><span data-stu-id="df285-134">Unlike `ref` and `out` arguments, you may use literal values or constants for the argument to an `in` parameter.</span></span> <span data-ttu-id="df285-135">此外，不同於 `ref` 或 `out` 參數，您也不需要在呼叫位置套用 `in` 修飾詞。</span><span class="sxs-lookup"><span data-stu-id="df285-135">Also, unlike a `ref` or `out` parameter, you don't need to apply the `in` modifier at the call site.</span></span> <span data-ttu-id="df285-136">下列程式碼示範兩種呼叫 `CalculateDistance` 方法的範例。</span><span class="sxs-lookup"><span data-stu-id="df285-136">The following code shows you two examples of calling the `CalculateDistance` method.</span></span> <span data-ttu-id="df285-137">第一種使用兩個利用參考傳遞的區域變數。</span><span class="sxs-lookup"><span data-stu-id="df285-137">The first uses two local variables passed by reference.</span></span> <span data-ttu-id="df285-138">第二種則包含建立為方法呼叫之一部份的暫存變數。</span><span class="sxs-lookup"><span data-stu-id="df285-138">The second includes a temporary variable created as part of the method call.</span></span> 

[!code-csharp[UseInArgument](../../samples/csharp/reference-semantics/Program.cs#UseInArgument "Specifying an In argument")]

<span data-ttu-id="df285-139">編譯器確保強制讓 `in` 引數為唯讀性質的方式有數種。</span><span class="sxs-lookup"><span data-stu-id="df285-139">There are several ways in which the compiler ensures that the read-only nature of an `in` argument is enforced.</span></span>  <span data-ttu-id="df285-140">首先，呼叫的方法不可直接指派到 `in` 參數。</span><span class="sxs-lookup"><span data-stu-id="df285-140">First of all, the called method can't directly assign to an `in` parameter.</span></span> <span data-ttu-id="df285-141">當該值為 `struct` 類型時，該方法不可直接指派到 `in` 參數的任何欄位。</span><span class="sxs-lookup"><span data-stu-id="df285-141">It can't directly assign to any field of an `in` parameter when that value is a `struct` type.</span></span> <span data-ttu-id="df285-142">此外，您也無法使用 `ref` 或 `out` 修飾詞將 `in` 參數傳遞至任何方法。</span><span class="sxs-lookup"><span data-stu-id="df285-142">In addition, you cannot pass an `in` parameter to any method using the `ref` or `out` modifier.</span></span>
<span data-ttu-id="df285-143">這些規則適用於所有 `in` 參數的欄位，提供的欄位為 `struct` 類型，且參數也為 `struct` 類型。</span><span class="sxs-lookup"><span data-stu-id="df285-143">These rules apply to any field of an `in` parameter, provided the field is a `struct` type and the parameter is also a `struct` type.</span></span> <span data-ttu-id="df285-144">實際上，這些規則適用於成員存取的多個層級，提供所有成員存取層級的類型為 `structs`。</span><span class="sxs-lookup"><span data-stu-id="df285-144">In fact, these rules apply for multiple layers of member access provided the types at all levels of member access are `structs`.</span></span> <span data-ttu-id="df285-145">編譯器會強制 `struct` 類型作為 `in` 引數傳遞，且其 `struct` 成員在作為引數對其他方法使用時為唯讀變數。</span><span class="sxs-lookup"><span data-stu-id="df285-145">The compiler enforces that `struct` types passed as  `in` arguments and their `struct` members are read-only variables when used as arguments to other methods.</span></span>

<span data-ttu-id="df285-146">使用 `in` 參數可避免製作複本可能產生的效能成本。</span><span class="sxs-lookup"><span data-stu-id="df285-146">The use of `in` parameters avoids the potential performance costs of making copies.</span></span> <span data-ttu-id="df285-147">這不會變更任何方法呼叫的語意。</span><span class="sxs-lookup"><span data-stu-id="df285-147">It does not change the semantics of any method call.</span></span> <span data-ttu-id="df285-148">因此，您不需要指定呼叫位置的 `in` 修飾詞。</span><span class="sxs-lookup"><span data-stu-id="df285-148">Therefore, you do not need to specify the `in` modifier at the call site.</span></span> <span data-ttu-id="df285-149">但是，忽略呼叫位置的 `in` 修飾詞會告知編譯器可以下列理由製作引數的複本：</span><span class="sxs-lookup"><span data-stu-id="df285-149">However, omitting the `in` modifier at the call site informs the compiler that it is allowed to make a copy of the argument for the following reasons:</span></span>

- <span data-ttu-id="df285-150">引數類型對參數類型有隱含轉換但沒有身分識別轉換。</span><span class="sxs-lookup"><span data-stu-id="df285-150">There is an implicit conversion but not an identity conversion from the argument type to the parameter type.</span></span>
- <span data-ttu-id="df285-151">引數為運算式但不具有已知的儲存體變數。</span><span class="sxs-lookup"><span data-stu-id="df285-151">The argument is an expression but does not have a known storage variable.</span></span>
- <span data-ttu-id="df285-152">存在受 `in` 存在與否影響的多載。</span><span class="sxs-lookup"><span data-stu-id="df285-152">An overload exists that differs by the presence or absence of `in`.</span></span> <span data-ttu-id="df285-153">在此情況下，傳值多載是最佳相符項目。</span><span class="sxs-lookup"><span data-stu-id="df285-153">In that case, the by value overload is a better match.</span></span>

<span data-ttu-id="df285-154">當您更新現有的程式碼以使用唯讀參考引數時，這些規則相當實用。</span><span class="sxs-lookup"><span data-stu-id="df285-154">These rules are useful as you update existing code to use read-only reference arguments.</span></span> <span data-ttu-id="df285-155">在呼叫的方法中，您可呼叫所有使用傳值參數的執行個體方法。</span><span class="sxs-lookup"><span data-stu-id="df285-155">Inside the called method, you can call any instance method that uses by value parameters.</span></span> <span data-ttu-id="df285-156">在那些執行個體中，會建立 `in` 參數的複本。</span><span class="sxs-lookup"><span data-stu-id="df285-156">In those instances, a copy of the `in` parameter is created.</span></span> <span data-ttu-id="df285-157">因為編譯器可為任何 `in` 參數建立暫存變數，所以您也可以為任何 `in` 參數指定預設值。</span><span class="sxs-lookup"><span data-stu-id="df285-157">Because the compiler can create a temporary variable for any `in` parameter, you can also specify default values for any `in` parameter.</span></span> <span data-ttu-id="df285-158">下列程式碼會將原點 (點 0,0) 指定為第二個點的預設值：</span><span class="sxs-lookup"><span data-stu-id="df285-158">The following code specifies the origin (point 0,0) as the default value for the second point:</span></span>

[!code-csharp[InArgumentDefault](../../samples/csharp/reference-semantics/Program.cs#InArgumentDefault "Specifying defaults for an in parameter")]

<span data-ttu-id="df285-159">若要強制編譯器依參考傳遞唯讀引數，請在呼叫位置指定引數上的 `in` 修飾詞，如下列程式碼所示：</span><span class="sxs-lookup"><span data-stu-id="df285-159">To force the compiler to pass read only arguments by reference, specify the `in` modifer on the arguments at the call site, as shown in the following code:</span></span>

[!code-csharp[UseInArgument](../../samples/csharp/reference-semantics/Program.cs#ExplicitInArgument "Specifying an In argument")]

<span data-ttu-id="df285-160">此行為能在可提升效能時，使在大型程式碼基底中採用 `in` 參數一段時間更加輕鬆。</span><span class="sxs-lookup"><span data-stu-id="df285-160">This behavior makes it easier to adopt `in` parameters over time in large codebases where performance gains are possible.</span></span> <span data-ttu-id="df285-161">您必須先將 `in` 修飾詞新增至方法簽章。</span><span class="sxs-lookup"><span data-stu-id="df285-161">You add the `in` modifier to method signatures first.</span></span> <span data-ttu-id="df285-162">隨後便可在呼叫位置新增 `in` 修飾詞並建立 `readonly struct` 類型，以避免編譯器在更多位置建立 `in` 參數的防禦性複本。</span><span class="sxs-lookup"><span data-stu-id="df285-162">Then, you can add the `in` modifier at callsites and create `readonly struct` types to enable the compiler to avoid creating defensive copies of `in` parameters in more locations.</span></span>

<span data-ttu-id="df285-163">參考型別或數值也可使用 `in` 參數指定。</span><span class="sxs-lookup"><span data-stu-id="df285-163">The `in` parameter designation can also be used with reference types or numeric values.</span></span> <span data-ttu-id="df285-164">但這兩種用法的優點皆不彰顯 (若有的話)。</span><span class="sxs-lookup"><span data-stu-id="df285-164">However, the benefits in both cases are minimal, if any.</span></span>

## <a name="ref-readonly-returns"></a><span data-ttu-id="df285-165">`ref readonly` 傳回</span><span class="sxs-lookup"><span data-stu-id="df285-165">`ref readonly` returns</span></span>

<span data-ttu-id="df285-166">同時也建議您利用參考傳回實值型別，但不允許呼叫端修改該值。</span><span class="sxs-lookup"><span data-stu-id="df285-166">You may also want to return a value type by reference, but disallow the caller from modifying that value.</span></span> <span data-ttu-id="df285-167">請使用 `ref readonly` 修飾詞來表示該設計目的。</span><span class="sxs-lookup"><span data-stu-id="df285-167">Use the `ref readonly` modifier to express that design intent.</span></span> <span data-ttu-id="df285-168">該修飾詞會通知讀取器，目前正在將參考傳回現有資料，但不允許修改。</span><span class="sxs-lookup"><span data-stu-id="df285-168">It notifies readers that you are returning a reference to existing data, but not allowing modification.</span></span> 

<span data-ttu-id="df285-169">編譯器會強制呼叫端無法修改該參考。</span><span class="sxs-lookup"><span data-stu-id="df285-169">The compiler enforces that the caller cannot modify the reference.</span></span> <span data-ttu-id="df285-170">若嘗試直接指派到值，則會產生編譯時間錯誤。</span><span class="sxs-lookup"><span data-stu-id="df285-170">Attempts to assign the value directly generate a compile-time error.</span></span> <span data-ttu-id="df285-171">但編譯器無法知道是否有任何成員方法修改了該結構的狀態。</span><span class="sxs-lookup"><span data-stu-id="df285-171">However, the compiler cannot know if any member method modifies the state of the struct.</span></span>
<span data-ttu-id="df285-172">為確保物件未經修改，編譯器會建立複本，並使用該複本呼叫成員參考。</span><span class="sxs-lookup"><span data-stu-id="df285-172">To ensure that the object is not modified, the compiler creates a copy and calls member references using that copy.</span></span> <span data-ttu-id="df285-173">任何修改皆僅會修改防禦複本。</span><span class="sxs-lookup"><span data-stu-id="df285-173">Any modifications are to that defensive copy.</span></span> 

<span data-ttu-id="df285-174">使用 `Point3D` 的程式庫似乎通常會在整篇程式碼中使用原點。</span><span class="sxs-lookup"><span data-stu-id="df285-174">It's likely that the library using `Point3D` would often use the origin throughout the code.</span></span> <span data-ttu-id="df285-175">每個執行個體都會在堆疊上建立新的物件。</span><span class="sxs-lookup"><span data-stu-id="df285-175">Every instance creates a new object on the stack.</span></span> <span data-ttu-id="df285-176">如此有助建立常數並利用參考傳回常數。</span><span class="sxs-lookup"><span data-stu-id="df285-176">It may be advantageous to create a constant and return it by reference.</span></span> <span data-ttu-id="df285-177">但是，如果您將參考傳回內部儲存體，可能會希望強制限制呼叫端不可修改參考的儲存體。</span><span class="sxs-lookup"><span data-stu-id="df285-177">But, if you return a reference to internal storage, you may want to enforce that the caller cannot modify the referenced storage.</span></span> <span data-ttu-id="df285-178">下列程式碼定義了將 `readonly ref` 傳回至指定原點之 `Point3D` 的唯讀屬性。</span><span class="sxs-lookup"><span data-stu-id="df285-178">The following code defines a read-only property that returns a `readonly ref` to a `Point3D` that specifies the origin.</span></span>

[!code-csharp[OriginReference](../../samples/csharp/reference-semantics/Point3D.cs#OriginReference "Creating a readonly Origin reference")]

<span data-ttu-id="df285-179">建立 ref readonly 傳回的複本十分簡單：只要將其指派到並非利用 `ref readonly` 修飾詞宣告的變數即可。</span><span class="sxs-lookup"><span data-stu-id="df285-179">Creating a copy of a ref readonly return is easy: Just assign it to a variable not declared with the `ref readonly` modifier.</span></span> <span data-ttu-id="df285-180">編譯器會產生程式碼，將物件複製為指派的一部分。</span><span class="sxs-lookup"><span data-stu-id="df285-180">The compiler generates code to copy the object as part of the assignment.</span></span> 

<span data-ttu-id="df285-181">當您指派變數到 `ref readonly return` 時，可指定 `ref readonly` 變數或唯讀參考的傳值複本：</span><span class="sxs-lookup"><span data-stu-id="df285-181">When you assign a variable to a `ref readonly return`, you can specify either a `ref readonly` variable, or a by-value copy of the read-only reference:</span></span>

[!code-csharp[AssignRefReadonly](../../samples/csharp/reference-semantics/Program.cs#AssignRefReadonly "Assigning a ref readonly")]

<span data-ttu-id="df285-182">在上述程式碼中的第一項指派，會建立 `Origin` 常數的複本，並指派該複本。</span><span class="sxs-lookup"><span data-stu-id="df285-182">The first assignment in the preceding code makes a copy of the `Origin` constant and assigns that copy.</span></span> <span data-ttu-id="df285-183">第二項指派則會指派參考。</span><span class="sxs-lookup"><span data-stu-id="df285-183">The second assigns a reference.</span></span> <span data-ttu-id="df285-184">請注意，`readonly` 修飾詞必須是變數宣告的一部分。</span><span class="sxs-lookup"><span data-stu-id="df285-184">Notice that the `readonly` modifier must be part of the declaration of the variable.</span></span> <span data-ttu-id="df285-185">其參考項目無法修改。</span><span class="sxs-lookup"><span data-stu-id="df285-185">The reference to which it refers can't be modified.</span></span> <span data-ttu-id="df285-186">如果嘗試修改，會導致編譯時期錯誤。</span><span class="sxs-lookup"><span data-stu-id="df285-186">Attempts to do so result in a compile-time error.</span></span>

## <a name="readonly-struct-type"></a><span data-ttu-id="df285-187">`readonly struct` 類型</span><span class="sxs-lookup"><span data-stu-id="df285-187">`readonly struct` type</span></span>

<span data-ttu-id="df285-188">將 `ref readonly` 套用到高流量的結構應該已足夠。</span><span class="sxs-lookup"><span data-stu-id="df285-188">Applying `ref readonly` to high-traffic uses of a struct may be sufficient.</span></span>
<span data-ttu-id="df285-189">其他請況下，則建議您建立不可變的結構。</span><span class="sxs-lookup"><span data-stu-id="df285-189">Other times, you may want to create an immutable struct.</span></span> <span data-ttu-id="df285-190">您隨後便可隨時利用傳值參考進行傳遞。</span><span class="sxs-lookup"><span data-stu-id="df285-190">Then you can always pass by read-only reference.</span></span> <span data-ttu-id="df285-191">該做法會移除在您存取用作為 `in` 參數之結構方法時，所產生的保護複本。</span><span class="sxs-lookup"><span data-stu-id="df285-191">That practice removes the defensive copies that take place when you access methods of a struct used as an `in` parameter.</span></span>

<span data-ttu-id="df285-192">建立 `readonly struct` 類型即可執行此作業。</span><span class="sxs-lookup"><span data-stu-id="df285-192">You can do that by creating a `readonly struct` type.</span></span> <span data-ttu-id="df285-193">您可以將 `readonly` 修飾詞新增至結構宣告。</span><span class="sxs-lookup"><span data-stu-id="df285-193">You can add the `readonly` modifier to a struct declaration.</span></span> <span data-ttu-id="df285-194">編譯器會將該結構的所有執行個體成員，強制為 `readonly`，且 `struct` 不得可變動。</span><span class="sxs-lookup"><span data-stu-id="df285-194">The compiler enforces that all instance members of the struct are `readonly`; the `struct` must be immutable.</span></span>

<span data-ttu-id="df285-195">`readonly struct` 有其他最佳化。</span><span class="sxs-lookup"><span data-stu-id="df285-195">There are other optimizations for a `readonly struct`.</span></span> <span data-ttu-id="df285-196">您可以在每個 `readonly struct` 為引數的位置處，使用 `in` 修飾詞。</span><span class="sxs-lookup"><span data-stu-id="df285-196">You can use the `in` modifier at every location where a `readonly struct` is an argument.</span></span> <span data-ttu-id="df285-197">此外，當您傳回存留期超過方法傳回物件之範圍的物件時，可將 `readonly struct` 傳回為 `ref return`。</span><span class="sxs-lookup"><span data-stu-id="df285-197">In addition, you can return a `readonly struct` as a `ref return` when you are returning an object whose lifetime extends beyond the scope of the method returning the object.</span></span>

<span data-ttu-id="df285-198">最後，當您呼叫 `readonly struct` 的成員時，編譯器會產生極具效率的程式碼：`this` 參考 (而非接收器的複本) 會一律為利用參考傳遞至成員方法的 `in` 參數。</span><span class="sxs-lookup"><span data-stu-id="df285-198">Finally, the compiler generates more efficient code when you call members of a `readonly struct`: The `this` reference, instead of a copy of the receiver, is always an `in` parameter passed by reference to the member method.</span></span> <span data-ttu-id="df285-199">當您使用 `readonly struct` 時，此項最佳化可儲存更多複製內容。</span><span class="sxs-lookup"><span data-stu-id="df285-199">This optimization saves more copying when you use a `readonly struct`.</span></span> <span data-ttu-id="df285-200">`Point3D` 是此項變極佳的候選項目。</span><span class="sxs-lookup"><span data-stu-id="df285-200">The `Point3D` is a great candidate for this change.</span></span> <span data-ttu-id="df285-201">下列程式碼示範更新過後的 `ReadonlyPoint3D` 結構：</span><span class="sxs-lookup"><span data-stu-id="df285-201">The following code shows an updated `ReadonlyPoint3D` structure:</span></span>

[!code-csharp[ReadonlyOnlyPoint3D](../../samples/csharp/reference-semantics/Point3D.cs#ReadonlyOnlyPoint3D "Defining an immutable structure")]

## <a name="ref-struct-type"></a><span data-ttu-id="df285-202">`ref struct` 類型</span><span class="sxs-lookup"><span data-stu-id="df285-202">`ref struct` type</span></span>

<span data-ttu-id="df285-203">另一項相關的語言功能，是可宣告必須配置堆疊的實值型別。</span><span class="sxs-lookup"><span data-stu-id="df285-203">Another related language feature is the ability to declare a value type that must be stack allocated.</span></span> <span data-ttu-id="df285-204">換句話說，這些類型在堆積上絶對不會建立為另一種類別的成員。</span><span class="sxs-lookup"><span data-stu-id="df285-204">In other words, these types can never be created on the heap as a member of another class.</span></span> <span data-ttu-id="df285-205">此功能的主要動機是 <xref:System.Span%601> 及相關的結構。</span><span class="sxs-lookup"><span data-stu-id="df285-205">The primary motivation for this feature was <xref:System.Span%601> and related structures.</span></span> <span data-ttu-id="df285-206"><xref:System.Span%601> 的受控指標可能是其成員之一，而另一則為範圍的長度。</span><span class="sxs-lookup"><span data-stu-id="df285-206"><xref:System.Span%601> may contain a managed pointer as one of its members, the other being the length of the span.</span></span> <span data-ttu-id="df285-207">因為 C# 不支援不安全的內容外之受控記憶體的指標，所以實作上會有些微的差異。</span><span class="sxs-lookup"><span data-stu-id="df285-207">It's implemented a bit differently because C# doesn't support pointers to managed memory outside of an unsafe context.</span></span> <span data-ttu-id="df285-208">任何變更指標和長度的寫入，都非不可部分完成。</span><span class="sxs-lookup"><span data-stu-id="df285-208">Any write that changes the pointer and the length is not atomic.</span></span> <span data-ttu-id="df285-209">這表示 <xref:System.Span%601> 會受限於超出範圍的錯誤，或有其他類型的安全違規不限於單一堆疊框架。</span><span class="sxs-lookup"><span data-stu-id="df285-209">That means a <xref:System.Span%601> would be subject to out of range errors or other type safety violations were it not constrained to a single stack frame.</span></span> <span data-ttu-id="df285-210">此外，將受控指標放置於 GC 堆積上通常會於 JIT 時間損毀。</span><span class="sxs-lookup"><span data-stu-id="df285-210">In addition, putting a managed pointer on the GC heap typically crashes at JIT time.</span></span>

<span data-ttu-id="df285-211">當您使用以 [`stackalloc`](language-reference/keywords/stackalloc.md) 建立的記憶體，或使用來自 Interop API 的記憶體時，可能會有類似需求。</span><span class="sxs-lookup"><span data-stu-id="df285-211">You may have similar requirements working with memory created using [`stackalloc`](language-reference/keywords/stackalloc.md) or when using memory from interop APIs.</span></span> <span data-ttu-id="df285-212">您可依照那些需求定義自己的 `ref struct` 類型。</span><span class="sxs-lookup"><span data-stu-id="df285-212">You can define your own `ref struct` types for those needs.</span></span> <span data-ttu-id="df285-213">本文中提供的範例利用了 `Span<T>` 達到簡潔性。</span><span class="sxs-lookup"><span data-stu-id="df285-213">In this article, you see examples using `Span<T>` for simplicity.</span></span>

<span data-ttu-id="df285-214">`ref struct` 宣告會宣告此類型的結構必須位於堆疊中。</span><span class="sxs-lookup"><span data-stu-id="df285-214">The `ref struct` declaration declares a struct of this type must be on the stack.</span></span> <span data-ttu-id="df285-215">語言規則可確保會安全地使用這些類型。</span><span class="sxs-lookup"><span data-stu-id="df285-215">The language rules ensure the safe use of these types.</span></span> <span data-ttu-id="df285-216">其他宣告為 `ref struct` 的類型包括 <xref:System.ReadOnlySpan%601>。</span><span class="sxs-lookup"><span data-stu-id="df285-216">Other types declared as `ref struct` include <xref:System.ReadOnlySpan%601>.</span></span> 

<span data-ttu-id="df285-217">希望將類型 `ref struct` 保留為配置有堆疊的變數之目標，會讓編譯器強制對所有 `ref struct` 類型進行數項規則。</span><span class="sxs-lookup"><span data-stu-id="df285-217">The goal of keeping a `ref struct` type as a stack-allocated variable introduces several rules that the compiler enforces for all `ref struct` types.</span></span>

- <span data-ttu-id="df285-218">您無法分隔 `ref struct`。</span><span class="sxs-lookup"><span data-stu-id="df285-218">You can't box a `ref struct`.</span></span> <span data-ttu-id="df285-219">您不可為類型是 `object`、`dynamic` 或任何介面類型的變數，指派 `ref struct` 類型。</span><span class="sxs-lookup"><span data-stu-id="df285-219">You cannot assign a `ref struct` type to a variable of type `object`, `dynamic`, or any interface type.</span></span>
- <span data-ttu-id="df285-220">您不可將 `ref struct` 宣告為類別或一般結構的成員。</span><span class="sxs-lookup"><span data-stu-id="df285-220">You can't declare a `ref struct` as a member of a class or a normal struct.</span></span>
- <span data-ttu-id="df285-221">您不可在非同步方法中宣告類型為 `ref struct` 的區域變數。</span><span class="sxs-lookup"><span data-stu-id="df285-221">You cannot declare local variables that are `ref struct` types in async methods.</span></span> <span data-ttu-id="df285-222">但可以在傳回 `Task`、`Task<T>` 或類似工作之類型的同步方法中，宣告這些區域變數。</span><span class="sxs-lookup"><span data-stu-id="df285-222">You can declare them in synchronous methods that return `Task`, `Task<T>` or Task-like types.</span></span>
- <span data-ttu-id="df285-223">您不可在迭代器中宣告 `ref struct` 區域變數。</span><span class="sxs-lookup"><span data-stu-id="df285-223">You cannot declare `ref struct` local variables in iterators.</span></span>
- <span data-ttu-id="df285-224">您不可在 Lambda 運算式或區域函式中擷取 `ref struct` 變數。</span><span class="sxs-lookup"><span data-stu-id="df285-224">You cannot capture `ref struct` variables in lambda expressions or local functions.</span></span>

<span data-ttu-id="df285-225">這些限制可確保您不會意外使用 `ref struct` 而將其升階至受控堆積。</span><span class="sxs-lookup"><span data-stu-id="df285-225">These restrictions ensure you do not accidentally use a `ref struct` in a manner that could promote it to the managed heap.</span></span>

## <a name="readonly-ref-struct-type"></a><span data-ttu-id="df285-226">`readonly ref struct` 類型</span><span class="sxs-lookup"><span data-stu-id="df285-226">`readonly ref struct` type</span></span>

<span data-ttu-id="df285-227">宣告結構為 `readonly ref` 會結合 `ref struct` 與 `readonly struct` 宣告的優點與限制。</span><span class="sxs-lookup"><span data-stu-id="df285-227">Declaring a struct as `readonly ref` combines the benefits and restrictions of `ref struct` and `readonly struct` declarations.</span></span> 

<span data-ttu-id="df285-228">下列範例示範了 `readonly ref struct` 的宣告。</span><span class="sxs-lookup"><span data-stu-id="df285-228">The following example demonstrates the declaration of `readonly ref struct`.</span></span>

```csharp
readonly ref struct ReadOnlyRefPoint2D
{
    public int X { get; }
    public int Y { get; }
    
    public ReadOnlyRefPoint2D(int x, int y) => (X, Y) = (x, y);
}
```

## <a name="conclusions"></a><span data-ttu-id="df285-229">結論</span><span class="sxs-lookup"><span data-stu-id="df285-229">Conclusions</span></span>

<span data-ttu-id="df285-230">這些 C# 語言的增強功能專為注重效能的演算法所設計，對於這些演算法來說，記憶體的配置對於達到所需效能至關重要。</span><span class="sxs-lookup"><span data-stu-id="df285-230">These enhancements to the C# language are designed for performance critical algorithms where memory allocations can be critical to achieving the necessary performance.</span></span> <span data-ttu-id="df285-231">您會發現到您不常在您撰寫的程式碼中使用這些功能。</span><span class="sxs-lookup"><span data-stu-id="df285-231">You may find that you don't often use these features in the code you write.</span></span> <span data-ttu-id="df285-232">但在 .NET Framework 中，已有許多位置採用這些增強功能。</span><span class="sxs-lookup"><span data-stu-id="df285-232">However, these enhancements have been adopted in many locations in the .NET Framework.</span></span> <span data-ttu-id="df285-233">隨著愈來愈多的 API 利用到這些功能之後，您會發現自己的應用程式效能會有所改善。</span><span class="sxs-lookup"><span data-stu-id="df285-233">As more and more APIs make use of these features, you'll see the performance of your own applications improve.</span></span>
