---
title: 非同步方法的傳回型別 (C#)
ms.date: 04/14/2020
ms.assetid: ddb2539c-c898-48c1-ad92-245e4a996df8
ms.openlocfilehash: c2584f1e285a7ab76eb43f9a211a8d2a51c2c55e
ms.sourcegitcommit: c76c8b2c39ed2f0eee422b61a2ab4c05ca7771fa
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 05/21/2020
ms.locfileid: "83761872"
---
# <a name="async-return-types-c"></a><span data-ttu-id="c0563-102">非同步方法的傳回型別 (C#)</span><span class="sxs-lookup"><span data-stu-id="c0563-102">Async Return Types (C#)</span></span>

<span data-ttu-id="c0563-103">非同步方法可有下列傳回型別：</span><span class="sxs-lookup"><span data-stu-id="c0563-103">Async methods can have the following return types:</span></span>

- <span data-ttu-id="c0563-104">傳回值的非同步方法為 <xref:System.Threading.Tasks.Task%601>。</span><span class="sxs-lookup"><span data-stu-id="c0563-104"><xref:System.Threading.Tasks.Task%601>, for an async method that returns a value.</span></span>
- <span data-ttu-id="c0563-105">執行作業但不傳回任何值的非同步方法為 <xref:System.Threading.Tasks.Task>。</span><span class="sxs-lookup"><span data-stu-id="c0563-105"><xref:System.Threading.Tasks.Task>, for an async method that performs an operation but returns no value.</span></span>
- <span data-ttu-id="c0563-106">處理常式為 `void`。</span><span class="sxs-lookup"><span data-stu-id="c0563-106">`void`, for an event handler.</span></span>
- <span data-ttu-id="c0563-107">自 C# 7.0 開始，任何具有可存取 `GetAwaiter` 方法的型別。</span><span class="sxs-lookup"><span data-stu-id="c0563-107">Starting with C# 7.0, any type that has an accessible `GetAwaiter` method.</span></span> <span data-ttu-id="c0563-108">`GetAwaiter` 方法傳回的物件必須實作 <xref:System.Runtime.CompilerServices.ICriticalNotifyCompletion?displayProperty=nameWithType> 介面。</span><span class="sxs-lookup"><span data-stu-id="c0563-108">The object returned by the `GetAwaiter` method must implement the <xref:System.Runtime.CompilerServices.ICriticalNotifyCompletion?displayProperty=nameWithType> interface.</span></span>
- <span data-ttu-id="c0563-109">從 c # 8.0 開始， <xref:System.Collections.Generic.IAsyncEnumerable%601> 適用于傳回*非同步資料流程*的非同步方法。</span><span class="sxs-lookup"><span data-stu-id="c0563-109">Starting with C# 8.0, <xref:System.Collections.Generic.IAsyncEnumerable%601>, for an async method that returns an *async stream*.</span></span>

<span data-ttu-id="c0563-110">如需非同步方法的詳細資訊，請參閱[使用 async 和 await 進行非同步程式設計 (C#)](./index.md)。</span><span class="sxs-lookup"><span data-stu-id="c0563-110">For more information about async methods, see [Asynchronous Programming with async and await (C#)](./index.md).</span></span>  
  
## <a name="tasktresult-return-type"></a><span data-ttu-id="c0563-111"> Task\<TResult\> 傳回型別</span><span class="sxs-lookup"><span data-stu-id="c0563-111">Task\<TResult\> Return Type</span></span>  
<span data-ttu-id="c0563-112">傳回 <xref:System.Threading.Tasks.Task%601> 型別用於非同步方法，其中包含[return](../../../language-reference/keywords/return.md) （c #）語句，其中運算元為 `TResult` 。</span><span class="sxs-lookup"><span data-stu-id="c0563-112">The <xref:System.Threading.Tasks.Task%601> return type is used for an async method that contains a [return](../../../language-reference/keywords/return.md) (C#) statement in which the operand is `TResult`.</span></span>  
  
<span data-ttu-id="c0563-113">在下列範例中，`GetLeisureHours` 非同步方法包含一個傳回整數的 `return` 陳述式。</span><span class="sxs-lookup"><span data-stu-id="c0563-113">In the following example, the `GetLeisureHours` async method contains a `return` statement that returns an integer.</span></span> <span data-ttu-id="c0563-114">因此，方法宣告必須指定 `Task<int>` 傳回型別。</span><span class="sxs-lookup"><span data-stu-id="c0563-114">Therefore, the method declaration must specify a return type of `Task<int>`.</span></span>  <span data-ttu-id="c0563-115"><xref:System.Threading.Tasks.Task.FromResult%2A> 非同步方法是傳回字串作業的預留位置。</span><span class="sxs-lookup"><span data-stu-id="c0563-115">The <xref:System.Threading.Tasks.Task.FromResult%2A> async method is a placeholder for an operation that returns a string.</span></span>
  
:::code language="csharp" source="./snippets/async-return-types/async-returns1.cs" id="SnippetFirstExample":::

<span data-ttu-id="c0563-116">從 `ShowTodaysInfo` 方法的 await 運算式內呼叫 `GetLeisureHours` 時，await 運算式會擷取儲存在 `GetLeisureHours` 方法所傳回之工作中的整數值 (`leisureHours` 的值)。</span><span class="sxs-lookup"><span data-stu-id="c0563-116">When `GetLeisureHours` is called from within an await expression in the `ShowTodaysInfo` method, the await expression retrieves the integer value (the value of `leisureHours`) that's stored in the task returned by the `GetLeisureHours` method.</span></span> <span data-ttu-id="c0563-117">如需 await 運算式的詳細資訊，請參閱 [await](../../../language-reference/operators/await.md)。</span><span class="sxs-lookup"><span data-stu-id="c0563-117">For more information about await expressions, see [await](../../../language-reference/operators/await.md).</span></span>  
  
<span data-ttu-id="c0563-118">`await` `Task<T>` `GetLeisureHours` `await` 如下列程式碼所示，您可以藉由區隔對的呼叫，以進一步瞭解如何從取得的結果。</span><span class="sxs-lookup"><span data-stu-id="c0563-118">You can better understand how `await` retrieves the result from a `Task<T>` by separating the call to `GetLeisureHours` from the application of `await`, as the following code shows.</span></span> <span data-ttu-id="c0563-119">呼叫不會立即等候的 `GetLeisureHours` 方法，會傳回 `Task<int>`，如您所預期的方法宣告。</span><span class="sxs-lookup"><span data-stu-id="c0563-119">A call to method `GetLeisureHours` that isn't immediately awaited returns a `Task<int>`, as you would expect from the declaration of the method.</span></span> <span data-ttu-id="c0563-120">在範例中，工作會指派給 `integerTask` 變數。</span><span class="sxs-lookup"><span data-stu-id="c0563-120">The task is assigned to the `integerTask` variable in the example.</span></span> <span data-ttu-id="c0563-121">因為 `integerTask` 是 <xref:System.Threading.Tasks.Task%601>，所以它包含 `TResult` 類型的 <xref:System.Threading.Tasks.Task%601.Result> 屬性。</span><span class="sxs-lookup"><span data-stu-id="c0563-121">Because `integerTask` is a <xref:System.Threading.Tasks.Task%601>, it contains a <xref:System.Threading.Tasks.Task%601.Result> property of type `TResult`.</span></span> <span data-ttu-id="c0563-122">在本例中，`TResult` 代表整數類型。</span><span class="sxs-lookup"><span data-stu-id="c0563-122">In this case, `TResult` represents an integer type.</span></span> <span data-ttu-id="c0563-123">當 `await` 套用至 `integerTask` 時，await 運算式評估為 `integerTask` 之 <xref:System.Threading.Tasks.Task%601.Result%2A> 屬性的內容。</span><span class="sxs-lookup"><span data-stu-id="c0563-123">When `await` is applied to `integerTask`, the await expression evaluates to the contents of the <xref:System.Threading.Tasks.Task%601.Result%2A> property of `integerTask`.</span></span> <span data-ttu-id="c0563-124">值會指派給 `ret` 變數。</span><span class="sxs-lookup"><span data-stu-id="c0563-124">The value is assigned to the `ret` variable.</span></span>  
  
> [!IMPORTANT]
> <span data-ttu-id="c0563-125"><xref:System.Threading.Tasks.Task%601.Result%2A> 屬性是封鎖的屬性。</span><span class="sxs-lookup"><span data-stu-id="c0563-125">The <xref:System.Threading.Tasks.Task%601.Result%2A> property is a blocking property.</span></span> <span data-ttu-id="c0563-126">如果您嘗試在其工作完成之前先存取它，目前使用中的執行緒會封鎖，直到工作完成並且有可用的值為止。</span><span class="sxs-lookup"><span data-stu-id="c0563-126">If you try to access it before its task is finished, the thread that's currently active is blocked until the task completes and the value is available.</span></span> <span data-ttu-id="c0563-127">在大部分情況下，您應該使用 `await` 來存取值，而不是直接存取屬性。</span><span class="sxs-lookup"><span data-stu-id="c0563-127">In most cases, you should access the value by using `await` instead of accessing the property directly.</span></span> <br/> <span data-ttu-id="c0563-128">前一個範例擷取 <xref:System.Threading.Tasks.Task%601.Result%2A> 屬性的值，封鎖主執行緒，讓 `ShowTodaysInfo` 方法在應用程式結束之前可以完成執行。</span><span class="sxs-lookup"><span data-stu-id="c0563-128">The previous example retrieved the value of the <xref:System.Threading.Tasks.Task%601.Result%2A> property to block the main thread so that the `ShowTodaysInfo` method could finish execution before the application ended.</span></span>  

:::code language="csharp" source="./snippets/async-return-types/async-returns1a.cs" id="SnippetSecondVersion":::

## <a name="task-return-type"></a><span data-ttu-id="c0563-129">工作傳回型別</span><span class="sxs-lookup"><span data-stu-id="c0563-129">Task Return Type</span></span>  
<span data-ttu-id="c0563-130">不包含 `return` 陳述式的非同步方法，或包含不會傳回運算元的 `return` 陳述式的非同步方法，通常具有傳回型別 <xref:System.Threading.Tasks.Task>。</span><span class="sxs-lookup"><span data-stu-id="c0563-130">Async methods that don't contain a `return` statement or that contain a `return` statement that doesn't return an operand usually have a return type of <xref:System.Threading.Tasks.Task>.</span></span> <span data-ttu-id="c0563-131">這類方法如果以同步方式執行，會傳回 `void`。</span><span class="sxs-lookup"><span data-stu-id="c0563-131">Such methods return `void` if they run synchronously.</span></span> <span data-ttu-id="c0563-132">如果您針對非同步方法使用 <xref:System.Threading.Tasks.Task> 傳回型別，則除非被呼叫的非同步方法完成，否則呼叫的方法可以使用 `await` 運算子暫止呼叫端完成。</span><span class="sxs-lookup"><span data-stu-id="c0563-132">If you use a <xref:System.Threading.Tasks.Task> return type for an async method, a calling method can use an `await` operator to suspend the caller's completion until the called async method has finished.</span></span>  
  
<span data-ttu-id="c0563-133">在下列範例中，`WaitAndApologize` 非同步方法不包含 `return` 陳述式，所以方法傳回 <xref:System.Threading.Tasks.Task> 物件。</span><span class="sxs-lookup"><span data-stu-id="c0563-133">In the following example, the `WaitAndApologize` async method doesn't contain a `return` statement, so the method returns a <xref:System.Threading.Tasks.Task> object.</span></span> <span data-ttu-id="c0563-134">傳回可等候的 `Task` `WaitAndApologize` 。</span><span class="sxs-lookup"><span data-stu-id="c0563-134">Returning a `Task` enables `WaitAndApologize` to be awaited.</span></span> <span data-ttu-id="c0563-135"><xref:System.Threading.Tasks.Task>類型並未包含 `Result` 屬性，因為它沒有傳回值。</span><span class="sxs-lookup"><span data-stu-id="c0563-135">The <xref:System.Threading.Tasks.Task> type doesn't include a `Result` property because it has no return value.</span></span>  

:::code language="csharp" source="./snippets/async-return-types/async-returns2.cs" id="SnippetTaskReturn":::

<span data-ttu-id="c0563-136">`WaitAndApologize` 是透過使用 await 陳述式而非 await 運算式成為等候的，類似同步 void 傳回方法的呼叫陳述式。</span><span class="sxs-lookup"><span data-stu-id="c0563-136">`WaitAndApologize` is awaited by using an await statement instead of an await expression, similar to the calling statement for a synchronous void-returning method.</span></span> <span data-ttu-id="c0563-137">在此情況下，await 運算子的應用不會產生值。</span><span class="sxs-lookup"><span data-stu-id="c0563-137">The application of an await operator in this case doesn't produce a value.</span></span>  
  
<span data-ttu-id="c0563-138">如先前的 <xref:System.Threading.Tasks.Task%601> 範例所示，您可以隔開對 `WaitAndApologize` 的呼叫和 await 運算子的應用程式，如下列程式碼所示。</span><span class="sxs-lookup"><span data-stu-id="c0563-138">As in the previous <xref:System.Threading.Tasks.Task%601> example, you can separate the call to `WaitAndApologize` from the application of an await operator, as the following code shows.</span></span> <span data-ttu-id="c0563-139">不過，請記住，`Task` 沒有 `Result` 屬性，且將 await 運算子套用至 `Task` 時不會產生任何值。</span><span class="sxs-lookup"><span data-stu-id="c0563-139">However, remember that a `Task` doesn't have a `Result` property, and that no value is produced when an await operator is applied to a `Task`.</span></span>  
  
<span data-ttu-id="c0563-140">下列程式碼隔開呼叫 `WaitAndApologize` 方法與等候方法傳回的工作。</span><span class="sxs-lookup"><span data-stu-id="c0563-140">The following code separates calling the `WaitAndApologize` method from awaiting the task that the method returns.</span></span>  

:::code language="csharp" source="./snippets/async-return-types/async-returns2a.cs" id="SnippetAwaitTask":::

## <a name="void-return-type"></a><span data-ttu-id="c0563-141"> Void 傳回型別</span><span class="sxs-lookup"><span data-stu-id="c0563-141">Void return type</span></span>

<span data-ttu-id="c0563-142">您在非同步事件處理常式中使用 `void` 傳回型別，這需要 `void` 傳回型別。</span><span class="sxs-lookup"><span data-stu-id="c0563-142">You use the `void` return type in asynchronous event handlers, which require a `void` return type.</span></span> <span data-ttu-id="c0563-143">對於不傳回值的事件處理常式以外的方法，您應該要改傳回 <xref:System.Threading.Tasks.Task>，因為傳回 `void` 的非同步方法不能是等候的。</span><span class="sxs-lookup"><span data-stu-id="c0563-143">For methods other than event handlers that don't return a value, you should return a <xref:System.Threading.Tasks.Task> instead, because an async method that returns `void` can't be awaited.</span></span> <span data-ttu-id="c0563-144">這種方法的任何呼叫端都必須繼續完成，而不需要等候呼叫的非同步方法完成。</span><span class="sxs-lookup"><span data-stu-id="c0563-144">Any caller of such a method must continue to completion without waiting for the called async method to finish.</span></span> <span data-ttu-id="c0563-145">呼叫端必須獨立于非同步方法所產生的任何值或例外狀況。</span><span class="sxs-lookup"><span data-stu-id="c0563-145">The caller must be independent of any values or exceptions that the async method generates.</span></span>  
  
<span data-ttu-id="c0563-146">傳回 void 的非同步方法的呼叫端無法攔截方法擲回的例外狀況，而且這類未處理的例外狀況可能會導致您的應用程式失敗。</span><span class="sxs-lookup"><span data-stu-id="c0563-146">The caller of a void-returning async method can't catch exceptions thrown from the method, and such unhandled exceptions are likely to cause your application to fail.</span></span> <span data-ttu-id="c0563-147">如果傳回或的方法擲 <xref:System.Threading.Tasks.Task> 回 <xref:System.Threading.Tasks.Task%601> 例外狀況，則例外狀況會儲存在傳回的工作中。</span><span class="sxs-lookup"><span data-stu-id="c0563-147">If a method that returns a <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601> throws an exception, the exception is stored in the returned task.</span></span> <span data-ttu-id="c0563-148">等待工作時，會重新擲回例外狀況。</span><span class="sxs-lookup"><span data-stu-id="c0563-148">The exception is rethrown when the task is awaited.</span></span> <span data-ttu-id="c0563-149">因此，請確定任何可能會產生例外狀況的非同步方法具有傳回型別 <xref:System.Threading.Tasks.Task> 或 <xref:System.Threading.Tasks.Task%601>，且會等候對方法的呼叫。</span><span class="sxs-lookup"><span data-stu-id="c0563-149">Therefore, make sure that any async method that can produce an exception has a return type of <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601> and that calls to the method are awaited.</span></span>  
  
<span data-ttu-id="c0563-150">如需如何在非同步方法中攔截例外狀況的詳細資訊，請參閱[try-catch](../../../language-reference/keywords/try-catch.md)文章的[async 方法](../../../language-reference/keywords/try-catch.md#exceptions-in-async-methods)一節中的例外狀況。</span><span class="sxs-lookup"><span data-stu-id="c0563-150">For more information about how to catch exceptions in async methods, see the [Exceptions in Async Methods](../../../language-reference/keywords/try-catch.md#exceptions-in-async-methods) section of the [try-catch](../../../language-reference/keywords/try-catch.md) article.</span></span>  
  
<span data-ttu-id="c0563-151">下列範例示範非同步事件處理常式的行為。</span><span class="sxs-lookup"><span data-stu-id="c0563-151">The following example shows the behavior of an async event handler.</span></span> <span data-ttu-id="c0563-152">在範例程式碼中，非同步事件處理常式必須讓主執行緒知道其完成時間。</span><span class="sxs-lookup"><span data-stu-id="c0563-152">In the example code, an async event handler must let the main thread know when it finishes.</span></span> <span data-ttu-id="c0563-153">然後主執行緒可以等候非同步事件處理常式完成，再結束程式。</span><span class="sxs-lookup"><span data-stu-id="c0563-153">Then the main thread can wait for an async event handler to complete before exiting the program.</span></span>

:::code language="csharp" source="./snippets/async-return-types/async-returns3.cs":::

## <a name="generalized-async-return-types-and-valuetasktresult"></a><span data-ttu-id="c0563-154">通用的非同步傳回型別和 ValueTask\<TResult\></span><span class="sxs-lookup"><span data-stu-id="c0563-154">Generalized async return types and ValueTask\<TResult\></span></span>

<span data-ttu-id="c0563-155">自 C# 7.0 開始，非同步方法可以傳回具有可存取 `GetAwaiter` 方法的任何型別。</span><span class="sxs-lookup"><span data-stu-id="c0563-155">Starting with C# 7.0, an async method can return any type that has an accessible `GetAwaiter` method.</span></span>

<span data-ttu-id="c0563-156">因為 <xref:System.Threading.Tasks.Task> 和 <xref:System.Threading.Tasks.Task%601> 是參考型別，所以效能關鍵路徑中的記憶體配置，會對效能造成不良影響，特別是當配置出現在緊密迴圈中時。</span><span class="sxs-lookup"><span data-stu-id="c0563-156">Because <xref:System.Threading.Tasks.Task> and <xref:System.Threading.Tasks.Task%601> are reference types, memory allocation in performance-critical paths, particularly when allocations occur in tight loops, can adversely affect performance.</span></span> <span data-ttu-id="c0563-157">支援通用的傳回型別，表示您可以傳回輕量型的實值型別，而不是參考型別，以避免額外的記憶體配置。</span><span class="sxs-lookup"><span data-stu-id="c0563-157">Support for generalized return types means that you can return a lightweight value type instead of a reference type to avoid additional memory allocations.</span></span>

<span data-ttu-id="c0563-158">.NET 提供 <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> 結構作為通用工作傳回值的輕量級實作。</span><span class="sxs-lookup"><span data-stu-id="c0563-158">.NET provides the <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> structure as a lightweight implementation of a generalized task-returning value.</span></span> <span data-ttu-id="c0563-159">若要使用 <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> 類型，您必須將 `System.Threading.Tasks.Extensions` NuGet 套件新增至專案。</span><span class="sxs-lookup"><span data-stu-id="c0563-159">To use the <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> type, you must add the `System.Threading.Tasks.Extensions` NuGet package to your project.</span></span> <span data-ttu-id="c0563-160">下列範例會使用 <xref:System.Threading.Tasks.ValueTask%601> 結構，擷取擲兩次骰子的值。</span><span class="sxs-lookup"><span data-stu-id="c0563-160">The following example uses the <xref:System.Threading.Tasks.ValueTask%601> structure to retrieve the value of two dice rolls.</span></span>
  
:::code language="csharp" source="./snippets/async-return-types/async-valuetask.cs":::

## <a name="async-streams-with-iasyncenumerablet"></a><span data-ttu-id="c0563-161">具有 IAsyncEnumerable T 的非同步資料流程 \<\></span><span class="sxs-lookup"><span data-stu-id="c0563-161">Async streams with IAsyncEnumerable\<T\></span></span>

<span data-ttu-id="c0563-162">從 c # 8.0 開始，非同步方法可能會傳回以表示的*非同步資料流程* <xref:System.Collections.Generic.IAsyncEnumerable%601> 。</span><span class="sxs-lookup"><span data-stu-id="c0563-162">Starting with C# 8.0, an async method may return an *async stream*, represented by <xref:System.Collections.Generic.IAsyncEnumerable%601>.</span></span> <span data-ttu-id="c0563-163">當以重複非同步呼叫的區塊產生元素時，非同步資料流程提供了一種方法來列舉從資料流程讀取的專案。</span><span class="sxs-lookup"><span data-stu-id="c0563-163">An async stream provides a way to enumerate items read from a stream when elements are generated in chunks with repeated asynchronous calls.</span></span> <span data-ttu-id="c0563-164">下列範例顯示會產生非同步資料流程的非同步方法：</span><span class="sxs-lookup"><span data-stu-id="c0563-164">The following example shows an async method that generates an async stream:</span></span>

:::code language="csharp" source="./snippets/async-return-types/AsyncStreams.cs" id="SnippetGenerateAsyncStream":::

<span data-ttu-id="c0563-165">上述範例會以非同步方式讀取字串中的行。</span><span class="sxs-lookup"><span data-stu-id="c0563-165">The preceding example reads lines from a string asynchronously.</span></span> <span data-ttu-id="c0563-166">讀取每一行之後，程式碼會列舉字串中的每個字。</span><span class="sxs-lookup"><span data-stu-id="c0563-166">Once each line is read, the code enumerates each word in the string.</span></span> <span data-ttu-id="c0563-167">呼叫端會使用語句來列舉每個字組 `await foreach` 。</span><span class="sxs-lookup"><span data-stu-id="c0563-167">Callers would enumerate each word using the `await foreach` statement.</span></span> <span data-ttu-id="c0563-168">方法會等候何時需要以非同步方式讀取來源字串中的下一行。</span><span class="sxs-lookup"><span data-stu-id="c0563-168">The method awaits when it needs to asynchronously read the next line from the source string.</span></span>

## <a name="see-also"></a><span data-ttu-id="c0563-169">另請參閱</span><span class="sxs-lookup"><span data-stu-id="c0563-169">See also</span></span>

- <xref:System.Threading.Tasks.Task.FromResult%2A>
- [<span data-ttu-id="c0563-170">逐步解說：使用 async 和 await 存取 Web (C#)</span><span class="sxs-lookup"><span data-stu-id="c0563-170">Walkthrough: Accessing the Web by Using async and await (C#)</span></span>](./walkthrough-accessing-the-web-by-using-async-and-await.md)
- [<span data-ttu-id="c0563-171">非同步程式中的控制流程（c #）</span><span class="sxs-lookup"><span data-stu-id="c0563-171">Control Flow in Async Programs (C#)</span></span>](./control-flow-in-async-programs.md)
- [<span data-ttu-id="c0563-172">async</span><span class="sxs-lookup"><span data-stu-id="c0563-172">async</span></span>](../../../language-reference/keywords/async.md)
- [<span data-ttu-id="c0563-173">遇到</span><span class="sxs-lookup"><span data-stu-id="c0563-173">await</span></span>](../../../language-reference/operators/await.md)
