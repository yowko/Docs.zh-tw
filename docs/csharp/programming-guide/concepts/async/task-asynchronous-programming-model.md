---
title: '工作非同步程式設計 (利用 async 和 await (c # ) 」來利用) 模型」'
description: '瞭解何時及如何使用以工作為基礎的非同步程式設計，這是以 c # 進行非同步程式設計的簡化方法。'
ms.date: 08/19/2020
ms.assetid: 9bcf896a-5826-4189-8c1a-3e35fa08243a
ms.openlocfilehash: 1014e38dcb3e2c4f56c8b3f3dade9bdbff3abd27
ms.sourcegitcommit: 27a15a55019f6b5f2733961738babe94aec0def3
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 09/15/2020
ms.locfileid: "90556033"
---
# <a name="task-asynchronous-programming-model"></a><span data-ttu-id="19050-103">非同步工作程式設計模型</span><span class="sxs-lookup"><span data-stu-id="19050-103">Task asynchronous programming model</span></span>

<span data-ttu-id="19050-104">您可以使用非同步程式設計，避免發生效能瓶頸並增強應用程式的整體回應性。</span><span class="sxs-lookup"><span data-stu-id="19050-104">You can avoid performance bottlenecks and enhance the overall responsiveness of your application by using asynchronous programming.</span></span> <span data-ttu-id="19050-105">不過，撰寫非同步應用程式的傳統技術可能很複雜，因而難以撰寫、偵錯和維護。</span><span class="sxs-lookup"><span data-stu-id="19050-105">However, traditional techniques for writing asynchronous applications can be complicated, making them difficult to write, debug, and maintain.</span></span>

<span data-ttu-id="19050-106">[C# 5](../../../whats-new/csharp-version-history.md#c-version-50) 引進了一種簡化的方法 (非同步程式設計)，來運用 .NET Framework 4.5 和更新版本、.NET Core 以及 Windows 執行階段中的非同步支援。</span><span class="sxs-lookup"><span data-stu-id="19050-106">[C# 5](../../../whats-new/csharp-version-history.md#c-version-50) introduced a simplified approach, async programming, that leverages asynchronous support in the .NET Framework 4.5 and higher, .NET Core, and the Windows Runtime.</span></span> <span data-ttu-id="19050-107">編譯器會代替開發人員處理過去經常要處理的困難工作，而您的應用程式仍保有類似同步程式碼的邏輯結構。</span><span class="sxs-lookup"><span data-stu-id="19050-107">The compiler does the difficult work that the developer used to do, and your application retains a logical structure that resembles synchronous code.</span></span> <span data-ttu-id="19050-108">因此，您可以輕鬆擁有非同步程式設計的所有優點。</span><span class="sxs-lookup"><span data-stu-id="19050-108">As a result, you get all the advantages of asynchronous programming with a fraction of the effort.</span></span>

<span data-ttu-id="19050-109">本主題提供使用非同步程式設計的時機和使用方式的概觀，並加入包含詳細資料及範例的支援主題連結。</span><span class="sxs-lookup"><span data-stu-id="19050-109">This topic provides an overview of when and how to use async programming and includes links to support topics that contain details and examples.</span></span>

## <a name="async-improves-responsiveness"></a><a name="BKMK_WhentoUseAsynchrony"></a> <span data-ttu-id="19050-110">非同步可改善回應性</span><span class="sxs-lookup"><span data-stu-id="19050-110">Async improves responsiveness</span></span>

<span data-ttu-id="19050-111">非同步對於可能在像是 Web 存取時會進行封鎖的活動而言相當重要。</span><span class="sxs-lookup"><span data-stu-id="19050-111">Asynchrony is essential for activities that are potentially blocking, such as web access.</span></span> <span data-ttu-id="19050-112">存取 Web 資源的速度有時會變慢或延遲。</span><span class="sxs-lookup"><span data-stu-id="19050-112">Access to a web resource sometimes is slow or delayed.</span></span> <span data-ttu-id="19050-113">如果這類活動在同步處理序中遭到封鎖，整個應用程式就必須等候。</span><span class="sxs-lookup"><span data-stu-id="19050-113">If such an activity is blocked in a synchronous process, the entire application must wait.</span></span> <span data-ttu-id="19050-114">在非同步處理序中，應用程式可以繼續處理其他與 Web 資源不相關的工作，直到可能的封鎖工作完成。</span><span class="sxs-lookup"><span data-stu-id="19050-114">In an asynchronous process, the application can continue with other work that doesn't depend on the web resource until the potentially blocking task finishes.</span></span>

<span data-ttu-id="19050-115">下表顯示非同步程式設計一般會改善回應速度的部分。</span><span class="sxs-lookup"><span data-stu-id="19050-115">The following table shows typical areas where asynchronous programming improves responsiveness.</span></span> <span data-ttu-id="19050-116">從 .NET 和 Windows 執行階段列出的 API 包含支援非同步程式設計的方法。</span><span class="sxs-lookup"><span data-stu-id="19050-116">The listed APIs from .NET and the Windows Runtime contain methods that support async programming.</span></span>

| <span data-ttu-id="19050-117">應用程式區域</span><span class="sxs-lookup"><span data-stu-id="19050-117">Application area</span></span> | <span data-ttu-id="19050-118">使用非同步方法的 .NET 類型</span><span class="sxs-lookup"><span data-stu-id="19050-118">.NET types with async methods</span></span> | <span data-ttu-id="19050-119">使用非同步方法的 Windows 執行階段類型</span><span class="sxs-lookup"><span data-stu-id="19050-119">Windows Runtime types with async methods</span></span> |
|--|--|--|
| <span data-ttu-id="19050-120">Web 存取</span><span class="sxs-lookup"><span data-stu-id="19050-120">Web access</span></span> | <xref:System.Net.Http.HttpClient> | <xref:Windows.Web.Http.HttpClient?displayProperty=nameWithType> <br> <xref:Windows.Web.Syndication.SyndicationClient> |
| <span data-ttu-id="19050-121">處理檔案</span><span class="sxs-lookup"><span data-stu-id="19050-121">Working with files</span></span> | <xref:System.Text.Json.JsonSerializer> <br> <xref:System.IO.StreamReader> <br> <xref:System.IO.StreamWriter> <br> <xref:System.Xml.XmlReader> <br> <xref:System.Xml.XmlWriter> | <xref:Windows.Storage.StorageFile> |
| <span data-ttu-id="19050-122">使用映像</span><span class="sxs-lookup"><span data-stu-id="19050-122">Working with images</span></span> |  | <xref:Windows.Media.Capture.MediaCapture> <br> <xref:Windows.Graphics.Imaging.BitmapEncoder> <br> <xref:Windows.Graphics.Imaging.BitmapDecoder> |
| <span data-ttu-id="19050-123">WCF 程式設計</span><span class="sxs-lookup"><span data-stu-id="19050-123">WCF programming</span></span> | [<span data-ttu-id="19050-124">同步和非同步作業</span><span class="sxs-lookup"><span data-stu-id="19050-124">Synchronous and Asynchronous Operations</span></span>](../../../../framework/wcf/synchronous-and-asynchronous-operations.md) |  |

<span data-ttu-id="19050-125">非同步對於存取 UI 執行緒的應用程式而言確實特別有用，因為所有 UI 相關活動通常都會共用一個執行緒。</span><span class="sxs-lookup"><span data-stu-id="19050-125">Asynchrony proves especially valuable for applications that access the UI thread because all UI-related activity usually shares one thread.</span></span> <span data-ttu-id="19050-126">如果同步應用程式中有任何處理序遭到封鎖，所有處理序都會遭到封鎖。</span><span class="sxs-lookup"><span data-stu-id="19050-126">If any process is blocked in a synchronous application, all are blocked.</span></span> <span data-ttu-id="19050-127">您的應用程式會停止回應，而且您可能會認為應用程式失敗，但實際上只是在等候。</span><span class="sxs-lookup"><span data-stu-id="19050-127">Your application stops responding, and you might conclude that it has failed when instead it's just waiting.</span></span>

<span data-ttu-id="19050-128">當您使用非同步方法時，應用程式會繼續回應 UI。</span><span class="sxs-lookup"><span data-stu-id="19050-128">When you use asynchronous methods, the application continues to respond to the UI.</span></span> <span data-ttu-id="19050-129">例如，您可以調整視窗大小或將視窗縮到最小，如果不想要等待應用程式完成，也可以將它關閉。</span><span class="sxs-lookup"><span data-stu-id="19050-129">You can resize or minimize a window, for example, or you can close the application if you don't want to wait for it to finish.</span></span>

<span data-ttu-id="19050-130">非同步方法會在設計非同步作業時，於選項清單中加入自動傳輸的對等項目讓您選擇。</span><span class="sxs-lookup"><span data-stu-id="19050-130">The async-based approach adds the equivalent of an automatic transmission to the list of options that you can choose from when designing asynchronous operations.</span></span> <span data-ttu-id="19050-131">也就是說，除了擁有傳統非同步程式設計的所有優點之外，開發人員所需投入的時間也大為減少。</span><span class="sxs-lookup"><span data-stu-id="19050-131">That is, you get all the benefits of traditional asynchronous programming but with much less effort from the developer.</span></span>

## <a name="async-methods-are-easy-to-write"></a><a name="BKMK_HowtoWriteanAsyncMethod"></a> <span data-ttu-id="19050-132">非同步方法很容易撰寫</span><span class="sxs-lookup"><span data-stu-id="19050-132">Async methods are easy to write</span></span>

<span data-ttu-id="19050-133">C# 中的 [async](../../../language-reference/keywords/async.md) 和 [await](../../../language-reference/operators/await.md) 關鍵字都是非同步程式設計的核心。</span><span class="sxs-lookup"><span data-stu-id="19050-133">The [async](../../../language-reference/keywords/async.md) and [await](../../../language-reference/operators/await.md) keywords in C# are the heart of async programming.</span></span> <span data-ttu-id="19050-134">藉由使用這兩個關鍵字，您可以使用 .NET Framework、.NET Core 或 Windows 執行階段中的資源來建立異步方法，幾乎就像建立同步方法一樣容易。</span><span class="sxs-lookup"><span data-stu-id="19050-134">By using those two keywords, you can use resources in .NET Framework, .NET Core, or the Windows Runtime to create an asynchronous method almost as easily as you create a synchronous method.</span></span> <span data-ttu-id="19050-135">使用 `async` 關鍵字的非同步方法就稱為*非同步方法*。</span><span class="sxs-lookup"><span data-stu-id="19050-135">Asynchronous methods that you define by using the `async` keyword are referred to as *async methods*.</span></span>

<span data-ttu-id="19050-136">下列範例將示範非同步方法。</span><span class="sxs-lookup"><span data-stu-id="19050-136">The following example shows an async method.</span></span> <span data-ttu-id="19050-137">程式碼中幾乎所有的東西都應該看起來很熟悉。</span><span class="sxs-lookup"><span data-stu-id="19050-137">Almost everything in the code should look familiar to you.</span></span>

<span data-ttu-id="19050-138">您可以 [使用 c # 中的 async 和 await](/samples/dotnet/samples/async-and-await-cs)，找到可從非同步程式設計下載的完整 WINDOWS PRESENTATION FOUNDATION (WPF) 範例。</span><span class="sxs-lookup"><span data-stu-id="19050-138">You can find a complete Windows Presentation Foundation (WPF) example available for download from [Asynchronous programming with async and await in C#](/samples/dotnet/samples/async-and-await-cs).</span></span>

:::code language="csharp" source="snippets/access-web/Program.cs" id="ControlFlow":::

<span data-ttu-id="19050-139">您可以從上述範例中了解數個做法。</span><span class="sxs-lookup"><span data-stu-id="19050-139">You can learn several practices from the preceding sample.</span></span> <span data-ttu-id="19050-140">從方法簽章開始。</span><span class="sxs-lookup"><span data-stu-id="19050-140">Start with the method signature.</span></span> <span data-ttu-id="19050-141">它包括 `async` 修飾詞。</span><span class="sxs-lookup"><span data-stu-id="19050-141">It includes the `async` modifier.</span></span> <span data-ttu-id="19050-142">傳回型別是 `Task<int>` (請參閱＜傳回型別＞一節以取得更多選項)。</span><span class="sxs-lookup"><span data-stu-id="19050-142">The return type is `Task<int>` (See "Return Types" section for more options).</span></span> <span data-ttu-id="19050-143">方法名稱結尾為 `Async`。</span><span class="sxs-lookup"><span data-stu-id="19050-143">The method name ends in `Async`.</span></span> <span data-ttu-id="19050-144">在方法的主體中，`GetStringAsync` 會傳回 `Task<string>`。</span><span class="sxs-lookup"><span data-stu-id="19050-144">In the body of the method, `GetStringAsync` returns a `Task<string>`.</span></span> <span data-ttu-id="19050-145">也就是說，當您 `await` 工作時，您會收到 `string` (`contents`)。</span><span class="sxs-lookup"><span data-stu-id="19050-145">That means that when you `await` the task you'll get a `string` (`contents`).</span></span> <span data-ttu-id="19050-146">等候工作之前，您可以從 `GetStringAsync` 執行不依賴 `string` 的工作。</span><span class="sxs-lookup"><span data-stu-id="19050-146">Before awaiting the task, you can do work that doesn't rely on the `string` from `GetStringAsync`.</span></span>

<span data-ttu-id="19050-147">密切注意 `await` 運算子。</span><span class="sxs-lookup"><span data-stu-id="19050-147">Pay close attention to the `await` operator.</span></span> <span data-ttu-id="19050-148">它會暫停 `GetUrlContentLengthAsync` ：</span><span class="sxs-lookup"><span data-stu-id="19050-148">It suspends `GetUrlContentLengthAsync`:</span></span>

- <span data-ttu-id="19050-149">`GetUrlContentLengthAsync` 無法繼續，直到 `getStringTask` 完成為止。</span><span class="sxs-lookup"><span data-stu-id="19050-149">`GetUrlContentLengthAsync` can't continue until `getStringTask` is complete.</span></span>
- <span data-ttu-id="19050-150">同時，控制項會傳回 `GetUrlContentLengthAsync` 的呼叫端。</span><span class="sxs-lookup"><span data-stu-id="19050-150">Meanwhile, control returns to the caller of `GetUrlContentLengthAsync`.</span></span>
- <span data-ttu-id="19050-151">當 `getStringTask` 完成時，控制項會繼續這裡執行。</span><span class="sxs-lookup"><span data-stu-id="19050-151">Control resumes here when `getStringTask` is complete.</span></span>
- <span data-ttu-id="19050-152">接著，`await` 運算子會從 `getStringTask` 擷取 `string` 結果。</span><span class="sxs-lookup"><span data-stu-id="19050-152">The `await` operator then retrieves the `string` result from `getStringTask`.</span></span>

<span data-ttu-id="19050-153">return 陳述式會指定整數結果。</span><span class="sxs-lookup"><span data-stu-id="19050-153">The return statement specifies an integer result.</span></span> <span data-ttu-id="19050-154">正在等待 `GetUrlContentLengthAsync` 的任何方法都會擷取長度值。</span><span class="sxs-lookup"><span data-stu-id="19050-154">Any methods that are awaiting `GetUrlContentLengthAsync` retrieve the length value.</span></span>

<span data-ttu-id="19050-155">如果 `GetUrlContentLengthAsync` 在呼叫 `GetStringAsync` 與等候其完成之間沒有任何可以執行的工作，您可以在下列單一陳述式中呼叫和等候，以簡化程式碼。</span><span class="sxs-lookup"><span data-stu-id="19050-155">If `GetUrlContentLengthAsync` doesn't have any work that it can do between calling `GetStringAsync` and awaiting its completion, you can simplify your code by calling and awaiting in the following single statement.</span></span>

```csharp
string contents = await client.GetStringAsync("https://docs.microsoft.com/dotnet");
```

<span data-ttu-id="19050-156">下列特性會摘要說明上述範例的非同步方法：</span><span class="sxs-lookup"><span data-stu-id="19050-156">The following characteristics summarize what makes the previous example an async method:</span></span>

- <span data-ttu-id="19050-157">方法簽章包含 `async` 修飾詞。</span><span class="sxs-lookup"><span data-stu-id="19050-157">The method signature includes an `async` modifier.</span></span>
- <span data-ttu-id="19050-158">按照慣例，非同步方法的名稱是以 "Async" 後置字元為結尾。</span><span class="sxs-lookup"><span data-stu-id="19050-158">The name of an async method, by convention, ends with an "Async" suffix.</span></span>
- <span data-ttu-id="19050-159">傳回型別是下列其中一種類型：</span><span class="sxs-lookup"><span data-stu-id="19050-159">The return type is one of the following types:</span></span>

  - <span data-ttu-id="19050-160">如果方法的 return 陳述式中運算元的類型為 `TResult`，則為 <xref:System.Threading.Tasks.Task%601>。</span><span class="sxs-lookup"><span data-stu-id="19050-160"><xref:System.Threading.Tasks.Task%601> if your method has a return statement in which the operand has type `TResult`.</span></span>
  - <span data-ttu-id="19050-161">如果方法沒有 return 陳述式或是 return 陳述式沒有運算元，則為 <xref:System.Threading.Tasks.Task>。</span><span class="sxs-lookup"><span data-stu-id="19050-161"><xref:System.Threading.Tasks.Task> if your method has no return statement or has a return statement with no operand.</span></span>
  - <span data-ttu-id="19050-162">如果您撰寫的是非同步事件處理常式，則為 `void`。</span><span class="sxs-lookup"><span data-stu-id="19050-162">`void` if you're writing an async event handler.</span></span>
  - <span data-ttu-id="19050-163">任何具有 `GetAwaiter` 方法的其他類型 (從 C# 7.0 開始)。</span><span class="sxs-lookup"><span data-stu-id="19050-163">Any other type that has a `GetAwaiter` method (starting with C# 7.0).</span></span>

  <span data-ttu-id="19050-164">如需詳細資訊，請參閱傳回 [類型和參數](#BKMK_ReturnTypesandParameters) 一節。</span><span class="sxs-lookup"><span data-stu-id="19050-164">For more information, see the [Return types and parameters](#BKMK_ReturnTypesandParameters) section.</span></span>

- <span data-ttu-id="19050-165">方法通常至少包含一個 `await` 運算式，表示方法在等候的非同步作業完成後才能繼續的點。</span><span class="sxs-lookup"><span data-stu-id="19050-165">The method usually includes at least one `await` expression, which marks a point where the method can't continue until the awaited asynchronous operation is complete.</span></span> <span data-ttu-id="19050-166">此時，方法已暫停，而且控制權返回到方法的呼叫端。</span><span class="sxs-lookup"><span data-stu-id="19050-166">In the meantime, the method is suspended, and control returns to the method's caller.</span></span> <span data-ttu-id="19050-167">本主題的下一節將說明暫停點會發生什麼情況。</span><span class="sxs-lookup"><span data-stu-id="19050-167">The next section of this topic illustrates what happens at the suspension point.</span></span>

<span data-ttu-id="19050-168">在非同步方法中，您會使用提供的關鍵字和類型表示您想要執行的工作，而編譯器會完成其餘的部分，包括追蹤控制權返回已暫停方法中的等候點時必須進行的作業。</span><span class="sxs-lookup"><span data-stu-id="19050-168">In async methods, you use the provided keywords and types to indicate what you want to do, and the compiler does the rest, including keeping track of what must happen when control returns to an await point in a suspended method.</span></span> <span data-ttu-id="19050-169">某些常式處理序像是迴圈和例外狀況處理，在傳統非同步程式碼中可能不容易處理。</span><span class="sxs-lookup"><span data-stu-id="19050-169">Some routine processes, such as loops and exception handling, can be difficult to handle in traditional asynchronous code.</span></span> <span data-ttu-id="19050-170">在非同步方法中，您可以像在同步方案中一樣撰寫這些項目，如此就可以解決這個問題了。</span><span class="sxs-lookup"><span data-stu-id="19050-170">In an async method, you write these elements much as you would in a synchronous solution, and the problem is solved.</span></span>

<span data-ttu-id="19050-171">如需舊版 .NET Framework 非同步詳細資訊，請參閱 [TPL 和傳統 .NET Framework 非同步程式設計](../../../../standard/parallel-programming/tpl-and-traditional-async-programming.md)。</span><span class="sxs-lookup"><span data-stu-id="19050-171">For more information about asynchrony in previous versions of .NET Framework, see [TPL and traditional .NET Framework asynchronous programming](../../../../standard/parallel-programming/tpl-and-traditional-async-programming.md).</span></span>

## <a name="what-happens-in-an-async-method"></a><a name="BKMK_WhatHappensUnderstandinganAsyncMethod"></a> <span data-ttu-id="19050-172">非同步方法中執行了哪些工作</span><span class="sxs-lookup"><span data-stu-id="19050-172">What happens in an async method</span></span>

<span data-ttu-id="19050-173">在非同步程式設計中要了解的最重要事情，就是控制流程如何在方法之間移動。</span><span class="sxs-lookup"><span data-stu-id="19050-173">The most important thing to understand in asynchronous programming is how the control flow moves from method to method.</span></span> <span data-ttu-id="19050-174">下圖將引導您了解整個程序：</span><span class="sxs-lookup"><span data-stu-id="19050-174">The following diagram leads you through the process:</span></span>

:::image type="content" source="media/task-asynchronous-programming-model/navigation-trace-async-program.png" alt-text="非同步控制流程的追蹤導覽" lightbox="media/task-asynchronous-programming-model/navigation-trace-async-program.png":::

<span data-ttu-id="19050-176">圖表中的數位會對應至下列步驟，這些步驟會在呼叫方法呼叫 async 方法時起始。</span><span class="sxs-lookup"><span data-stu-id="19050-176">The numbers in the diagram correspond to the following steps, initiated when a calling method calls the async method.</span></span>

1. <span data-ttu-id="19050-177">呼叫方法會呼叫並等候 `GetUrlContentLengthAsync` async 方法。</span><span class="sxs-lookup"><span data-stu-id="19050-177">A calling method calls and awaits the `GetUrlContentLengthAsync` async method.</span></span>

1. <span data-ttu-id="19050-178">`GetUrlContentLengthAsync` 會建立 <xref:System.Net.Http.HttpClient> 執行個體並呼叫 <xref:System.Net.Http.HttpClient.GetStringAsync%2A> 非同步方法，將網站的內容當做字串下載。</span><span class="sxs-lookup"><span data-stu-id="19050-178">`GetUrlContentLengthAsync` creates an <xref:System.Net.Http.HttpClient> instance and calls the <xref:System.Net.Http.HttpClient.GetStringAsync%2A> asynchronous method to download the contents of a website as a string.</span></span>

1. <span data-ttu-id="19050-179">`GetStringAsync` 中發生了導致進度暫停的一些狀況。</span><span class="sxs-lookup"><span data-stu-id="19050-179">Something happens in `GetStringAsync` that suspends its progress.</span></span> <span data-ttu-id="19050-180">可能必須等待網站下載或其他封鎖活動。</span><span class="sxs-lookup"><span data-stu-id="19050-180">Perhaps it must wait for a website to download or some other blocking activity.</span></span> <span data-ttu-id="19050-181">為了避免封鎖資源，`GetStringAsync` 會將控制權遞交 (Yield) 給它的呼叫端 `GetUrlContentLengthAsync`。</span><span class="sxs-lookup"><span data-stu-id="19050-181">To avoid blocking resources, `GetStringAsync` yields control to its caller, `GetUrlContentLengthAsync`.</span></span>

     <span data-ttu-id="19050-182">`GetStringAsync` 會傳回 <xref:System.Threading.Tasks.Task%601> (其中 `TResult` 是字串)，而 `GetUrlContentLengthAsync` 則會將工作指派給 `getStringTask` 變數。</span><span class="sxs-lookup"><span data-stu-id="19050-182">`GetStringAsync` returns a <xref:System.Threading.Tasks.Task%601>, where `TResult` is a string, and `GetUrlContentLengthAsync` assigns the task to the `getStringTask` variable.</span></span> <span data-ttu-id="19050-183">工作代表對 `GetStringAsync` 之呼叫的進行中程序，並承諾會在工作完成時產生實際字串值。</span><span class="sxs-lookup"><span data-stu-id="19050-183">The task represents the ongoing process for the call to `GetStringAsync`, with a commitment to produce an actual string value when the work is complete.</span></span>

1. <span data-ttu-id="19050-184">因為尚未等候 `getStringTask`，所以 `GetUrlContentLengthAsync` 可以繼續進行其他不相依於 `GetStringAsync` 之最終結果的其他工作。</span><span class="sxs-lookup"><span data-stu-id="19050-184">Because `getStringTask` hasn't been awaited yet, `GetUrlContentLengthAsync` can continue with other work that doesn't depend on the final result from `GetStringAsync`.</span></span> <span data-ttu-id="19050-185">這項工作是由對同步方法 `DoIndependentWork` 的呼叫來表示。</span><span class="sxs-lookup"><span data-stu-id="19050-185">That work is represented by a call to the synchronous method `DoIndependentWork`.</span></span>

1. <span data-ttu-id="19050-186">`DoIndependentWork` 是完成其工作並傳回其呼叫端的同步方法。</span><span class="sxs-lookup"><span data-stu-id="19050-186">`DoIndependentWork` is a synchronous method that does its work and returns to its caller.</span></span>

1. <span data-ttu-id="19050-187">`GetUrlContentLengthAsync` 已完成所有可處理的工作，但未取得來自 `getStringTask` 的結果。</span><span class="sxs-lookup"><span data-stu-id="19050-187">`GetUrlContentLengthAsync` has run out of work that it can do without a result from `getStringTask`.</span></span> <span data-ttu-id="19050-188">`GetUrlContentLengthAsync` 接著要計算和傳回下載字串的長度，但是方法必須等到有字串時才能計算該值。</span><span class="sxs-lookup"><span data-stu-id="19050-188">`GetUrlContentLengthAsync` next wants to calculate and return the length of the downloaded string, but the method can't calculate that value until the method has the string.</span></span>

    <span data-ttu-id="19050-189">因此，`GetUrlContentLengthAsync` 會使用 await 運算子暫停其進度，並將控制權遞交 (Yield) 給呼叫 `GetUrlContentLengthAsync` 的方法。</span><span class="sxs-lookup"><span data-stu-id="19050-189">Therefore, `GetUrlContentLengthAsync` uses an await operator to suspend its progress and to yield control to the method that called `GetUrlContentLengthAsync`.</span></span> <span data-ttu-id="19050-190">`GetUrlContentLengthAsync` 會將 `Task<int>` 傳回呼叫端。</span><span class="sxs-lookup"><span data-stu-id="19050-190">`GetUrlContentLengthAsync` returns a `Task<int>` to the caller.</span></span> <span data-ttu-id="19050-191">這項工作代表承諾會產生相當於下載字串長度的整數結果。</span><span class="sxs-lookup"><span data-stu-id="19050-191">The task represents a promise to produce an integer result that's the length of the downloaded string.</span></span>

    > [!NOTE]
    > <span data-ttu-id="19050-192">如果 `GetStringAsync` (和 `getStringTask`) 在 `GetUrlContentLengthAsync` 等候它之前先完成，控制權仍會留在 `GetUrlContentLengthAsync`。</span><span class="sxs-lookup"><span data-stu-id="19050-192">If `GetStringAsync` (and therefore `getStringTask`) completes before `GetUrlContentLengthAsync` awaits it, control remains in `GetUrlContentLengthAsync`.</span></span> <span data-ttu-id="19050-193">`GetUrlContentLengthAsync`如果被呼叫的非同步處理已 `getStringTask` 完成，而 `GetUrlContentLengthAsync` 不需要等候最後的結果，則暫停和返回的費用將會浪費。</span><span class="sxs-lookup"><span data-stu-id="19050-193">The expense of suspending and then returning to `GetUrlContentLengthAsync` would be wasted if the called asynchronous process `getStringTask` has already completed and `GetUrlContentLengthAsync` doesn't have to wait for the final result.</span></span>

    <span data-ttu-id="19050-194">在呼叫方法內，處理模式會繼續。</span><span class="sxs-lookup"><span data-stu-id="19050-194">Inside the calling method the processing pattern continues.</span></span> <span data-ttu-id="19050-195">呼叫端可能會在等候結果之前執行其他不取決於 `GetUrlContentLengthAsync` 之結果的工作，或者呼叫端可能立即等候。</span><span class="sxs-lookup"><span data-stu-id="19050-195">The caller might do other work that doesn't depend on the result from `GetUrlContentLengthAsync` before awaiting that result, or the caller might await immediately.</span></span> <span data-ttu-id="19050-196">呼叫方法正在等候，正在 `GetUrlContentLengthAsync` `GetUrlContentLengthAsync` 等候 `GetStringAsync` 。</span><span class="sxs-lookup"><span data-stu-id="19050-196">The calling method is waiting for `GetUrlContentLengthAsync`, and `GetUrlContentLengthAsync` is waiting for `GetStringAsync`.</span></span>

1. <span data-ttu-id="19050-197">`GetStringAsync` 完成並產生字串結果。</span><span class="sxs-lookup"><span data-stu-id="19050-197">`GetStringAsync` completes and produces a string result.</span></span> <span data-ttu-id="19050-198">字串結果不會依照您預期的方式透過呼叫 `GetStringAsync` 來傳回 </span><span class="sxs-lookup"><span data-stu-id="19050-198">The string result isn't returned by the call to `GetStringAsync` in the way that you might expect.</span></span> <span data-ttu-id="19050-199">(請記住，方法已在步驟 3 傳回工作)。字串結果會改為儲存在表示方法 `getStringTask` 完成的工作中。</span><span class="sxs-lookup"><span data-stu-id="19050-199">(Remember that the method already returned a task in step 3.) Instead, the string result is stored in the task that represents the completion of the method, `getStringTask`.</span></span> <span data-ttu-id="19050-200">await 運算子會從 `getStringTask` 擷取結果。</span><span class="sxs-lookup"><span data-stu-id="19050-200">The await operator retrieves the result from `getStringTask`.</span></span> <span data-ttu-id="19050-201">指派陳述式會將擷取的結果指派給 `contents`。</span><span class="sxs-lookup"><span data-stu-id="19050-201">The assignment statement assigns the retrieved result to `contents`.</span></span>

1. <span data-ttu-id="19050-202">當 `GetUrlContentLengthAsync` 擁有字串結果時，方法就可以計算字串的長度。</span><span class="sxs-lookup"><span data-stu-id="19050-202">When `GetUrlContentLengthAsync` has the string result, the method can calculate the length of the string.</span></span> <span data-ttu-id="19050-203">然後 `GetUrlContentLengthAsync` 的工作也已完成，而且等候事件處理常式可以繼續執行。</span><span class="sxs-lookup"><span data-stu-id="19050-203">Then the work of `GetUrlContentLengthAsync` is also complete, and the waiting event handler can resume.</span></span> <span data-ttu-id="19050-204">在本主題最後的完整範例中，您可以確認事件處理常式會擷取並列印長度結果的值。</span><span class="sxs-lookup"><span data-stu-id="19050-204">In the full example at the end of the topic, you can confirm that the event handler retrieves and prints the value of the length result.</span></span>
<span data-ttu-id="19050-205">如果您不熟悉非同步程式設計，請花一分鐘思考同步和非同步行為之間的差異。</span><span class="sxs-lookup"><span data-stu-id="19050-205">If you are new to asynchronous programming, take a minute to consider the difference between synchronous and asynchronous behavior.</span></span> <span data-ttu-id="19050-206">同步方法會在其工作完成時傳回 (步驟 5)，而非同步方法則會在其工作暫停時傳回工作值 (步驟 3 和步驟 6)。</span><span class="sxs-lookup"><span data-stu-id="19050-206">A synchronous method returns when its work is complete (step 5), but an async method returns a task value when its work is suspended (steps 3 and 6).</span></span> <span data-ttu-id="19050-207">當非同步方法最後完成其工作時，工作會標示為已完成，而結果 (如果有的話) 會儲存在工作中。</span><span class="sxs-lookup"><span data-stu-id="19050-207">When the async method eventually completes its work, the task is marked as completed and the result, if any, is stored in the task.</span></span>

## <a name="api-async-methods"></a><a name="BKMK_APIAsyncMethods"></a> <span data-ttu-id="19050-208">API 非同步方法</span><span class="sxs-lookup"><span data-stu-id="19050-208">API async methods</span></span>

<span data-ttu-id="19050-209">您可能會想知道哪裡可以找到支援非同步程式設計的方法，例如 `GetStringAsync`。</span><span class="sxs-lookup"><span data-stu-id="19050-209">You might be wondering where to find methods such as `GetStringAsync` that support async programming.</span></span> <span data-ttu-id="19050-210">.NET Framework 4.5 或更高版本，且 .NET Core 包含許多可搭配和使用的成員 `async` `await` 。</span><span class="sxs-lookup"><span data-stu-id="19050-210">.NET Framework 4.5 or higher and .NET Core contain many members that work with `async` and `await`.</span></span> <span data-ttu-id="19050-211">您可以透過附加至成員名稱的 "Async" 尾碼，以及其對或的傳回型別來辨識它們 <xref:System.Threading.Tasks.Task> <xref:System.Threading.Tasks.Task%601> 。</span><span class="sxs-lookup"><span data-stu-id="19050-211">You can recognize them by the "Async" suffix that's appended to the member name, and by their return type of <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601>.</span></span> <span data-ttu-id="19050-212">例如，相對於同步方法 <xref:System.IO.Stream.CopyTo%2A>、<xref:System.IO.Stream.Read%2A> 和 <xref:System.IO.Stream.Write%2A>，`System.IO.Stream` 類別也包含一些方法，例如 <xref:System.IO.Stream.CopyToAsync%2A>、<xref:System.IO.Stream.ReadAsync%2A> 和 <xref:System.IO.Stream.WriteAsync%2A>。</span><span class="sxs-lookup"><span data-stu-id="19050-212">For example, the `System.IO.Stream` class contains methods such as <xref:System.IO.Stream.CopyToAsync%2A>, <xref:System.IO.Stream.ReadAsync%2A>, and <xref:System.IO.Stream.WriteAsync%2A> alongside the synchronous methods <xref:System.IO.Stream.CopyTo%2A>, <xref:System.IO.Stream.Read%2A>, and <xref:System.IO.Stream.Write%2A>.</span></span>

<span data-ttu-id="19050-213">Windows 執行階段也包含許多您可以在 Windows 應用程式中與 `async` 和 `await` 搭配使用的方法。</span><span class="sxs-lookup"><span data-stu-id="19050-213">The Windows Runtime also contains many methods that you can use with `async` and `await` in Windows apps.</span></span> <span data-ttu-id="19050-214">如需詳細資訊，請參閱適用於 UWP 開發的[執行緒和非同步程式設計](/windows/uwp/threading-async/)，如果您使用舊版的 Windows 執行階段，則請參閱[非同步程式設計 (Microsoft Store 應用程式)](/previous-versions/windows/apps/hh464924(v=win.10)) 和[快速入門：在 C# 或 Visual Basic 中呼叫非同步 API](/previous-versions/windows/apps/hh452713(v=win.10))。</span><span class="sxs-lookup"><span data-stu-id="19050-214">For more information, see [Threading and async programming](/windows/uwp/threading-async/) for UWP development, and [Asynchronous programming (Windows Store apps)](/previous-versions/windows/apps/hh464924(v=win.10)) and [Quickstart: Calling asynchronous APIs in C# or Visual Basic](/previous-versions/windows/apps/hh452713(v=win.10)) if you use earlier versions of the Windows Runtime.</span></span>

## <a name="threads"></a><a name="BKMK_Threads"></a> <span data-ttu-id="19050-215">執行緒</span><span class="sxs-lookup"><span data-stu-id="19050-215">Threads</span></span>

<span data-ttu-id="19050-216">非同步方法主要做為非封鎖作業使用。</span><span class="sxs-lookup"><span data-stu-id="19050-216">Async methods are intended to be non-blocking operations.</span></span> <span data-ttu-id="19050-217">`await`當等候的工作正在執行時，非同步方法中的運算式不會封鎖目前的執行緒。</span><span class="sxs-lookup"><span data-stu-id="19050-217">An `await` expression in an async method doesn't block the current thread while the awaited task is running.</span></span> <span data-ttu-id="19050-218">運算式會改為註冊方法的其餘部分做為接續，並將控制權交還給非同步方法的呼叫端。</span><span class="sxs-lookup"><span data-stu-id="19050-218">Instead, the expression signs up the rest of the method as a continuation and returns control to the caller of the async method.</span></span>

<span data-ttu-id="19050-219">`async` 和 `await` 關鍵字不會導致建立其他執行緒。</span><span class="sxs-lookup"><span data-stu-id="19050-219">The `async` and `await` keywords don't cause additional threads to be created.</span></span> <span data-ttu-id="19050-220">由於非同步方法不會在本身的執行緒上執行，因此非同步方法不需要多執行緒。</span><span class="sxs-lookup"><span data-stu-id="19050-220">Async methods don't require multithreading because an async method doesn't run on its own thread.</span></span> <span data-ttu-id="19050-221">方法會在目前的同步處理內容執行，而且只有在方法為作用中時才會在執行緒上花費時間。</span><span class="sxs-lookup"><span data-stu-id="19050-221">The method runs on the current synchronization context and uses time on the thread only when the method is active.</span></span> <span data-ttu-id="19050-222">您可以使用 <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> 將受限於 CPU 的工作移到背景執行緒，但是背景執行緒無法協助處理正在等待結果產生的處理序。</span><span class="sxs-lookup"><span data-stu-id="19050-222">You can use <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> to move CPU-bound work to a background thread, but a background thread doesn't help with a process that's just waiting for results to become available.</span></span>

<span data-ttu-id="19050-223">非同步程式設計的非同步方法幾乎是所有案例的現有方法當中較好的方法。</span><span class="sxs-lookup"><span data-stu-id="19050-223">The async-based approach to asynchronous programming is preferable to existing approaches in almost every case.</span></span> <span data-ttu-id="19050-224">特別是，這個方法比受限於 I/O 作業的 <xref:System.ComponentModel.BackgroundWorker> 類別還要好，因為程式碼較簡單，而且不需要防範競爭條件。</span><span class="sxs-lookup"><span data-stu-id="19050-224">In particular, this approach is better than the <xref:System.ComponentModel.BackgroundWorker> class for I/O-bound operations because the code is simpler and you don't have to guard against race conditions.</span></span> <span data-ttu-id="19050-225">相較于 <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> 方法，非同步程式設計比針對 CPU 系結作業更好， <xref:System.ComponentModel.BackgroundWorker> 因為非同步程式設計會將執行程式碼的協調細節，從 `Task.Run` 傳輸到執行緒集區的工作分離出來。</span><span class="sxs-lookup"><span data-stu-id="19050-225">In combination with the <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> method, async programming is better than <xref:System.ComponentModel.BackgroundWorker> for CPU-bound operations because async programming separates the coordination details of running your code from the work that `Task.Run` transfers to the thread pool.</span></span>

## <a name="async-and-await"></a><a name="BKMK_AsyncandAwait"></a> <span data-ttu-id="19050-226">async 和 await</span><span class="sxs-lookup"><span data-stu-id="19050-226">async and await</span></span>

<span data-ttu-id="19050-227">如果您使用 [async](../../../language-reference/keywords/async.md) 修飾詞來將方法指定為非同步方法，就會啟用下列兩項功能。</span><span class="sxs-lookup"><span data-stu-id="19050-227">If you specify that a method is an async method by using the [async](../../../language-reference/keywords/async.md) modifier, you enable the following two capabilities.</span></span>

- <span data-ttu-id="19050-228">標記的非同步方法可以使用 [await](../../../language-reference/operators/await.md) 來指定暫停點。</span><span class="sxs-lookup"><span data-stu-id="19050-228">The marked async method can use [await](../../../language-reference/operators/await.md) to designate suspension points.</span></span> <span data-ttu-id="19050-229">`await` 運算子會告知編譯器，非同步方法只有在等候的非同步處理序完成後，才能繼續通過該點。</span><span class="sxs-lookup"><span data-stu-id="19050-229">The `await` operator tells the compiler that the async method can't continue past that point until the awaited asynchronous process is complete.</span></span> <span data-ttu-id="19050-230">同時，控制權會返回非同步方法的呼叫端。</span><span class="sxs-lookup"><span data-stu-id="19050-230">In the meantime, control returns to the caller of the async method.</span></span>

     <span data-ttu-id="19050-231">非同步方法在運算式中的暫止 `await` 不會構成方法的結束，而且區塊也 `finally` 不會執行。</span><span class="sxs-lookup"><span data-stu-id="19050-231">The suspension of an async method at an `await` expression doesn't constitute an exit from the method, and `finally` blocks don't run.</span></span>

- <span data-ttu-id="19050-232">標記的非同步方法本身可以做為其呼叫方法的等候目標。</span><span class="sxs-lookup"><span data-stu-id="19050-232">The marked async method can itself be awaited by methods that call it.</span></span>

<span data-ttu-id="19050-233">非同步方法通常會包含一或多個出現的 `await` 運算子，但不存在的 `await` 運算式不會造成編譯器錯誤。</span><span class="sxs-lookup"><span data-stu-id="19050-233">An async method typically contains one or more occurrences of an `await` operator, but the absence of `await` expressions doesn't cause a compiler error.</span></span> <span data-ttu-id="19050-234">如果非同步方法未使用 `await` 運算子來標記暫停點，則不論修飾詞為何，方法都會以同步方法執行 `async` 。</span><span class="sxs-lookup"><span data-stu-id="19050-234">If an async method doesn't use an `await` operator to mark a suspension point, the method executes as a synchronous method does, despite the `async` modifier.</span></span> <span data-ttu-id="19050-235">編譯器將對這類方法發出警告。</span><span class="sxs-lookup"><span data-stu-id="19050-235">The compiler issues a warning for such methods.</span></span>

<span data-ttu-id="19050-236">`async` 和 `await` 都是內容關鍵字。</span><span class="sxs-lookup"><span data-stu-id="19050-236">`async` and `await` are contextual keywords.</span></span> <span data-ttu-id="19050-237">如需詳細資訊和範例，請參閱下列主題：</span><span class="sxs-lookup"><span data-stu-id="19050-237">For more information and examples, see the following topics:</span></span>

- [<span data-ttu-id="19050-238">async</span><span class="sxs-lookup"><span data-stu-id="19050-238">async</span></span>](../../../language-reference/keywords/async.md)
- [<span data-ttu-id="19050-239">await</span><span class="sxs-lookup"><span data-stu-id="19050-239">await</span></span>](../../../language-reference/operators/await.md)

## <a name="return-types-and-parameters"></a><a name="BKMK_ReturnTypesandParameters"></a> <span data-ttu-id="19050-240">傳回類型和參數</span><span class="sxs-lookup"><span data-stu-id="19050-240">Return types and parameters</span></span>

<span data-ttu-id="19050-241">async 方法通常會傳回 <xref:System.Threading.Tasks.Task> 或 <xref:System.Threading.Tasks.Task%601>。</span><span class="sxs-lookup"><span data-stu-id="19050-241">An async method typically returns a <xref:System.Threading.Tasks.Task> or a <xref:System.Threading.Tasks.Task%601>.</span></span> <span data-ttu-id="19050-242">在非同步方法內，會將 `await` 運算子套用到呼叫另一個非同步方法所傳回的工作。</span><span class="sxs-lookup"><span data-stu-id="19050-242">Inside an async method, an `await` operator is applied to a task that's returned from a call to another async method.</span></span>

<span data-ttu-id="19050-243"><xref:System.Threading.Tasks.Task%601>如果方法包含 [`return`](../../../language-reference/keywords/return.md) 指定類型運算元的語句，您可以指定做為傳回型別 `TResult` 。</span><span class="sxs-lookup"><span data-stu-id="19050-243">You specify <xref:System.Threading.Tasks.Task%601> as the return type if the method contains a [`return`](../../../language-reference/keywords/return.md) statement that specifies an operand of type `TResult`.</span></span>

<span data-ttu-id="19050-244">如果方法沒有 return 陳述式，或者方法的 return 陳述式不會傳回運算元，請使用 <xref:System.Threading.Tasks.Task> 作為傳回型別。</span><span class="sxs-lookup"><span data-stu-id="19050-244">You use <xref:System.Threading.Tasks.Task>  as the return type if the method has no return statement or has a return statement that doesn't return an operand.</span></span>

<span data-ttu-id="19050-245">從 C# 7.0 開始，您也可以指定任何其他傳回型別，前提是該類型包括 `GetAwaiter` 方法。</span><span class="sxs-lookup"><span data-stu-id="19050-245">Starting with C# 7.0, you can also specify any other return type, provided that the type includes a `GetAwaiter` method.</span></span> <span data-ttu-id="19050-246">這類類型的範例是 <xref:System.Threading.Tasks.ValueTask%601>。</span><span class="sxs-lookup"><span data-stu-id="19050-246"><xref:System.Threading.Tasks.ValueTask%601> is an example of such a type.</span></span> <span data-ttu-id="19050-247">它提供於 [System.Threading.Tasks.Extension](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) NuGet 套件中。</span><span class="sxs-lookup"><span data-stu-id="19050-247">It is available in the [System.Threading.Tasks.Extension](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) NuGet package.</span></span>

<span data-ttu-id="19050-248">下列範例顯示如何宣告和呼叫傳回 <xref:System.Threading.Tasks.Task%601> 或的方法 <xref:System.Threading.Tasks.Task> ：</span><span class="sxs-lookup"><span data-stu-id="19050-248">The following example shows how you declare and call a method that returns a <xref:System.Threading.Tasks.Task%601> or a <xref:System.Threading.Tasks.Task>:</span></span>

```csharp
async Task<int> GetTaskOfTResultAsync()
{
    int hours = 0;
    await Task.Delay(0);

    return hours;
}


Task<int> returnedTaskTResult = GetTaskOfTResultAsync();
int intResult = await returnedTaskTResult;
// Single line
// int intResult = await GetTaskOfTResultAsync();

async Task GetTaskAsync()
{
    await Task.Delay(0);
    // No return statement needed
}

Task returnedTask = GetTaskAsync();
await returnedTask;
// Single line
await GetTaskAsync();
```

<span data-ttu-id="19050-249">每項傳回的工作都代表進行中的工作。</span><span class="sxs-lookup"><span data-stu-id="19050-249">Each returned task represents ongoing work.</span></span> <span data-ttu-id="19050-250">工作會封裝這個非同步處理序狀態的相關資訊，以及處理序的最終結果，或是處理序不成功時，則會封裝處理序引發的例外狀況。</span><span class="sxs-lookup"><span data-stu-id="19050-250">A task encapsulates information about the state of the asynchronous process and, eventually, either the final result from the process or the exception that the process raises if it doesn't succeed.</span></span>

<span data-ttu-id="19050-251">非同步方法的傳回型別也可以是 `void`。</span><span class="sxs-lookup"><span data-stu-id="19050-251">An async method can also have a `void` return type.</span></span> <span data-ttu-id="19050-252">這個傳回類型主要用於定義需要 `void` 傳回類型的事件處理常式。</span><span class="sxs-lookup"><span data-stu-id="19050-252">This return type is used primarily to define event handlers, where a `void` return type is required.</span></span> <span data-ttu-id="19050-253">非同步事件處理常式通常做為非同步程式的起點。</span><span class="sxs-lookup"><span data-stu-id="19050-253">Async event handlers often serve as the starting point for async programs.</span></span>

<span data-ttu-id="19050-254">具有傳回類型的非同步方法 `void` 無法等候，而且 void 傳回方法的呼叫端無法攔截方法擲回的任何例外狀況。</span><span class="sxs-lookup"><span data-stu-id="19050-254">An async method that has a `void` return type can't be awaited, and the caller of a void-returning method can't catch any exceptions that the method throws.</span></span>

<span data-ttu-id="19050-255">非同步方法無法宣告 [in](../../../language-reference/keywords/in-parameter-modifier.md)、[ref](../../../language-reference/keywords/ref.md) 或 [out](../../../language-reference/keywords/out-parameter-modifier.md) 參數，但是此方法可以呼叫具有這類參數的方法。</span><span class="sxs-lookup"><span data-stu-id="19050-255">An async method can't declare [in](../../../language-reference/keywords/in-parameter-modifier.md), [ref](../../../language-reference/keywords/ref.md) or [out](../../../language-reference/keywords/out-parameter-modifier.md) parameters, but the method can call methods that have such parameters.</span></span> <span data-ttu-id="19050-256">同樣地，async 方法無法以傳址方式傳回值，但可以使用 ref 傳回值呼叫方法。</span><span class="sxs-lookup"><span data-stu-id="19050-256">Similarly, an async method can't return a value by reference, although it can call methods with ref return values.</span></span>

<span data-ttu-id="19050-257">如需詳細資訊和範例，請參閱 [非同步傳回類型 (c # ) ](async-return-types.md)。</span><span class="sxs-lookup"><span data-stu-id="19050-257">For more information and examples, see [Async return types (C#)](async-return-types.md).</span></span> <span data-ttu-id="19050-258">如需如何在非同步方法中攔截例外狀況的詳細資訊，請參閱 [try-catch](../../../language-reference/keywords/try-catch.md)。</span><span class="sxs-lookup"><span data-stu-id="19050-258">For more information about how to catch exceptions in async methods, see [try-catch](../../../language-reference/keywords/try-catch.md).</span></span>

<span data-ttu-id="19050-259">Windows 執行階段程式設計中的非同步 API 具有下列其中一種傳回型別 (類似於工作)：</span><span class="sxs-lookup"><span data-stu-id="19050-259">Asynchronous APIs in Windows Runtime programming have one of the following return types, which are similar to tasks:</span></span>

- <span data-ttu-id="19050-260"><xref:Windows.Foundation.IAsyncOperation%601>，對應至 <xref:System.Threading.Tasks.Task%601></span><span class="sxs-lookup"><span data-stu-id="19050-260"><xref:Windows.Foundation.IAsyncOperation%601>, which corresponds to <xref:System.Threading.Tasks.Task%601></span></span>
- <span data-ttu-id="19050-261"><xref:Windows.Foundation.IAsyncAction>，對應至 <xref:System.Threading.Tasks.Task></span><span class="sxs-lookup"><span data-stu-id="19050-261"><xref:Windows.Foundation.IAsyncAction>, which corresponds to <xref:System.Threading.Tasks.Task></span></span>
- <xref:Windows.Foundation.IAsyncActionWithProgress%601>
- <xref:Windows.Foundation.IAsyncOperationWithProgress%602>

## <a name="naming-convention"></a><a name="BKMK_NamingConvention"></a> <span data-ttu-id="19050-262">命名慣例</span><span class="sxs-lookup"><span data-stu-id="19050-262">Naming convention</span></span>

<span data-ttu-id="19050-263">依照慣例，傳回常用可等候型別的方法 (例如，、、、 `Task` `Task<T>`) 的 `ValueTask` `ValueTask<T>` 名稱應該以 "Async" 結尾。</span><span class="sxs-lookup"><span data-stu-id="19050-263">By convention, methods that return commonly awaitable types (for example, `Task`, `Task<T>`, `ValueTask`, `ValueTask<T>`) should have names that end with "Async".</span></span> <span data-ttu-id="19050-264">會開始執行非同步作業但不會傳回可等候類型的方法不應該具有結尾為 "Async" 的名稱，但其名稱開頭可以是 "Begin"、"Start" 或一些其他動詞，以建議此方法不會傳回或擲回作業結果。</span><span class="sxs-lookup"><span data-stu-id="19050-264">Methods that start an asynchronous operation but do not return an awaitable type should not have names that end with "Async", but may start with "Begin", "Start", or some other verb to suggest this method does not return or throw the result of the operation.</span></span>

<span data-ttu-id="19050-265">當事件、基底類別或介面合約採用不同的名稱時，您可以忽略慣例。</span><span class="sxs-lookup"><span data-stu-id="19050-265">You can ignore the convention where an event, base class, or interface contract suggests a different name.</span></span> <span data-ttu-id="19050-266">例如，您不應該重新命名常見的事件處理常式，例如 `OnButtonClick` 。</span><span class="sxs-lookup"><span data-stu-id="19050-266">For example, you shouldn't rename common event handlers, such as `OnButtonClick`.</span></span>

## <a name="related-topics-and-samples-visual-studio"></a><a name="BKMK_RelatedTopics"></a> <span data-ttu-id="19050-267">相關主題和範例 (Visual Studio)</span><span class="sxs-lookup"><span data-stu-id="19050-267">Related topics and samples (Visual Studio)</span></span>

| <span data-ttu-id="19050-268">Title</span><span class="sxs-lookup"><span data-stu-id="19050-268">Title</span></span> | <span data-ttu-id="19050-269">描述</span><span class="sxs-lookup"><span data-stu-id="19050-269">Description</span></span> | <span data-ttu-id="19050-270">範例</span><span class="sxs-lookup"><span data-stu-id="19050-270">Sample</span></span> |
|--|--|--|
| [<span data-ttu-id="19050-271">如何使用 async 和 await，同時發出多個 web 要求 (c # ) </span><span class="sxs-lookup"><span data-stu-id="19050-271">How to make multiple web requests in parallel by using async and await (C#)</span></span>](./index.md) | <span data-ttu-id="19050-272">示範如何同時啟動數個工作。</span><span class="sxs-lookup"><span data-stu-id="19050-272">Demonstrates how to start several tasks at the same time.</span></span> | [<span data-ttu-id="19050-273">非同步範例：平行進行多個 Web 要求 (英文)</span><span class="sxs-lookup"><span data-stu-id="19050-273">Async Sample: Make Multiple Web Requests in Parallel</span></span>](https://code.msdn.microsoft.com/Async-Make-Multiple-Web-49adb82e) |
| [<span data-ttu-id="19050-274">非同步傳回類型 (c # ) </span><span class="sxs-lookup"><span data-stu-id="19050-274">Async return types (C#)</span></span>](async-return-types.md) | <span data-ttu-id="19050-275">說明非同步方法可以傳回的類型，並說明每個類型的適用時機。</span><span class="sxs-lookup"><span data-stu-id="19050-275">Illustrates the types that async methods can return, and explains when each type is appropriate.</span></span> |  |
| <span data-ttu-id="19050-276">使用解除標記做為信號機制來取消工作。</span><span class="sxs-lookup"><span data-stu-id="19050-276">Cancel tasks with a cancellation token as a signaling mechanism.</span></span> | <span data-ttu-id="19050-277">顯示如何將下列功能加入至您的非同步方案：</span><span class="sxs-lookup"><span data-stu-id="19050-277">Shows how to add the following functionality to your async solution:</span></span><br><br> <span data-ttu-id="19050-278">- [取消 (c # ) 的工作清單 ](cancel-an-async-task-or-a-list-of-tasks.md)</span><span class="sxs-lookup"><span data-stu-id="19050-278">- [Cancel a list of tasks (C#)](cancel-an-async-task-or-a-list-of-tasks.md)</span></span><br><span data-ttu-id="19050-279">- [在一段時間後取消工作 (c # ) ](cancel-async-tasks-after-a-period-of-time.md)</span><span class="sxs-lookup"><span data-stu-id="19050-279">- [Cancel tasks after a period of time (C#)](cancel-async-tasks-after-a-period-of-time.md)</span></span><br><span data-ttu-id="19050-280">- [在非同步工作完成時加以處理 (c # ) ](start-multiple-async-tasks-and-process-them-as-they-complete.md)</span><span class="sxs-lookup"><span data-stu-id="19050-280">- [Process asynchronous task as they complete (C#)](start-multiple-async-tasks-and-process-them-as-they-complete.md)</span></span> |  |
| [<span data-ttu-id="19050-281">使用 async 進行檔案存取 (c # ) </span><span class="sxs-lookup"><span data-stu-id="19050-281">Using async for file access (C#)</span></span>](using-async-for-file-access.md) | <span data-ttu-id="19050-282">列出並示範使用 async 和 await 存取檔案的優點。</span><span class="sxs-lookup"><span data-stu-id="19050-282">Lists and demonstrates the benefits of using async and await to access files.</span></span> |  |
| [<span data-ttu-id="19050-283">以工作為基礎的非同步模式 (點擊) </span><span class="sxs-lookup"><span data-stu-id="19050-283">Task-based asynchronous pattern (TAP)</span></span>](../../../../standard/asynchronous-programming-patterns/task-based-asynchronous-pattern-tap.md) | <span data-ttu-id="19050-284">描述非同步模式，此模式是以 <xref:System.Threading.Tasks.Task> 和類型為基礎 <xref:System.Threading.Tasks.Task%601> 。</span><span class="sxs-lookup"><span data-stu-id="19050-284">Describes an asynchronous pattern, the pattern is based on the <xref:System.Threading.Tasks.Task> and <xref:System.Threading.Tasks.Task%601> types.</span></span> |  |
| [<span data-ttu-id="19050-285">Channel 9 上的非同步影片</span><span class="sxs-lookup"><span data-stu-id="19050-285">Async Videos on Channel 9</span></span>](https://channel9.msdn.com/search?term=async%20&type=All#pubDate=year&ch9Search&lang-en=en) | <span data-ttu-id="19050-286">提供有關非同步程式設計的各種不同視訊連結。</span><span class="sxs-lookup"><span data-stu-id="19050-286">Provides links to a variety of videos about async programming.</span></span> |  |

## <a name="see-also"></a><span data-ttu-id="19050-287">另請參閱</span><span class="sxs-lookup"><span data-stu-id="19050-287">See also</span></span>

- [<span data-ttu-id="19050-288">async</span><span class="sxs-lookup"><span data-stu-id="19050-288">async</span></span>](../../../language-reference/keywords/async.md)
- [<span data-ttu-id="19050-289">await</span><span class="sxs-lookup"><span data-stu-id="19050-289">await</span></span>](../../../language-reference/operators/await.md)
- [<span data-ttu-id="19050-290">非同步程式設計</span><span class="sxs-lookup"><span data-stu-id="19050-290">Asynchronous programming</span></span>](../../../async.md)
- [<span data-ttu-id="19050-291">非同步總覽</span><span class="sxs-lookup"><span data-stu-id="19050-291">Async overview</span></span>](../../../../standard/async.md)
