---
title: 如何使用模式匹配和作為運算子安全地強制轉換
description: 了解如何使用模式比對技術，將變數安全地轉換為不同類型。 您可以使用模式比對、is 和 as 運算子，安全地進行轉換類型。
ms.date: 09/05/2018
helpviewer_keywords:
- cast operators [C#], as and is operators
- as operator [C#]
- is operator [C#]
ms.openlocfilehash: 34c34865a2feece5fe037daeb035292aafd015b9
ms.sourcegitcommit: 99b153b93bf94d0fecf7c7bcecb58ac424dfa47c
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 03/25/2020
ms.locfileid: "80249353"
---
# <a name="how-to-safely-cast-by-using-pattern-matching-and-the-is-and-as-operators"></a><span data-ttu-id="fce5e-104">如何使用模式匹配和作為運算子安全地強制轉換</span><span class="sxs-lookup"><span data-stu-id="fce5e-104">How to safely cast by using pattern matching and the is and as operators</span></span>

<span data-ttu-id="fce5e-105">因為物件都是多型的，所以可能會針對基底類別類型的變數來保存衍生[類型](../programming-guide/types/index.md)。</span><span class="sxs-lookup"><span data-stu-id="fce5e-105">Because objects are polymorphic, it's possible for a variable of a base class type to hold a derived [type](../programming-guide/types/index.md).</span></span> <span data-ttu-id="fce5e-106">若要存取衍生類型的執行個體成員，就需要將值[轉換](../programming-guide/types/casting-and-type-conversions.md)回衍生類型。</span><span class="sxs-lookup"><span data-stu-id="fce5e-106">To access the derived type's instance members, it's necessary to [cast](../programming-guide/types/casting-and-type-conversions.md) the value back to the derived type.</span></span> <span data-ttu-id="fce5e-107">不過，轉換帶有擲回 <xref:System.InvalidCastException> 的風險。</span><span class="sxs-lookup"><span data-stu-id="fce5e-107">However, a cast creates the risk of throwing an <xref:System.InvalidCastException>.</span></span> <span data-ttu-id="fce5e-108">C# 提供[模式比對](../pattern-matching.md)陳述式，只有在成功時才會有條件地執行轉換。</span><span class="sxs-lookup"><span data-stu-id="fce5e-108">C# provides [pattern matching](../pattern-matching.md) statements that perform a cast conditionally only when it will succeed.</span></span> <span data-ttu-id="fce5e-109">C# 也提供 [is](../language-reference/operators/type-testing-and-cast.md#is-operator) 和 [as](../language-reference/operators/type-testing-and-cast.md#as-operator) 運算子，藉此測試值是否為特定類型。</span><span class="sxs-lookup"><span data-stu-id="fce5e-109">C# also provides the [is](../language-reference/operators/type-testing-and-cast.md#is-operator) and [as](../language-reference/operators/type-testing-and-cast.md#as-operator) operators to test if a value is of a certain type.</span></span>

<span data-ttu-id="fce5e-110">下面的示例演示如何使用模式匹配`is`語句：</span><span class="sxs-lookup"><span data-stu-id="fce5e-110">The following example shows how to use the pattern matching `is` statement:</span></span>

[!code-csharp[Pattern matching is statement](../../../samples/snippets/csharp/how-to/safelycast/patternmatching/Program.cs#PatternMatchingIs)]

<span data-ttu-id="fce5e-111">上述範例示範模式比對語法的一些功能。</span><span class="sxs-lookup"><span data-stu-id="fce5e-111">The preceding sample demonstrates a few features of pattern matching syntax.</span></span> <span data-ttu-id="fce5e-112">語句`if (a is Mammal m)`將測試與初始化分配相結合。</span><span class="sxs-lookup"><span data-stu-id="fce5e-112">The `if (a is Mammal m)` statement combines the test with an initialization assignment.</span></span> <span data-ttu-id="fce5e-113">只有在測試成功時，才會進行指派。</span><span class="sxs-lookup"><span data-stu-id="fce5e-113">The assignment occurs only when the test succeeds.</span></span> <span data-ttu-id="fce5e-114">變數 `m` 僅會在已指派的內嵌 `if` 陳述式範圍內。</span><span class="sxs-lookup"><span data-stu-id="fce5e-114">The variable `m` is only in scope in the embedded `if` statement where it has been assigned.</span></span> <span data-ttu-id="fce5e-115">您隨後無法使用相同的方法存取 `m`。</span><span class="sxs-lookup"><span data-stu-id="fce5e-115">You cannot access `m` later in the same method.</span></span> <span data-ttu-id="fce5e-116">前面的示例還演示如何使用[`as`運算子](../language-reference/operators/type-testing-and-cast.md#as-operator)將物件轉換為指定類型。</span><span class="sxs-lookup"><span data-stu-id="fce5e-116">The preceding example also shows how to use the [`as` operator](../language-reference/operators/type-testing-and-cast.md#as-operator) to convert an object to a specified type.</span></span>

<span data-ttu-id="fce5e-117">如果[空數值型別](../language-reference/builtin-types/nullable-value-types.md)具有值，也可以使用相同的語法進行測試，如以下示例所示：</span><span class="sxs-lookup"><span data-stu-id="fce5e-117">You can also use the same syntax for testing if a [nullable value type](../language-reference/builtin-types/nullable-value-types.md) has a value, as shown in the following example:</span></span>

[!code-csharp[Pattern matching with nullable types](../../../samples/snippets/csharp/how-to/safelycast/nullablepatternmatching/Program.cs#PatternMatchingNullable)]

<span data-ttu-id="fce5e-118">上述範例示範模式比對轉換的其他功能，以便與轉換一起使用。</span><span class="sxs-lookup"><span data-stu-id="fce5e-118">The preceding sample demonstrates other features of pattern matching to use with conversions.</span></span> <span data-ttu-id="fce5e-119">您可以特別檢查 `null` 值，藉此測試 Null 模式的變數。</span><span class="sxs-lookup"><span data-stu-id="fce5e-119">You can test a variable for the null pattern by checking specifically for the `null` value.</span></span> <span data-ttu-id="fce5e-120">當執行階段變數的值是 `null`，則 `is` 陳述式的類型檢查一律會傳回 `false`。</span><span class="sxs-lookup"><span data-stu-id="fce5e-120">When the runtime value of the variable is `null`, an `is` statement checking for a type always returns `false`.</span></span> <span data-ttu-id="fce5e-121">模式比對 `is` 陳述式不允許可為 Null 的值型別 (例如 `int?` 或 `Nullable<int>`)，但您可以測試其他值的型別。</span><span class="sxs-lookup"><span data-stu-id="fce5e-121">The pattern matching `is` statement doesn't allow a nullable value type, such as `int?` or `Nullable<int>`, but you can test for any other value type.</span></span> <span data-ttu-id="fce5e-122">上`is`例中的模式不限於空數值型別。</span><span class="sxs-lookup"><span data-stu-id="fce5e-122">The `is` patterns from the preceding example are not limited to the nullable value types.</span></span> <span data-ttu-id="fce5e-123">您還可以使用這些模式來測試參考型別的變數是否具有值或該`null`值。</span><span class="sxs-lookup"><span data-stu-id="fce5e-123">You also can use those patterns to test if a variable of a reference type has a value or it's `null`.</span></span>

<span data-ttu-id="fce5e-124">前面的示例還顯示了如何在`switch`語句中使用類型模式，其中變數可能是許多不同類型的類型之一。</span><span class="sxs-lookup"><span data-stu-id="fce5e-124">The preceding sample also shows how you use the type pattern in a `switch` statement where the variable may be one of many different types.</span></span>

<span data-ttu-id="fce5e-125">如果要測試變數是否為給定類型，但不將其分配給新變數，則可以使用`is`和`as`運算子進行參考型別和空數值型別。</span><span class="sxs-lookup"><span data-stu-id="fce5e-125">If you want to test if a variable is a given type, but not assign it to a new variable, you can use the `is` and `as` operators for reference types and nullable value types.</span></span> <span data-ttu-id="fce5e-126">下列程式碼示範在模式比對推出之前，如何使用屬於 C# 語言的 `is` 和 `as` 陳述式測試變數是否屬於特定類型：</span><span class="sxs-lookup"><span data-stu-id="fce5e-126">The following code shows how to use the `is` and `as` statements that were part of the C# language before pattern matching was introduced to test if a variable is of a given type:</span></span>

[!code-csharp[testing variable types with the is and as statements](../../../samples/snippets/csharp/how-to/safelycast/asandis/Program.cs#IsAndAs)]

<span data-ttu-id="fce5e-127">透過將此程式碼與模式比對程式碼進行比較，明顯可見，模式比對語法藉著在單一陳述式中結合測試和指派來提供更強大的功能。</span><span class="sxs-lookup"><span data-stu-id="fce5e-127">As you can see by comparing this code with the pattern matching code, the pattern matching syntax provides more robust features by combining the test and the assignment in a single statement.</span></span> <span data-ttu-id="fce5e-128">請盡可能使用模式比對語法。</span><span class="sxs-lookup"><span data-stu-id="fce5e-128">Use the pattern matching syntax whenever possible.</span></span>

<span data-ttu-id="fce5e-129">您可以通過查看[GitHub 存儲庫](https://github.com/dotnet/samples/tree/master/snippets/csharp/how-to/safelycast)中的代碼來嘗試這些示例。</span><span class="sxs-lookup"><span data-stu-id="fce5e-129">You can try these samples by looking at the code in our [GitHub repository](https://github.com/dotnet/samples/tree/master/snippets/csharp/how-to/safelycast).</span></span> <span data-ttu-id="fce5e-130">或者，您可以將範例下載[為 ZIP 檔案](https://github.com/dotnet/samples/raw/master/snippets/csharp/how-to/safelycast.zip)。</span><span class="sxs-lookup"><span data-stu-id="fce5e-130">Or you can download the samples [as a zip file](https://github.com/dotnet/samples/raw/master/snippets/csharp/how-to/safelycast.zip).</span></span>
