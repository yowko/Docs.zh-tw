---
title: 編譯器錯誤 CS0246
ms.date: 01/23/2018
f1_keywords:
- CS0246
helpviewer_keywords:
- CS0246
ms.assetid: 4948fae2-2cc0-4ce4-b98c-ea69a8120b71
ms.openlocfilehash: 2a1637a641987b35c2edc8379bf6cdd2c87b9aa3
ms.sourcegitcommit: 2eceb05f1a5bb261291a1f6a91c5153727ac1c19
ms.translationtype: HT
ms.contentlocale: zh-TW
ms.lasthandoff: 09/04/2018
ms.locfileid: "43507157"
---
# <a name="compiler-error-cs0246"></a><span data-ttu-id="d89fa-102">編譯器錯誤 CS0246</span><span class="sxs-lookup"><span data-stu-id="d89fa-102">Compiler Error CS0246</span></span>
<span data-ttu-id="d89fa-103">找不到類型或命名空間名稱 'type/namespace' (您是否遺漏 using 指示詞或組件參考？)</span><span class="sxs-lookup"><span data-stu-id="d89fa-103">The type or namespace name 'type/namespace' could not be found (are you missing a using directive or an assembly reference?)</span></span>  
  
 <span data-ttu-id="d89fa-104">找不到在程式中使用的類型或命名空間。</span><span class="sxs-lookup"><span data-stu-id="d89fa-104">A type or namespace that is used in the program was not found.</span></span> <span data-ttu-id="d89fa-105">您可能忘了參考 ([-reference](../../../csharp/language-reference/compiler-options/reference-compiler-option.md)) 包含類型的組件，或者您可能未新增必要的 [using 指示詞](../../../csharp/language-reference/keywords/using-directive.md)。</span><span class="sxs-lookup"><span data-stu-id="d89fa-105">You might have forgotten to reference ([-reference](../../../csharp/language-reference/compiler-options/reference-compiler-option.md)) the assembly that contains the type, or you might not have added the required [using directive](../../../csharp/language-reference/keywords/using-directive.md).</span></span>  <span data-ttu-id="d89fa-106">或者，您嘗試參考的組件可能發生問題。</span><span class="sxs-lookup"><span data-stu-id="d89fa-106">Or, there might be an issue with the assembly you are trying to reference.</span></span>  
  
 <span data-ttu-id="d89fa-107">下列狀況會造成編譯器錯誤 CS0246。</span><span class="sxs-lookup"><span data-stu-id="d89fa-107">The following situations cause compiler error CS0246.</span></span>  
  
-   <span data-ttu-id="d89fa-108">是否拼錯類型或命名空間的名稱？</span><span class="sxs-lookup"><span data-stu-id="d89fa-108">Did you misspell the name of the type or namespace?</span></span> <span data-ttu-id="d89fa-109">如果名稱不正確，則編譯器會找不到類型或命名空間的定義。</span><span class="sxs-lookup"><span data-stu-id="d89fa-109">Without the correct name, the compiler cannot find the definition for the type or namespace.</span></span> <span data-ttu-id="d89fa-110">因為類型名稱中所使用的大小寫不正確，所以通常會發生這個錯誤。</span><span class="sxs-lookup"><span data-stu-id="d89fa-110">This often occurs because the casing used in the name of the type is not correct.</span></span> <span data-ttu-id="d89fa-111">例如， `Dataset ds;` 會產生 CS0246，因為 Dataset 中的 s 必須大寫。</span><span class="sxs-lookup"><span data-stu-id="d89fa-111">For example, `Dataset ds;` generates CS0246 because the s in Dataset must be capitalized.</span></span>  
  
-   <span data-ttu-id="d89fa-112">如果是命名空間名稱發生錯誤，您是否在包含命名空間的組件新增了參考 ([-reference](../../../csharp/language-reference/compiler-options/reference-compiler-option.md))？</span><span class="sxs-lookup"><span data-stu-id="d89fa-112">If the error is for a namespace name, did you add a reference ([-reference](../../../csharp/language-reference/compiler-options/reference-compiler-option.md)) to the assembly that contains the namespace?</span></span> <span data-ttu-id="d89fa-113">例如，您的程式碼可能包含 `using Accessibility`指示詞。</span><span class="sxs-lookup"><span data-stu-id="d89fa-113">For example, your code might contain the directive `using Accessibility`.</span></span> <span data-ttu-id="d89fa-114">不過，如果您的專案未參考的組件 Accessibility.dll，則會報告錯誤 CS0246。</span><span class="sxs-lookup"><span data-stu-id="d89fa-114">However, if your project does not reference the assembly Accessibility.dll, error CS0246 is reported.</span></span> <span data-ttu-id="d89fa-115">如需詳細資訊，請參閱[管理專案中的參考](/visualstudio/ide/managing-references-in-a-project)</span><span class="sxs-lookup"><span data-stu-id="d89fa-115">For more information, see [Managing references in a project](/visualstudio/ide/managing-references-in-a-project)</span></span>  
  
-   <span data-ttu-id="d89fa-116">如果是類型名稱發生錯誤，您是否納入了適當的 [using 指示詞](../../../csharp/language-reference/keywords/using-directive.md) 或完整限定類型的名稱？</span><span class="sxs-lookup"><span data-stu-id="d89fa-116">If the error is for a type name, did you include the proper [using directive](../../../csharp/language-reference/keywords/using-directive.md), or, alternatively, fully qualify the name of the type?</span></span> <span data-ttu-id="d89fa-117">請考慮下列宣告： `DataSet ds`。</span><span class="sxs-lookup"><span data-stu-id="d89fa-117">Consider the following declaration: `DataSet ds`.</span></span> <span data-ttu-id="d89fa-118">若要使用 `DataSet` 類型，您需要兩個項目。</span><span class="sxs-lookup"><span data-stu-id="d89fa-118">To use the `DataSet` type, you need two things.</span></span> <span data-ttu-id="d89fa-119">首先，您需要包含 `DataSet` 類型之定義組件的參考。</span><span class="sxs-lookup"><span data-stu-id="d89fa-119">First, you need a reference to the assembly that contains the definition for the `DataSet` type.</span></span> <span data-ttu-id="d89fa-120">其次，您需要 `using` 所在命名空間的 `DataSet` 指示詞。</span><span class="sxs-lookup"><span data-stu-id="d89fa-120">Second, you need a `using` directive for the namespace where `DataSet` is located.</span></span> <span data-ttu-id="d89fa-121">例如，因為 `DataSet` 位於 **System.Data** 命名空間中，所以程式碼開頭需要下列指示詞： `using System.Data`。</span><span class="sxs-lookup"><span data-stu-id="d89fa-121">For example, because `DataSet` is located in the **System.Data** namespace, you need the following directive at the beginning of your code: `using System.Data`.</span></span>  
  
     <span data-ttu-id="d89fa-122">不需要 `using` 指示詞。</span><span class="sxs-lookup"><span data-stu-id="d89fa-122">The `using` directive is not required.</span></span> <span data-ttu-id="d89fa-123">不過，如果您省略這個指示詞，則必須在參考它時完整限定 `DataSet` 類型。</span><span class="sxs-lookup"><span data-stu-id="d89fa-123">However, if you omit the directive, you must fully qualify the `DataSet` type when referring to it.</span></span> <span data-ttu-id="d89fa-124">完整限定表示每次參考您程式碼中的類型時，都需要同時指定命名空間和類型。</span><span class="sxs-lookup"><span data-stu-id="d89fa-124">Full qualification means that you specify both the namespace and the type each time you refer to the type in your code.</span></span> <span data-ttu-id="d89fa-125">如果您省略上述範例中的 `using` 指示詞，則必須寫入 `System.Data.DataSet ds` 來宣告 `ds` ，而非 `DataSet ds`。</span><span class="sxs-lookup"><span data-stu-id="d89fa-125">If you omit the `using` directive in the previous example, you must write `System.Data.DataSet ds` to declare `ds` instead of `DataSet ds`.</span></span>  
  
-   <span data-ttu-id="d89fa-126">是否使用必須要有類型的變數或某個其他語言項目？</span><span class="sxs-lookup"><span data-stu-id="d89fa-126">Did you use a variable or some other language element where a type was expected?</span></span> <span data-ttu-id="d89fa-127">例如，在 **is** 陳述式中，如果您使用 `Type` 物件，而非實際類型，則會收到錯誤 CS0246。</span><span class="sxs-lookup"><span data-stu-id="d89fa-127">For example, in an **is** statement, if you use a `Type` object instead of an actual type, you get error CS0246.</span></span>  

-   <span data-ttu-id="d89fa-128">您是否參考了針對版本高於程式目標架構的架構版本而建置的版本？</span><span class="sxs-lookup"><span data-stu-id="d89fa-128">Did you reference the assembly that was built against a higher framework version than the target framework of the program?</span></span> <span data-ttu-id="d89fa-129">或者，您參考的專案是否以版本高於程式目標架構的架構版本為目標？</span><span class="sxs-lookup"><span data-stu-id="d89fa-129">Or did you reference the project that is targeting a higher framework version than the target framework of the program?</span></span> <span data-ttu-id="d89fa-130">舉例來說，假設您使用以 .NET Framework 4.6.1 為目標的專案，而您使用的類型來自以 .NET Framework 4.7.1 為目標的專案。</span><span class="sxs-lookup"><span data-stu-id="d89fa-130">For example, you work on the project that is targeting .NET Framework 4.6.1 and use the type from the project that is targeting .NET Framework 4.7.1.</span></span> <span data-ttu-id="d89fa-131">然後收到錯誤 CS0246。</span><span class="sxs-lookup"><span data-stu-id="d89fa-131">Then you get error CS0246.</span></span>
  
-   <span data-ttu-id="d89fa-132">是否使用未完整限定類型名稱的 *「using 別名指示詞」* (using alias directive)？</span><span class="sxs-lookup"><span data-stu-id="d89fa-132">Did you use a *using alias directive* without fully qualifying the type name?</span></span> <span data-ttu-id="d89fa-133">`using` 別名指示詞未在原始程式碼檔中使用 `using` 指示詞來解析類型。</span><span class="sxs-lookup"><span data-stu-id="d89fa-133">A `using` alias directive does not use the `using` directives in the source code file to resolve types.</span></span> <span data-ttu-id="d89fa-134">下列範例會產生 CS0246，因為未完整限定類型 `List<int>` 。</span><span class="sxs-lookup"><span data-stu-id="d89fa-134">The following example generates CS0246 because the type `List<int>` is not fully qualified.</span></span> <span data-ttu-id="d89fa-135">`using` 的 `System.Collections.Generic` 指示詞無法防止錯誤。</span><span class="sxs-lookup"><span data-stu-id="d89fa-135">The `using` directive for `System.Collections.Generic` does not prevent the error.</span></span>  
  
    ```csharp  
    using System.Collections.Generic;  
  
    // The following declaration generates CS0246.  
    using myAliasName = List<int>;   
  
    // To avoid the error, fully qualify List.  
    using myAliasName2 = System.Collections.Generic.List<int>;  
    ```  
  
     <span data-ttu-id="d89fa-136">如果您在先前運作的程式碼中收到這個錯誤，請先在方案總管中尋找遺漏或無法解析的參考。</span><span class="sxs-lookup"><span data-stu-id="d89fa-136">If you get this error in code that was previously working, first look for missing or unresolved references in Solution Explorer.</span></span> <span data-ttu-id="d89fa-137">需要重新安裝 [NuGet](https://www.nuget.org/) 套件嗎？</span><span class="sxs-lookup"><span data-stu-id="d89fa-137">Do you need to re-install a [NuGet](https://www.nuget.org/) package?</span></span> <span data-ttu-id="d89fa-138">如需建置系統如何搜尋參考的相關資訊，請參閱 [解析 Team Build 中的檔案參考](https://blogs.msdn.com/b/manishagarwal/archive/2005/09/28/474769.aspx)。</span><span class="sxs-lookup"><span data-stu-id="d89fa-138">For information about how the build system searches for references, see [Resolving file references in team build](https://blogs.msdn.com/b/manishagarwal/archive/2005/09/28/474769.aspx).</span></span> <span data-ttu-id="d89fa-139">如果所有參考似乎都正確，請查看原始檔控制記錄，以查看您 .csproj 檔及/或本機原始程式檔中的變更內容。</span><span class="sxs-lookup"><span data-stu-id="d89fa-139">If all references seem to be correct, look in your source control history to see what has changed in your .csproj file and/or your local source file.</span></span>  
  
     <span data-ttu-id="d89fa-140">如果您還沒有順利存取參考，請使用物件瀏覽器檢查應該包含這個命名空間的組件，以及確認命名空間已經存在。</span><span class="sxs-lookup"><span data-stu-id="d89fa-140">If you haven’t successfully accessed the reference yet, use the Object Browser to inspect the assembly that is supposed to contain this namespace and verify that the namespace is present.</span></span> <span data-ttu-id="d89fa-141">如果您使用物件瀏覽器來驗證組件是否包含命名空間，請嘗試移除命名空間的 `using` 指示詞，並查看其他中斷的部分。</span><span class="sxs-lookup"><span data-stu-id="d89fa-141">If you verify with Object Browser that the assembly contains the namespace, try removing the `using` directive for the namespace and see what else breaks.</span></span> <span data-ttu-id="d89fa-142">根本問題可能是另一個組件中的某個其他類型。</span><span class="sxs-lookup"><span data-stu-id="d89fa-142">The root problem may be with some other type in another assembly.</span></span>  
  
 <span data-ttu-id="d89fa-143">下列範例會產生 CS0246，因為遺漏必要 `using` 指示詞。</span><span class="sxs-lookup"><span data-stu-id="d89fa-143">The following example generates CS0246 because a necessary `using` directive is missing.</span></span>  
  
```csharp  
// CS0246.cs  
//using System.Diagnostics;  
  
public class MyClass  
{  
    // The following line causes CS0246. To fix the error, uncomment  
    // the using directive for the namespace for this attribute,  
    // System.Diagnostics.  
    [Conditional("A")]  
    public void Test()  
    {  
    }  
  
    public static void Main()  
    {  
    }  
}  
```  
  
 <span data-ttu-id="d89fa-144">下列範例會造成 CS0246，因為在必須要有實際類型時使用類型 `Type` 的物件。</span><span class="sxs-lookup"><span data-stu-id="d89fa-144">The following example causes CS0246 because an object of type `Type` was used where an actual type was expected.</span></span>  
  
```csharp  
// CS0246b.cs  
using System;  
  
class ExampleClass  
{  
    public bool supports(object o, Type t)  
    {  
        // The following line causes CS0246. You must use an  
        // actual type, such as ExampleClass, String, or Type.  
        if (o is t)  
        {  
            return true;  
        }  
        return false;  
    }  
}  
  
class Program  
{  
    public static void Main()  
    {  
        ExampleClass myC = new ExampleClass();  
        myC.supports(myC, myC.GetType());  
    }  
}  
```
