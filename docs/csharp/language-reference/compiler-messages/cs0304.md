---
title: 編譯器錯誤 CS0304
ms.date: 07/20/2015
f1_keywords:
- CS0304
helpviewer_keywords:
- CS0304
ms.assetid: 22dc7211-57a8-4c92-96f6-fc1cf0263b68
ms.openlocfilehash: cf751cc099598f39b00e616154fe438c1edb142e
ms.sourcegitcommit: 986f836f72ef10876878bd6217174e41464c145a
ms.translationtype: HT
ms.contentlocale: zh-TW
ms.lasthandoff: 08/19/2019
ms.locfileid: "69603818"
---
# <a name="compiler-error-cs0304"></a><span data-ttu-id="dc673-102">編譯器錯誤 CS0304</span><span class="sxs-lookup"><span data-stu-id="dc673-102">Compiler Error CS0304</span></span>

<span data-ttu-id="dc673-103">無法建立變數類型 'type' 的執行個體，的因為沒有 new() 限制式。</span><span class="sxs-lookup"><span data-stu-id="dc673-103">Cannot create an instance of the variable type 'type' because it does not have the new() constraint</span></span>  
  
 <span data-ttu-id="dc673-104">當您實作泛型類別，而且想要使用 `new` 關鍵字來建立為型別參數 `T` 提供之任何類型的新執行個體時，您必須在類別宣告中將 [new() 限制式](../keywords/new-constraint.md)套用至 `T`，如下例所示。</span><span class="sxs-lookup"><span data-stu-id="dc673-104">When you implement a generic class, and you want to use the `new` keyword to create a new instance of any type that is supplied for a type parameter `T`, you must apply the [new() constraint](../keywords/new-constraint.md) to `T` in the class declaration, as shown in the following example.</span></span>  
  
```csharp  
class C<T> where T : new()  
```  
  
 <span data-ttu-id="dc673-105">`new()` 限制式強制執行型別安全的方式，是保證為 `T` 提供的任何具象類型皆有預設值：無參數的建構函式。</span><span class="sxs-lookup"><span data-stu-id="dc673-105">The `new()` constraint enforces type safety by guaranteeing that any concrete type that is supplied for `T` has a default, parameterless constructor.</span></span> <span data-ttu-id="dc673-106">如果當 `T` 未指定 `new()` 限制式時，您嘗試在類別主體中使用 `new` 運算子來建立型別參數 `T` 的執行個體，就會發生 CS0304。</span><span class="sxs-lookup"><span data-stu-id="dc673-106">CS0304 occurs if you attempt to use the `new` operator in the body of the class to create an instance of type parameter `T` when `T` does not specify the `new()` constraint.</span></span> <span data-ttu-id="dc673-107">若用戶端程式碼嘗試使用沒有無參數建構函式的型別具現化泛型類別，則該程式碼會產生[編譯器錯誤 CS0310](./cs0310.md)。</span><span class="sxs-lookup"><span data-stu-id="dc673-107">On the client side, if code attempts to instantiate the generic class with a type that has no parameterless constructor, that code will generate [Compiler Error CS0310](./cs0310.md).</span></span>  
  
 <span data-ttu-id="dc673-108">下列範例會產生 CS0304。</span><span class="sxs-lookup"><span data-stu-id="dc673-108">The following example generates CS0304.</span></span>  
  
```csharp  
// CS0304.cs  
// Compile with: /target:library.  
class C<T>  
{  
    // The following line generates CS0304.  
    T t = new T();  
}  
```  
  
 <span data-ttu-id="dc673-109">類別的方法中也不允許 `new` 運算子。</span><span class="sxs-lookup"><span data-stu-id="dc673-109">The `new` operator also is not allowed in methods of the class.</span></span>  
  
```csharp  
// Compile with: /target:library.  
class C<T>  
{  
    public void ExampleMethod()  
    {  
        // The following line generates CS0304.  
        T t = new T();  
    }  
}  
```  
  
 <span data-ttu-id="dc673-110">若要避免此錯誤，請使用 `new()` 限制式宣告類別，如下例所示。</span><span class="sxs-lookup"><span data-stu-id="dc673-110">To avoid the error, declare the class by using the `new()` constraint, as shown in the following example.</span></span>  
  
```csharp  
// Compile with: /target:library.  
class C<T> where T : new()  
{  
    T t = new T();  
  
    public void ExampleMethod()  
    {  
        T t = new T();  
    }  
}  
```  
  
## <a name="see-also"></a><span data-ttu-id="dc673-111">另請參閱</span><span class="sxs-lookup"><span data-stu-id="dc673-111">See also</span></span>

- [<span data-ttu-id="dc673-112">C# 編譯器錯誤</span><span class="sxs-lookup"><span data-stu-id="dc673-112">C# Compiler Errors</span></span>](./index.md)
