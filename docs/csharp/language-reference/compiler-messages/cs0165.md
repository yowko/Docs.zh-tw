---
description: 編譯器錯誤 CS0165
title: 編譯器錯誤 CS0165
ms.date: 07/20/2015
f1_keywords:
- CS0165
helpviewer_keywords:
- CS0165
ms.assetid: e7eb7c90-af16-4734-b747-214030696975
ms.openlocfilehash: 980910718173424c42b2f51965f73d01004f238b
ms.sourcegitcommit: 27a15a55019f6b5f2733961738babe94aec0def3
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 09/15/2020
ms.locfileid: "90537258"
---
# <a name="compiler-error-cs0165"></a><span data-ttu-id="97d41-103">編譯器錯誤 CS0165</span><span class="sxs-lookup"><span data-stu-id="97d41-103">Compiler Error CS0165</span></span>

<span data-ttu-id="97d41-104">使用未指派的區域變數 'name'</span><span class="sxs-lookup"><span data-stu-id="97d41-104">Use of unassigned local variable 'name'</span></span>  
  
<span data-ttu-id="97d41-105">C # 編譯器不允許使用未初始化的變數。</span><span class="sxs-lookup"><span data-stu-id="97d41-105">The C# compiler doesn't allow the use of uninitialized variables.</span></span> <span data-ttu-id="97d41-106">如果編譯器偵測到使用可能尚未初始化之變數的情形，就會產生編譯器錯誤 CS0165。</span><span class="sxs-lookup"><span data-stu-id="97d41-106">If the compiler detects the use of a variable that might not have been initialized, it generates compiler error CS0165.</span></span> <span data-ttu-id="97d41-107">如需詳細資訊，請參閱[欄位](../../programming-guide/classes-and-structs/fields.md)。</span><span class="sxs-lookup"><span data-stu-id="97d41-107">For more information, see [Fields](../../programming-guide/classes-and-structs/fields.md).</span></span> <span data-ttu-id="97d41-108">當編譯器遇到可能會導致使用未指派變數的結構時，就會產生此錯誤，即使您的特定程式碼不會。</span><span class="sxs-lookup"><span data-stu-id="97d41-108">This error is generated when the compiler encounters a construct that might result in the use of an unassigned variable, even if your particular code does not.</span></span> <span data-ttu-id="97d41-109">這可避免明確指派的過度複雜規則。</span><span class="sxs-lookup"><span data-stu-id="97d41-109">This avoids the necessity of overly complex rules for definite assignment.</span></span>  
  
<span data-ttu-id="97d41-110">如需詳細資訊，請參閱[為何遞迴 Lambda 造成明確指派錯誤？](/archive/blogs/ericlippert/why-does-a-recursive-lambda-cause-a-definite-assignment-error) \(英文\)。</span><span class="sxs-lookup"><span data-stu-id="97d41-110">For more information, see [Why does a recursive lambda cause a definite assignment error?](/archive/blogs/ericlippert/why-does-a-recursive-lambda-cause-a-definite-assignment-error).</span></span>  
  
## <a name="example"></a><span data-ttu-id="97d41-111">範例</span><span class="sxs-lookup"><span data-stu-id="97d41-111">Example</span></span>  

<span data-ttu-id="97d41-112">下列範例會產生 CS0165：</span><span class="sxs-lookup"><span data-stu-id="97d41-112">The following sample generates CS0165:</span></span>  
  
```csharp  
// CS0165.cs  
using System;  
  
class MyClass  
{  
    public int i;  
}  
  
class MyClass2  
{  
    public static void Main(string[] args)  
    {  
        // i and j are not initialized.  
        int i, j;  
  
        // You can provide a value for args[0] in the 'Command line arguments'  
        // text box on the Debug tab of the project Properties window.  
        if (args[0] == "test")  
        {  
            i = 0;  
        }  
        // If the following else clause is absent, i might not be  
        // initialized.  
        //else  
        //{  
        //    i = 1;  
        //}  
  
        // Because i might not have been initialized, the following
        // line causes CS0165.  
        j = i;  
  
        // To resolve the error, uncomment the else clause of the previous  
        // if statement, or initialize i when you declare it.  
  
        // The following example causes CS0165 because myInstance is  
        // declared but not instantiated.  
        MyClass myInstance;  
        // The following line causes the error.  
        myInstance.i = 0;
  
        // To resolve the error, replace the previous declaration with  
        // the following line.  
        //MyClass myInstance = new MyClass();  
    }  
}  
```  
  
## <a name="example"></a><span data-ttu-id="97d41-113">範例</span><span class="sxs-lookup"><span data-stu-id="97d41-113">Example</span></span>  

<span data-ttu-id="97d41-114">編譯器錯誤 CS0165 可能會在遞迴委派定義中發生。</span><span class="sxs-lookup"><span data-stu-id="97d41-114">Compiler error CS0165 can occur in recursive delegate definitions.</span></span> <span data-ttu-id="97d41-115">您可以藉由在兩個陳述式中定義委派的方式防止發生此錯誤，這樣一來，變數就不會在未初始化之前使用。</span><span class="sxs-lookup"><span data-stu-id="97d41-115">You can avoid the error by defining the delegate in two statements so that the variable is not used before it is initialized.</span></span> <span data-ttu-id="97d41-116">下列範例將示範此錯誤和解決方法。</span><span class="sxs-lookup"><span data-stu-id="97d41-116">The following example demonstrates the error and the resolution.</span></span>  
  
```csharp  
class Program  
{  
    delegate void Del();  
    static void Main(string[] args)  
    {  
        // The following line causes CS0165 because variable d is used
        // as an argument before it has been initialized.  
        Del d = delegate() { System.Console.WriteLine(d); };
  
        //// To resolve the error, initialize d in a separate statement.  
        //Del d = null;  
        //// After d is initialized, you can use it as an argument.  
        //d = delegate() { System.Console.WriteLine(d); };  
        //d();  
    }  
}  
```
